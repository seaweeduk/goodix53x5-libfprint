# FUN_18003c440

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18003c440..0x18003c51e`; sole caller `FUN_1800392f0`.
- Callees: `FUN_18003d250` and `FUN_18003c2e0`.

## Recovered Behavior

The five arguments are the impulse-corrected unsigned-16 residual plane, dense
byte mask, rows, columns, and border. The function zero-initializes a 204-byte
stack vector, calls `FUN_18003d250`, then calls `FUN_18003c2e0` in model-table
order with:

| Model | Coefficients | Bias |
| --- | --- | --- |
| 1 | `0x1800dfa20` | `-66765` |
| 2 | `0x1800dfaf0` | `-5325` |
| 3 | `0x1800dfbc0` | `-51610` |

It compares model 1 against model 2 first, retaining model 1 on equality. It
then compares model 3 against that result and retains model 3 on equality.
Only the signed-dword minimum score is returned; no model index or identity is
published, so either equality preference produces the same observable value.
The exact vector and one-model contracts are documented in
`FUN_18003d250.md`, `FUN_1800397f0.md`, and `FUN_18003c2e0.md`.

## Profile-9 Reachability And Publication

The sole caller `FUN_1800392f0` decodes the profile geometry and calls this
function at `0x1800395f1` after in-place impulse correction. For packed subtype
12 the decoded dimensions are `88x108` and the fifth argument is border `2`.
Both exported production routes are direct:

```text
identifyImage   -> FUN_1800392f0 -> FUN_18003c440
enrolAddImage   -> FUN_1800392f0 -> FUN_18003c440
```

At `0x1800395fe`, `FUN_1800392f0` stores `EAX` at anti-fake offset `+0x12d8`.
The complete `0x1abc` anti-fake object is subsequently serialized in each
feature element, so this dword is the persisted model output as well as the
in-memory probe value. Candidate records/count are neither read nor written by
this function.

## Failure Behavior

The function and its called classifier functions return no recoverable error
status. Valid empty mask input produces the zero vector and therefore scores
`-66765`, the minimum of the three biases. Invalid pointers reached by those
functions fault normally.

Confidence is high for production reachability, argument geometry, evaluation
order, tie behavior, score ownership, and persisted publication.
