# FUN_180073820

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180073820..0x18007382a`.
- Caller: `FUN_180048260` at `0x180048624`.
- Role: compose the packed extraction classification scalar later serialized as
  optional feature tag `c7`.

## Contract

```c
void FUN_180073820(int32_t *output, int32_t low_class, int32_t high_class)
{
  *output = high_class * 0x100 + low_class;
}
```

The complete body shifts `R8D` left by eight, adds `EDX`, and stores the dword
through `RCX`. At `FUN_180048260:0x180048615..0x180048624`, `RCX` is the
caller-owned packed-classification dword, `EDX` is exactly configuration
`+0x10` (the entry-decoded low class), and `R8D` is `R14D` after current-class,
auxiliary, prior-high, and hysteresis transitions. The result is later stored by
`FUN_18004ae70` at live feature `+0x150` and emitted as tag `c7` when nonzero.

## Evidence And Confidence

- Confidence is high from the complete four-instruction body and caller
  assembly.
- A direct low-quality extraction trace observes decoder output zero followed by
  this stage's `high=2`, `low=0`, and final live-feature value `0x200` before
  anti-fake construction.
- There is no `0x200` immediate or profile-9 special case in this helper; that
  value follows only from the observed operands.
