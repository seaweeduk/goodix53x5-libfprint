# FUN_18005b440

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18005b440..0x18005b6e4`.
- Ghidra body: 64 blocks, 53 conditional branches, no callees.
- Role: contextual candidate rejection before post-admission refinement.

## Call Contract

The exact recovered boundary relevant to the profile-9/type-12 caller is:

```c
int32_t FUN_18005b440(
  const feature *probe,
  const int32_t candidate[77],
  int32_t sensor_type,
  int32_t accumulated_high,
  int32_t external_class,
  const int32_t context_counts[3]);
```

At `FUN_180055a40:0x180056083..0x1800560a4`, `RCX` is the live probe, `RDX`
is the current candidate, `R8D` is enrolled sensor type 12, and `R9D` is the
current accumulated high class. Stack `+0x20` is the decoded external class and
stack `+0x28` points to the three caller-local overlap context counts. The
external class is feature `+0x158[0]` zero-extended by `FUN_180073830`; the
three counts are the words at caller locals `+0x154..+0x15c`. All inputs are
read-only, and the sensor-type argument is not read by this helper. Return zero
retains the candidate and nonzero discards it immediately.

The live scalar is quality at `probe+0x11c`, not coverage. The only candidate
words read are exactly `0`, `5`, `6`, `9`, `10`, and `11`. In particular, the
scaled term is signed `candidate[11] * candidate[9] >> 8`.

For context counts `c0`, `c1`, and `c2`, the recovered shared terms are:

```c
denom = c0 + c1 + c2 + 1;
combined_ratio = ((c0 + c2) * 256) / denom;
both_large = 25 * denom < 100 * c0 && 25 * denom < 100 * c2;

secondary_denom = c1 + c2 + 1;
secondary_ratio = (c2 << 8) / secondary_denom;
secondary_guard = c2 + 1 + 25 * c0 + c1 < 100 * secondary_denom ||
                  secondary_denom > 250;
```

All divisions are signed integer divisions truncating toward zero.

External classes 2, 3, 4, and 5 have distinct rejection families. They combine
strict and inclusive boundaries at quality 50/60/70/90, candidate words `0`,
`5`, `6`, `9`, `10`, and `11`, count 8/15, the scaled threshold 20, and the two
ratios above. Classes outside 2..5 return the prior result, which is zero when
class 5 is not selected.

Ordinary v3 probes supply external class zero and context counts `[0,0,0]`.
They therefore take no class-2..5 rejection family and return zero. This gate is
noncausal for the v3 mismatch even though omitting it can broaden admission for
other inputs.

## Evidence

A standalone translation was compared directly with the DLL for 100,000 fixed-
seed valid states and 1,440 targeted threshold/class/accumulator controls. It
passes all 101,440 calls, including 4,851 rejection results, twice with
byte-identical output SHA-256
`f735bc0ea5269d1d89ec277627e94415546770ed285e23e7d76bb48bf2d7d6a1`.
Artifacts are under `match-sibling-caller-admission-audit-v1`.

## Native Status

Current native candidate admission does not invoke this gate before refinement.
That broadens native admission. No implementation is recommended until the
context-count producer and full caller composition have direct coverage.

## Confidence And Unresolved

- Confidence is high for the complete arithmetic model and return polarity.
- Confidence is high that ordinary v3 inputs return zero and cannot cause the v3
  mismatch.
- Semantic names for the three context counts and decoded class remain
  provisional.
- Natural-frequency and hardware-security effects are not established.
