# FUN_18005b440

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18005b440..0x18005b6e4`.
- Ghidra body: 64 blocks, 53 conditional branches, no callees.
- Role: contextual candidate rejection before post-admission refinement.

## Call Contract

The exact recovered boundary relevant to the profile-9/type-12 caller is:

```c
int32_t FUN_18005b440(
  const feature *probe,
  const int32_t candidate[77],
  int32_t sensor_type,
  int32_t accumulated_high,
  int32_t external_class,
  const int32_t context_counts[3]);
```

At `FUN_180055a40:0x180056083..0x1800560a4`, `RCX` is the live probe, `RDX`
is the current candidate, `R8D` is enrolled sensor type 12, and `R9D` is the
current accumulated high class. Stack `+0x20` is the decoded external class and
stack `+0x28` points to the three caller-local overlap context counts. The
external class is feature `+0x158[0]` zero-extended by `FUN_180073830`; the
three counts are the words at caller locals `+0x154..+0x15c`. All inputs are
read-only, and the sensor-type argument is not read by this helper. Return zero
retains the candidate and nonzero discards it immediately.

The live scalar is quality at `probe+0x11c`, not coverage. The only candidate
words read are exactly `0`, `5`, `6`, `9`, `10`, and `11`. In particular, the
scaled term is signed `candidate[11] * candidate[9] >> 8`.

For context counts `c0`, `c1`, and `c2`, the recovered shared terms are:

```c
denom = c0 + c1 + c2 + 1;
combined_ratio = ((c0 + c2) * 256) / denom;
both_large = 25 * denom < 100 * c0 && 25 * denom < 100 * c2;

secondary_denom = c1 + c2 + 1;
secondary_ratio = (c2 << 8) / secondary_denom;
secondary_guard = c2 + 1 + 25 * c0 + c1 < 100 * secondary_denom ||
                  secondary_denom > 250;
```

All divisions are signed integer divisions truncating toward zero.

## Complete Class Predicates

Write `q = probe[+0x11c]`, `h = accumulated_high`, `d = candidate[5]`,
`scaled = candidate[11] * candidate[9] >> 8`, and `r = combined_ratio`.

Class5 returns zero independently of counts if any of these is true:

```text
d >= 220
q >= 91
candidate[10] >= 76 && (q >= 51 || candidate[0] >= 9 || d >= 200)
```

Otherwise it returns one exactly when `both_large` and any of these is true:

```text
h >= 4 && ((r >= 166 && d <= 214) ||
           (r >= 129 && candidate[9] <= 79))
h <= 3 && r >= 166 && d <= 209
r > 220
```

For nonnegative counts bounded by a 52×44 plane, `r` lies in0..255 and
`both_large` requires each equal-class population to exceed one quarter of
`c0+c1+c2+1`. `both_large` alone does not imply any of the ratio thresholds.

Class2 returns one exactly when `scaled <= 19`, `q <= 60`, `h >= 2`,
`d <= 219`, `candidate[6] <= 215`, and `candidate[0] <= 15`.

Classes3/4 first require `secondary_ratio >= 211`, `secondary_guard`,
`scaled <= 19`, and `q <= 70`. Under those guards they return one if:

```text
secondary_ratio >= 226 && d <= 214 && candidate[6] <= 215
class == 3 && h >= 2 && d <= 214 && candidate[6] <= 214 && candidate[0] <= 15
d <= 209 && candidate[6] <= 209 && candidate[0] <= 15
```

Classes outside2..5 return zero. Null live summary is decoded as class0 by the
caller and does not enter a rejection family.

## Count Ownership And Continuation

The counts come from `FUN_180071d40`'s optional masked classifier, with both
binary images derived from the live probe. Its unwritten temporary bytes can
affect admitted warped samples; see `FUN_180071d40.md` and `FUN_180072710.md`.
This helper's count dependence is therefore not eliminated by successful
extraction or by class5 publication.

The ordinary mode5 producer's stable-component and packed-high constraints do
not imply any of the strong-metric return-zero guards above. A native-produced
mode5 probe and a valid image-derived gallery can reach the weak class5 family.
At the fresh temporary-allocation boundary, different initial contents of bytes
left unwritten by the class loop can change `both_large` and this helper's
return while the producer images, live projection, records and admitted affine
remain identical. Thus extraction/record ownership alone does not establish
contextual-veto invariance.

The call at `0x1800560a4` precedes initial flags, affine refinement, subsequent
flag policy and `FUN_180058700` status admission. A nonzero result skips those
steps for that feature. If retained and status-admitted, the original counts
also supply `FUN_1800553f0`'s support ratio after refinement; that helper's
ratio-dependent family can clear the match flag. Neither later stage recomputes
the masked counts from the refined affine.

## Downstream State Of An Early Rejection

The nonzero-return branch at `FUN_180055a40:0x1800560ab`
goes to the next-occurrence path, not to a clearing or rollback block. The
physical candidate row already contains counts, original overlap/coverage,
record metrics and penalties. Its affine words15..20 and low-bitmap words6..8
are still zero: affine stores are at `0x1800561ab..0x1800561b9`, and
low-bitmap materialization follows the second classifier. Thus early rejection
does not erase the row. If other occurrences enable aggregate rescue, its
`word1 > 4 && word5 > 189` scan can still visit that partial row; see
`FUN_18005d9e0.md`. A retained occurrence instead supplies the later completed
row, including a replacement affine when refinement publishes one.

For an ordinary live class5/high5 probe, high cannot decrease. This excludes the
one-shot high increment, queue insertion, and zero-score compact/fallback
finalization, but not the status-counter cutoff: eleven status-one returns
still stop candidate construction at the next non-skipped occurrence
(`0x180055e36..0x180055e3a`). An early-vetoed occurrence never calls the status
helper and therefore cannot increment that counter. See `FUN_180055a40.md` for
the full traversal state and `FUN_180072860.md` for refinement publication.

Zero final flags do not alone make the retained continuation inert. After
status zero, `word4 > configuration[0] + 207 && word5 > 195` still adds the
retained-count Q8 term, contributes to rescue admission, and can replace the
ranked candidate. The optional-result copy at `0x180056cc1..0x180056ce5`
publishes that ranked candidate's word9 even when score remains zero. Ordinary
`identifyImage` passes a null optional-result pointer; its actual signed-score
acceptance test is strictly greater than zero (`0x180001e87..0x180001ea3`).
Neither the compact validator nor rescue's separate score-above40 test is an
external acceptance threshold. Direct publication uses nonzero candidate
flag64, independently of match flag60, and increments gallery feature `be`
before the eventual external score test. These are the publication owners
documented in `FUN_180055a40.md` and `identifyImage.md`.

An admitted zero-flag contributor also survives in a later direct publication's
score prefix. For ordinary type-12 recognition, its term is
`((word1 << 8) + 21) / 42`; the later direct score is
`((100 * accumulated_terms) / contributor_count) >> 8`, with the caller's
signed arithmetic. The next feature does not reset either that accumulator or
configuration word19. Thus a continuation ending flags0/0 can change a whole-call
positive score even when the next feature's classification and selected affine
are independent of the changed recognition mode. A retained-count16 contributor
adds98; a following retained-count22 direct candidate adds134. Including both
publishes45, whereas skipping the first publishes52. These values occur with
image-derived feature owners and an ordinary live class5/high5 probe; no metric
or policy-output assignment is required.

For that route both classifiers produce flags1/1 for the second feature, which
keeps candidate flag1 after status clears match flag60. Consequently recognition
mode0 versus1 does not change its direct eligibility. Status returns zero and
does not increment the counter for either evaluated feature. The selected
physical feature, affine, evidence object and gallery lifecycle update can
remain identical while the published score differs. Visiting the direct
positive feature first exits the ordinary non-retention loop before the weak
feature, excluding that prefix contribution for that traversal. See
`FUN_180055a40.md` for score latching and traversal exit, and
`FUN_180053220.md` for the shared recognition-state predicates. Both45 and52
satisfy the external signed-score-above-zero decision; this score distinction
alone does not establish an acceptance reversal.

## Ordinary Mode5 Decision-Route Bounds

At accumulated high5, the late-status return has a smaller exact predicate.
Write `n = probe[+0xf0]`, `q120 = probe[+0x120]`, and let `A` be the area
returned by `FUN_180071370` for the selected affine and 104-by-88 dimensions.
With ordinary nonnegative scalar ranges, define
`margin = 20 + max(q120 - trunc(100*n/120), 0)/10`. Status is one exactly when:

```text
selected affine is mode5-close
or (n < 50 && q120 > 80 && word9 < 50)
or (100*A > (100-margin)*9152)
```

Mode5-close is the componentwise inclusive bound
`abs(affine-[256,0,0,0,256,0]) <= [45,40,3840,40,45,3840]`.
All other transform-class status producers are subsets of this close region;
the remaining area-entry predicate is unconditional at high5. The final
small-primary minimum-margin adjustment cannot raise the base20 margin. The
status predicate neither reads the incoming flags nor the shared recognition
mode. Consequently recognition-mode divergence alone cannot create a status
counter divergence for an otherwise identical completed row and affine. Early
veto divergence can still omit the entire status call. See `FUN_180058700.md`
for scalar-field naming, flag clearing and exactly-once counter increments, and
`FUN_180055a40.md` for the eleven-return cutoff.

The separate recognition-sensitive flag path is owned by `FUN_180053220.md`
and `FUN_1800572e0.md`, including the native primary12 coverage threshold115.
Eligibility of that leaf alone does not establish that the dispatcher reaches it
or that its outputs survive the remaining caller stages.

The early-rejected partial rescue row is not an empty footprint. If its retained
count exceeds four and original detail exceeds189, rescue admits it with all six
affine words still zero. `FUN_180071370` then admits every cell of the52-by-44
rectangle: both mapped coordinates are zero for every destination cell. The
`FUN_180072600` subtraction removes all remaining valid mask bits. If this is
the first eligible row, its word8 is zero and every later marginal area is zero.
Zero total area fails immediately; positive total area has weighted low detail
zero and at most one large increment, also excluding acceptance. If earlier
rows removed area, this row instead consumes the
residual and can change their aggregate weights. It is therefore incorrect to
treat an early-vetoed row as absent from rescue. See `FUN_18005d9e0.md` for
eligibility, thresholded popcounts and ordered marginal ownership.

Two independent rescue-failure implications bound weak-only continuations.
With ordinary nonnegative marginal areas and no arithmetic overflow, all
eligible detail words at most208 imply weighted detail at most208, excluding
the strict acceptance gate. Also, repeating one identical selected affine cannot
produce a second positive marginal area: subtraction of the same footprint is
idempotent. Thus it cannot provide two increments above1400. These implications
do not apply to arbitrary completed refinement rows or distinct footprints.
