# FUN_180039220

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180039220..0x1800392ee`; sole caller `FUN_1800392f0`.
- Callees: `FUN_180073ff0`, `FUN_180073850`, allocation, copy, and free.

## Recovered Behavior

The function applies one radius-two cross dilation followed by four radius-two
cross erosions to the dense anti-fake mask. It alternates source/destination
buffers, copies the final temporary plane to the caller's plane, and frees the
temporary allocation. Dimensions and byte count are caller supplied.

For profile 9/type 12, the plane is exactly `88x108` bytes. Both primitives use
the nine-point cross consisting of the center and horizontal/vertical offsets
one and two. Dilation emits `0xff` if any in-bounds footprint byte is nonzero;
erosion emits zero if any in-bounds footprint byte is zero and otherwise emits
`0xff`. A footprint position outside the plane is ignored rather than treated
as zero. The sequence is therefore:

```text
caller plane -> dilate -> temporary
temporary    -> erode  -> caller plane
caller plane -> erode  -> temporary
temporary    -> erode  -> caller plane
caller plane -> erode  -> temporary
temporary    -> copy   -> caller plane
```

The allocation request uses the low 32 bits of the supplied dimension product.
The function does not validate dimensions or an allocation result. The fixed
profile-9 product is 9,504 and is valid; malformed dimensions or allocation
failure remain native fault/corruption behavior rather than a returned status.

Evidence is the exact call sequence at `0x180039267..0x1800392c8`. Confidence
is high; no unresolved behavior affects profile-9 candidate construction.
