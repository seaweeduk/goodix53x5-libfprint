# FUN_180045a50

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180045a50..0x180045d33`.
- Role: deep-copy one live feature into an already allocated destination.

## Call Graph

- Caller relevant here: `FUN_1800462a0`; it also supports ordinary study
  replacement paths.
- Callees: `FUN_180068660`, `FUN_180037690`, `FUN_180003800`, and `memcpy`.

## Recovered Behavior

Given a live template, source feature, and preallocated destination feature, the
helper copies scalar fields and deep-copies the owned matrices, active record
array, bitmap/mask storage, and optional `0x1abc` registration block. It resets
transient destination fields such as residual/action counters instead of
aliasing source pointers. Null source or destination returns `-1`; success
returns 1.

The transient reset is explicit rather than a consequence of zero allocation:
`0x180045bc5..0x180045bd9` clears live `+0x128/+0x12c`, `+0x138`, and
`+0x148`. In particular, a queued `P->Q` copy starts `Q.+0x148` at zero instead
of copying the source feature's gallery overlap count. The queue probe never
becomes the owner of the current gallery vector: replacement preserves the
target gallery slot's `+0x148`, append starts a preallocated zero slot, and only
`FUN_180047120` recomputes the complete gallery vector at its recovered
normalization boundaries.

For the profile-9/type-12 queue path, this is a transformed copy rather than a
byte-preserving record copy. Queue enqueue passes nonzero transform mode to
`FUN_180037690`; every copied `0x38`-byte record in Q has its dword at `+0x24`
and qword at `+0x30` cleared. P is unchanged. Q owns the transformed records
independently and does not preserve P's bytes `36..39` or `48..55`.

Queue enqueue therefore does not transfer or borrow the current probe. The
live template continues to own its preallocated cache slot, while the probe
remains independently owned and can be destroyed after `templateStudy`.

## Evidence And Confidence

- Matrix and scalar copy: `0x180045a74..0x180045c10`.
- Transient scalar reset: `0x180045bc5..0x180045bd9`.
- Optional registration deep copy: `0x180045c50..0x180045d1a`.
- Profile-9/type-12 queue `P->Q` reaches `FUN_180037690` with nonzero transform
  mode; the callee's mode-dependent clearing is documented at
  `0x180037809..0x180037843`.
- Confidence: high for non-aliasing ownership and copied live-feature content.
- Allocation failure inside optional copying is not surfaced transactionally;
  native code must validate before publishing a queue entry.

## Action-5 Representation Boundary

The copy's content, not only its ownership, is required by queued re-match. The
authoritative sequence-3 Q retains 115 active 56-byte records and all derived
live feature state until `FUN_18005d330` calls the ordinary matcher. Current
native Q instead reaches its callback as a 13,069-byte serialized one-feature
packed template. A gallery-only substitution with the DLL unfinalized G still
returns native score/selected `39/8`, mask `0x181`, rather than DLL `100/9`,
mask `0x200`. Native queue implementation must therefore deep-copy the full
prepared/live object and may not reconstruct this boundary from packed 32-byte
records.

The native action-0 late-enqueue control now verifies this boundary directly:
the occupied queue owner is complete and pointer-independent from the original
live probe before finalization. The queue owner remains transient and is freed by
queue cleanup without publication.

## Matrix-Relevant Copy Content

The deep copy retains the complete active-record count and all bytes outside the
two transformed spans, but clears descriptor bytes `36..39` and `48..55` in Q.
It does not copy either caller-stack match matrix. A later queued rematch
allocates new matrices and recomputes them from Q's transformed records; it must
not consume the corresponding source-P spans as if queue enqueue preserved them.

## Native Ownership Mapping

Current `goodix_match_info_copy` models this boundary for queue `P->Q`. It gives
Q its own `GBytes` template backing and record allocation, clears record payload
`+24..+27` and `+36..+43` (record offsets `+0x24` and `+0x30`), and copies record/
partition counts, all three feature bitmaps, inline and rescue masks, antifake
state, quality, coverage, and optional `c7` by value. P and Q therefore share no
mutable record ownership. Replay of this corrected transformed-copy boundary
remains pending.
