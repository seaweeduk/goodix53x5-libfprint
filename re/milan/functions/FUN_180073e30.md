# FUN_180073e30

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180073e30..0x180073fc1`.
- Caller: `FUN_180073ba0`.
- Role: collect spatial record pairs through an inverse affine and index map.

## Recovered Behavior

The helper inverts the supplied Q8 affine, transforms each source record into the mapped feature's pixel frame with two signed rounding shifts, and looks up the mapped record index. It reports both in-bounds and matched counts and emits ordered `[source_index, mapped_index]` pairs for nonnegative map entries.

## Confidence And Unresolved

- Confidence: high for transform direction, rounding, bounds, pair order, and count outputs.
- Unresolved: none for exercised type-12 inputs.

## 2026-07-28 Retrospective Result

No new arithmetic defect was found in this leaf. It receives a caller-selected
partition range, calls `FUN_1800689a0`, and emits indices relative to that range;
`FUN_180073ba0` owns later rebasing. Native uses the exact shared wrapped inverse
and matches this scan when supplied the same range. The open defect is the
caller's range ownership, not this leaf.
