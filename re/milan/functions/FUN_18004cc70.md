# FUN_18004cc70

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18004cc70..0x18004ce3e`; relevant callers:
  `FUN_180049b90` and `FUN_18004b770`.
- Callees: `FUN_180049120`, `FUN_180048980`.

## Recovered Behavior

It builds the descriptor payload for one retained candidate. Profile-9/type-12
uses the mode-1 descriptor layout before the caller quantizes coordinates and
orientation. The sequence-72 skip-retry stream is byte-exact with native across
all 54 final records, and the kernel-100 projection is exact across all 48
natural records, proving this helper is downstream and already exact.

The helper zeroes both descriptor workspaces, calls `FUN_180049120` for 128
samples, then `FUN_180048980` in mode zero. Because type 12 is outside the
special `9/10/17/18` predicate, it copies exactly the four central `8`-sample
cells and calls `FUN_180048980` again in mode one with count 32. It neither
allocates nor owns the destination record and has no failure result. Current
source correspondence is `feature/materialization.c:532-557`.

The first descriptor call at `0x18004cd49..0x18004cd68` defines record
`+0x10..+0x27`. The type-12 second call at
`0x18004cd7f..0x18004ce17` completes `+0x28..+0x37`. Zero-initialized stack
workspaces and the clearing contracts in `FUN_180048980` ensure all 40 bytes are
defined before `FUN_180047ae0` transforms them.

## Confidence And Unresolved

- Confidence: high for per-record byte ownership, complete initialization, type
  predicate, central-cell selection, destination ownership, and sequence-72
  negative control.
- Unresolved: layouts used only by non-type-12 selectors.
