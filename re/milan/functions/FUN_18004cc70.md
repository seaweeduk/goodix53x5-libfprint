# FUN_18004cc70

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18004cc70..0x18004ce3e`; relevant callers:
  `FUN_180049b90` and `FUN_18004b770`.
- Callees: `FUN_180049120`, `FUN_180048980`.

## Recovered Behavior

It builds the descriptor payload for one retained candidate. Profile-9/type-12
uses the mode-1 descriptor layout before the caller quantizes coordinates and
orientation. The sequence-72 skip-retry stream is byte-exact with native across
all 54 final records, and the kernel-100 projection is exact across all 48
natural records, proving this helper is downstream and already exact.

The reached configuration is type 12, scale `0x1adfe`, first median layout
`(width,stride)=(32,4)`, and second layout `(32,1)`. The helper zeroes its
512-byte sample and 128-byte central workspaces, calls `FUN_180049120` for all
128 samples, then calls `FUN_180048980` with mode zero/count 128. The complete
generic selector is `((type-9)&0xfffffff6)==0`, true exactly for
`9,10,17,18`; type 12 fails it both here and inside the encoder. This owner
therefore copies exactly four central `8`-sample cells from offsets
`40,48,72,80` and calls `FUN_180048980` with mode one/count 32.

The extraction caller `FUN_180049b90` initializes the process-global
`128x128` Hadamard table through `FUN_180047610` before entering its descriptor
loop and passes that table here, so an uninitialized table is not reachable on
the scoped extraction path. Current source computes signs on demand and has no
mutable global dependency. The helper neither allocates nor
owns the destination record, validates pointers/configuration, catches native
integer faults, nor returns status; malformed inputs can fault or partially
write. Current source correspondence is `feature/materialization.c:532-557` and
has the same void/fixed-input production contract.

The first descriptor call at `0x18004cd49..0x18004cd68` clears and defines
record `+0x10..+0x27`: four transform dwords at `+0x10..+0x1f`, one median dword
at `+0x20`, and zero `+0x24..+0x27`. The type-12 second call at
`0x18004cd7f..0x18004ce17` clears and defines `+0x28..+0x37`: transform at
`+0x28`, median bits at `+0x2c`, and zero `+0x30..+0x37`. It never touches
record `+0x00..+0x0f`. Thus all 40 descriptor bytes are owned and initialized
before later record transformation; no prior destination contents survive.

## Confidence And Unresolved

- Confidence: high for per-record byte ownership, complete initialization,
  configuration, exact generic predicate, table reachability, central-cell
  selection/order, destination ownership, no-status behavior, and sequence-72
  negative control.
- Unresolved: layouts used only by non-type-12 selectors.
