# FUN_18004cc70

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18004cc70..0x18004ce3e`; relevant callers:
  `FUN_180049b90` and `FUN_18004b770`.
- Callees: `FUN_180049120`, `FUN_180048980`.

## Recovered Behavior

It builds the descriptor payload for one retained extraction candidate or one
anti-fake temporary record. Both profile-9/type-12 callers use a mode-zero
128-sample encoding followed by a mode-one 32-sample encoding. Coordinate and
orientation quantization after descriptor construction belongs to the ordinary
extraction caller; anti-fake enrichment instead transforms the temporary record
and projects its descriptor bytes into a 48-byte anti-fake record.

Both callers supply type 12, first median layout `(width,stride)=(32,4)`, and
second layout `(32,1)`. Descriptor scale is a separate fourth argument:
`FUN_180049b90` supplies its configured `0x1adfe`, while `FUN_18004b770`
supplies `0xc117`. This helper forwards that argument to `FUN_180049120`;
it does not replace it with configuration word 5 or a fixed extraction scale.
The helper zeroes its
512-byte sample and 128-byte central workspaces, calls `FUN_180049120` for all
128 samples, then calls `FUN_180048980` with mode zero/count 128. The complete
generic selector is `((type-9)&0xfffffff6)==0`, true exactly for
`9,10,17,18`; type 12 fails it both here and inside the encoder. This owner
therefore copies exactly four central `8`-sample cells from offsets
`40,48,72,80` and calls `FUN_180048980` with mode one/count 32.

## Cross-Caller Table And Scale Ownership

Both callers independently initialize the same process-global `128x128`
signed-16 Hadamard table at `0x180257520` through `FUN_180047610` before their
descriptor loops. `FUN_180049b90` initializes it after selecting the final
pass; `FUN_18004b770` calls the initializer at `0x18004ba0e`, after constructing
its support planes and before testing the signed candidate count. Empty
anti-fake calls therefore still initialize the table. Enrichment does not
depend on a preceding ordinary extraction to prepare descriptor support.

`FUN_180047610` takes only the destination pointer. It seeds the 2x2 matrix
`[1,1;1,-1]` and doubles the dimension through 128 by copying the preceding
quadrant into the other two positive quadrants and its negation into the
bottom-right quadrant, with a fixed 128-element row stride. It consumes no
descriptor scale, image, type, or prior table contents. Both callers therefore
rebuild identical coefficients despite their different sample scales; there is
no extraction-scale table state to carry into enrichment or back again.

This helper borrows the table as argument 7 and forwards it to both
`FUN_180048980` calls. It neither initializes the table nor owns its lifetime.
The anti-fake caller's complete local configuration is
`[12,0,300,40,0,0xc117,32,4,0,32,1,0,0]`; see `FUN_18004b770.md` for its
residual/support producers and `FUN_1800392f0.md` for object publication.

The helper neither allocates nor owns the destination record, validates
pointers/configuration, catches integer faults, nor returns status; malformed
inputs can fault or partially write. Its current corresponding owner is
`feature/materialization.c:feature_build_descriptor`.

## Descriptor Byte Ownership

The first descriptor call at `0x18004cd49..0x18004cd68` clears and defines
record `+0x10..+0x27`: four transform dwords at `+0x10..+0x1f`, one median dword
at `+0x20`, and zero `+0x24..+0x27`. The type-12 second call at
`0x18004cd7f..0x18004ce17` clears and defines `+0x28..+0x37`: transform at
`+0x28`, median bits at `+0x2c`, and zero `+0x30..+0x37`. It never touches
record `+0x00..+0x0f`. Thus all 40 descriptor bytes are owned and initialized
before later record transformation; no prior destination contents survive.
