# FUN_18004dd10

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18004dd10..0x18004de99`, in-place valid-mask edge
  erosion.

## Call Graph

- Relevant caller: `FUN_18004ee80`.

## Exact Contract

The function copies an input byte mask to an output mask, then independently
processes every row and every column. From each of the four ends it clears the
first `remove_count` nonzero samples encountered. Zero samples do not consume
the removal budget. The four passes observe the original input when deciding
which positions consume a budget, so earlier output clears do not alter later
admission.

For profile 9/type 12, dimensions are 88 rows by 108 columns and
`remove_count = 8`. The current counterpart is `milan_profile9_erode_valid()`.
Every surviving nonzero pixel has at least eight original nonzero samples
before and after it in its row and column. In particular, support is confined
to rows 8..79 and columns 8..99, at most `72*92 = 6624` pixels. A fully nonzero
input attains that rectangle. Holes do not become nonzero, and clearing eight
samples from the ends of a row/column does not erode every internal hole or
enforce connectedness, convexity, or a minimum corridor width.

`FUN_180051bd0` writes zero gradients outside this support. The component
candidate plane in `FUN_18004f540` is consequently a subset of this rectangle
when its signed-word candidate threshold is nonnegative. Its separate age
predicate can remove additional vertices but does not add geometric regularity.
The support implication must not be applied to a negative signed-word
candidate threshold: zero gradients then pass the gradient comparison.
See [`FUN_18004f540.md`](FUN_18004f540.md) for the FIFO frontier and retained-age
contracts.

## Evidence

- Input copy: `0x18004dd31..0x18004dd3f`.
- Left/right row passes: `0x18004dd48..0x18004dddf`.
- Top/bottom column passes: `0x18004dde5..0x18004de80`.
