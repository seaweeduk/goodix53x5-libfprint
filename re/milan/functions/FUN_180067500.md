# FUN_180067500

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x180067500..0x1800683c5`, profile-aware update-source, gain,
  and persistent-map evolution routine.

## Call Graph

- Sole caller: `FUN_1800668f0`.
- Direct substantive callees include `FUN_180066690`, `FUN_180064170`,
  `FUN_180063a80`, and `FUN_180067050`; image allocation/copy helpers support
  temporary planes.

## Behavior

- For packed subtype 12, the input live frame and setup plane have already been
  scaled by three in `FUN_1800668f0`.
- The production specialized branch computes each source sample as
  `max(scaled_setup - scaled_live - 0x2ffd, 0)`. Since the setup plane contains
  `normalized_setup + 7095`, this is exactly
  `max(9000 + 3 * (normalized_setup - live), 0)`.
- The clamp applies to the complete signed-baseline expression. It is not
  `9000 + 3 * max(setup - live, 0)` and not
  `9000 + 3 * max(live - setup, 0)`.
- The remainder selects update admission, derives gain maps, applies Q13 map
  combinations, and conditionally evolves persistent calibration state.
- Profile-9 admission is initially tested at 80%, but packed subtype 12
  overrides it with the strict test `admitted_mask_pixels * 10 >
  pixel_count * 9`. The admitted count comes from the `FUN_18006a4c0` mask
  structure before later rendering-mask hole fill.
- A non-admitted subtype-12 frame still uses the signed-baseline source above.
  Admission controls frame-derived gain creation and persistent-map evolution;
  it does not fall back to generic `max(setup - live, 0)` rendering. The
  temporary gain starts at Q13 unity, but the common subtype-12 tail still
  combines it with the existing application and auxiliary gain maps before the
  caller renders the working frame.
- Admitted and non-admitted subtype-12 frames share the same primary-divisor
  tail. The persistent ready flag is updated before admission-controlled map
  evolution and before that tail. An auxiliary-map sample count of zero with
  workspace sample count strictly greater than 5 sets it, while auxiliary-map
  sample count at least 15 clears it. When ready is clear, the tail computes
  `round(round(temporary_gain * application_gain / 8192) *
  auxiliary_gain / 8192)` with a rounding step after each product. When ready is
  set, it computes only
  `round(temporary_gain * application_gain / 8192)`.
- The admission predicate does not own the ready flag. Admission controls the
  frame-derived temporary gain and map evolution; ready controls which
  persistent maps the shared tail applies. This distinction is reached by both
  production subtype-12 branches.
- The transition observes entry counts. On an admitted update, the first
  `FUN_180063a80` call later increments the auxiliary-map sample count, capped
  at 30; gain composition follows, then `FUN_180067050` increments the workspace
  sample count, capped at 400. A non-admitted call increments neither count.
- The ready pointer is descriptor slot 5. `FUN_1800668f0` always fills that slot
  with global RVA `0x1efbfc`; the only direct xrefs to the global are descriptor
  construction at `0x180066a65` and `0x180066a6c`. No generation initializer,
  profile initializer, exported entry point, or teardown writes the global.
  It is zero-initialized by PE loading, then survives calibration generation
  replacement and admitted/rejected calls until DLL unload/process exit.
- At `0x1800679f2..0x180067a05`, `R10` points to auxiliary-map sample-count
  global `DAT_1801efbf4`, `RCX` points to the workspace sample count at
  `DAT_180219670`, and `RAX` points to ready global `DAT_1801efbfc`.
  Initialization global `DAT_1801efbf8` is a separate input to
  `FUN_1800672e0` and is not read by this transition.
- The corresponding current fields are `auxiliary_sample_count`, `sample_count`,
  and `calibration_ready`. Current source passes `sample_count` and
  `stable_count` to a helper whose parameter names imply different semantics.
  This formal argument mismatch is dormant in the validated lifecycle: normal
  admitted calls advance native workspace and auxiliary counts together, and
  the post-render path can only advance the auxiliary count further while its
  temporary workspace count is exactly 5. Neither path produces auxiliary
  count zero with workspace count greater than 5.
- A normal adapter setup refresh loads calibration before `preprocessor_init`.
  If the loaded workspace sample count is zero, the first subsequent
  `FUN_1800672e0` call clears the auxiliary count before this transition. A set
  transition therefore requires a pre-existing calibration blob with workspace
  sample count greater than 5 while the process auxiliary count is zero. The
  adapter's only direct save path runs immediately after setup initialization,
  so a clean zero-state lifecycle does not produce such a blob.
- Current Linux source resets `calibration_ready` at base-generation
  publication, whereas the DLL scalar is process-global. This storage-lifetime
  difference has no established profile-9 output effect because no validated
  natural lifecycle has set the DLL scalar.
- Admitted subtype-12 frames pass the ratio plane produced by `FUN_180063a80`
  to `FUN_180067050`. Its update covers every sample, including the outer and
  second-from-edge columns.

## Evidence

- Subtype-12 specialized source branch and `-0x2ffd` constant:
  `0x180067500..0x1800678eb`.
- Subtype-12 update-count threshold override: branch ending at
  `0x1800679c5`.
- Subtype-12 strict 90% override: `0x1800679a2..0x1800679c2`.
- Ready evaluation is `0x1800679f2..0x180067a05`. The admitted auxiliary-count
  update is the `FUN_180063a80` call at `0x180067b46`; the workspace sample-count
  update is the `FUN_180067050` call at `0x180068377` after gain composition.
- The subtype-12 common gain-combination tail is
  `0x180067feb..0x180068347`. The ready transitions are
  `0x1800679f2..0x180067a05`; the ready-clear two-product primary path is
  `0x180068219..0x180068345`, and the ready-set one-product path is
  `0x180068134..0x180068214`. With admission false, the temporary gain remains
  unity.
- The adjacent secondary-divisor output is independently gated by update count
  at `0x180067a07..0x180067a2f` and uses the secondary auxiliary map. It does not
  select the primary rendering divisor. Subtype 11 shares the common-tail family
  but is not the validated profile-9/type-12 device path; other subtype families
  branch before `0x180067feb` and remain outside this focused implementation.
- Global ownership: xrefs `0x180066a65/0x180066a6c`; indirect set/clear stores
  `0x1800679fc/0x180067a05`. A complete program xref audit finds no reset owner.
- A natural retained 13-use profile-9 generation remained exact between current
  source and the approved DLL. Three further valid identify calls extended the
  same state through count 16. Ready remained zero across the `>5` and `>=15`
  thresholds, and every status, quality, coverage, gain-state hash, and
  processed byte matched.
- Exact action-7 rejected-stage replay confirms that this divisor plus the
  caller's final write loop reproduces all 9,504 working samples at both stage
  4 and stage 7.

## Confidence

High for the production source expression, clamp placement, subtype-12
admission threshold, non-admitted working source, ready operands and ordering,
process lifetime, aliases, and profile-9 gain-map combination; medium for the
vendor semantic names of the persistent maps.

## Unresolved

- Whether a naturally deployed calibration file can contain workspace sample
  count greater than 5 before the DLL process has accumulated auxiliary samples
  is not established. The adapter's in-DLL save timing does not create that
  state from a clean file.
