# FUN_180058ee0

## Binary And Body

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180058ee0` / `0x180058ee0-0x1800598c1`.
- Inferred signature: `void f(metrics, config, inout_flag)`.
- Caller: `FUN_180053220`; callee: `FUN_18005c3b0`.

## Behavior

This is a large type-family veto stage. It derives adjusted detail/coverage
quantities from metrics `0,1,4,5,8,9,10,11-14`, dispatches on a clamped count,
and clears the input flag when any threshold family rejects the candidate. For
configuration type 12 (`config+0x3c == 0xc`), surviving candidates receive an
additional `FUN_18005c3b0` filter.

## Evidence And Confidence

- The sole ordinary rejection sink writes `*inout_flag = 0`; the type-12 tail call is explicit near the accepted threshold path.
- The production type-12 trace reaches this helper on 47 of 58 dispatcher calls
  when `rsp+0x60` is nonzero. Twenty-nine calls continue into
  `FUN_18005c3b0`; the helper family is clear-only for `rsp+0x60`.
- Confidence is high for veto behavior and type-12 chaining, low for human-readable meanings of every metric.
- The profile-9/type-12 threshold family now has a purpose-specific native
  implementation. The gate reproduces all `47/47` entries and their `29`
  `FUN_18005c3b0` continuations. Other profiles/types remain outside this claim.
- The accepted-score residual trace adds `89/2` feature 0 as a same-input
  counterexample beyond that original cohort. This parent helper reaches its
  type-12 continuation with exact input and initial output; the first difference
  is the documented bucket-6 retained-count operand in `FUN_18005c3b0`, not this
  function's pre-continuation threshold family.
