# FUN_180042180

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180042180`, `0x180042180..0x1800422de`.
- Role: establish the first runtime graph anchor from the strongest direct relation.

## Call Graph

- Direct caller: `FUN_180042840`.
- Direct callee: `FUN_180071d40`.

## Recovered Behavior

It selects the candidate with the greatest direct inlier count. If that count exceeds five, it evaluates image overlap using the direct transform. When overlap exceeds the configured threshold, it stores the candidate index at context `+0x87d8`, marks both the new and selected prior feature active at `+0x110`, marks the graph established at `+0x87e4`, copies the selected direct relation to the caller, and encodes the selected index in the progress word.

Evidence is strongest-edge selection at `0x1800421a2..0x1800421f7`, overlap gate at `0x180042227`, and anchor/activation writes through `0x1800422d6`.

## Confidence And Unresolved

- Confidence: high; the runtime anchor and active transitions match the frozen lifecycle.
- Unresolved: source name and units of the overlap threshold in `param_3[6]`.

## Action-2 Boundary

Ghidra reconfirmation on 2026-07-29 shows that the candidate list is not rooted
at feature 0. The function searches every supplied prior index, chooses the
strict-greatest direct count, and only then writes the selected prior as the
runtime graph anchor. In the action-2 lifecycle this first succeeds at stage 6
for prior feature 1, producing relation
`[12,16,245,65,-3504,-72,255,9906]`, activating features 1 and 5, and setting
graph reference/established to `1/1`.

The former native combiner instead initialized feature 0 as connected and could
not establish a graph whose first admissible edge excluded feature 0. Splitting
first-anchor establishment from established-graph routing reproduces Windows
relations and graph metadata exactly through all eight action-2 stages while
leaving the independent action-7 lifecycle exact.
