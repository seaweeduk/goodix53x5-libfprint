# FUN_180033850

## Identity And Callers

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900.
- Address: `0x180033850`; logged name `_CompareTemplateFeatureSet_Unify`.
- Callers: identify `FUN_180020b30` and verify `FUN_180020600`.
- Calls `FUN_18002e360` with the record's buffer/size, original array index,
  total count, and output matched index.

## Context And Arguments

The engine context is pipeline `+0x38`. A missing context returns
`0x80004003`. Each invocation clears context reject detail `+0x120`, but does
not unconditionally clear match byte `+0x11c`.

Engine-context `+0x198` is forwarded as the identify auxiliary pointer;
`+0x190` supplies the other required caller object. Recognition mode is
booleanized from context dword `+0x94`. Anti-fake mode is context dword
`+0x1c4`. Attach initializes `+0x94` to one and copies anti-fake mode from
configuration byte `+0x423`. See `FUN_18002e360.md` for the final-load matching
boundary. The compiled `IsOpenAntiFake` default is one.

## Status Translation

| Comparison status | Match byte `+0x11c` | Reject detail `+0x120` | Return |
| --- | --- | --- | --- |
| `0` | unchanged | zero | zero |
| `2` | one | zero | zero |
| `3`, `0x8005`, `0x8011`, `0x8013` | zero | zero | `0x80098005` |
| `0x8007` | zero | zero-extended context byte `+0x15d` | `0x80098008` |
| `0x8008` | zero | zero-extended context byte `+0x15c` | `0x80098008` |
| other nonzero | unchanged | zero | `0x8000ffff` |

A nonzero direct return from `FUN_18002e360` also maps to `0x8000ffff`.
The wrapper does not compare scores, reorder candidates, or publish an identity.
