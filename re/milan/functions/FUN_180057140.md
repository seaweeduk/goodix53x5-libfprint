# FUN_180057140

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180057140`, `0x180057140..0x1800572d1`.
- Role: remove affine inliers whose record orientations disagree with the fitted rotation.

## Call Graph

- Direct caller: `FUN_18005b900`.
- Direct callees: `FUN_180074940` and `FUN_180074290`.

## Recovered Behavior

It estimates rotation from the affine linear part using the average column length and `atan2((c-b)/2,(d+a)/2)`. For each retained pair it adds that rotation to `enrolled_orientation - probe_orientation`, measures distance to both the direct and half-period axes, and clears both pair indices when the minimum exceeds `0x506`.

The orientation period is `0x6488`; the half-period is `0x3244`. The helper does not alter the affine.

## Confidence And Unresolved

- Confidence: high; the complete loop and constants are recovered.
- Type-12 unresolved behavior: none. The fixed period and thresholds define the
  arithmetic; the source-level unit name is nonsemantic.

The source-level unit name is not required for parity: period `0x6488`, opposite
axis `0x3244`, and strict rejection above `0x506` are the complete fixed-point
contract. Direct type-12 controls cover retained and rejected orientations.

## Dependency Unit 5 Failure Contract

This helper can reduce an accepted robust model to retained count `0`, `1`, or
`2` without changing the affine or creating estimator failure. Direct controls
force accepted identity plus retained masks `0..4`: returned counts follow the
mask exactly, and all five retain identity. Refinement remains suppressed until
retained count exceeds three. Native model validity must survive this filter even
when retained count becomes zero.
