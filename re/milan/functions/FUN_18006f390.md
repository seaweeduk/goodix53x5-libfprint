# FUN_18006f390

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18006f390`, `0x18006f390..0x18006f6ec`.
- Role: transform source corners, construct a destination-clamped crop, adjust
  affine translation to crop-local coordinates, allocate output matrices, and
  inverse-warp one or two source matrices.

## Call Graph

- Direct callers: `FUN_18006fc80`, `FUN_1800717e0`, and `FUN_180072710`.
- Direct callees: `FUN_1800800e0`, `FUN_180068700`, `FUN_180068bb0`, `memset`,
  and the security-cookie helper.
- `FUN_1800717e0` owns ordinary profile-9/type-12 bitmap overlap.
- `FUN_180072710` owns the optional masked reclassification path.

## Recovered Signature

```c
bool FUN_18006f390(
  matrix_desc *first_source,
  matrix_desc *second_source_or_null,
  const int32_t transform[6],
  matrix_desc **first_warped,
  matrix_desc **second_warped_or_null,
  warp_context *context);
```

The names are semantic. The pointer and output behavior are fixed by the three
call sites.

## Warp Context

The fields consumed or published by this helper are:

```text
+0x00 int32 destination_columns
+0x04 int32 destination_rows
+0x08 int32 source_min_x
+0x0c int32 source_max_x_exclusive
+0x10 int32 source_inset
+0x14 int32 sampling_border
+0x18 int32 origin_x                 (output)
+0x1c int32 origin_y                 (output)
+0x20 int32_t *adjusted_transform    (optional)
```

The profile-9/type-12 ordinary overlap call supplies destination `52x44`, source
X range `[0,52)`, zero inset, and zero sampling border. Its source corners are
therefore `(0,0)`, `(51,0)`, `(51,43)`, and `(0,43)`.

## Crop Geometry

`FUN_1800800e0` transforms the four corners in top-left, top-right,
bottom-right, top-left-column/bottom-row order. The helper takes signed minima
and maxima and clamps them to the inclusive destination rectangle:

```text
minimum_x = max(min(corner_x), 0)
minimum_y = max(min(corner_y), 0)
maximum_x = min(max(corner_x), destination_columns - 1)
maximum_y = min(max(corner_y), destination_rows - 1)
```

It returns false when either inclusive extent is empty. Otherwise:

```text
origin_x = minimum_x
origin_y = minimum_y
warped_columns = maximum_x - minimum_x + 1
warped_rows = maximum_y - minimum_y + 1
```

The transform copied for sampling is made crop-local with wrapped dword
subtractions:

```text
adjusted[2] = transform[2] - origin_x * 0x100
adjusted[5] = transform[5] - origin_y * 0x100
```

The other four words are unchanged. The first output is always allocated on a
successful crop. The second output is allocated and warped only when both its
source and output-pointer arguments are nonnull. Each output is initialized to
`0xff` before `FUN_180068bb0` runs.

## Profile-9 Geometry Controls

| Fixture | Transformed corners TL/TR/BR/BL | Origin | Extent |
| --- | --- | --- | --- |
| `static-0207-overlap` | `(-37,-8) (10,-25) (21,17) (-26,34)` | `(0,0)` | `22x35` |
| `static-0259-overlap` | `(0,-7) (55,-1) (50,46) (-5,40)` | `(0,0)` | `52x44` |
| `static-0320-overlap` | `(0,2) (52,1) (46,40) (-6,40)` | `(0,1)` | `52x40` |
| `static-0376-overlap` | `(25,-20) (74,-26) (81,17) (32,23)` | `(25,0)` | `27x24` |
| `production-full-0357 f38` | `(14,-16) (69,-21) (71,22) (16,27)` | `(14,0)` | `38x28` |

The nonzero-origin controls require adjusted translations `ty=139` for row 320
and `tx=-96` for row 376. Using the unadjusted transform changes every inverse
sample in those crops. The reached feature-38 crop requires adjusted
translation `tx=100` from half-resolution input `tx=3684`.

## Failure And Publication

An empty clamped rectangle returns false before publishing crop origin or an
output matrix. A zero inverse determinant is not a failure here:
`FUN_180068bb0` is void and applies its determinant-zero coefficient state.
Callers must not describe singularity as a `FUN_18006f390` rejection.

## Confidence And Unresolved

- Confidence: high for corner ownership and order, Q8 rounding, inclusive
  clamp, extent arithmetic, translation adjustment, allocation shape, sentinel
  initialization, optional second-plane behavior, and origin publication.
- No profile-9/type-12 geometry behavior remains unresolved.
