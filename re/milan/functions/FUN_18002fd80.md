# FUN_18002fd80

## Identity And Call Graph

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18002fd80..0x18002fe65`.
- Caller in scope: `_CheckUpdateTemplate_E` after positive template packing.
- Callees: CRC initialization/update and fixed copy helpers.

## Behavior

For a nonnull outer update buffer, computes CRC over `size-4` bytes and writes
the four-byte CRC at the tail. It returns true for nonnull input and false for
null input; no size-underflow guard exists. `_CheckUpdateTemplate_E` ignores the
return value. Direct code-2/code-3 adapter controls include this final CRC and
produce deterministic wrapper hashes.

## Confidence

High for range and write location. CRC polynomial naming is not required for the
action/persistence boundary.
