# FUN_180047c50

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180047c50..0x1800481d4`; caller: `FUN_18004c100`.

## Recovered Behavior

For candidate `(x,y,s)`, the center response is the wrapping signed-16
difference `D_s = (int16)(G_(s+1) - G_s)` (`0x180047ca8..0x180047cc2`). It is
eligible only when `abs(D_s) > 0x148`. Neighbor responses are instead computed
as signed 32-bit differences of zero-extended 16-bit samples, without the
center's signed-16 truncation. The function compares the center with all 26
neighbors in the `3x3x3` neighborhood: a positive center requires every
neighbor to be `<= D_s`, and a negative center requires every neighbor to be
`>= D_s`. Equality is therefore retained; only a strictly more positive or
more negative same-direction response rejects the point.

`FUN_18004c100` supplies the profile-9/type-12 scan domain at
`0x18004c1d3..0x18004c36f`: scales `1..3`, rows `6..height-7`, and columns
`6..width-7`, in scale/row/column order. Thus all reads here are in bounds for
the shipping `104x88`, six-plane scale space. Accepted extrema proceed to
`FUN_18004c5a0`. Current source implements the same widths, strict threshold,
non-strict neighbor comparisons, order, and boundaries in
`drivers/goodix53x5/milan/feature/extrema.c:28..71`.

## Confidence And Unresolved

- Confidence: high for the complete profile-9/type-12 extrema predicate,
  integer widths, scan order, and boundaries.
- Unresolved: none on this path.
