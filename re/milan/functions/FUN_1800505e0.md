# FUN_1800505e0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x1800505e0..0x18005072d`, four-direction exclusive run
  counter.

## Call Graph

- Caller in scope: `FUN_18004eba0`.
- The other caller, `FUN_1800503f0`, is outside this auxiliary-value scope.

## Exact Algorithm

All four output byte planes are cleared first. The input is a row-major binary
plane with `rows` passed before `columns`. For each pixel `(y,x)`, outputs are
the consecutive nonzero run lengths adjacent to, but excluding, the current
pixel:

```text
left[y,x]  = x == 0        ? 0 : input[y,x-1] + left[y,x-1]
right[y,x] = x == columns-1? 0 : input[y,x+1] + right[y,x+1]
up[y,x]    = y == 0        ? 0 : input[y-1,x] + up[y-1,x]
down[y,x]  = y == rows-1   ? 0 : input[y+1,x] + down[y+1,x]
```

Arithmetic is byte addition. Profile-9 dimensions 88x108 cannot overflow it.
`FUN_18004eba0` first canonicalizes every nonzero input byte to one.
