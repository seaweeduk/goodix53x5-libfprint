# FUN_180076850

## Identity

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180076850`, `0x180076850..0x180076ae3`.
- Role: affine-guided alternate correspondence builder for `FUN_18005a3c0`.

## Call Graph And Behavior

The direct caller is `FUN_18005a3c0`. It initializes best/second arrays, chooses
special `FUN_1800756b0` or generic `FUN_180074ba0` matching by template type,
then applies `FUN_180075a60` with capacity `31` or `42`. Type 12 uses capacity
`42` and calls `FUN_180074ba0` twice with disjoint foreground-class ranges
before running the shared selector. The first call scans the leading partitions;
the second scans the remaining partitions.

Native now executes the two foreground-partition-bounded scans before the shared
selector. Diagnostic v4 finds exact alternate pairs, affine results, and
post-orientation masks for all `35/35` calls, superseding the former `24/35`
pair result. The separate `187/2` feature-6 defect occurs before this helper.

For `16/1` feature 2, exact-record index normalization shows the 34-pair native
alternate set is the complete 33-pair official set plus only
`(enrolled 0, native-only record 9)`. The builder's two-partition, geometry,
descriptor, ratio, spacing, and capacity rules therefore do not differ. The
extra upstream record changes the primary affine, then the alternate robust and
orientation masks, producing post-orientation count `21` instead of official
`22`.

## Confidence And Unresolved

- Confidence: high for branch selection, capacity, and helper sequence.
- Type-12 unresolved behavior: none. Exact field operations and bounds are
  recovered below; source-level field names are nonsemantic.

## Evolved-Gallery Units 7-9 Controls

Captured official inputs for `197/1 f39` and `197/2 f6` reproduce the complete
alternate pair sets, post-orientation counts 9 and 17, and affines
`[238,45,17491,-46,241,-5847]` and
`[234,84,-11101,-92,233,-2263]`. The first row proves an alternate affine may
win overlap ranking even when its filtered count ties the primary. The second
proves a larger-count alternate may lose affine ranking and must not therefore
be assumed to own the later forward-overlap transform. Both distinctions belong
to caller `FUN_18005a3c0`; this builder remains exact for the supplied inputs.

## Complete Type-12 Partition And Arithmetic Contract

Type 12 always performs two stored-boundary scans:
`[0,enrolled+0x118)x[0,probe+0x118)` and
`[enrolled+0x118,enrolled+0xf0)x[probe+0x118,probe+0xf0)`, then one capacity-42
selection. Direct controls cover empty leading/trailing, one-element leading,
and populated dual partitions. `FUN_1800689a0` wraps determinant at 32 bits,
copies wrapped-singular input, divides signed 64-bit values toward zero, and the
scan uses dword-wrapping affine products. Identity, singular, and negative
nonidentity guides reach both scans with the exact inverse. No type-12 behavior
remains unresolved.

## FUN_18005a3c0 Dependency Unit 1

Structurally valid partition scans may produce zero, one, or two selected pairs.
That is a valid builder result, not a defined affine. The caller must preserve
the selected count for diagnostics if desired, but must not infer affine
validity until `FUN_18005b900` receives at least three pairs and fully defines
its result. The fail-closed contract changes no partition, selector, ratio, or
capacity rule in this builder.

## FUN_18005a3c0 Dependency Unit 5

Selected count at least three proves estimation was attempted, not that a model
was accepted. Degenerate, quality-rejected, or exhausted sets produce wrapper
count zero and deterministic affine zero. The caller therefore needs a separate
model-valid result from the fit stage; this builder's selected count cannot serve
as validity. No partition or selection behavior changes under the unit-5 safety
contract.

## FUN_18005a3c0 Dependency Unit 6

The exact type-12 stored-boundary provenance is the generic arm at
`0x180076947..0x1800769f7`. The first range descriptor loads enrolled/probe
`+0x118` into ends with starts zero; the second reloads the same values as starts
and uses `+0xf0` counts as ends. Both calls share the same physical record arrays
and one capacity-42 selector, so no repartition or cross-class reorder occurs.

Production now threads both stored boundaries into the alternate builder. A
negative or greater-than-record-count boundary is invalid before this helper;
the native half-open ranges additionally reject begin/end violations. This is a
fail-closed safety boundary, not DLL crash parity. Contradictory foreground bytes
do not redefine the ranges. Wrapped determinant, signed 64-bit inverse division,
dword scan products/additions/shifts, negative values, and the signed division
overflow guard are covered by the unit-6 gate. Invalid or overflowed state emits
no alternate pairs and cannot become a model, rank owner, promoted count, score,
or downstream candidate.

## Distance-Matrix Input

The type-12 builder borrows the distance matrix previously filled by
`FUN_180078a70 -> FUN_180076400` in the same `FUN_18005a3c0` invocation. Both
stored physical partition scans pass that matrix to `FUN_180074ba0`; the layout
matrix is not an input to this alternate path. The matrix remains caller-stack
storage and is not mutated, retained, or serialized here. See
`re/milan/MATCH-MATRIX-CONTRACT.md`.
