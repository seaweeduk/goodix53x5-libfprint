# FUN_180077e20

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180077e20`, `0x180077e20..0x180078741`.
- Role: bounded three-point robust affine estimator.

## Call Graph

- Direct caller: `FUN_18005b900`.
- Relevant callees: `FUN_1800683d0` and `FUN_180080130`.

## Recovered Behavior

It enumerates geometrically plausible correspondence triangles, checks edge-length
ratios and wrapped orientation consistency, builds a three-point affine, and
scores all pairs with per-axis `<0x281` and squared-distance `<0x64000` gates. It
retains the model with the most inliers, breaking ties by lower mean residual,
and stops early above 20 inliers. The `0x3b2` threshold is checked only at an
outer-loop boundary, so it is not an absolute triangle-call cap: a fixed 42-pair
mode-1 control reaches that check after 1,188 accepted triangle solves.

Profile-9 mode uses period `0x3244`, half-period `0x1922`, and orientation tolerance `0x400`. `FUN_180080130` validates the affine with bounds `401` and `163`.

## Confidence And Unresolved

- Confidence: high for enumeration bounds, geometry gates, tie break, and output mask.
- Type-12 unresolved behavior: none; the operational argument values are fixed
  below. Source names are not behavioral evidence.

For the production caller these are now resolved operationally: argument 9 is
minimum accepted inlier count 2 and argument 10 selects period/half-period/
tolerance `0x3244/0x1922/0x400`. Entry initializes residual `0x190000`; the
no-model epilogue writes six affine zeros. It does **not** initialize the supplied
42-byte output mask on failure. Caller `FUN_18005b900` owns zero initialization
of that private mask before this call. A direct no-model call with poisoned mask
therefore preserves the poison, while the production wrapper observes mask zero.
Exact count/residual ties retain the earlier triangle. Source-level names outside
type 12 remain irrelevant; the type-12 contract is complete.

## Dependency Unit 5 Failure Contract

The estimator has no return status. Its output families are:

- no accepted model: residual `0x190000`, six affine zeros, caller-owned mask;
- accepted model: fitted affine, accepted inlier mask, and rounded residual;
- early success: the same accepted outputs when best inlier count exceeds 20.

Degenerate solves, coefficient pre-gate rejection, `FUN_180080130` rejection,
and search exhaustion all converge on the same no-model output. The estimator
temporarily writes a candidate affine before the validator call, but if no model
is accepted the epilogue overwrites it with zero. Model validity must therefore
be carried explicitly by native code; neither all-zero, identity, residual, mask,
nor later retained count is a validity test.

Ghidra reports 109 blocks, 602 instructions, and 66 conditional branches in
`0x180077e20..0x180078741`. Profile-9/type-12 fixes count to `3..42`, minimum
inliers to 2, and mode to 1. Count-below-three entry, mode-zero outcomes, and the
bounds panic at `0x18007873d` are statically excluded. Direct non-campaign
controls cover collinear no-model, validator rejection, outer-loop exhaustion,
identity/translated success, and mask/residual/affine poisoning. Evidence is
under `fun-18005a3c0-unit5-failure-contract-v1` in the persistent derived root.

## Native Implementation

Native captures `model_valid` when the robust search has selected a model and
before orientation filtering can reduce its inlier count to zero. No-model keeps
the deterministic zero affine/residual state and remains invalid; refinement does
not change validity. Focused native controls cover attempted no-model, accepted
identity, accepted orientation-filtered zero, and atomic refinement failure under
`fun-18005a3c0-unit5-native-v1`.
