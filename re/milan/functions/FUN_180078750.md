# FUN_180078750

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180078750`, `0x180078750..0x180078a63`.
- Role: build direct descriptor candidates for two feature objects.

## Call Graph

- Direct caller: `FUN_180041ac0`.
- Direct callee: `FUN_180075a60`.

## Candidate And Selection Contract

For each source record in physical order, the function compares record bytes
`16..39` against every target record. All six 32-bit descriptor words, 192 bits,
participate through the DLL popcount table. Best and second distances initialize
to 192 and the best index to `-1`. Updates are strict: an equal best leaves the
earlier target as best and may become second, while an equal second leaves the
earlier second.

The XOR/popcount loop is at `0x180078859..0x1800789a6`. The function initializes
42 output slots at `0x1800789ed`, then calls `FUN_180075a60` through
`0x180078a56` with capacity 31. The selector applies strict ratio, signed-dword
spatial collision, replacement/pruning, and capacity policy, then publishes
compact `(source,target)` pairs with `-1,-1` padding.

## Failure Contract

The DLL allocates nothing and has no recoverable failure path. Feature-object
counts are signed and unchecked: nonpositive counts leave the output unset,
while a positive source count beyond the fixed 180-entry best/index storage can
corrupt the native stack. Current `goodix_milan_estimate_relation()` validates
non-null arrays and counts `1..150` before reaching its translation, so this
unchecked surface is malformed-only for profile 9.

## Confidence And Unresolved

- Confidence: high for descriptor layout, scan and tie order, capacity 31,
  output orientation, and malformed-count ownership.
- Unresolved: semantic names of the two ratio constants passed to the selector.
