# FUN_180073ba0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180073ba0..0x180073d08`.
- Caller: `FUN_180072860`.
- Callees: `FUN_180073e30` and `FUN_1800732c0`.
- Role: construct the class-partitioned spatial pairs and alternative affine used by direct refinement.

## Recovered Behavior

The helper runs `FUN_180073e30` once for each foreground partition, appends the second partition after the first, and rebases both record indices by their partition starts. It copies the input affine to the output and replaces it with the `FUN_1800732c0` similarity fit only when at least three pairs exist.

## Confidence And Unresolved

- Confidence: high for partition ordering, index rebasing, pair threshold, and fit call.
- Native row `142/1` is exact after correcting the upstream coverage-derived
  record-selection limit. No helper-chain correction was required.

## 2026-07-28 Stored-Boundary Correction

Both `FUN_180073e30` calls are parameterized by stored feature boundaries at
`+0x118`. The second call starts at `records + boundary * 0x38`, and its emitted
record indices are rebased by the enrolled and probe stored boundaries before
the optional fit. Native reproduces the append/rebase/fit order but chooses the
ranges from record `foreground` bytes. Exact row `142/1` proves only the reached
equal-boundary context; contradictory metadata and reorder remain divergent.

## 2026-07-28 Native Implementation

`milan_refine_record_similarity()` now takes enrolled/probe stored boundaries
as explicit inputs. Its first and second scans use the corresponding half-open
ranges, preserve append order, and retain physical index rebasing by those same
boundaries. Contradictory foreground bytes and within-partition physical reorder
no longer reconstruct or regroup the ranges. Malformed boundaries fail before
pair or refined-affine publication.
