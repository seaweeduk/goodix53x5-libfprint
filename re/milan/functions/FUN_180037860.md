# FUN_180037860

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180037860..0x180037a20`.
- Role: destroy one complete live template object.

## Call Graph

- Callers: exported `templateDelete` and decoder failure cleanup in
  `FUN_18003e3a0`.
- Relevant callees: `FUN_180037b10`, `FUN_180068b70`, and `free`.

## Recovered Behavior

The helper destroys every populated gallery feature pointer from `+0x28` up to
the configured maximum at `+0x20`, then calls `FUN_180037b10` on all 20 cache
feature owners at `+0x8d10`. Null feature pointers are accepted. Finally it
frees the `0x8e08` live object and clears the caller's owning pointer.

This same owner is used by exported deletion and by `FUN_18003e3a0` failure
unwind. The public eight-byte handle remains the wrapper's responsibility; this
helper receives only an address of the internal owning pointer.

The cache cleanup does not inspect ranks at `+0x8db0` or state at `+0x8e00`.
Those fields govern use and allocation, not ownership: any allocated cache slot
is always owned by the live template and dies with it.

## Evidence And Confidence

- Gallery-feature loop: `0x180037879..0x1800379c8`.
- Fixed 20-slot cache-owner loop: `0x1800379d4..0x1800379ef`.
- Confidence: high for complete live-template and cache-slot ownership.
- No unresolved profile-9/type-12 behavior remains.
