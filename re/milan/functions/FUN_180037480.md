# FUN_180037480

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180037480`, `0x180037480..0x18003768e`.
- Role: in-place partition of feature records by low flag bits.

## Call Graph

- Direct callers: `FUN_180037690` and `FUN_18004ae70`.
- Direct callees: none.

## Recovered Behavior

It independently partitions the primary record array at `+0xf8` and optional secondary array at `+0x100`. Each record is `0x38` bytes. A two-ended scan moves records whose low two flag bits equal zero to the front and records whose bits equal one away from the front, then writes the primary partition count to `+0x118` and the secondary count to `+0x10c`.

Evidence is the full leaf body: primary partition `0x180037491..0x1800374f3`, secondary partition `0x18003750d..0x180037681`.

## Confidence And Unresolved

- Confidence: high; the native partition reproduces all 8,400 record bytes and the nine-record flag-zero prefix.
- Unresolved: semantics of low-bit values other than zero and one in unobserved modes.

## Sequence-72 Control

The discarded kernel-0 first pass reaches this unchanged partition with 54
records and split `28/26`; the selected kernel-100 retry reaches it with 48 and
split `24/24`. Suppressing only the retry makes the complete post-partition
54-record stream byte-identical to native, while the native kernel-100
projection makes the complete 48-record stream byte-identical to official.
Therefore partitioning changes neither count and is not the first divergence.

## FUN_18005a3c0 Dependency Unit 6 Consumer Contract

The primary count written at feature `+0x118` is authoritative matcher metadata,
not a hint to be reconstructed from later record bytes. Native extraction now
retains this value in `GoodixMatchInfo`, copies it with live features, and passes
it through ordinary and live-gallery profile-9/type-12 matching. Packed feature
parsing continues to expose the same value as tagged scalar index 2. Matcher-side
validation accepts the complete range `0..record_count` and rejects negative or
greater values before candidate construction.
