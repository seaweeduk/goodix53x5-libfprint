# FUN_18003c520

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18003c520..0x18003c675`.
- Sole caller: `FUN_18003a3e0` at `0x18003a487`.
- Callee: affine inverse `FUN_1800689a0` at `0x18003c556`.

## Behavior

This helper constructs the first-object-domain packed support intersection for
anti-fake pair comparison. It copies the first packed mask, obtains the inverse
Q8 transform from `FUN_1800689a0`, and visits exactly `rows * columns` bits.
Each active first-domain coordinate is inverse-mapped with wrapping dword
multiply/add, wrapping `+0x80`, and arithmetic shift right eight. The helper
clears that first-domain bit when either mapped coordinate is outside the
half-open image bounds or the corresponding second-mask bit is zero.

Profile 9 supplies `88x104`. The helper reads neither candidate records nor
anti-fake scalars. It copies `(rows * columns + 7) >> 3` bytes, then visits
bits in row-major order, least-significant bit first within each byte. Only
the separate output mask is modified; neither input mask nor the transform is
written. Signed bounds reject negative coordinates and coordinates equal to
the respective dimension. The rounded mapping is `sar32(wrap32(q8 + 128), 8)`,
including negative half-pixel inputs; it is not truncation toward zero.
