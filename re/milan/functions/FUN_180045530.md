# FUN_180045530

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180045530..0x1800456a4`.
- Role: choose the primary capacity replacement candidate.

## Call Graph

- Caller: `FUN_1800469f0`.
- Callee: `FUN_180046780` on the flag-one candidate-validation path.

## Recovered Behavior

The matched feature's live `+0x128` field is the first branch. If it is zero,
the helper returns the matched feature immediately. Otherwise it considers
features whose `b5` at `+0x110` is nonzero, excluding the live reference index
at template `+0x87d8`, and ranks them lexicographically by:

1. lowest live `+0x128`;
2. lowest coverage at `+0x120`;
3. highest derived residual metric at `+0x148`.

Every comparison is strict, so a complete tie keeps the earlier feature index.
The scan does not seed itself from the first eligible feature. Its initial
ranking tuple is residual `0xfffff`, coverage `0xfffff`, overlap `0`, with
selected index `-1`. An eligible feature must strictly beat that tuple before
it can become the candidate. Normal profile-9 normalized values are far below
the sentinels, but a generic implementation must preserve this initialization
or explicitly validate equivalent field bounds.

When the evidence flag supplied by `FUN_1800469f0` is zero, the ranked
candidate is retained only if its `+0x128` is zero and at least three active
features have `+0x128 == 0`. When the flag is one, `FUN_180046780` computes the
probe residual not covered by the other active gallery features. Template
counter `+0x8cf8 > 700` adds 20 to that result. The candidate is retained when
the adjusted result is greater than or equal to its `+0x128`; strict less-than
rejects it.

The `+20` is a 32-bit `ADD` at `0x18004566b`, followed by a signed comparison.
There is no saturation or overflow guard. Normal mask residuals are bounded, but
an exact generic translation must use wrapped 32-bit arithmetic or validate an
equivalent input range.

The flag-zero count uses exact `b5 == 1` at
`0x180045620..0x180045635`, while scan eligibility uses `b5 != 0` at
`0x1800455a3..0x1800455aa`. These predicates are equivalent for normal
Boolean-produced type-12 state, but they are not interchangeable in the generic
contract.

The function returns the selected index or `-1`. It does not assign the study
action or mutate the selected feature.

## Evidence And Validation

- Matched-zero fast path: `0x180045544..0x180045557`.
- Candidate scan and ordering: `0x18004558e..0x1800455f4`.
- Flag-zero three-candidate rule: `0x1800455ff..0x180045647`.
- Flag-one residual validation and strict counter boundary:
  `0x18004564c..0x180045681`.
- The persistent action-policy controls exercise a direct flag-one code-4
  result and a direct flag-zero code-2 result without relying on a campaign row:
  `<evidence-root>/derived/milan-native-capture-20260723/study-action-policy-v1/action-controls.jsonl`.

## Confidence And Unresolved

- Confidence: high for all branch order, fields, comparisons, and strict
  boundaries from decompile, assembly, and direct DLL controls.
- Semantic vendor names for `+0x128` and `+0x148` remain unknown.
- The corrected natural corpus proves 286 final direct code-4 actions but does
  not preserve the original live-only `+0x148` values; a post-unpack recreation
  must not be treated as chronological target-selection authority.
- Native capacity selection derives residual and overlap state generically from
  current masks, reference relations, and transforms. The 798-row projection
  validates action identity only and deliberately carries no target row IDs;
  target-state byte parity remains bounded by chronological replay.

## Normalized-BB Selection Control

Unit-4 Ghidra revalidation confirms there is no hidden candidate exclusion in
this helper. The complete order remains active nonreference features by lowest
`bb`, lowest coverage, then highest `+0x148`, with strict comparisons and earlier
complete ties. On the synthetic-valid full-capacity discriminator, stale values
select feature 13. Caller normalization creates multiple zero-`bb` candidates;
feature 34 then wins because its coverage 88 is the lowest in that zero-`bb`
set. Dispatcher and explicit refresh/normalize/selector staging both return 34,
while direct selector entry returns 13. This control proves the input ordering;
it adds no selector threshold or row rule.
