# FUN_1800740f0

## Identity

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800740f0`, `0x1800740f0..0x18007419b`.
- Role: simpler four-class overlap score used when mode is zero.

## Recovered Behavior

This is the mode-zero score leaf selected by
`FUN_1800717e0:0x1800719bf..0x1800719df`. Its arguments are signed 32-bit
`valid_count`, `full_count`, class-0 count, class-3 count, and a three-word
weights pointer. For nonzero valid count it uses the same wrapping 32-bit
arithmetic and signed divisions as `FUN_1800731d0`:

```text
half = valid_count >> 1
numerator = half + class0 * 256
raw = valid_count > (full_count >> 1)
        ? weights[0] + 38 + numerator / valid_count
        : numerator / (valid_count + 1)
          + (valid_count * 19) / (full_count >> 1)
          + weights[1] + 19
quality = (half + class3 * 256) / valid_count + weights[2]
```

It returns `raw` only for `quality > 23` or
`raw > 230 && quality > 16`; every comparison is signed and strict. It returns
`128` without reading weights when valid count is zero. This leaf has no raw
score, confidence, or context-count output. It trusts all scalar inputs; a
negative count entering the low-valid arm with `full_count >> 1 == 0` raises a
native division exception. Production profile-9/type-12 supplies class totals
from `FUN_180073540` and a positive `44*52` full area.

For valid classifier-produced inputs, current `score.c` matches this mode-zero
arithmetic and both strict acceptance predicates; no production change is
required. Its public helper rejects malformed dimensions, classes, and required
pointers instead of reproducing native faults.

## Confidence And Unresolved

- Confidence: high for both raw-score arms and all acceptance equalities.
- Unresolved: semantic names of the three weight fields.
