# FUN_180081d30

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x180081d30..0x180081e92`, masked candidate correlation.

## Call Graph

- Sole caller: `FUN_18006e7f0`.
- Substantive callees: `FUN_180082130` and `FUN_180081fc0`.

## Behavior

- Intersects each candidate bytewise with the supplied mask into a separate
  stack plane. For each position whose original mask byte is zero, it then adds
  `122` to the zero intersection byte; all other intersections are retained.
- Thus the production `0/255` mask preserves admitted candidate bytes and
  replaces excluded bytes with `122`. A noncanonical nonzero mask byte instead
  bitwise-truncates the candidate and does not receive the replacement value.
- It computes integer Pearson correlation over all `rows * columns` bytes,
  including the replacement pixels, and scales the result by `256`. The means
  use integer truncation, and the denominator uses integer square root
  `FUN_180074980`.
- Both temporary outputs are disjoint from the candidates and mask in the sole
  production caller, so byte-plane aliasing is not part of the profile-9 path.

## Evidence

- Candidate/mask intersections: `0x180081d9e..0x180081dcc`.
- Excluded-pixel replacement: `0x180081dd1..0x180081e4c`.
- Correlation call and return: `0x180081e4e..0x180081e71`.

## Confidence

High.

## Unresolved

- None for the profile-9 candidate-selection path.
