# FUN_18004a6d0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18004a6d0..0x18004a97c`.
- Role: rank and retain materialized feature candidates up to the caller's
  dynamic record-selection limit.

## Call Graph

- Caller: `FUN_180049b90`.
- Callees: integer-pair sorting, `memcpy`, stack/security helpers.

## Recovered Behavior

When candidate count does not exceed the selection limit, it copies candidates
and auxiliary state in original order. When count exceeds the limit, it sorts
candidate indices by the signed strength word at record offset `+8`, retains up
to the limit through the profile's staged eligibility rules, copies records and
24-byte auxiliary entries, and encodes retained order in the record foreground
word before later partitioning.

For authoritative profile-9 row `142/1`, 140 materialized candidates exceed the
coverage-derived limit 135. Entering this path produces the exact Windows order,
fields, and descriptors; treating storage capacity 150 as the limit bypasses the
sort and produces the pre-fix 140-record object.

Row `16/1` proves that the type-12 coverage-derived limit is truncated upstream:
coverage 79 and configured maximum 150 produce 118, not nearest-rounded 119.
The 118 official output records are an exact subset of native's 119 records.
This function consumes the supplied limit; no row-16 ranking or copy-rule change
is authorized.

## Confidence And Unresolved

- Confidence: high for the count/limit branch, strength ranking, copy order, and
  profile-9 `142/1` result.
- Unresolved: semantic names for the staged auxiliary eligibility fields used by
  profiles whose candidate count greatly exceeds the limit.

## Sequence-72 Negative Control

Passive hooks at both call sites show that sequence 72 does not enter this
helper. Its type-specific extraction branch performs the below-limit copy
inline, and both candidate counts `54` and `48` are below limit `75`.
Consequently this helper is not the `54/48` surface. The earlier row-142 result
still establishes the externally observed 140-to-135 strength-ranked stream,
but its exact call-site attribution should not be generalized to the sequence-72
type branch without a fresh hook.
