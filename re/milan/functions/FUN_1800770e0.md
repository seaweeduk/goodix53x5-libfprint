# FUN_1800770e0

## Identity

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800770e0`, `0x1800770e0..0x18007738f`.
- Role: fill best/second descriptor candidates for selected record-index lists.

## Call Graph And Behavior

`FUN_180075220` is the direct caller. The helper consults a precomputed layout matrix, compares the selected descriptor tail orientation, thresholds Hamming distance, and updates per-source best/second distance and target index arrays.

## Confidence And Unresolved

- Confidence: high for list traversal, matrix indexing, layout meaning, tail
  offsets, threshold, and best/second updates.
- Unresolved: semantic meaning of caller-selected record classes 1 and 2 belongs
  to `FUN_180075220`, not this consumer.

## 2026-07-28 Complete Sibling Input Contract

The two byte matrices are independent borrowed caller inputs, indexed by
`source_index * 180 + target_index`. The first rejects the pair when its signed
byte is `-1`. The second chooses the target descriptor tail: zero selects record
offset 48, nonzero selects offset 40. The source tail always begins at offset
40. Two 32-bit words are compared, distance must be at most the internal scan
descriptor's word 8, sourced from profile configuration word 5 (`16`), and
strict best/second updates are written into the caller arrays.

`FUN_180075220` reuses these matrices across both stored physical partitions and
both class directions. Native currently has no equivalent matrix parameters in
its sibling API and recomputes pair eligibility/layout from records. Exact
sequence-100 inputs prove this changes raw class sets from production `4/7` to
native `7/10` before robust fitting. Confidence is high for consumption and
indexing.

## Producer And Direct-Control Closure

The first matrix's non-sentinel byte is not used as a distance by this helper;
direct DLL controls prove values 0 and 111 behave identically. Layout selects
the only tested target tail, and inclusive tail 16 is accepted while 17 is
rejected for both layout values. Producer, stack lifetime, queue/persistence
provenance, and malformed-input handling are complete in
`re/milan/MATCH-MATRIX-CONTRACT.md`.

## Native Implementation Closure

`milan_cross_class_descriptor_distance()` now receives the limit threaded from
the caller's profile scan configuration rather than comparing the selected tail
to `MILAN_MATCH_DESCRIPTOR_DISTANCE_LIMIT`. The producer still owns 23/47 and
layout selection; this consumer still treats only sentinel `-1` as ineligible,
uses the producer-selected target offset, and performs strict best/second
updates. Native boundaries 15/16/17/22/23/24 and configuration values
15/16/23/24 pass for both layouts. Profile9/type12 supplies 16.
