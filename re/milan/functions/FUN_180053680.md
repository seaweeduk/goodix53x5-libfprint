# FUN_180053680

## Binary And Body

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180053680` / `0x180053680-0x180053ead`.
- Inferred signature: `void f(metrics, config, flag_a, flag_b)`.
- Caller: `FUN_180053220`; callees: `FUN_180070d90`, `FUN_180057e60`.

## Behavior

This post-classifier veto handles the `0x413000` type family when the first
dispatcher flag is still zero. It derives count and bitmap/detail combinations,
decodes the packed mode at `config[0x12]`, and clears both flags for multiple
mode- and count-dependent rejection families. Surviving type-12 candidates are
passed to `FUN_180057e60` for another veto.

### Raw packed-mode clear at `0x180053868..0x1800538ae`

After the preceding `metrics[14] == 1` family, this branch runs only while
`flag64` remains nonzero. It clears both flags when all of the following hold:

```text
config[18] != 0
metrics[9] > 200
combined = int32_wrap(
  metrics[8] + metrics[5] - config[0]
    - 4 * (metrics[12] + metrics[13] + metrics[14])
)
signed(combined) < 405
(
  (metrics[10] < 45 && metrics[11] < 15 && metrics[70] > 16)
  || metrics[4] == 128
)
```

The test at `0x180053872` is against the raw packed word. Unlike the earlier
family at `0x180053700..0x1800537ba`, it does not call `FUN_180070d90` and does
not require a particular decoded low or high class. The clear at
`0x1800538a4..0x1800538aa` also preserves mutable recognition mode
`config[19]`; only the two flags are written. The combined expression uses
32-bit dword arithmetic, so intermediate overflow wraps modulo `2^32` before
the final signed comparison.

## Evidence And Confidence

- Both output words are cleared together throughout the body; `config[0xf] == 0xc` gates the final helper.
- Direct DLL boundary replay isolates the raw packed-mode family with
  `config[18]=512`, `metrics[9]=201`, adjusted combined value `404`, and the
  `metrics[4]=128` alternative. It clears `flag60/flag64` from `0/1` to `0/0`
  while preserving `config[19]=1`. Controls at `config[18]=0`,
  `metrics[9]=200`, adjusted combined value `405`, and `metrics[4]=127/129`
  preserve `0/1`, confirming the strict comparisons and equality sentinel.
- The production type-12 trace reaches this helper on 39 of 58 dispatcher calls
  when `rsp+0x64 != 0 && rsp+0x60 == 0`; 18 continue into `FUN_180057e60`.
  An early rejection here can also clear mutable `config+0x4c`, so the later
  eligibility guard must observe post-helper configuration state.
- Confidence is high for the veto chain, medium for field roles.
- The profile-9/type-12 threshold expressions now reproduce all `39/39` frozen
  entries and the exact 18-call continuation occupancy. This is not a claim for
  unobserved profile/type families.

## Clean-Room Comparison

The clean-room `post_veto()` translates the preceding and following rejection
families but omits the raw packed-mode clear. With the retained
sequence-70/enrollment-1 feature-7 input adjusted only to `metrics[4]=128`,
`metrics[9]=202`, and `metrics[12]=1`, direct dispatcher results are:

| Implementation | `flag60` | `flag64` | `config[19]` |
|---|---:|---:|---:|
| DLL | 0 | 0 | 0 |
| Clean room | 0 | 1 | 0 |

Controls at `metrics[9]=200`, adjusted combined value `405`, and
`metrics[4]=127/129` are exact between the DLL and clean room. A raw packed word
of zero is also exact, although the earlier classifiers then produce `1/1/1`.
The original 58-call gate therefore remains valid for its frozen values but did
not cover this conjunction.

Each replacement value is producer-observed, but their composition is a
synthetic boundary tuple. The frozen candidate flow produces `metrics[4]=128`
in 13 features. The frozen dispatcher table contains `metrics[9]=202` at
sequence 146 enrollment 1 feature 4 and `metrics[12]=1` at sequence 195
enrollment 1 feature 4. Packed mode `512` is the natural sequence-70 policy
input. The strict-neighbor value `metrics[9]=201` used by the direct helper test
is not present in that dispatcher table and is not used for the natural-impact
claim.

Complete one-gallery study replay establishes the branch's observable effect.
The unmodified DLL run scores `53`, writes after-match SHA-256
`4cd25a1214c171576fb6e5ff4eda01a14a2ddcbd502ef69755c577af21bf4edb`,
and writes final candidate SHA-256
`7c8997cd42c5c79c656ca4561ab162857bba1c0b668a7e6c7c2ddc80d4c0e27e`.
Mutating the target once to the tuple above reaches this helper and its clear,
scores `92`, and changes those hashes to
`810cc40345912c39707726ccdddebdd8d5a1188590bf015aba1c58f936fb0e47`
and `36c9d79c7ce4c9e3b8f8f4d495eec56d07cb403534451517c4982d3a0d9f6787`.
Skipping only the clear instruction restores score `53` and both baseline
hashes. The clean-room baseline and once-mutated runs likewise remain at score
`53` with both baseline hashes, consistent with the omitted branch.

All three DLL runs retain matched index `0`, quality `88`, coverage `100`,
recognition mode `1`, active records `113` partitioned `57/56`, study action
`1`, queue occupancy `0/0/0`, and complete preprocess/extract/study lifecycles.
The target does not enter late eligibility. The branch therefore changes the
match score and gallery mutation while leaving queue and lifecycle routing
unchanged for this witness.

The corresponding clean-room contract belongs after the `metrics[14]` family
while `flag64` remains nonzero. It tests raw `config[18]`, retains every strict
bound and 32-bit arithmetic rule above, clears both flags, and does not write
`config[19]`.

## Natural Reachability Status

No complete natural witness is retained for this clear. The 58-call native
dispatcher authority has no row satisfying the complete metric conjunction;
its producer-observed values support the synthetic composition above, not a
same-candidate natural hit.

Read-only observation at the exact clean-room insertion point, after the
preceding `metrics[14]` family and while the candidate flag is still live,
provides a wider negative search without feeding observed state back into the
runner. The sealed 643-operation authority reaches that point 322 times. None
of those states takes either final route: raw `metrics[4]` spans `206..234`, and
no state combines `metrics[10] < 45`, `metrics[11] < 15`, and
`metrics[70] > 16`. Six states have `metrics[9] > 200`; three also have nonzero
packed mode and adjusted combined value below 405, but all fail the final route.

The complete retained parity work area contains 2,843 corpus copies and 2,573
distinct case-input contracts after accounting for byte-identical duplicates
and colliding legacy case IDs. Current-source replay reaches the insertion point
1,034 times with no complete conjunction. In this broader current-only set,
raw `metrics[4]` spans `199..236`. Four states satisfy the three-metric route,
but their `metrics[9]` values are only `106`, `114`, `145`, and `192`; the five
states with nonzero packed mode, `metrics[9] > 200`, and adjusted combined value
below 405 all fail the final route. This wider scan is supporting clean-room
coverage, not proof that the native producer emits identical candidate state.

The authoritative transaction result remains exact production head `c182b140`
against the approved DLL for all 643 replayable operations and 1,104 gallery
rows, including score, acceptance, after-match bytes, winner, candidate, queue,
action, and lifecycle outputs. The report is
`<parity-state>/reports/candidate-admission-c182b140-natural-643-20260820.json`
(SHA-256
`f5a4b6bd90def01f58b1b93deba27492415587b4feb864a423babe56a1fa63ff`).
Corpus absence does not prove the branch unreachable, but there is no valid
natural branch or output witness. The omission therefore remains a no-change
observation and does not authorize a clean-room source correction.
