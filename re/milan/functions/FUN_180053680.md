# FUN_180053680

## Binary And Body

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180053680` / `0x180053680-0x180053ead`.
- Inferred signature: `void f(metrics, config, flag_a, flag_b)`.
- Caller: `FUN_180053220`; callees: `FUN_180070d90`, `FUN_180057e60`.

## Behavior

This post-classifier veto handles the `0x413000` type family when the first
dispatcher flag is still zero. It derives count and bitmap/detail combinations,
decodes the packed mode at `config[0x12]`, and clears both flags for multiple
mode- and count-dependent rejection families. Surviving type-12 candidates are
passed to `FUN_180057e60` for another veto.

### Raw packed-mode clear at `0x180053868..0x1800538ae`

After the preceding `metrics[14] == 1` family, this branch runs only while
`flag64` remains nonzero. It clears both flags when all of the following hold:

```text
config[18] != 0
metrics[9] > 200
combined = int32_wrap(
  metrics[8] + metrics[5] - config[0]
    - 4 * (metrics[12] + metrics[13] + metrics[14])
)
signed(combined) < 405
(
  (metrics[10] < 45 && metrics[11] < 15 && metrics[70] > 16)
  || metrics[4] == 128
)
```

The test at `0x180053872` is against the raw packed word. Unlike the earlier
family at `0x180053700..0x1800537ba`, it does not call `FUN_180070d90` and does
not require a particular decoded low or high class. The clear at
`0x1800538a4..0x1800538aa` also preserves mutable recognition mode
`config[19]`; only the two flags are written. The combined expression uses
32-bit dword arithmetic, so intermediate overflow wraps modulo `2^32` before
the final signed comparison.

## Evidence And Confidence

- Both output words are cleared together throughout the body; `config[0xf] == 0xc` gates the final helper.
- Direct DLL boundary replay isolates the raw packed-mode family with
  `config[18]=512`, `metrics[9]=201`, adjusted combined value `404`, and the
  `metrics[4]=128` alternative. It clears `flag60/flag64` from `0/1` to `0/0`
  while preserving `config[19]=1`. Controls at `config[18]=0`,
  `metrics[9]=200`, adjusted combined value `405`, and `metrics[4]=127/129`
  preserve `0/1`, confirming the strict comparisons and equality sentinel.
- The production type-12 trace reaches this helper on 39 of 58 dispatcher calls
  when `rsp+0x64 != 0 && rsp+0x60 == 0`; 18 continue into `FUN_180057e60`.
  An early rejection here can also clear mutable `config+0x4c`, so the later
  eligibility guard must observe post-helper configuration state.
- Confidence is high for the veto chain, medium for field roles.
- The profile-9/type-12 threshold expressions now reproduce all `39/39` frozen
  entries and the exact 18-call continuation occupancy. This is not a claim for
  unobserved profile/type families.

## Producer Reachability Contract

The `metrics[4] == 128` route removes any dependency on words 10, 11, and 70
from the final disjunction, but all predicate fields still come from the normal
profile-9/type-12 candidate pipeline:

| State | DLL producer | Current producer | Domain and coupling |
|---|---|---|---|
| `metrics[4], [5], [9]` | `FUN_180071d40`, called at `FUN_180055a40:0x180056012`; stores at `0x18005601d`, `0x180056028`, and `0x18005603b..0x180056048` | `goodix_milan_match_overlap_metrics_with_context()` at `match.c:1028`, published at `match.c:1047..1051` | One `52x44` high-bitmap classification, bounded by 2,288 accepted pixels, supplies three signed dwords. Word 4 is a selected raw score or exact sentinel `128`; word 5 is the coupled class-detail result; word 9 is `floor(coverage_q8 * 246 / 256)` in `0..246`. |
| `metrics[8]` | `FUN_180070c70 -> FUN_18006fc80`, call `0x180056267` | `goodix_milan_match_low_bitmap_metrics()` within the same overlap wrapper | The enrolled/probe `52x44` low bitmaps, `26x22` packed inline masks, and selected affine jointly produce words 6..8 in `0..256`. |
| `metrics[10], [11], [66..70]` | `FUN_180074e80` under `FUN_18005a3c0`/`FUN_180057900`, with optional `FUN_180072860` replacement | `goodix_milan_match_record_metrics_internal()` and `goodix_milan_match_candidate_set_record_metrics()` | One record set of at most 150 records and one affine produce valid/matched counts, topology `0..100`, filtered/valid geometry, and average descriptor distance `0..64`. Words 10/11 copy words 68/69. |
| `metrics[12..14]` | `FUN_180070e90`, direct caller site `0x180055fb1`; stores at `0x18005604d..0x18005607e` | `goodix_milan_match_affine_penalties()` | Three one-bit penalties from the same affine: average scale outside `[234,281]`, Q16 column dot at least `0x147b`, and Q16 dot at least `0x28f6`. |
| policy-entry flags | second `FUN_180054ec0` call at `0x18005622b`, then the earlier `FUN_180053220` stages | `goodix_milan_match_initial_flags()` at `match.c:1168`, then `goodix_milan_matcher_policy_evaluate()` | Dword flags use states 0/1/2 and depend on counts, overlap/detail/coverage, topology/geometry, penalties, image quality/coverage, and configuration. The branch sees the post-first-veto pair. |
| `config[0]` | `FUN_18005e230` profile-9 initializer | `goodix_milan_matcher_policy_init()` | Signed threshold dword; profile 9 supplies zero. |
| `config[18]` | ordinary caller `FUN_18005edb0:0x18005ee09..0x18005ee1b` copies probe `c7/+0x150` | `match.c:839..840` copies probe `optional_c7` | Raw signed dword; `0`, `0x100`, and `0x200` are ordinary type-12 values. The predicate tests the raw word, not a decoded class. |
| `config[19]` | `FUN_18005edb0:0x18005ee10` initializes one | profile-9 initializer word 19 | Mutable dword recognition mode, ordinarily 0/1. This branch reads no decoded mode and does not modify it. |

A bounded construction exercises the current translations of those producers
without assigning final metric words. It uses 22 valid enrolled/probe records
with unique descriptors and coordinates related by affine
`[222,0,0,0,245,0]`. The correspondence and robust-affine producers return 22
pairs, 22 filtered pairs, and that exact affine. Record metrics are
`22,22,100,100,0` in words 66..70; words 10/11 are `100/100`. The affine has
average scale `233`, so words 12..14 are `1,0,0`. Decoded packed mode two does
not trigger the transform-proximity skip, and the post-admission refinement
returns the same affine without replacement.

Both features use valid 286-byte high/low bitmaps and 72-byte inline masks. The
probe mask is full. The enrolled target mask differs by one interior packed
cell between the two cases; because each mask bit expands to `2x2`, this is one
valid four-pixel geometry change. Running the current overlap producer gives:

| Output | Adjacent control | Target |
|---|---:|---:|
| Q8 high-bitmap coverage | 209 | 210 |
| `metrics[4], metrics[5]` | `128,181` | `128,181` |
| `metrics[6..8]` | `0,0,0` | `0,0,0` |
| `metrics[9]` | 200 | 201 |
| wrapped combined | 177 | 177 |
| policy-entry `flag60/flag64/config[19]` | `0/1/1` | `0/1/1` |

There is no cross-field invariant in the mapped producer equations excluding
the target. The score sentinel depends on class quality rather than low
valid-count coverage; high coverage can therefore coexist with
`metrics[4] == 128`. The scale penalty makes adjusted overlap `126`, avoiding
the separate adjusted-overlap equality clear, while retained count 22 survives
the generic low-combined family. The sole target/control difference is the
strict `metrics[9] > 200` edge.

For finalization, the retained one-gallery transaction supplies extraction,
gallery, queue, and study state, and its one target dispatcher candidate is
replaced by the complete 77-word producer output above. No individual predicate
field is patched independently. Direct policy and finalization produce:

| Implementation/case | Dispatcher `flag60/flag64/config[19]` | Score | Accepted | After-match SHA-256 | Final candidate SHA-256 | Study action |
|---|---|---:|---|---|---|---:|
| DLL control (`m9=200`) | `0/1/1` | 54 | yes | `4cd25a1214c171576fb6e5ff4eda01a14a2ddcbd502ef69755c577af21bf4edb` | `7c8997cd42c5c79c656ca4561ab162857bba1c0b668a7e6c7c2ddc80d4c0e27e` | 1 |
| DLL target (`m9=201`) | `0/0/1` | 92 | yes | `810cc40345912c39707726ccdddebdd8d5a1188590bf015aba1c58f936fb0e47` | `36c9d79c7ce4c9e3b8f8f4d495eec56d07cb403534451517c4982d3a0d9f6787` | 1 |
| Current control (`m9=200`) | `0/1/1` | 54 | yes | `4cd25a1214c171576fb6e5ff4eda01a14a2ddcbd502ef69755c577af21bf4edb` | `7c8997cd42c5c79c656ca4561ab162857bba1c0b668a7e6c7c2ddc80d4c0e27e` | 1 |
| Current target (`m9=201`) | `0/1/1` | 54 | yes | `4cd25a1214c171576fb6e5ff4eda01a14a2ddcbd502ef69755c577af21bf4edb` | `7c8997cd42c5c79c656ca4561ab162857bba1c0b668a7e6c7c2ddc80d4c0e27e` | 1 |

All four runs retain winner index/position `0/0`, queue occupancy `0/0/0`, and
complete preprocess, extraction, and study lifecycles. The realistic occurrence
classification is **possible only under valid synthetic feature geometry**: the
state is emitted by the current translations of the mapped native producers and
satisfies the native caller contract, but no captured raw probe is claimed to
contain this complete geometry.

The parity contract is one clear-only branch in `post_veto()` after the
`metrics[14]` family and before later generic vetoes. It must use wrapping dword
arithmetic, preserve every strict comparison, test raw `configuration[18]`,
clear both flags, and leave `configuration[19]` unchanged.
