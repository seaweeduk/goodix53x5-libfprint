# FUN_18007f2c0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18007f2c0..0x18007f493`.
- Caller: `FUN_18007eb30`.
- Role: orientation-selected seven-tap enhancement interpolation.

## Recovered Behavior

For every pixel, the function interprets the orientation byte with unsigned
arithmetic. If `(uint8_t)(orientation - 8) < 165`, direction is
`(orientation - 8) / 15 + 1`; otherwise direction is zero. This yields the 12
direction rows at table `0x1800f31d0`, each containing seven signed 32-bit
`(x,y)` offsets. The complete table is identical to
`enhancement.c:180-193`. Tap weights are `[1,2,4,8,4,2,1]`.

Each tap is admitted only when both coordinates are in bounds. Edge pixels
therefore discard out-of-image taps rather than reflecting or clamping them,
and divide the unsigned 32-bit weighted sum by the retained weight sum with
integer truncation. A zero retained sum writes `0xff`, although every current
table row includes the in-bounds center tap for nonempty images. Output is
independent per pixel and cannot change record count or ownership.

Current `goodix_milan_feature_enhance()` implements this contract at
`drivers/goodix53x5/milan/feature/enhancement.c:272-295`.

## Confidence And Unresolved

- Confidence: high for the generic bin predicate, complete offset table,
  weights, edge handling, division, and type-12 reachability.
- Unresolved: none for profile 9/type 12.
