# FUN_180037b10

## Identity

- DLL: `GoodixEngineAdapter.dll`
- DLL SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`
- Address/body: `0x180037b10..0x180037c42`.
- Role: destructor for one allocated feature object.

## Call Graph

- Called by `templateStudy` when it consumes the retained live probe and by
  internal template graph destructors.
- Calls `FUN_180068b70` for nested matrix objects and `free` for owned buffers
  and the feature object itself.

## Recovered Behavior

Given a pointer to an owning feature-object pointer, the function destroys
nested objects at offsets `+0x08`, `+0x10`, `+0x18`, `+0x20`, and `+0x140`, frees
buffers at `+0xf8`, `+0x100`, `+0x158`, and `+0x160`, frees the feature object,
and clears the caller's pointer. A null owner or null object returns
`0x80000002`.

This supports the observed `templateStudy` lifecycle: the retained probe global
created by `identifyImage` is single-use and is consumed and cleared by study.
The oracle calls study before unloading the DLL and does not attempt to reuse
that retained object.

## Confidence And Open Questions

- Confidence: high for ownership and single-use cleanup.
- Semantic names for all nested fields remain unresolved.

## Rare-Action Ownership Control

Direct exported code-2/code-3 controls confirm this destructor clears global
`0x18024ebe8` after every ordinary study result while global matched gallery
`0x18024e548` remains the same live object owned by the public handle. The latter
is subsequently packed and then deleted by the adapter update helper. This
ownership is identical for update 0, 2, and 3.

Native unit-5 tests preserve the corresponding caller ownership: study borrows
the original `GoodixMatchInfo`, late enqueue deep-copies it, and the caller frees
the original exactly once after the transaction. No diagnostic result owns or
aliases the probe.
