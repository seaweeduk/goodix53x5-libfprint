# FUN_1800624c0

## Binary And Body

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800624c0` / `0x1800624c0-0x180062686`.
- Inferred signature: `bool f(transform[6], mode, sensor_type)`.
- Callers include `FUN_180055a40`, `FUN_180058700`, `FUN_180055670`, `FUN_18005d5f0`, `FUN_180060fe0`, and `FUN_180062a30`; no internal callees.

## Behavior

Checks whether a six-word transform is within mode- and sensor-family-specific
distance bounds around identity `[0x100,0,0,0,0x100,0]`. Modes 1-5 select
threshold sets; mode 3 scales thresholds and selected sensor families use an
alternate threshold table. Returns one only when every component is in range.

## Evidence And Confidence

- Identity and threshold arrays are initialized at function entry; six absolute differences are checked.
- The `FUN_180058700` status review fixes the profile-9/type-12 mode-4 vector at
  `[38,38,3072,38,38,3072]`. Bounds are inclusive: each component passes at
  exactly its limit and fails at limit plus one. `31/1` feature 3 has deltas
  `[1,26,2863,29,9,1857]` and passes; its adjacent feature-5 control fails at
  least `abs(t1)=44>38` and `abs(t2)=4832>3072`.
- Confidence is high. Native transform-proximity behavior now matches these
  boundaries after correcting the sixth Q8 translation limits from truncated
  scalar values to `0x400`, `0xc00`, `0xf00`, and alternate-table `0x700`.
- Unresolved: authoritative names for all sensor-type masks.

## Profile-9/Type-12 Complete Table

Unit-16 static recovery and direct DLL controls fix the complete table selected
by subtype 12. Subtype 12 takes the alternate sensor-family table at
`0x180062606`, not the primary table at `0x1800625b0`:

| Mode | Inclusive absolute identity-delta limits `[t0..t5]` |
|---:|---|
| 1 | `[25,21,1792,21,25,1792]` |
| 2 | `[25,21,1792,21,25,1792]` |
| 3 | `[25,21,1792,21,25,1792]` |
| 4 | `[38,38,3072,38,38,3072]` |
| 5 | `[45,40,3840,40,45,3840]` |

Modes outside `1..5` return zero. The mode-2/mode-3 multiplier applies only to
the primary sensor-family loop; it does not scale subtype 12's alternate table.
For every listed mode and all six components, direct controls pass at both
`identity-limit` and `identity+limit`, and fail one unit beyond either side.
That is 120 transform-boundary controls plus mode-zero and sensor-table controls.
The direct artifact is under
`<evidence-root>/derived/milan-native-capture-20260723/match-unit16-fun-180058700-contract-v1/`.

## Fallback Control

The fallback finalizer uses the maximum class decoded from configuration word
18. A passive `12/2` feature-2 control supplies mode `1` and transform
`[253,-12,451,-20,256,1217]`; Windows returns `1` and skips the workspace before
`geometry_seen`. The corrected alternate-table sixth limit is `0x700`, so the
Q8 translation delta `1217` is in range. The old native value `7` incorrectly
returned zero and created a spurious fallback event.

After correction and exact caller enable staging, immutable replay is exact for
all `798/798` scores, decisions, and candidate indices, and the frozen 33-row
fallback event projection is exact for all 36 evaluated events.
