# FUN_180075220

## Identity

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180075220`, `0x180075220..0x1800756af`.
- Role: class-partitioned sibling correspondence construction.

## Call Graph And Behavior

`FUN_180057900` calls it to split records by flag value `1` or `2`, run four combinations through `FUN_1800770e0`, and select bounded pairs with `FUN_180075a60`. It emits two candidate pair sets for later affine filtering.

## Confidence And Unresolved

- Confidence: high for partitioning, matrix input, class directions, and helper
  topology.
- Unresolved: production meaning of flag classes `1` and `2`.

## 2026-07-28 Stored-Boundary Correction

The helper does not infer its two physical partitions from record flags.
Assembly/decompilation shows enrolled boundary reads from the feature object at
`+0x118` and probe boundary reads from the live object at `+0x118`. It performs
the class-1/class-2 scans separately over `[0,boundary)` and
`[boundary,count)`, preserving physical record order before `FUN_180075a60`.

Native `milan_match_secondary_diagnostics_capacity()` instead scans a leading
run of `foreground == 0` in each record array. A fixed generated input with
conceptual stored split `2/2` changes selected count `4 -> 0` when only those
record flags are contradicted. Confidence is high for stored-boundary ownership;
the semantic names of classes 1 and 2 remain unresolved.

## 2026-07-28 Native Implementation

`milan_match_secondary_diagnostics_capacity()` now receives explicit enrolled
and probe partition counts validated from feature metadata. Both class scans use
the two stored half-open physical ranges; the previous leading-foreground scans
are removed. Agreement, contradiction, physical reorder, independent/empty
partitions, and malformed direct inputs pass focused controls. Class meaning
remains unresolved and no class, descriptor, ratio, capacity, or selection rule
changed.

## 2026-07-28 Matrix-Ownership Correction

The native implementation above is exact for stored range ownership but is not
the complete production input contract. `FUN_180075220` receives two additional
caller-owned matrices and passes both to each `FUN_1800770e0` invocation. One
matrix marks pair eligibility (`-1` rejects); the other chooses whether the
probe tail begins at record offset 40 or 48. The helper does not derive either
decision from the two records during this call.

Native `milan_match_cross_class_correspondences()` instead calls
`milan_recognition_descriptor_details()` for every candidate pair and locally
derives both eligibility and tail layout. That is observably different even
when feature records, stored partitions, and the complete primary result are
identical. Sequence-100 feature 33/23 yields production raw class sets `4/7`
but native raw sets `7/10`. The production sets filter to `0/3`; direct native
filtering of those same arrays reproduces the exact production affines and
counts. Confidence is high that matrix ownership is the first unequal boundary.

## 2026-07-28 Complete Matrix Contract

The producer and lifetime are now resolved. `FUN_18005a3c0` initializes a
`180x180` distance matrix to `0xff` and a same-sized layout matrix to zero;
`FUN_180078a70 -> FUN_180076400` fills only the two same-stored-partition
rectangles. This function borrows those unchanged arrays through
`FUN_180057900`, scans classes 1 and 2 separately in both physical partitions,
and invokes `FUN_1800770e0` in both class directions before shared selection.

Sequence-100 validation confirms that native's `7/10` raw sets are strict
three-pair supersets of production's `4/7`. The extra primary pairs
`(2,4),(28,11),(117,111)` have selected tails `17,18,18`; alternate extras
`(6,36),(38,8),(134,100)` have `19,18,18`. All are producer-eligible normal
layouts. Production consumer limit 16 rejects all six; native limit 23 admits
them. The generic contract and implementation authorization are in
`re/milan/MATCH-MATRIX-CONTRACT.md`.

## 2026-07-28 Native Tail-Admission Closure

Native now passes profile configuration word 5 into both class-direction scans
over both stored physical partitions. The ordinary selected-tail helper applies
that supplied limit after producer eligibility/layout recomputation; for
profile9/type12 it is 16. No class filtering, partition, physical order,
best/second, ratio, collision, capacity, pair publication, or robust-fit rule
changed. Direct normal/shifted and pair-order controls pass, and sequence 100
now reaches the exact production `4/7` consequence and downstream score 27.
Confidence is high; class semantics remain unresolved but do not affect this
completed admission contract.
