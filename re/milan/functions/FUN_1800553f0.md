# FUN_1800553f0

## Binary And Body

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800553f0..0x1800555c7` (472 bytes).
- Ghidra CFG: 56 basic blocks, 106 edges, 51 conditional branches, no loop or
  callee.
- Sole caller: `FUN_180055a40`, call at `0x1800563be`.

## Recovered Interface

```c
void FUN_1800553f0(
  int32_t metrics[77],
  int32_t probe_coverage,
  int32_t accumulated_high_class,
  int32_t probe_low_class,
  int32_t support_ratio_q8,
  int32_t *flag64,
  int32_t *flag60);
```

The caller computes `support_ratio_q8` immediately before the call. The helper
also computes `scaled = metrics[11] * metrics[9] >> 8` with signed 32-bit
multiplication and arithmetic shift. Argument roles are fixed by
`FUN_180055a40:0x18005636d..0x1800563be` and direct DLL controls.

## Complete Clear-Only Contract

The helper runs only when
`(accumulated_high_class >= 2 || probe_low_class != 0) && flag64 != 0`.
It never sets or normalizes either flag. The first family applies only when
`flag64 == 1` and clears both flags when one entry guard and one weak-metric
guard are true.

Entry guards:

```text
A1 = accumulated_high >= 3 && probe_low >= 4 &&
     coverage < 80 && metrics[10] < 70
A2 = accumulated_high >= 2 && probe_low >= 2 &&
     coverage < 55 && scaled < 20
```

Weak-metric guards:

```text
B1 = metrics[0] < 19 && metrics[5] < 201 &&
     metrics[8] < 200 && scaled < 15
B2 = metrics[0] < 13 &&
     ((accumulated_high >= 4 && metrics[5] < 201 && metrics[8] < 200) ||
      (metrics[5] < 216 && metrics[8] < 210 && scaled < 18))
B3 = metrics[0] < 11 &&
     ((metrics[5] < 211 && metrics[8] < 215 && scaled < 15) ||
      (metrics[5] < 201 && metrics[8] < 200))
```

`(A1 || A2) && (B1 || B2 || B3)` clears `flag64` and `flag60` together.

The second family independently clears only `flag60` when any condition is
true:

```text
C1 = accumulated_high >= 4 && probe_low == 1 &&
     coverage < 50 && support_ratio_q8 > 219
C2 = accumulated_high >= 3 && probe_low >= 2 && coverage < 50 &&
     metrics[0] < 8 && metrics[5] < 203 && metrics[8] < 200 &&
     scaled < 17 && metrics[9] < 170
C3 = accumulated_high >= 2 &&
     ((probe_low >= 3 && support_ratio_q8 > 149 &&
       metrics[5] < 201 && metrics[8] < 200 && scaled < 17) ||
      (coverage < 35 && metrics[5] < 208 &&
       metrics[8] < 197 && scaled < 20))
```

All comparisons are strict as written. `flag64 == 2` is producer-valid and is
preserved while the second family may clear `flag60`.

The production packed-`c7` decoder yields probe low classes `0`, `2`, and `3`.
Consequently A1's `probe_low >= 4` and C1's `probe_low == 1` arms are present in
the function but caller-excluded for valid profile-9/type-12 input. A2, C2, and
C3 remain caller-reachable families; the direct set deliberately records both
whole-function and valid-caller boundaries rather than conflating them.

## Validation And Native Gap

Twenty-three fixed-seed direct controls cover entry bypasses, both clear
families, all strict equality neighbors, and incoming `flag64` values zero, one,
and two. Predictions derived from the static body are byte-exact to 23 direct
DLL calls after line-ending normalization.

The earlier 60-call profile-9/type-12 diagnostic did not exercise a clearing
branch, so its exact tuples proved only natural-call occupancy. Native
`goodix_milan_matcher_policy_evaluate()` explicitly treats this helper as a
cohort no-op and has no implementation of the contract above. This is a real
statically caller-reachable policy omission despite immutable output parity
`798/798` on the supplied corpus.

The focused `79/2` trace remains correctly attributed to the earlier caller
veto: this helper receives and preserves `[flag64,flag60] = [0,0]` there.

Artifacts:
`<evidence-root>/derived/milan-native-capture-20260723/match-decision-retrospective-v1/`.
Confidence is high for the interface, complete Boolean contract, strict
boundaries, clear-only side effects, and native omission.

## Native Closure

`goodix_milan_matcher_policy_apply_final()` now implements the complete contract
above, including wrapped signed multiplication for `metrics[11] * metrics[9]`,
portable arithmetic shift, all entry bypasses, and independent output families.
`goodix_milan_matcher_policy_support_ratio_q8()` exposes the exact caller
producer recovered at `FUN_180055a40:0x18005636d..0x1800563ad`:
`((class0 + class2) << 8) / (class0 + 1 + class1 + class2)` with 32-bit wrap and
signed `IDIV` preconditions.

All 23 direct DLL controls run twice and are byte-identical to their frozen
predictions. The same 23 controls run twice through native code and are exact.
This covers the whole-function A1/C1 arms even though valid profile-9 packed
classes exclude them, plus caller-reachable A2/C2/C3, equality neighbors, and
flags zero/one/two. The earlier natural no-op statement is superseded.
