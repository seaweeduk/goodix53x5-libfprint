# FUN_1800553f0

## Binary And Body

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900.
- Entry/body: `0x1800553f0..0x1800555c7` (472 bytes).
- Ghidra CFG: 56 basic blocks, 106 edges, 51 conditional branches, no loop or
  callee.
- Sole caller: `FUN_180055a40`, call at `0x1800563be`.

## Recovered Interface

```c
void FUN_1800553f0(
  int32_t metrics[77],
  int32_t probe_coverage,
  int32_t accumulated_high_class,
  int32_t probe_histogram_class,
  int32_t support_ratio_q8,
  int32_t *flag64,
  int32_t *flag60);
```

The caller computes `support_ratio_q8` immediately before the call. The helper
also computes `scaled = metrics[11] * metrics[9] >> 8` with signed 32-bit
multiplication and arithmetic shift. Argument roles are fixed by
`FUN_180055a40:0x18005636d..0x1800563be`.

Argument two is raw probe `+0x11c` (the caller's quality scalar), called
`coverage` in the predicates below; it is not the histogram class or Q8
candidate coverage.

The fourth argument is the first unsigned byte of the live probe's `+0x158`
summary owner, widened by `FUN_180073830`. A null owner leaves it zero.
`0x180055cf6..0x180055d08` saves it at caller `[RSP+0x68]`;
`0x180056319` loads `EDI` from that slot, and `0x180056379` supplies it in
`R9D`. It is independent of the packed-`c7` low class, which is decoded into
caller `[RBP+0x1ac]` by `FUN_180070d90`.

## Complete Clear-Only Contract

The helper runs only when
`(accumulated_high_class >= 2 || probe_histogram_class != 0) && flag64 != 0`.
It never sets or normalizes either flag. The first family applies only when
`flag64 == 1` and clears both flags when one entry guard and one weak-metric
guard are true.

Entry guards:

```text
A1 = accumulated_high >= 3 && histogram_class >= 4 &&
     coverage < 80 && metrics[10] < 70
A2 = accumulated_high >= 2 && histogram_class >= 2 &&
     coverage < 55 && scaled < 20
```

Weak-metric guards:

```text
B1 = metrics[0] < 19 && metrics[5] < 201 &&
     metrics[8] < 200 && scaled < 15
B2 = metrics[0] < 13 &&
     ((accumulated_high >= 4 && metrics[5] < 201 && metrics[8] < 200) ||
      (metrics[5] < 216 && metrics[8] < 210 && scaled < 18))
B3 = metrics[0] < 11 &&
     ((metrics[5] < 211 && metrics[8] < 215 && scaled < 15) ||
      (metrics[5] < 201 && metrics[8] < 200))
```

`(A1 || A2) && (B1 || B2 || B3)` clears `flag64` and `flag60` together.

The second family independently clears only `flag60` when any condition is
true:

```text
C1 = accumulated_high >= 4 && histogram_class == 1 &&
     coverage < 50 && support_ratio_q8 > 219
C2 = accumulated_high >= 3 && histogram_class >= 2 && coverage < 50 &&
     metrics[0] < 8 && metrics[5] < 203 && metrics[8] < 200 &&
     scaled < 17 && metrics[9] < 170
C3 = accumulated_high >= 2 &&
      ((histogram_class >= 3 && support_ratio_q8 > 149 &&
       metrics[5] < 201 && metrics[8] < 200 && scaled < 17) ||
      (coverage < 35 && metrics[5] < 208 &&
       metrics[8] < 197 && scaled < 20))
```

All comparisons are strict as written. A supplied `flag64 == 2` is preserved
while the second family may clear `flag60`; ordinary classifier outputs are
Boolean.

Packed-`c7` decoder ranges do not constrain this histogram argument. Ordinary
type-12 invocations reaching zero-score compact finalization have histogram
class below two and accumulated high class zero or one. The accumulator starts
at zero from the required zero raw probe `c7`; an encountered feature high class
above three excludes compact finalization, and the sole remaining increment is
latched once. All writes in this helper require accumulated high class at least
two, so it preserves both flags throughout that route. See `FUN_1800608b0.md`
for the enclosing accumulator and compact-validation contract.

## Support-Ratio Producer

At `FUN_180055a40:0x18005636d..0x1800563ad`, the caller computes
`((class0 + class2) << 8) / (class0 + 1 + class1 + class2)` with 32-bit
wrap and signed `IDIV` preconditions. This supplies `support_ratio_q8`;
it is separate from the histogram-class argument.

## Count-Dependent Decision Surface

Only C1 and the first arm of C3 read the support ratio. They can clear only
`flag60` (the match flag); the candidate flag `flag64` is changed exclusively
by the ratio-independent first family. With an incoming match flag of zero,
every ratio therefore produces the same two output flags. With an incoming
candidate flag of zero, the entire helper is inactive.

The sole caller enables its masked descriptor only when the same live histogram
class is above2; otherwise all three counts remain zero. Consequently its
histogram-class1 calls supply ratio0 and cannot take C1's `ratio > 219` arm.
That arm remains part of the helper's standalone contract, but is inactive under
this caller's count/class correlation.

For live histogram class5, C1 is excluded. A ratio-dependent output change
requires incoming nonzero candidate and match flags, accumulated high at least2,
`metrics[5] < 201`, `metrics[8] < 200`, and `scaled < 17`. It additionally requires
that neither the first family nor another second-family predicate already clears
the match flag. Under those conditions the remaining boundary is exactly
`support_ratio_q8 > 149`. This is a standalone conditional consumer predicate;
the ordinary mode5 caller imposes the stronger implication below.

## Ordinary Mode5 Ratio Noninterference

For ordinary profile-9/type-12 recognition of a live extraction-produced mode5
probe, varying only this helper's support ratio cannot change either outgoing
flag. The domain requires the actual caller's dispatcher/late-status order,
Boolean candidate flag, nonnegative bounded bitmap metrics, and the three
one-bit affine penalties. It excludes an independently supplied flag/metric
tuple or a summary whose packed classification was replaced independently.

The relevant producer relation is packed high5, not histogram5 alone.
`FUN_180048260:0x180048504..0x180048510` promotes the live mode to5;
`0x180048584..0x18004858b` raises the packed high seed to that mode and
`0x180048621..0x180048624` passes it to `FUN_180073820`.
`FUN_18005edb0` copies the resulting probe `+0x150` to configuration word18.
The high decoder maps raw5 to5. The caller's accumulator starts at5 and does
not decrease. See `FUN_180048260.md`, `FUN_180070d90.md`, and
`FUN_180055a40.md` for these owners and the queue-call exclusion.

Let `D=metrics[5]`, `L=metrics[8]`, `P=metrics[0]`,
`N=metrics[12]+metrics[13]+metrics[14]`, and
`S=(metrics[11]*metrics[9])>>8`. Only `D<=200 && L<=199 && S<=16`
needs consideration: outside it the class5 ratio-dependent arm is false.

1. The caller's `flag60==2 && accumulated_high<=1` bypass cannot apply.
   `FUN_180053220:0x18005332e..0x18005334a` sends every nonzero match flag
   through `FUN_180058ee0`. Its type-12 surviving path at
   `0x1800598a4..0x1800598b8` calls `FUN_18005c3b0`.
   That continuation subtracts `2*N` from D and L and tests decoded packed
   high at least2, adjusted D below213, and adjusted L below210
   (`0x18005c41d..0x18005c46b`). All three conditions hold here: `N` is
   nonnegative and at most3. It clears match and recognition mode at
   `0x18005c474..0x18005c476`. Earlier generic rejections instead already
   clear match. Thus match is zero after the first-veto stage regardless of
   which initial/fallback classifier produced it.
2. Post veto only clears flags. The only later dispatcher operation that can
   restore match is `FUN_1800572e0`, called with zero temporary flags and only
   when current recognition mode is1. Under `D<=200`, its strict ladder can
   set match only for **P exactly11**, overlap above219, D above195, topology
   above35, candidate coverage above119, raw probe quality in16..30, and raw
   probe coverage at least65. All other strict primary buckets require
   D above200; the broad ladder sets candidate only. The decisive P11 branch
   is `0x1800573b7..0x1800573d8`, and the flag stores are
   `0x1800575bb..0x1800575d1`. Type12 skips quality normalization. See
   `FUN_180053220.md` for dispatcher and late-helper ownership.
3. The caller's intervening vetoes and `FUN_180058700` cannot set match or
   candidate. Nonzero status skips this helper entirely; status zero preserves
   the implication from step2 for any still-nonzero match. See
   `FUN_180058700.md` for the exact skip and clear-only contract.
4. A restored match therefore arrives with candidate1, P11, and the same raw
   quality at most30 as argument2 here. If S<=16, ratio-independent A2 holds
   (`high>=2`, class>=2, quality<55, S<20), and B2 holds (P<13, high>=4,
   D<201, L<200). Instructions `0x18005546a..0x18005548b` and
   `0x1800554ad..0x1800554c7` reach the two zero stores at
   `0x18005550d..0x180055510` before the ratio-dependent arm. If the first
   family does not clear, match was already zero. Hence C3's ratio arm never
   changes an outgoing value on this domain, even if its ratio comparison
   changes truth value.

This is stronger than a late evidence overwrite: both flags, metrics, and
configuration are identical before contribution and winner selection. The
count ratio has no other output or side effect. Consequently, with the earlier
contextual-veto outcome fixed, it cannot alter Q8 score/contributors, retained
winner, direct selected feature/affine, evidence `+0x684/+0x688`, lifecycle
increments, rescue/fallback continuation, queue admission, or subsequent study
gates. An evidence clear after winner selection is not needed for this result.
See `FUN_180055a40.md` for those consumers. This implication does not eliminate
the separate count-sensitive early contextual veto `FUN_18005b440`.
