# FUN_1800687c0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800687c0`, `0x1800687c0..0x180068856`.
- Role: normalize the rotational component of a six-word Q8 affine.

## Call Graph

- Direct caller: `FUN_180068860`.
- Direct callee: `FUN_180074940`.

## Recovered Behavior

For `[a,b,tx,c,d,ty]`, all operations before division are signed 32-bit and
wrap:

```text
average  = sar32(wrap32(a + d), 1)
rotation = sar32(wrap32(c - b), 1)
squares  = wrap32(rotation*rotation + average*average), interpreted uint32
norm     = floor(sqrt_unsigned32(squares))
```

`FUN_180074940` supplies the exact unsigned integer square root. If `norm` is
zero, the linear part becomes `[256,0;0,256]`. Otherwise:

```text
diagonal = idiv32(wrap32((norm >> 1) + wrap32(average << 8)), norm)
upper    = idiv32(wrap32((norm >> 1) - wrap32(rotation << 8)), norm)
[a,b;c,d] = [diagonal,upper;-upper,diagonal]
```

Signed division truncates toward zero. The unconditional positive
`floor(norm/2)` bias gives nearest-style rounding for positive numerators but
is asymmetric for negative numerators, biasing them toward zero. There is no
saturation. Scale and shear are discarded; translations are always preserved.

Evidence is the complete body; normalization divisions occur at `0x180068803..0x18006884f`.

Direct controls cover identity, rotation/scale projection, negative components,
zero norm with preserved translation, and signed add/square wrap. All are exact
against the width-controlled model.

## Confidence And Unresolved

- Confidence: high; exact widths, unsigned square-root input, half bias,
  division, zero fallback, and translation preservation are directly controlled.
- Unresolved: none on exercised inputs.

## Native Implementation

The relation module now wraps average, rotation, squares, and biased numerators
at 32 bits before unsigned integer square root and signed truncating division.
The zero-norm identity substitution and translation preservation are unchanged.
