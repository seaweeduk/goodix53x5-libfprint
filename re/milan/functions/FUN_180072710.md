# FUN_180072710

## Identity

- DLL: `GoodixEngineAdapter.dll`
- DLL SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`
- Address: `0x180072710`
- Body: `0x180072710..0x180072857`
- Role: warp a temporary binary mask into a second mask's frame, classify it
  under two validity masks, and fold four classes into three outputs.
- Current native equivalent:
  `goodix_milan_index0_match_masked_classes()`.

## Recovered Signature

Ghidra and the x64 calling convention recover the effective signature as:

```c
bool FUN_180072710(
  masked_context *context,
  int32_t outputs[3]);
```

`outputs` receives `[class0, class1 + class2, class3]`. The return value is
false only when `context->warped_validity` at offset `+0x20` is null.

## Callers

- `FUN_180071d40` is the only direct caller.

## Direct Callees

- `FUN_18006f390`: computes transformed bounds, allocates a cropped output,
  adjusts translation by crop origin, and inverse-warps the first mask.
- `FUN_180073540`: classifies direct and warped mask pixels under two validity
  masks.
- `FUN_180068b70`: releases the temporary warped descriptor.
- `__security_check_cookie`.

## Data Layouts

### Masked context

```text
+0x00 matrix_desc *first_mask
+0x08 matrix_desc *second_mask
+0x10 matrix_desc *unused_by_this_helper
+0x18 matrix_desc *direct_validity
+0x20 matrix_desc *warped_validity
+0x28 int32_t transform[6]
+0x30 mode/config pointer (unused by this helper)
+0x38 int32_t crop/config value copied into the local warp context
```

### Matrix descriptor

The fields consumed by this helper and its direct callees are:

```text
+0x00 int32_t columns
+0x04 int32_t rows
+0x08 int32_t element/type field
+0x0c int32_t element count or stride-derived count
+0x10 int32_t allocation/type field
+0x18 uint8_t *data
```

`FUN_18006f390` writes crop origin to context words `[6]` and `[7]`, which
become `origin_x` and `origin_y` arguments to `FUN_180073540`.

### Parent temporary masks

The parent function maps captured class-mask bytes as follows before calling
this helper:

```text
1 -> 0
2 -> 1
other -> cleared
```

## High-Level Pseudocode

```c
if (!context->warped_validity)
  return false;

copy first-mask dimensions and crop configuration;
if (!FUN_18006f390(context->first_mask, NULL, context->transform,
                   &warped_first_mask, NULL, &crop_context))
  return false; /* effective behavior still needs a failing fixture */

classes = {0, 0, 0, 0};
valid_count = 0;
FUN_180073540(context->second_mask, warped_first_mask,
              context->direct_validity, context->warped_validity,
              origin_y, origin_x, classes, &valid_count, NULL);
release(warped_first_mask);

outputs[0] = classes[0];
outputs[1] = classes[1] + classes[2];
outputs[2] = classes[3];
return true;
```

## Constants And Branches

- The parent enters this path when its mode descriptor selects masked
  reclassification.
- Parent-side mask remapping constants are `1 -> 0` and `2 -> 1`.
- The parent allocates both temporary masks with `FUN_180003800`, copies the
  first mask to the second with `FUN_18008a9c0`, and initializes them to zero.
- Warped output bytes are initialized to `0xff`; `FUN_180073540` only accepts
  direct and warped values below `2`.
- `FUN_180073540` uses `class_index = direct + warped * 2`.
- Output folding is exactly `[class0, class1 + class2, class3]`.
- The optional morphology/mismatch output of `FUN_180073540` is null here, so
  its `FUN_18006f840` and `FUN_180070820` path is not entered by this helper.

## Authoritative Artifacts

Natural eight-enrollment observation:

```text
re/milan/private/oracle/decision-parity-captures/fun-180072710-enroll8/
```

The parent predicate executed four times, but every candidate had stage `0`
and a null auxiliary mask. Consequently, the helper did not execute naturally.

Explicit oracle-only forced fixture:

```text
re/milan/private/oracle/decision-parity-captures/fun-180072710-enroll8-forced/
```

The forced fixture changes only the dormant predicate inputs: stage becomes
`3`, and the auxiliary mask becomes a deterministic 52x44 checker containing
values `1` and `2`. It is evidence for helper semantics, not evidence that the
production corpus naturally enters the branch.

Focused parent capture for static replay row 207:

```text
re/milan/private/oracle/decision-parity-captures/static-0207-masked/
```

This capture records eight secondary selections. The first vector is
`primary=212, secondary=230, secondary_detail=229, result=230`; all eight
vectors replace the primary result.

### Hook addresses

- Parent predicate: `0x180055f49`.
- Masked helper entry: `0x180072710`.
- Internal classes: `0x1800727fa`.
- Masked helper result: `0x180072830`.
- Secondary selector inputs: `0x180072137`.
- Secondary selector result: `0x18007217a`.

## Validation

Run:

```sh
./re/milan/scripts/run-native-match-masked-test.sh \
  re/milan/private/oracle/decision-parity-captures/fun-180072710-enroll8-forced
```

Current exact results:

- Four masked calls are exact for crop origin, all four internal classes,
  folded outputs, valid total, and return value.
- Call 0: origin `0,0`, classes `1152,11,0,1125`, outputs `1152,11,1125`.
- Call 1: origin `15,20`, classes `133,287,288,129`, outputs `133,575,129`.
- Call 2: origin `0,0`, classes `1152,11,0,1125`, outputs `1152,11,1125`.
- Call 3: origin `0,0`, classes `142,301,293,135`, outputs `142,594,135`.
- `goodix_milan_index0_match_secondary_result()` is exact for all `22`
  captured vectors, including `16` replacements.

The selector is exactly:

```c
secondary_score > primary_score && secondary_detail > 195
  ? secondary_score
  : primary_score;
```

The static and learned 404-row replay gates remain `404/404`. Applying the
selected secondary result to candidate ranking changed none of the strict
result counters, so no production policy change was retained.

The packed-template bitmaps and affine transforms use a different
representation and direction from this Windows helper boundary. Directly
feeding the helper result into the current policy path is therefore invalid
without an independently validated packed-to-matrix and inverse-affine bridge.

## Unresolved

- A natural Windows fixture that executes the masked path without oracle-only
  predicate mutation.
- Whether a `FUN_18006f390` failure is reachable with a nonnull
  `warped_validity` and what outputs remain in that case.
- Exact semantic names for context offsets `+0x10`, `+0x30`, and `+0x38`.
- A natural negative-coordinate fixture; forced call 1 already validates a
  nonzero crop origin.
