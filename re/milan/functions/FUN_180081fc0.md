# FUN_180081fc0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x180081fc0..0x1800820d4`, integer correlation core.

## Call Graph

- Caller in this milestone: `FUN_180081d30`.
- Substantive callee: integer square root `FUN_180074980`.

## Behavior

- Each mean follows the native instruction order
  `((sum << 8) / count) >> 8`, which is the floor integer mean for the
  nonnegative byte sums used here.
- Centered byte products are formed as signed 32-bit values and accumulated in
  signed 64-bit covariance and variance sums. For the production `88x108`
  plane, the arithmetic remains in range.
- The denominator is `floor(sqrt(first_variance * second_variance))`. A nonzero
  denominator returns `(covariance << 8) / denominator` using signed division,
  so negative results truncate toward zero.
- Returns zero when the denominator is zero.
- For 9504 byte samples, each centered value has magnitude at most `255`.
  Each variance sum and the absolute covariance are therefore bounded by
  `9504 * 255 * 255 = 617997600`. The variance product and covariance scaled by
  `256` fit signed 64-bit arithmetic. The square root is at most `617997600`,
  so the restoring loop's 32-bit doubled partial root also remains in range.

## Evidence

- Sum and Q8 mean/divide sequence: `0x180081ff5..0x18008204e`.
- Covariance and variance loop: `0x180082053..0x180082096`.
- Integer square root, division, and zero case: `0x180082096..0x1800820bc`.

## Confidence

High.

## Unresolved

- None for the profile-9 candidate-selection path.
