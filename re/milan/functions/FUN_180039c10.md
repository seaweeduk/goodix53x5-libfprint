# FUN_180039c10

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180039c10..0x180039d72`.
- Relevant caller: `FUN_18003a9b0`.

## Recovered Behavior

This is an in-place signed-32-bit order-statistic selector. For a count `n`, it
partitions the input until it can return element `(n - 1) / 2`, so even counts
select the lower middle value. `FUN_18003a9b0` calls it with count nine for both
the roughness set and the replacement residual set. Residual `uint16_t` values
are promoted into nine 32-bit slots before this call.

The selector mutates only its supplied scratch array. It does not read image
geometry or residual storage and therefore does not own the distinction between
the caller's clamped roughness centers and direct flat replacement offsets.

## Confidence

High for in-place selection, signed comparison, returned rank, and both
profile-9 anti-fake call sites. The vendor semantic name is unknown.
