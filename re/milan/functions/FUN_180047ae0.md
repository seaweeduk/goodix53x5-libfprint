# FUN_180047ae0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180047ae0..0x180047c45`.
- Relevant callers: `FUN_18004ae70` at `0x18004b694` and `FUN_18004b770`.

## Recovered Behavior

The function rearranges and conditionally inverts descriptor nibbles/bits in a
56-byte feature record. With a nonzero mode flag, it also reverses one 32-bit
word and derives the final eight bytes from the preceding descriptor tail. It
does not inspect candidate masks or change record count.

For each pair at record `+0x10..+0x1f`, it exchanges the low nibbles while
retaining high nibbles, then places the two results according to fixed order
`[0,1,1,0,1,0,0,1]` into two eight-byte spans. It always clears descriptor
bytes `+0x24..+0x27` at `0x180047b7e..0x180047bd3`. With the profile-9 mode-1
flag, `0x180047b8a..0x180047bbe` bit-reverses the word at `+0x20` into `+0x24`
and `0x180047bdd..0x180047c25` writes `+0x30..+0x37` as
`[b40,~b41,~b42,b43,b47,b46,b45,b44]`. Normal extraction applies this after
all records are formed; `FUN_18004b770` applies it per anti-fake record before
copying the selected 40 descriptor bytes.

The mode is caller-owned and is not implied by profile 9 alone. At
`FUN_18004ae70:0x18004b694`, the caller passes argument 5 in `EDX`. Exported
WBF enrollment supplies mode 0, while `identifyImage` supplies mode 1. Mode 0
performs the common nibble transformation and leaves `+0x30..+0x37` unchanged;
nonzero mode derives `+0x24..+0x27` and `+0x30..+0x37`. Mode does not alter
coordinates, orientation, strength, source descriptors, record count,
partition, or physical order.

A zero mode flag leaves `+0x30..+0x37` unchanged; any nonzero flag selects the
expansion. The DLL assumes a nonnull record from its internal callers.

Confidence is high for the in-place 56-byte-record transform and count
independence; semantic names for the descriptor halves remain unresolved.
