# FUN_180003800

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180003800..0x180003807`.
- Role: import thunk for the process C runtime allocator used throughout the
  engine.

## Call Graph

- The thunk jumps through IAT slot `0x18009a328` to `malloc`.
- Relevant caller `FUN_180068700` passes `payload_size + 0x20` at
  `0x180068779`; the returned allocation owns both the matrix header and inline
  payload.
- The paired C runtime `free` import is reached through IAT slot
  `0x18009a330` and is used by `FUN_180068b70` and the feature destructor.

## Recovered Behavior

The thunk preserves the unsigned 32-bit size in `ECX` and tail-jumps to the
runtime allocator. It does not add metadata, clear memory, or retain a separate
owner. For the profile-9 `52x44` byte matrix, `FUN_180068700` requests exactly
`0x20 + 2288` bytes and stores an inline data pointer at allocation `+0x18`.

The controlled-boundary oracle resolves the same DLL IAT allocator/free pair to
create a `0x20 + 2289` shadow, copies the complete original header and all 2,288
defined bytes, repoints only the inline data pointer to shadow `+0x20`, and
replaces the feature's owned matrix pointer. The unchanged DLL destructor can
therefore release the shadow through its normal allocator family.

## Evidence And Confidence

- Complete thunk assembly: `MOV ECX,ECX; JMP [0x18009a328]`.
- `FUN_180068700:0x18006875e..0x180068799` proves allocation size, fields, and
  inline-pointer construction.
- `FUN_180068b70` and `FUN_180037b10` prove normal nested ownership and free.
- Confidence is high. The exact runtime allocator implementation behind the
  imported `malloc` cannot provide a source-level value for the first byte past
  this allocation. Heap adjacency, allocator metadata, and reuse may affect it,
  but none is owned or constrained by the matrix, profile, device, or service
  lifecycle. Allocator-family identity remains relevant only to safe oracle
  replacement and destruction.
