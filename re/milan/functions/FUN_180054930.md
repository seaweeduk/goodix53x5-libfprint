# FUN_180054930

## Identity And Interface

- Body: `0x180054930..0x180054c66`.
- Role: second initial per-feature classifier, called by `FUN_180053220` when candidate flag64 remains zero.
- Inputs: candidate metrics, two unused image scalars, configuration, match output, candidate output.
- No internal callees apart from the stack-cookie check.

## Ordinary Type-12 Policy

Configuration word 16 is zero. Let `p=m0`, `n=m1`, `c=m9`, and `S=0x0fffffff`.
The three ten-entry tables are:

```text
detail   = [S,195,195,195,185,175,170,170,160,160]
low      = [S,207,205,200,199,193,185,185,185,185]
coverage = [S, 90, 85, 80, 73, 63, 55, 55, 55, 55]
```

When signed primary count is below five, replace the entire detail table with:

```text
[S,205,205,205,204,204,203,200,192,180]
```

This selection is at `0x180054ac5..0x180054af8`. It uses primary count, not
retained count or an adjusted count. When retained count is below 14, the
penalty is `3*((c<91)+(c<76)) + 5*(m13+m14)`; otherwise it is zero.
Set `d=m5-penalty`, `l=m8-penalty`.

The broad candidate output is the conjunction:

```text
i = clamp_signed(n - configuration[1] - 4, 0, 9)
l - configuration[0] >= low[i]
d - configuration[0] >= detail[i]
c >= coverage[i]
```

The strict match output is independently assigned from:

```text
j = clamp_signed(n - 4, 0, 9)
(p >= 6 || n >= 16 || (p >= 5 && m10 >= 56 && c >= 125))
&& l >= low[j] + 2
&& d >= 206
&& c >= min(100, coverage[j] + 10)
```

Strict success sets match one and ORs candidate with one. Strict failure sets
match zero without removing a broad success. All comparisons are signed.
Neither strict nor broad tests raw image quality or raw image coverage; the
coverage table reads candidate Q8 overlap coverage, not the image scalar.
Outputs are Boolean. Metrics and configuration are unchanged.
