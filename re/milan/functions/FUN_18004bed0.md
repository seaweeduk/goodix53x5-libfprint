# FUN_18004bed0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18004bed0..0x18004c0f6`; caller: `FUN_18004c5a0`.

## Recovered Behavior

It solves the 3x3 Hessian system for the Q12 refinement offset using cofactors,
determinant, signed bit-length scaling, and integer division. Ratios requiring
nine or more extra numerator bits saturate to positive `0x00ffffff`, regardless
of numerator sign (`0x18004c07a..0x18004c08b`). Signed bit length is one plus
the number of arithmetic right shifts needed to reduce the absolute magnitude
to zero, so zero has length one. For each axis, if the larger of numerator and
determinant bit lengths exceeds 32, both signed values are arithmetic-shifted by
that excess before signed division. The retained result is
`(scaled_numerator << 12) / scaled_determinant`, truncating toward zero
(`0x18004c08d..0x18004c0c9`).

A zero 3x3 determinant is not solver failure: the helper writes three zero
offsets and returns one at `0x18004bf88..0x18004bf93`. The caller may therefore
treat the solve as converged, but its later spatial determinant and contrast
predicates still govern admission. Intermediate cofactor products are signed
32-bit in the DLL and are widened for the determinant/numerators; the derivative
bounds from `FUN_180048c60` keep every such 32-bit product/difference
representable. Current source uses wider temporaries but is exact on that
reachable domain at `drivers/goodix53x5/milan/feature/extrema.c:154..219`.
Its `INT64_MIN` absolute-value and signed-shift edge cases require values that
the bounded derivative producer cannot emit.

## Confidence And Unresolved

- Confidence: high for the cofactors, widths, determinant-zero result,
  saturation, scaling, and signed division.
- Unresolved: none on the profile-9/type-12 path.
