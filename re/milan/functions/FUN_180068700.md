# FUN_180068700

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180068700..0x180068796`.
- Role: allocate a matrix object and its inline storage.

## Recovered Behavior

The helper allocates a 32-byte matrix header followed by storage. It records width, height, element size, row stride, storage size, and a pointer to the inline payload. Element size `8` is the packed-bit special case; the `FUN_180072860` path requests two-byte elements for signed record-index maps.

The complete allocation boundary is `0x18006875e..0x180068799`. It computes
`payload_size + 0x20`, calls the `malloc` thunk `FUN_180003800` at
`0x180068779`, records payload size at `+0x0c`, and points `+0x18` to allocation
`+0x20`. For a `52x44` one-byte matrix this is exactly 2,320 bytes; no byte is
allocated for payload index 2,288.

## Confidence And Unresolved

- Confidence: high for allocation size, allocator family, inline ownership, and
  matrix fields used by `FUN_180072860` and the profile-9 anti-fake boundary.
- Unresolved: source-level matrix type names.
