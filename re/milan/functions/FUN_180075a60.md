# FUN_180075a60

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180075a60`, `0x180075a60..0x180075db1`.
- Role: select bounded, spatially distinct descriptor correspondences.

## Call Graph

- Direct callers include `FUN_1800749c0`, `FUN_180075220`, `FUN_180076850`,
  `FUN_180077780`, `FUN_180078750`, `FUN_180078a70`, and `FUN_180079060`.
- Direct callees: `memset` and `__security_check_cookie`.

## Recovered Behavior

It filters candidate best/second distances by a ratio cross-product, rejects target indices below zero, and enforces target-coordinate separation: squared Q8 distance must be at least `0x10000`. A better-distance collision replaces the existing pair. When capacity is full, a better candidate replaces the currently worst selected distance. It emits ordered `(source_index,target_index)` pairs and fills the remaining capacity with `-1,-1`.

Capacity is caller-selected. Profile-9/type-12 primary and alternate paths pass
`42`; the reverse/sibling path at return RVA `0x79267` passes `31`. Evidence is
selection at `0x180075ae8..0x180075cec`, full-capacity replacement around
`0x180075c30`, and output/fill at `0x180075cf2..0x180075d9c`.

The corrected correspondence diagnostic v4 reproduces all `76/76` primary
ordered pair arrays from exact Windows probe records. This validates the native
ratio, collision, replacement, capacity, and sentinel-fill policy for this
profile; the earlier v3 `53/76` result incorrectly treated padded `-1,-1` slots
as selected pairs.

After partition-bounded alternate scanning, the same selector also produces
exact alternate ordered pairs `35/35`; primary pairs remain exact `76/76`.

The affine-guided sibling audit confirms that this helper is downstream of the
five-row retained-count defect. Once `FUN_180075dc0` uses its official limit
`16`, the selected pair sets and subsequent filtered counts are official-exact.
At the collision loop, a candidate that has already replaced one nearby pair
continues scanning and removes every later worse collision; a later equal or
better pair does not undo that prior replacement. A temporary correction of
the native early-break translation did not change any of the five target rows,
so that separate control-flow difference is not part of this coding contract.

Post-implementation replay reaches those exact five-row counts by changing only
the upstream tail-Hamming limit. This selector's ratio, collision, replacement,
capacity, and sentinel-fill implementation is unchanged.

Row `16/1` adds a type-12 capacity-42 boundary: normalized primary and alternate
native pair sets each equal the official set plus one pair targeting the sole
native-only record. The pair passes the existing strict ratio because native
best/second is `46/63`; official `63/65` fails. No ratio, spacing, collision,
replacement, or capacity change is indicated.

## Confidence And Unresolved

- Confidence: high; exact ordered pairs and both observed capacities are frozen.
- Type-12 unresolved behavior: none. The exact ratio operands are fixed below;
  source-level labels are nonsemantic.

The labels are operationally fixed for profile 9: strict
`best_distance*40 < second_distance*38`. Direct generated controls cover
equality, one-unit acceptance, duplicate tie/replacement/pruning, first-worst
capacity tie, strict capacity replacement, compact order, and sentinel fill.
No type-12 selector behavior remains unresolved.
