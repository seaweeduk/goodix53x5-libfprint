# FUN_180073ff0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900.
- Entry/body: `0x180073ff0..0x1800740e2`; callers include `FUN_180039220`
  and the profile-9 quality-validity builder `FUN_1800446f0`.

## Recovered Behavior

For every byte, the function writes `0xff` when the source pixel or any valid
horizontal/vertical neighbor at distance one or two is nonzero, otherwise zero.
Bounds suppress out-of-plane reads. This is the radius-two cross dilation used
by the anti-fake mask.

The quality-validity caller supplies a completed temporary input plane and a
distinct output plane. In that ownership path, the operation is one dilation
of the original support, not an iterative or in-place expansion. Argument 3 is
height and argument 4 is width; the scan advances densely by one byte per
column and uses width for vertical offsets. No diagonal neighbors participate.

The neighbor predicates and byte stores are at `0x180074020..0x1800740c3`.
