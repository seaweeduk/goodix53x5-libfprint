# FUN_18005ef60

## Binary And Body

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18005ef60..0x18005f146` (487 bytes).
- Ghidra CFG: 15 basic blocks, 26 edges, cyclomatic complexity 13, no loop.
- Role: combined matcher-and-study wrapper. It initializes fixed matcher state,
  dispatches the template family, then invokes the ordinary study dispatcher.

## Call Graph

- No direct code caller is resolved. Ghidra records three data references at
  `0x18027081c`, `0x1800ea78c`, and `0x1800ea79c`; targeted unit-5 inspection
  resolves them as PE runtime/unwind metadata, not indirect registration or a
  callable dispatch table.
- Direct callees: `FUN_180046ec0`, `FUN_180055a40`, `FUN_180062a30`,
  `FUN_180044fc0`, and `__security_check_cookie`.

## Recovered Interface

```c
int64_t FUN_18005ef60(
  void *score,
  void *probe,
  uint32_t *template_object,
  void *arg4,
  void *arg5,
  uint32_t initialization_mode,
  int32_t *study_action,
  void *alternate_context);
```

Names remain provisional. The template type at `template_object[0]`, output
action pointer, family calls, and final zero return are established. Ghidra's
current undefined return type is stale; every observed return writes zero to
`RAX`.

## Behavior

The wrapper first rejects a small legacy type bitset without matching. Other
types receive a fixed stack configuration and initialization through
`FUN_180046ec0`. The family split is the same one used by the ordinary matcher
dispatcher:

- Types `9`, `10`, `17`, and `18` call `FUN_180062a30`.
- Type `12`, including production profile 9, calls `FUN_180055a40`.

The type-12 configuration includes fixed words `5`, `218`, `23`, `47`, `40`,
`38`, and `-1`, matching the recovered policy family. Alternate-family types
adjust selected words (`120` for type 9, `100` for types 10/17/18, and `36` for
types 17/18).

After either matcher returns, the wrapper marks its local evidence object live,
calls `FUN_180044fc0(template, probe, evidence, result)`, copies the returned
study action to the caller, and returns zero. Thus the type-12 path is match
followed by study against the same live template/probe/evidence transaction; it
does not bypass the lifecycle or queue dispatchers.

## Evidence Addresses

- Legacy type-bitset return: `0x18005ef98..0x18005efa8`.
- Type-12 configuration and `FUN_180055a40`: `0x18005efae..0x18005f03c`.
- Alternate-family configuration and `FUN_180062a30`:
  `0x18005f041..0x18005f0fc`.
- Shared study call and action copy: `0x18005f0fc..0x18005f124`.
- Final zero return: `0x18005f127..0x18005f146`.

## Native Coverage And Confidence

The native equivalent is compositional rather than a one-for-one wrapper:
`goodix_match_serialized_feature_result_internal()` performs the match and
match-time lifecycle update, while `goodix_match_study_feature_internal()` and
the queued wrapper perform ordinary and follow-up study. Those components are
covered by immutable matching, chronological study, rare-action, and action-5
queue gates. No test establishes a live caller or every alternate-family
constant in this wrapper.

Confidence is high for the profile-9/type-12 family call, fixed-state setup,
shared study call, action copy, zero return, and absence of a resolved code
caller. Provisional argument names remain medium confidence. Alternate template
families are outside the current profile-9/type-12 audit.

## Unit-5 Reachability Boundary

The metadata triplet around `0x18027081c` contains function begin RVA
`0x5ef60`, end RVA `0x5efb7`, and unwind RVA `0xea768`; the corresponding
`0x1800ea78c/0x1800ea79c` references are metadata duplicates. No code xref calls
this wrapper. It therefore does not override the proven production adapter path
`FUN_18002ba60 -> templateStudy -> FUN_180044fc0`, where shared evidence
`+0x688==0` skips the dispatcher at the export. Reachability through an external
mechanism not represented by a code xref is not claimed, but there is no static
evidence for the formerly suggested indirect registration.

If this wrapper is reached externally, its subtype-12 call inherits all four
decision-half gaps documented in `FUN_180055a40.md`. Its unresolved production
reachability neither proves nor excludes those downstream contracts; it only
keeps this combined match-and-study entry outside the ordinary exported-call
frequency claim.
