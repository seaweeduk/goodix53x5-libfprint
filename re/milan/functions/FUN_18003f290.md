# FUN_18003f290

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18003f290`, `0x18003f290..0x18003f43f`.
- Role: encode the fixed Milan template tail object.

## Call Graph

- Caller: `FUN_18003eaf0`.
- Callees: none.

## Recovered Behavior

The helper writes tag `94`, then emits `a1` with fixed length 200 by copying
tail offsets `0x000..0x0c7` verbatim. It follows with tagged `a2` at `0x0c8`,
the 64-byte `a3` block at `0x0cc`, the 0x400-byte `a4` block at `0x10c`, and
tagged dwords `a5..a8` at `0x50c..0x51b`.

The study order vector occupies the complete 200-byte `a1` source, with the
defined prefix bounded by current feature count. The helper performs no sorting
or interpretation.

The independent audit confirms that the nine defined dwords are feature indices
and the remaining 41 dwords are `UINT32_MAX` in every accepted sequence-2
lineage. Direct helper runs with non-`UINT32_MAX` poisoned suffixes prove the
sorter leaves that range untouched before this verbatim copy.

## Confidence And Unresolved

- Confidence: high from straight-line assembly and exact native/DLL packing.
- Production-order audit report SHA-256:
  `06fda76c874dd0aa554835c5f69ba9d9289f461aa0bc046901f8be430ba7c435`.
- Reserved subranges outside the order vector retain the safety classification
  documented in `PARITY.md`.
