# FUN_180047580

## Identity

- Binary: `GoodixEngineAdapter.dll`.
- Binary SHA-256:
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180047580..0x180047608`.
- Role: count active bits in a packed matrix.

## Behavior

The function walks all payload bytes declared by the matrix header and sums a
256-entry byte popcount table. It returns zero when the total is below 20;
otherwise it returns the exact active-bit count. `FUN_18005d9e0` calls it before
and after each destructive interval removal, so their difference is the ordered
marginal area before optional half-resolution scaling.

## Evidence And Confidence

- The payload pointer is matrix offset `+0x18`; payload length is offset `+0x0c`.
- The frozen aggregate replay gives active counts `1976 -> 1286 -> 787`, hence
  unscaled marginal counts `690` and `499`.
- Confidence is high. The below-20 behavior is not reached by the frozen row but
  is explicit in the function body.
