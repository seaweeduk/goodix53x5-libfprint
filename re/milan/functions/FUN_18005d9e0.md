# FUN_18005d9e0

## Identity

- Binary: `GoodixEngineAdapter.dll`.
- Binary SHA-256:
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18005d9e0..0x18005ddc9`.
- Role: post-loop uncovered-area aggregate rescue and retained-feature
  selection.

## Call Graph

- Sole direct caller: `FUN_180055a40`; the relevant call is at
  `0x180056caf`.
- Direct callees: `FUN_180047580`, `FUN_180068660`, `FUN_180068700`,
  `FUN_180068b70`, `FUN_180071370`, `FUN_180072600`, `memset`, and
  `__security_check_cookie`.

## Recovered Interface

```c
void FUN_18005d9e0(
  const uint32_t *configuration,
  void *live_feature,
  const void *candidate_records,
  int32_t *score,
  void *match_evidence);
```

The names are provisional. The function consumes configuration dimensions and
type, an ordered retained-candidate index list, fixed-size candidate records,
and caller-owned score/evidence outputs.

## Behavior

The function clones the live feature's row-packed matrix at `+0x140` into a
temporary working matrix and iterates the ordered candidate indices at context
`+0x87e8`, with count at context `+0x1c`. A record is `0x134` bytes (`77`
signed words). It is eligible only when word 1 (`+0x04`) exceeds four and word 5
(`+0x14`) exceeds 189. For each eligible record it:

- applies the record transform to derive a clipped coverage workspace through
  `FUN_180071370`;
- subtracts the transformed region through `FUN_180072600`;
- measures newly uncovered area with `FUN_180047580`;
- accumulates area-weighted record words 5 and 8; and
- retains the feature index with the greatest candidate count.

When context `+0x0c` is nonzero, the working dimensions and transform
translations are arithmetic-shifted right by one, while each newly uncovered
area is multiplied by four. Linear transform terms are unchanged. Processing is
ordered and destructive: each transformed region is removed from the same
working matrix before the next candidate, so increments are marginal rather
than independent overlap areas.

The common gate is exactly:

```text
eligible_count > 1
total_uncovered_area != 0
weighted_word5 / total_uncovered_area > 208
sum_count / eligible_count > 6
weighted_word8 / total_uncovered_area > 183
count(new_area > area_threshold) > 1
```

`area_threshold` is `1100` for types 11 and 21 and `1400` otherwise. Types
12, 13, 16, and 22 (mask `0x413000`) use score denominator 42; all others use
31. Every division is signed integer truncation and every comparison above is
strict.

When the common gate succeeds, the function writes evidence `+0x684 = 1`,
stores the strict-greatest-count feature at `+0x648`, and writes
`score = best_count * 100 / denominator`. Equal counts retain the earlier
ordered feature. It sets `+0x688 = 1` and copies that record's unscaled transform
to `+0x650..+0x660` only when the selected index is valid and at least one
strong condition holds:

```text
incoming_score > 40
or count(new_area > area_threshold) > 2
or (type in {11,21} and weighted_word5 / area > 216
                    and weighted_word8 / area > 202)
```

The ordinary acceptance write is independent of this stronger branch. Thus a
rescue may select an index and score while retaining the caller's prior
transform and leaving rejection evidence clear.

## Decision-Divergence Evidence

The immutable six-row route trace classifies `81/1` here. Every per-feature
candidate on that row has final tuple `[0,0]`; after the feature loop,
`FUN_180055a40` calls this function at `0x180056caf`, which selects feature 2
and produces score 19. The same trace records no entry to `FUN_18005d5f0`,
`FUN_1800608b0`, or the other instrumented post-loop route. Thus `81/1` is an
aggregate rescue, not a direct-policy result or compact final-score result.

Frozen reference:

`<evidence-root>/references/milan-native-capture-20260723/windows-native-decision-divergence-classification-v2/`

Its complete tree SHA-256 is
`a34258e52a4985468370afc233e01bf057f3c29a2d31f343b008225fa07cef1f`.

## Focused Aggregate Trace

The missing non-forced input/loop/output trace is frozen at:

`<evidence-root>/references/milan-native-capture-20260723/windows-native-aggregate-rescue-v1/`

Frozen identities:

- Complete 27-artifact manifest/tree:
  `87b2b6a9b8e9cc729021f4e83263caf3de4906adb3e10214572a4c7c151e5bd2`.
- Comparison:
  `b511d361caf44ecf3db33ccde965eabac99f7b769b66b21602bf02ee54fb7997`.
- Repeated native projection, byte-identical:
  `2a2376579f72debf41ce2bd65104f0e466cefa22c4b39957060af074e1685088`.
- Repeated official summary/records/matrix:
  `2eab7c63de840f1663d68284b031ce1933a6aa24c855ee89c36a63cf5f1fb314`,
  `6352993e6268283e64d03e6e1834e04a970459cb5dd345a5aa084b0474b388d6`,
  and `8e90677b68d4090707f639eac4b43390286399359b89ddf917906442d9d9b35b`.
- Provenance/validation:
  `1c53fd903a863711fe4fe4dfb121e77f6cf9f4bb4b7a712dbab031844eef7089` /
  `cde6063f012d50ff54b52711542f39b6f96ab4d33ec0791be81d35db6f3040ab`.

Two independent sealed-prefix captures are byte-identical for every aggregate
artifact, and two fresh native processes produce the same native projection.
The instrumented processed frame and after-match template remain exact to the
frozen immutable row. Hooks force no branch, feed no state back, call no study,
and do not rerun the campaign.

Exact official `81/1` inputs and flow are:

- Caller gate operands are aggregate-candidate count `2`, disqualifying count
  `0`, rejection evidence `0`, type `12`, and incoming score `0`; all call
  predicates pass. The function context is `[12,104,88,1,1,150,150,8]` and the
  ordered feature list is `[0,1,2,3,4,5,6,7]`.
- The source matrix header is `[7,44,7,308,1]`. The function operates at
  `52x44`, clones the row-packed 308-byte source, and applies half-resolution
  translation and area scaling.
- Only feature 0 (`count=7`, word 5 `217`, word 8 `208`) and feature 2
  (`count=8`, word 5 `217`, word 8 `216`) are eligible. Their unscaled record
  transforms are `[276,-33,17267,38,246,-4711]` and
  `[270,6,5725,1,264,12639]`; coverage uses translations
  `8633/-2356` and `2862/6319` after arithmetic shifts.
- Ordered marginal uncovered areas are `2760` and `1996`. Final operands are
  eligible count `2`, area `4756`, weighted word 5 `217`, average count `7`,
  weighted word 8 `211`, and two increments strictly above `1400`. Every common
  predicate passes.
- Strict count ranking changes best feature/count from `0/7` to `2/8`.
  `8 * 100 / 42 == 19`. The stronger branch is false because incoming score is
  not above 40, only two increments exceed 1400, and type 12 is not special.
- State changes only as required: score `0 -> 19`, matched feature `-1 -> 2`,
  and acceptance `0 -> 1`. Rejection stays `0`, liveness stays `1`, numerator
  stays `0`, and the identity transform `[256,0,0,0,256,0]` is retained.

The official/native aggregate boundary is not same-input parity. Native feature
0 is all-77-word exact, but native feature 2 differs at words
`1,2,4,5,7,8,9,11,15,16,17,19,20,69`; its transform is
`[267,0,6025,1,266,12570]`. The native dense source mask differs from the
official row-packed matrix in 18 active cells across 17 bytes. These are frozen
upstream input differences, not permission to alter rescue thresholds. Current
native matching without this rescue returns feature `-1`, score `-1`, decision
0.

## Evidence Addresses

- Temporary matrix setup: `0x18005d9e0..0x18005da55`.
- Candidate loop and uncovered-area accumulation:
  `0x18005da55..0x18005dc5f`.
- Aggregate thresholds: `0x18005dc63..0x18005dd4b`.
- Evidence/index/transform and score writes:
  `0x18005dd4b..0x18005dda6`.
- Caller gate and invocation: `FUN_180055a40:0x180056c75..0x180056caf`.

## Confidence And Open Questions

- Confidence is high for the route, caller predicate, ordered inputs, eligibility,
  transforms, area arithmetic, all common and stronger predicates, selected
  index, score source, and `81/1` state mutation.
- Semantic names for record words 5 and 8 remain provisional. The stronger
  branch and type-11/21 alternatives are exact from assembly but are not
  exercised by this profile-9/type-12 row.
- A generic implementation must first replay the official frozen input exactly,
  then run unchanged thresholds against current native inputs. If native inputs
  do not naturally produce the expected rescue, fix their upstream mask or
  candidate-state source; do not hardcode row 81, substitute official values,
  or fit thresholds.

## Native Implementation

The generic implementation is isolated in
`drivers/goodix53x5/goodix53x5-match-rescue.c/.h` and runs before Q8 winner
finalization. Its focused gate replays the official matrix/records exactly,
including marginal areas `2760/1996`, aggregate values `4756/217/7/211/2`,
feature 2, score 19, strict caller/common/stronger boundaries, count ties,
denominator selection, transform retention, and reversed-order areas
`2656/2100`.

Unchanged native row `81/1` inputs naturally pass the common gate: marginal
areas are `2760/1972`, total area `4732`, weighted words are `217/211`, average
count is `8`, and two increments exceed 1400. Native therefore selects feature
2 and closes decision/index parity. It scores 21 rather than 19 because native
feature-2 word 1 is still 9 rather than official 8; exact score remains blocked
by the frozen 14-word candidate-state difference, not rescue thresholds.

## Match-Time Lifecycle Consequence

This rescue writes score, acceptance evidence, and selected feature but never
increments feature `be`. Its caller resumes at `0x180056cb4`; a positive rescue
keeps score nonzero and therefore bypasses both the no-direct aggregate loop and
`FUN_18005d5f0`. It adds no bit and does not roll back any direct increment that
may already have occurred in the feature loop. Repeated normal-live passive
traces of immutable e1 sequence 81 select feature 2 at score 19 with no direct
or aggregate increment event and an unchanged eight-feature `be` vector. The
previous native selected-bit mask `0x4` is not DLL behavior and must be replaced
with zero.

The current-source sequence-98 chronology adds the complementary positive
control. In both lanes a direct-positive feature exists before this rescue wins
the final route. Windows preserves that earlier direct `be` increment; the
rescue still contributes no new increment. Native rescue telemetry incorrectly
returns lifecycle mask zero despite direct masks `0x40000` and `0x20`. The fix
belongs to the caller's lifecycle handoff: retain prior direct bits, never add
the rescue-selected bit, and keep rescue-only row `81/1` at zero.

## Native Caller-Handoff Closure

`milan_match_prepared_probe()` now publishes the accumulated direct-positive
mask before returning from a successful rescue. `FUN_18005d9e0`'s native
equivalent remains unchanged: it still adds no lifecycle bit of its own.

Focused controls confirm both sides of the contract. Rescue-only `81/1` retains
mask zero, while mixed direct-then-rescue sequence 98 retains `0x40000` for
enrollment 1 and `0x20` for enrollment 2. Contributor-only and rescue-selected
bits are absent, and both after-match templates are byte-exact against the
official chronology reference. Score, selection, study action gate, update code,
and persistence advance are unchanged. This closes the caller handoff for the
predicted accepted-rescue-with-prior-direct-mask family; full chronology remains
deferred.

## Relation-Evidence Handoff Correction (Identify 305)

Identify 305 adds a second mixed direct-then-rescue handoff. Before this helper,
active direct-positive feature 33 owns evidence relation count/affine
`10/[254,-35,5792,35,254,-2496]`. This helper then strongly selects feature 8,
score 26, and selected affine `[268,74,-16487,-74,246,18204]`.

The complete body confirms that the strong branch at
`0x18005dd4b..0x18005dd7f` writes only `+0x688`, `+0x648`, and
`+0x650..+0x660`; the common success block at `0x18005dd88..0x18005dda3` writes
only `+0x684`, `+0x648`, and score. There is no access to relation count
`+0x668` or relation affine `+0x66c..+0x680`. Therefore relation evidence is a
second caller-owned state, like the previously corrected direct lifecycle mask:
the helper neither creates it nor clears it.

Current source incorrectly recomputes rescue relation evidence from selected
feature 8. The minimal generic caller correction preserves the independently
accumulated active relation winner across rescue. Earlier rescue controls did not
assert this state because they focused score/index/transform and lifecycle-mask
handoff. See
`re/milan/IDENTIFY-305-STUDY-RELATION-ROOT-CAUSE-20260808.md`.
