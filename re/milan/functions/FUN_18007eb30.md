# FUN_18007eb30

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18007eb30`; caller `FUN_18004ae70`.
- Role: allocate and produce the enhanced feature-extraction image.

## Recovered Behavior

It allocates an output matrix matching the input, builds local enhancement state
with `FUN_18007ebe0`, then writes the enhanced image through `FUN_18007f2c0`.
This is the source of descriptor gradients passed into `FUN_180049b90`; the
shared 135 authoritative `142/1` records already match byte-for-byte, so this
helper is not the row's first divergence.

The profile-9/type-12 input is the centered `104x88` byte image. The wrapper
passes radius `6` to `FUN_18007ebe0` through the fifth stack argument and keeps
the resulting orientation bytes in its 19,600-byte stack workspace. It then
passes orientation, the original input, the newly allocated output payload,
width, and height to `FUN_18007f2c0`. The returned matrix owns the enhanced
payload; the orientation workspace is transient. Current source implements the
same sequence in `drivers/goodix53x5/milan/feature/enhancement.c:173-305` and
calls it from `feature/extraction.c:373-381`.

## Confidence And Unresolved

- Confidence: high for allocation, dimensions, radius, ownership, call ordering,
  and exclusion from the row-142 count/ranking mismatch.
- The orientation and interpolation arithmetic are documented under
  `FUN_18007ebe0` and `FUN_18007f2c0`; no profile-9 enhancement workspace
  semantics remain unresolved.
