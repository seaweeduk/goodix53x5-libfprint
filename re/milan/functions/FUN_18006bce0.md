# FUN_18006bce0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18006bce0..0x18006c0a0`, rendering-mask gap fill and recount.

## Call Graph

- Sole caller: `FUN_18006d540` at `0x18006d933`.
- No substantive direct callees beyond stack-safety failure handling.

## Behavior

- Builds row and column occupancy summaries for the supplied mask, fills zeros
  bounded by occupied runs, and recounts the resulting mask.
- Production subtype 12 then clears samples whose normalized live value is not
  strictly between 100 and 3800, updating the count and percentage.
- `FUN_18006d540` uses the hole-filled/recounted clone as the render mask for
  the primary `FUN_18006b510` call and passes it through `FUN_18006e7f0` to the
  alternate `FUN_18006d430` renderer. The clone's recomputed percentage
  therefore selects masked versus unmasked row centering for the alternate
  image.
- The normal calibration call uses the earlier pre-fill mask structure. The
  conditional post-render `FUN_1800668f0` call uses the clone, so update
  admission and rendering can intentionally observe different counts from the
  same live frame.

## Evidence

- Row/column summaries: `0x18006bd6a..0x18006be35`.
- Bounded-gap fill and recount: `0x18006be42..0x18006bfcc`.
- Profile-12 range clearing and percentage: `0x18006bfd5..0x18006c08f`.

## Confidence

High for mask mutation, recount, and caller alias; medium for the vendor's
intended morphology name.

## Unresolved

- Functional impact on other captures remains open. Direct NOP and `UD2`
  call-site controls prove the downstream conditional post-render update is not
  reached in the supplied action-2 or action-7 enrollment lifecycle, so this
  mask cannot explain their relation behavior.
