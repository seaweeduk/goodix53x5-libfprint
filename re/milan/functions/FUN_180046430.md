# FUN_180046430

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180046430`, `0x180046430..0x18004677b`.
- Role: merge retained evidence transforms into the live relation graph.

## Call Graph

- Callers: `FUN_180044fc0` and `FUN_1800469f0`.
- Callees: `FUN_180068860`, `FUN_1800689a0`, and the security-cookie helper.

## Recovered Behavior

The recognition-evidence prefix is an array of 32-byte entries followed by its
count at evidence `+0x640`. Each entry contains a feature index at `+0x00`, a
six-word affine at `+0x04..+0x18`, and one unused-by-this-helper dword at
`+0x1c`. The helper loops exactly `count` entries with no internal count or index
validation.

For every entry it first writes literal `1` to the indexed live feature's
active/retained field at `+0x110` (`0x1800464bb..0x1800464d5`). It then inverts
the entry transform, composes it through the evidence relation transform at
`+0x66c` and the template runtime graph anchor at `+0x87d8`, initializes
unavailable triangular relation slots, and writes the selected direct or inverse
relation into template relation storage beginning at `+0x1b8`.

The exact slot is `feature[max(index,reference)].b6 + min(index,reference)`.
When the indexed feature is not the reference, the helper first resets all of
that feature's incident slots to `[leading=-1, affine identity]`, then writes the
reference edge with `leading=0`; it inverts only when needed for canonical
higher-index-to-lower-index storage direction. When the indexed feature is the reference, it
composes the refresh through every already-defined reference edge and preserves
their leading values.

The helper does not consult the matched feature at evidence `+0x648`, matcher
direct-positive flags, the final winner, or the study update code. Its feature
set is exactly the indices in the retained-evidence array. It neither reads nor
writes template order `+0x87e8`; the later type-12 sorter consumes the resulting
`b5` values but does not create them.

`FUN_180044fc0` calls this helper before append/replacement only when
`evidence+0x640 > 0 && evidence+0x644 == 1`. `FUN_1800469f0` has a second call
used only by its graph-unestablished append fallback when that append first
establishes the graph and evidence count is positive. The second call does not
test `+0x644`.

For each ordered entry affine `T`, the exact transform before relation mutation
is `X = compose(evidence_relation, inverse(T))`. For a nonreference entry, the
helper clears every incident edge and writes leading zero. It stores `X` when
the entry index is above the reference and `inverse(X)` when the entry index is
below it, preserving canonical higher-to-lower direction. For a reference entry,
it preserves leading words and composes all defined star edges: lower-index
edges receive `compose(old, X)`, while higher-index edges receive
`compose(inverse(X), old)`.

The first-graph caller makes helper ordering observable. With retained flag one,
`FUN_180044fc0` first invokes this helper against stale `f2` while `f5==0`.
`FUN_1800469f0` then sets `f2=matched`, creates the appended anchor, and invokes
this helper again. If the list contains the future reference, a first-pass
stale-reference edge can survive because the second-pass reference case composes
rather than clears it. Implementations must execute the two ordered passes; a
single replay against the final reference is not equivalent. With flag zero,
only the post-anchor fallback pass runs.

## Sequence-3 Validation

For normal-boundary enrollment 1 sequence 3, the retained-entry count is
four and the ordered entry indices are `[8,7,2,1]`; the matched feature is `1`.
The helper changes existing feature `b5` values at exactly `{1,2,7,8}` from zero
to one. Match-time lifecycle tag `be` changed on `{0,1,2,7,8}`, so the retained
set is not the complete direct/lifecycle set and is not selected-feature-only.

Across all 64 append rows in both authoritative chronological lanes, the
evidence entry set equals the inactive subset of the match-time lifecycle set.
Only three rows satisfy the caller condition, and their existing `b5`
activations equal the evidence indices exactly: enrollment 1 sequences 3 and 4,
and enrollment 2 sequence 5. All 61 controls outside the condition have no
existing-feature `b5` transition.

The current strict 643-operation authority expands the natural boundary to 91
retained entries with marker and incident-relation changes. Exact changed slots,
hashes, and one append/action-4 witness are maintained in
`../PROFILE9-TYPE12-STUDY-RELATIONS.md`.

Persistent analysis:
`<evidence-root>/derived/milan-native-capture-20260723/native-study-b5-retained-refresh-v1/analysis.json`,
SHA-256 `f06acddc3bbfda96828f2815f9bb00792b1eb76eeb55ec59c033a0e2a02133aa`.

## Native Boundary

The native matcher records the ordered inactive final-direct entries with each
six-word transform and carries the independent retained flag. Study validates
the 50-entry capacity and indices, applies the caller conjunction, updates `b5`,
and composes each retained transform through the separately retained evidence
relation transform in a bounded local matrix.

Independent controls reproduce `[5]`, `[8,7,2,1]`, `[5,6,3]`, `[]`, and `[7]`
for enrollment 1 sequences 2/3/4/6 and enrollment 2 sequence 5. Sequence 3's
native `b5` vector, order, relation count six, slots `[1,2,7,22,29,37]`, and all
defined relation values are exact.

The complete sequence-3 relation transition is now resolved. Before study, the
reference-0 star contains positive slot `[7]`. This helper materializes
zero-leading slots `[29,22,2,1]` for retained entries `[8,7,2,1]` (packed order
later becomes `[1,2,7,22,29]`). Append then creates feature 9 with `b6=37`, and
`FUN_180045d40` adds zero-leading slot `37`. The final packed list is exactly
`[1,2,7,22,29,37]`: five zero-leading edges plus the retained positive edge.

## Confidence And Unresolved

- Confidence: high for the entry layout, literal activation write, caller
  conditions, exact Q8 composition/direction, affected feature set, two-pass
  first-graph behavior, relation ownership, and exclusion from the order field.
- The source-level vendor name for the retained-evidence array remains unknown.
- Native focused matrix and integrated sequence-3 controls implement and verify
  the six-slot star above. Hardware and durable-runtime validation remain open.
- First-graph direct controls are under
  `<evidence-root>/derived/milan-native-capture-20260723/study-first-graph-unit3-v1/`.
  They prove empty and multiple lists, flag-zero fallback replay, flag-one
  two-pass stale-reference replay, identity/nonidentity arithmetic, and both
  canonical directions. Native now executes the caller prepass and selector
  postpass as two ordered calls, reuses owned feature copies safely across both,
  and reproduces all 13 packed DLL results exactly. Reference replay passes `X`
  to the re-anchor primitive, which performs the one required inversion and
  yields recovered `compose(inverse(X), old)` higher-index edges.

## Full-Capacity Consumer Ordering

For the primary type-12 caller, this helper completes before the exact
`feature_count==maximum_features` test and before
`FUN_180047550 -> FUN_180047120`. The normalizer therefore consumes this
helper's updated `b5` and relation matrix. A retained nonreference control proves
`b5 0->1`, incident-relation replacement, complete-gallery `bb/+0x148`
recomputation, action 0, queue shutdown, and transient order/tail finalization in
that order. A retained reference identity control leaves the star unchanged but
still discriminates stale feature-13 selection from normalized feature-34
selection.

The queue caller `FUN_18005d330` does not invoke this helper before its direct
`FUN_1800469f0` call. This pre-selector refresh/normalization contract is thus
owned by `FUN_180044fc0`, not by the selector or every selector caller. Unit-4
evidence is under `study-capacity-normalization-unit4-v1` in the persistent
derived root.

Native preserves that ownership with an explicit caller-context parameter.
Only primary dispatcher entry under positive count and flag one invokes
`milan_study_refresh_retained`; it projects the resulting matrix into the same
working gallery before whole-gallery normalization. Flag-zero/count-zero and
queued direct-selector paths do not call or validate this prepass. The ten-case
native gate is exact, including retained reference/nonreference and 39-to-40
bypass controls.
