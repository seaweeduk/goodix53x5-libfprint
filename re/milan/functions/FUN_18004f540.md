# FUN_18004f540

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18004f540..0x18004f84f`, profile broken-level
  component classifier with retained age state.
- Profile-9 caller: `FUN_18004d510`, reached through
  `FUN_18004ef70` / `FUN_1800508a0`.

## Static Contract

- Computes the positive-gradient average, then uses `average * 6 / 5` for
  packed chip type `0x0c` (profile 9 / sensor type 12).
- Reads retained global counter `DAT_1801dc9a0`. The per-pixel age threshold is
  `counter` for values zero through two, `counter - 1` above two, capped at two.
- Builds a candidate plane only where gradient is strictly above that threshold
  and the retained age byte is at least the derived age threshold.
- Labels four-connected components through `FUN_1800807a0`, admits label 71,
  selects the three largest components strictly larger than 75 pixels through
  `FUN_1800802c0`, and writes class 1 only into previously unclassified pixels.
- Retained component ages and `DAT_1801dc9a0` are updated only under the gate at
  `0x18004f6cb..0x18004f734`. For normal type-12 selected-plane values 0/1, the
  exact gate is integer active-mask coverage `>= 75`. With 9,504 pixels this
  starts at 7,128 active pixels. Coverage below 75 freezes both ages and count.
- The generic selected-plane value 2 preserves prior coverage in
  `DAT_1801dc9ac` and gates on prior coverage below 75, but the normal profile-9
  selector writes only 0 or 1. Mode 2 requires imported, stale, or manipulated
  workspace state and is not a normal type-12 transition.
- When the gate executes, gradients strictly above `floor(mean * 6 / 5)`
  increment age up to five; gradients at or below `mean` decrement nonzero age;
  the interval between them is unchanged. The retained counter increments up to
  five.
- Candidate construction precedes current-call mutation. Its age threshold is
  count 0/1/2 for retained count 0/1/2, and two for retained counts 3 through 5.
- Four-connected labels 1 through 71 are representable. At most the three
  largest components strictly larger than 75 pixels become class 1, only where
  the prior class plane is zero.

## Natural Multi-Use Witness (2026-08-06)

- Passive Linux campaign
  `milan-profile9-fdt-refresh-dispatch-stop-fix-v1`, capture session
  `b2d87afa-2c69-47e1-b173-68b920d4ad4d`, generation 27, contains a natural
  identify no-match at use 1 followed by a match at use 2.
- The approved DLL and current Linux replay agree on use-2 quality/coverage
  `51/84`, active records `126`, score `28`, winner `0`, study action `1`, and
  the complete gallery result.
- Processed output differs in only 57 bytes, all in row zero and all by one.
  Native row-zero parity decodes metadata mode 5; Linux selects mode 0 with
  class counts `1/8/0`.
- Native replay is deterministic across three fresh runs at processed SHA-256
  `be8b86b99e79ea81423bca29d948b4e2228bf279b660b92ef3ff958d865617b5`.
  Linux and the live capture agree at
  `641f5453eefd9a90220a90329381993dc5987887b845646abdecb0b27e94b0dd`.
- The preceding use-1 frame has coverage 67. Native freezes component ages and
  count; Linux currently advances them unconditionally. On use 2, native count
  therefore remains zero while Linux uses one. This is the complete immediate
  cause of native mode 5 versus Linux mode 0 for the witness.

## Context Fields

- `FUN_18006b290` context `+0x10`: prior selected-plane value, normally 0/1.
- `+0x14`: packed sensor type, exactly 12.
- `+0x18`: active-mask pixel count used for integer coverage.
- `+0x20`: output class-plane pointer.

See `FUN_1800501d0.md`, `FUN_1800512c0.md`, and `FUN_18004ff40.md` for the
complete retained-state lifecycle.

## Controlled Chronology Matrix (2026-08-06)

Using one captured profile-9 setup, one natural coverage-67 no-match frame, one
natural coverage-100 frame, and a fixed target/gallery:

- fresh, one-high, two-high, three-high, low-high, and high-low preludes remain
  byte-exact against the approved DLL;
- one through five repeated low preludes reproduce the row-zero metadata and
  candidate divergence while all matching behavior remains exact;
- this persistence is expected from the proven native freeze versus Linux
  unconditional component-age update and is not a sample-specific threshold
  adjustment.

Longer high-coverage sequences expose separate post-render and long-history
differences documented in `FUN_18006c510.md` and must be resolved independently.

## Linux Correction Validation (2026-08-06)

Direct assembly reinspection confirmed the retained-state block independently
of replay lifetime assumptions:

- `0x18004f6cb..0x18004f6e3` admits normal selected-plane values 0/1 only when
  integer active-mask coverage is at least 75;
- `0x18004f6f0..0x18004f710` increments ages above `mean * 6 / 5` up to five,
  decrements nonzero ages only at or below `mean`, and leaves the interval
  between those thresholds unchanged;
- `0x18004f71e..0x18004f72d` increments the retained count up to five only
  inside the same gate.

After applying those exact rules to Linux, rebuilt-current and approved-DLL
targeted replay are byte-exact for generation 27 use 1 (coverage-67 no-match)
and use 2 (the former epoch-59 mismatch), including processed image, extracted
record count, gallery mutations, score, study action, and final candidate.
Fresh high-coverage controls at epochs 56 and 61 also remain exact.

A fresh hardware campaign captured with the corrected debug driver then passed
41/41 native-comparable operations. Its generation 31 directly exercised the
fixed transition: use 2 had coverage 68 and no match, followed by use 3 at
coverage 98 with a successful match and study; both were byte-exact with the
approved DLL. Generation 36 additionally exercised the inclusive boundary with
coverage 75 followed by coverage 90, also exact. This confirms the gate on new
hardware output rather than only by replaying the older mismatch.
