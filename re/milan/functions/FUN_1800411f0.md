# FUN_1800411f0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800411f0..0x180041562`.
- Role: propagate a newly bridged enrollment relation toward lower-index
  inactive graph vertices through already-defined pair edges.

## Call Graph

- Sole caller: `FUN_180043110`.
- Callees: `FUN_180071d40`, `FUN_180068860`, `FUN_1800689a0`, and
  `__security_check_cookie`.

## Recovered Behavior

The five-argument Windows x64 ABI is `(context, pivot_to_reference, pivot,
overlap_context, threshold)`. The second argument points directly to
`FUN_180043110`'s stack-local transform at `[rsp+0x70]`; the helper never reloads
that transform from triangular relation storage.

The helper walks candidate features in descending physical index, considering
only inactive `+0x110 != 1` vertices. It searches the previously admitted
worklist backward for a defined 28-byte pair relation whose overlap from
`FUN_180071d40` is strictly greater than the supplied threshold. It composes
every defined worklist edge on that backward route in worklist order, inverts
the accumulated path, then left-composes the caller's base transform. The
candidate becomes active, is appended to the bounded local worklist, and the
result is stored in its canonical candidate/reference slot. Storage is inverted
only when candidate order is below the live reference. The written leading word
is zero.

The path is therefore repeated normalized composition: every call to
`FUN_180068860` projects the linear part before the next hop. It is not one
unbounded matrix product followed by a final normalization.

The caller supplies threshold 205. Focused non-campaign controls now distinguish
strict equality 205 from admission at 206, descending candidate order, reverse
worklist search, repeated per-hop normalization, pair inversion, activation,
and direction-dependent canonical storage.

Evidence is the caller argument setup at `0x1800432ec..0x180043302`, entry save
of `RDX` at `0x180041245`, the descending scan at `0x180041270`, defined-slot and strict
overlap checks through `0x180041315`, path fold at `0x1800413d0..0x18004142f`,
final invert/compose near `0x180041447`, and direction-dependent write through
`0x180041526`.

## Confidence And Unresolved

- Confidence: high for graph side, worklist order, strict threshold, repeated
  composition, activation, canonical direction, and zero-leading write. Static
  recovery is backed by fixed DLL-derived outputs in
  `test-native-enrollment-propagation.c`. The former native storage reload and
  double inversion are superseded.
- Exact source-level names of the overlap metric and worklist remain unknown.
