# FUN_18006c0b0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18006c0b0..0x18006c50f`, mask-disagreement metric and
  optional connected-component classifier.

## Call Graph

- Caller: `FUN_18006c510` at `0x18006c69f`.
- Substantive callees: `FUN_18006de90`, profile render helpers, matrix
  allocation/release helpers, and `FUN_180043cb0`.

## Behavior

- Renders a profile-qualified mask, counts source-mask pixels absent from the
  rendered mask, and returns that disagreement as an integer percentage.
- Profile-9 subtype 12 with field 2 uses strict thresholds 25 and 20 for its
  component branch. The out-flag is set only when disagreement clears the
  percentage threshold and the component result is below 25.
- Profile-9 subtype 12 with the production `field7 == 0` instead uses
  disagreement threshold 30, component-score threshold 15, and gradient
  threshold 80. The field-2 thresholds do not apply to production profile 9.
- `FUN_18006de90` turns the selected 16-bit working plane into an inverted
  histogram-stretched byte image. `FUN_18007e4b0` derives its threshold-80
  gradient mask. Without the component flag, the returned metric is source-mask
  coverage: `100 * count(source_mask) / pixel_count`. With the component flag,
  it is missing-gradient coverage:
  `100 * count(source_mask && !gradient_mask) / pixel_count`.
- When disagreement is strictly above 30%, overlap with the gradient mask is
  removed from a source-mask clone and `FUN_180043cb0` scores that residual.
  A score below 15 copies the residual mask to the caller and sets the component
  flag. `FUN_18006c510` then uses that copied mask for its quality differences.

## Evidence

- Profile-9 thresholds: `0x18006c0f0..0x18006c11f`.
- Percentage count and strict comparison: `0x18006c2ee..0x18006c425`.
- Component call and out-flag: `0x18006c425..0x18006c48f`.

## Confidence

High for arithmetic and thresholds; medium for semantic names.

## Unresolved

- Vendor names for the rendered masks and component score.
- The action-2 and action-7 call-site traps prove this helper's result cannot
  influence the conditional post-render update in either supplied enrollment
  lifecycle; no dynamic claim is made for other captures.
