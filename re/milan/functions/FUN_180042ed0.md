# FUN_180042ed0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180042ed0..0x180043107`.
- Role: propagate a newly bridged enrollment relation toward higher-index
  inactive graph vertices.

## Call Graph

- Sole caller: `FUN_180043110`.
- Callees: `FUN_180071d40`, `FUN_180068860`, `FUN_1800689a0`, and
  `__security_check_cookie`.

## Recovered Behavior

The five-argument Windows x64 ABI is `(context, pivot_to_reference, pivot,
overlap_context, threshold)`. `RDX` is the same caller stack-local transform
passed to the lower helper, preserved at function entry and used directly as
the first composition operand.

Starting immediately after the caller's pivot, the helper walks higher physical
feature indices. It skips active `+0x110 == 1` vertices and undefined direct
pair slots. For a defined slot, it calls `FUN_180071d40` and admits only overlap
strictly above the supplied threshold. The new transform is
`compose(caller_base, direct_pair)` (`caller_base` after the direct pair), so
normalization occurs at that hop. The admitted feature becomes active and the
result is written to its canonical relation-to-reference slot with leading word
zero; storage is inverted when feature order lies below the live reference.

Evidence is caller setup at `0x180043307..0x18004331d`, entry save of `RDX` at
`0x180042f02`, the forward loop at `0x180042f20`, defined-slot/strict overlap gate
through `0x180042fb0`, compose at `0x180042fd8`, and direction-dependent
invert/store through `0x1800430df`.

The caller supplies threshold 205. Focused non-campaign controls distinguish
equality 205 from admission at 206 and cover ascending order, active-feature
skip, normalized composition, activation, and canonical reference storage.

## Confidence And Unresolved

- Confidence: high for scan side/order, strict admission, compose order,
  activation, canonical direction, and zero-leading write. Static recovery is
  backed by fixed DLL-derived outputs in
  `test-native-enrollment-propagation.c`. The former native storage reload is
  superseded.
- Exact source-level names of the overlap metric and pivot state remain unknown.
