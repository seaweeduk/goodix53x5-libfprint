# FUN_180041ac0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180041ac0`, `0x180041ac0..0x180041f51`.
- Role: build direct relations from a new enrollment feature to every prior feature.

## Call Graph

- Direct caller: `FUN_180042c30`.
- Direct callees: `FUN_18003a3e0`, `FUN_180071d40`, `FUN_180078750`, `FUN_1800792d0`, `memset`, and `__security_check_cookie`.

## Recovered Behavior

For each prior feature, it calls `FUN_180078750` to obtain descriptor correspondences, copies matched Q8 coordinates, and estimates a six-word affine with `FUN_1800792d0`. Fewer than five raw or selected correspondences produce an unset identity relation. Otherwise it records the selected inlier count and transform only when overlap and quality gates pass. Accepted direct relations append the prior feature index to an output candidate list. An optional image-overlap path updates per-feature overlap metadata.

Evidence includes the 42-slot temporary match arrays at `0x180041b43`, the pair selector call around `0x180041bc4`, affine estimation around `0x180041d5b`, and the acceptance policy through `0x180041e33`. The selector itself is explicitly invoked with capacity 31; the larger caller arrays are storage, not capacity.

## Confidence And Unresolved

- Confidence: high for relation admission, direct count, transform, and 31-pair capacity; exact against every frozen stage.
- Unresolved: semantic names of the secondary image-overlap accumulators.

## Action-2 Anchor Evidence

The 2026-07-28 action-2 replay supplies exact Windows/native records and
registration bitmaps. Stages 1 through 5 produce no admitted direct relation.
At stage 6 the relation from new feature 5 to prior feature 1 is the first
admitted edge: count `16`, overlap detail/coverage `208/184`, transform
`[245,65,-3504,-72,255,9906]`, and packed relation index `12`. Stage 8 admits
new feature 7 to active prior feature 1 with count `13`, detail/coverage
`219/157`, transform `[242,85,4406,-92,243,37]`, and packed index `23`.

The native estimator and admission policy reproduce both records exactly. The
action-2 failure was downstream initial-anchor selection, not direct relation
construction or a threshold difference.

## Anti-Fake Pair-Score Ownership

For an admitted relation whose two anti-fake record counts are both greater
than `40`, this function calls `FUN_18003a3e0`. A successful comparison updates
each endpoint's pair score at `+0x1ab4`: stored `-1` is replaced directly;
otherwise `0x180041e8c..0x180041e93` and
`0x180041eb4..0x180041ebe` add the old and new scores as a wrapping dword and
divide the signed result by two with truncation toward zero. No other anti-fake
scalar is mutated here.

Production commit `188b515d` preserves the `-1` sentinel and replaces signed-C
addition with explicit dword wrapping plus bit-preserving signed
reinterpretation. The transient boundary control `INT32_MAX + 1` wraps to
`INT32_MIN` and stores the DLL average `-1073741824`.
