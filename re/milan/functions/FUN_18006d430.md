# FUN_18006d430

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18006d430..0x18006d531`, alternate/refined candidate
  renderer.

## Call Graph

- Callers: `FUN_18006e7f0` and `FUN_18006c510`.
- Type-12 processing callees: `FUN_180069730`, `FUN_18006b170`, and
  `FUN_18006b510`.

## Behavior

- Arguments are working u16 source, byte destination, dimensions/configuration,
  render-mask structure, optional contrast diagnostic object, and axis mode.
  Configuration dwords 0/1/2 are 108 columns, 88 rows, and 9504 pixels; dword 6
  is sensor type 12. Mask fields are count at +0, rows/columns at +4/+8,
  threshold at +12, signed 16-bit coverage at +14, and byte pixels at +16.
- Type 12 receives mode zero. `FUN_18006d540` derives the mode as zero when
  sensor type is at most 20 and its bit in `0x10110f` is set; bit 12 is set.
  The predicate is at `0x18006d783..0x18006d7a2`; the mode is loaded into
  R8D at `0x18006db6c` for selection call `0x18006dba5`.
  `FUN_18006e7f0:0x18006e935..0x18006e950` forwards that same mode to this
  wrapper. This means 88 contiguous rows of 108 words, not column centering.
- Row-centers the 16-bit working image, using mask-aware centering when the
  mask structure's coverage is below 96 and unmasked centering otherwise, then
  renders an 8-bit contrast candidate through `FUN_18006b510`.
- The working source is unsigned-word data, not a signed16-bounded plane.
  `FUN_1800668f0` can divide through unity and write twice the signed-baseline
  source after committing nonzero calibration, including on a first live call.
  Its output therefore does not eliminate the centering helpers' signed-word
  narrowing. See `FUN_1800668f0.md#type-12-working-word-range`.
- The tested coverage is the percentage recomputed by `FUN_18006bce0` after
  bounded-gap fill, subtype-12 strict live-range clearing, and recount. The
  earlier `FUN_18006a4c0` pre-fill count remains the normal calibration-update
  input and is not the scalar tested here.
- Coverage is compared as signed 16-bit against 95 at `0x18006d4d1`.
  Producer percentages lie in 0..100: below 96 calls `FUN_180069730` at
  `0x18006d4eb`; at least 96 calls `FUN_18006b170` at `0x18006d4dd`.
- Allocates and zeroes a separate 19008-byte word plane, centers into it, then
  calls `FUN_18006b510` at `0x18006d509` with the original mask and configuration.
  The renderer writes all 9504 destination bytes, including excluded positions.
  The centered plane is freed after rendering. Source, configuration and mask
  remain borrowed read-only inputs; the optional diagnostic object belongs to
  the renderer. This void wrapper has no allocation-failure/status publication
  branch; successful allocation is a precondition of the described path.
