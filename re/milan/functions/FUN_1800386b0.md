# FUN_1800386b0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800386b0..0x1800387b8`.
- Role: report occupied cache/extra-template counts.

## Call Graph

- Caller: exported `identifyImage` at `0x180001e6f`.
- No internal callees.

## Recovered Behavior

The helper counts nonnegative ranks in the 20-dword array at `+0x8db0`. For
profile-9/type-12, state `+0x8e00 == 0` returns that count through the first
output and zero through the second. State 1 returns zero for both outputs.
Legacy types `9/10/17/18` use the second output when state is nonzero.

`identifyImage` logs the outputs with the literal vendor string
`nCacheTempNum = %d, nExtraTempNum = %d`. This fixes the first array's role as
the cache-template queue while leaving the legacy extra-template distinction
outside profile 9.

## Evidence And Confidence

- Rank-count loops: `0x1800386dd..0x18003879f`.
- `identifyImage` call/log: `0x180001e5c..0x180001e82`; string at
  `0x1800e3d90`.
- Confidence: high for state selection, occupancy definition, and vendor count
  names.
- No unresolved profile-9/type-12 behavior remains.
