# FUN_18004b770

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18004b770..0x18004bb95`.
- Sole caller: `FUN_1800392f0` at `0x180039798`.
- Relevant callees: `FUN_18007ebe0`, `FUN_18004cad0`, `FUN_18004cc70`,
  `FUN_180047ae0`, matrix allocation/free, allocation, `memset`, and free.
- Reached production chain: `enrolAddImage:0x18000171b` or
  `identifyImage:0x180001dd3` -> `FUN_1800392f0:0x180039798` -> this function.
  The caller invokes it after candidate construction, scalar publication, and
  subtype-12 full-mask packing.

## Recovered Signature

```c
void FUN_18004b770(
  FeatureOwner **feature_owner,
  const uint16_t *residual,
  int32_t subtype);
```

## Recovered Behavior

This function enriches candidate records already constructed by
`FUN_18003b0b0`; it does not create or change their count. It converts the
supplied 16-bit residual to clamped 8-bit intensity (`/12`, or `/20` for chip
type 17), builds orientation/magnitude support planes, then iterates the count
at anti-fake `+0x12c0`.

For each 48-byte candidate it preserves coordinates at `+0x00/+0x04`, samples
the orientation plane at those coordinates, builds a descriptor through
`FUN_18004cc70`, transforms it with `FUN_180047ae0`, and writes the remaining
40 bytes at `+0x08..+0x2f`. The loop bound is the existing count; there is no
append, suppression, or count store in this body.

The source geometry comes from the live feature's first matrix descriptor and
is `104x88` for profile 9/type 12. The function consumes exactly `104*88`
sequential `uint16_t` values from the supplied residual pointer, converts each
to `min(value / 12, 255)`, and uses that same `104x88` geometry for dense
orientation, magnitude, gradient orientation, and candidate-coordinate
sampling. It does not row-crop or restride the original `108x88` residual here.

For subtype 12, residual conversion is exactly unsigned `min(source[i]/12,255)`
for sequential indices `0..9151`. The frame then has three dense row-major
support planes with width 104 and height 88:

- `FUN_18007ebe0(frame, dense_orientation, 104, 88, 7)` writes one orientation
  byte per pixel.
- A 32-bit plane receives `frame[i] << 8`, and `FUN_18004cad0` writes centered
  interior gradients to a zeroed `uint32_t` magnitude plane and zeroed
  `int16_t` gradient-orientation plane.
- The one-pixel magnitude/orientation borders remain zero, while dense
  orientation is defined for the complete plane.

The function zero-initializes its reusable 56-byte temporary feature record
before the record loop. Each iteration reads the candidate x/y low words,
forms Q8 signed-word coordinates `x<<8` and `y<<8`, and samples
`dense_orientation[y*104+x]`; produced type-12 coordinates are `x=4..103` and
`y=4..83`, so no narrowing changes them. It computes

```text
degrees = (270 - dense_orientation[y*104+x]) % 180
orientation = degrees * 0x6488 / 360
```

with truncation toward zero. This yields descriptor feature orientation in
`[0,0x31fc]`. Foreground and strength remain zero. `FUN_18004cc70` receives the
integer coordinate, fixed descriptor scale `0xc117`, and dense magnitude and
gradient-orientation matrices. It completely defines temporary bytes
`+0x10..+0x37`; no prior candidate metric or temporary payload survives.

Transformation precedes projection. `FUN_180047ae0(record,1)` first performs
the nibble rearrangement, clears and bit-reverses the first descriptor tail,
and expands the second tail. The owner then copies these exact spans:

```text
anti-fake +0x08..+0x17 <- temporary feature +0x28..+0x37
anti-fake +0x18..+0x27 <- temporary feature +0x10..+0x1f
anti-fake +0x28..+0x2f <- temporary feature +0x20..+0x27
```

This overwrites the candidate metric formerly at anti-fake `+0x18`; only the
original dword x/y at `+0x00/+0x04` are retained. Every published 48-byte record
is therefore fully defined. After this function returns, `FUN_1800392f0` writes
signed dword `-1` to pair score `+0x1ab4` at `0x1800397a0`; this function does
not initialize or score record pairs.

## Current Source Correspondence

`goodix_milan_antifake_feature_maps`,
`goodix_milan_antifake_feature_record`, and
`goodix_milan_antifake_feature_update` preserve the subtype-12 conversion,
`88x104` sequential geometry, dense plane layouts, orientation formula,
`0xc117` descriptor scale, transform-before-copy ordering, three projection
spans, existing count, and final `-1` pair-score initialization. Current source
uses explicit status returns and per-record zero initialization; neither changes
valid profile-9/type-12 bytes.

## Evidence

- Existing count load and loop gate: `0x18004ba13..0x18004ba19`.
- Coordinate reads and orientation sample: `0x18004ba30..0x18004ba70`.
- Descriptor construction/transform: `0x18004baa5..0x18004bb0f`.
- Forty-byte write preserving coordinates: `0x18004bb14..0x18004bb37`.
- Geometry load and sequential residual conversion: `0x18004b7ae..0x18004b875`.
- Temporary-record and descriptor-configuration initialization:
  `0x18004b96e..0x18004ba0e`.
- Descriptor-support matrices are complete immediately after
  `FUN_18004cad0`, at `0x18004b96e`. A passive identify-59 capture at that
  boundary gives exact current/DLL SHA-256 values `fb6bb428...` for dense
  orientation, `f6193181...` for magnitude, and `fa542d30...` for gradient
  orientation after the upstream impulse replacement is corrected.
- On a profile-9/type-12 54-record capture, current source at `c182b140`
  reproduces the captured dense orientation, magnitude, and gradient
  orientation planes exactly, with respective SHA-256 values
  `c0e32d3a4b9c2f37d40513a39965544d6b91aa494d70ff0a2eb713243cd9c44d`,
  `64f4a686a347cbed2cf44dd1fc02b787b9278d47cd6cca64f70fb5e71b483ace`,
  and `cf9977d2490c826a2590cca56eb184bf8127d41aa061ef7ad2d18dcad98d7690`.
  The first 56-byte pre-transform record at `(12,8)` is exact, SHA-256
  `06e4c8bea5bf06a4346dc221b474c2cc19ad9dcf5c4588492897167fe8ddcb89`.
  All 2,592 published anti-fake record bytes are exact, SHA-256
  `55c606b85a5b4fae044d80f44f841d1dcab545ae1b8be4e68cd6f3b0666696d5`.

## Failure Contract

The DLL has no status result and assumes valid feature ownership, residual
storage, candidate count, coordinates, dimensions, and successful allocations.
Allocation results are used without null checks; malformed counts or coordinates
can overrun or fault. The sole producer bounds count to 100 and coordinates to
the subtype-12 map. Current source rejects malformed storage/count/coordinates
and returns `-1` on allocation or helper failure; those paths are not
production-reachable profile-9/type-12 output states.

## Confidence And Unresolved

- Confidence: high for record/count ownership, source geometry, residual
  conversion, all support planes, temporary initialization, orientation and
  descriptor scale, transform/projection order, pair-score ownership, failure
  behavior, and exact production output bytes.
- Unresolved: semantic names for the descriptor temporaries; they do not affect
  candidate count or published bytes.
