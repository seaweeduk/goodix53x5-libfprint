# FUN_180078a70

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180078a70`, `0x180078a70..0x180078f02`.
- Role: primary descriptor-correspondence construction for `FUN_18005a3c0`.

## Call Graph

- Direct caller: `FUN_18005a3c0`.
- Relevant callees: `FUN_180076400`, `FUN_180077390`, and `FUN_180075a60`.

## Recovered Behavior

Capacity is `42` for the type mask `0x413000` and `31` otherwise. Profile-9/type-12 takes the generic `FUN_180076400` path twice: first across the leading record partitions, then across the remaining partitions. Each pass records best/second descriptor distances and best indices. `FUN_180075a60` then applies the ratio, spatial-collision, and bounded-capacity policy.

The generic distance uses the first two eight-byte descriptor segments, thresholds them through configuration words `0` and `1`, then chooses the smaller of normal and shifted tail layouts. This matches the structure of native `milan_recognition_descriptor_details()`; affine filtering remains a separate stage.

The frozen profile-9/type-12 configuration is
`[23,47,40,38,-1,16,246,1]`. Corrected diagnostic v4 reproduces descriptor
distances, best indices, selected pairs, affine results, and post-orientation
masks for all `76/76` primary calls when both implementations consume the same
Windows probe records.

Production mode-1 extraction and the separate alternate two-pass correction
leave all five primary parity totals exact `76/76`.

The row `16/1` feature-2 count diagnostic provides a normal-record counterexample
without changing this helper's rules. After mapping native records by exact
56-byte identity, native's primary selected set is the complete 36-pair official
set plus only `(enrolled 0, native-only record 9)`. Official enrolled source 0
has best/second `63/65`, which fails strict ratio `63*40 < 65*38`; native's extra
record changes it to `46/63`, which passes and creates the pair. The selector and
descriptor arithmetic remain exact; the first divergence is the upstream
type-12 record limit in `FUN_18004ae70`.

Accepted row `74/1` feature 2 is an additional exact control. Native reproduces
all 112 best descriptor inputs and the complete 42-pair primary set. The primary
builder is not that row's first divergence; primary-versus-alternate ranking in
`FUN_18005a3c0` is.

## Confidence And Unresolved

- Confidence: high for type-12 branch selection, partition ranges, capacity, and helper sequence.
- Type-12 unresolved behavior: none. Cross-profile configuration variability is
  outside scope; profile-9 values and operations are fixed below.

## Sequence-72 Input Boundary

Passive entry capture at `0x180078a70`, before this function executes, is the
first unequal matcher boundary for both evolved-gallery sequence-72 score rows.
The enrolled input is byte-identical at 117 records. The live probe input is
native/official `54/48` records, with different record-object hashes. Resulting
selected primary sets are e1 `38/37` pairs and e2 `42/40` pairs; filtered counts
become e1 `9/10` and e2 `16/17`.

Two independent official runs reproduce all 138 corresponding files exactly.
Adjacent controls distinguish the boundary: sequence 71 is `114/114` records
and sequence 73 is `106/106`. Descriptor selection, pair capacity, ratio policy,
and affine filtering are downstream observations, not authorized fix surfaces.
The next investigation must localize the sequence-72 live-record materialization
difference in `FUN_18004ae70`/`FUN_180049b90` and the native extractor.

Artifact root/tree:
`<evidence-root>/derived/milan-native-capture-20260723/evolved-score-sequence72-v1/`,
`0235afb4ea2f8d51b42d40206bc378e01e601eb23d3fa213015c08f79e979a54`.

## Evolved-Gallery Units 7-9 Controls

The selected sequence-197 calls provide additional exact primary-builder
controls. Captured official inputs reproduce the complete 42-pair primary sets,
filtered counts, and affines for `197/1 f39` and `197/2 f6`. The resulting
primary counts are 9 and 14 respectively. The later unequal selected affine and
retained count arise only after this helper and `FUN_18005b900` return, inside
`FUN_18005a3c0` ranking/promotion ownership. No ratio, partition, capacity, or
descriptor-distance change is authorized.

## Complete Type-12 Partition, Tie, And Capacity Contract

Type 12 excludes the `9/10/17/18` branch and calls generic `FUN_180076400`
twice over stored half-open boundaries
`[0,e_split)x[0,p_split)` and
`[e_split,e_count)x[p_split,p_count)`. Equal distance preserves the earlier
best. Selection requires strict `best*40 < second*38`, uses strict duplicate
and capacity replacement, keeps the first maximum on a worst-distance tie,
emits compact current order, and fills all remaining 42 slots with `-1,-1`.

Direct controls cover empty/single/multiple partitions, equal-ratio rejection,
one-below acceptance, negative index, equal/better duplicates, multi-collision
pruning, equal-worst rejection, and strict capacity replacement. Cross-profile
configuration variability is excluded from this profile-9/type-12 contract; no
type-12 behavior remains unresolved.

## FUN_18005a3c0 Dependency Unit 1

The primary builder and fit remain independent of alternate failure. At
alternate entry, every positive primary filtered count owns a fitted affine;
counts one and two reflect post-fit orientation removal rather than fewer than
three primary selected pairs. Unit 1 therefore preserves this primary result
instead of rejecting the complete candidate. No primary correspondence rule is
changed or inferred from the alternate safety deviation.

## FUN_18005a3c0 Dependency Unit 6

Operand-level Ghidra recovery confirms the type-12 generic arm reads both
feature boundaries directly from `+0x118`: first-scan setup at
`0x180078b73..0x180078be0`, second-scan setup at
`0x180078be5..0x180078c34`, followed by one selector at
`0x180078c39..0x180078c61`. The ranges are exactly
`[0,e_split)x[0,p_split)` then
`[e_split,e_count)x[p_split,p_count)`.

The production primary builder now receives those stored values rather than
finding a prefix from record foreground bytes. It validates signed metadata and
both count bounds before scanning. Empty, all-leading, all-trailing,
single-leading, dual, independent enrolled/probe splits, contradictory
foreground bytes, physical order, and capacity 42 are covered. Existing public
record-only diagnostics retain a foreground-derived convenience wrapper, but the
profile-9/type-12 matcher does not use it.

## Matrix Production And Handoff

The two generic type-12 scans also pass the same caller-owned `180x180`
eligibility/distance and layout matrices to `FUN_180076400`. Only the two stored
same-partition rectangles are written; cross-partition cells retain initialized
`0xff/0`. Record classes, active state, masks, and bitmaps do not affect those
cells.

The matrices are borrowed stack storage from the enclosing matcher call. The
distance matrix is reused by `FUN_180076850 -> FUN_180074ba0`; both matrices are
later reused by `FUN_180057900 -> FUN_180075220`. They are not feature fields or
cached results. Producer arithmetic, lifetime, and direct controls are complete
in `re/milan/MATCH-MATRIX-CONTRACT.md`.
