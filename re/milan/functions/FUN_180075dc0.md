# FUN_180075dc0

## Identity

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180075dc0`, `0x180075dc0..0x180076128`.
- Role: generic affine-guided direct descriptor scan used by related matcher paths.

## Recovered Behavior

It inverts the affine, checks both transformed coordinate directions against
six-pixel borders and a spatial tolerance, compares the two generic tail
layouts, thresholds the lower distance, and updates best/second arrays.

The exact profile-9/type-12 controls are:

```text
source range: caller-selected foreground partition
inverse point: 5 < x < width-5 and 5 < y < height-5
target range: matching caller-selected foreground partition
forward Q8 residual: abs(dx) <= 0x1500 and abs(dy) <= 0x1500
forward point: 5 < x <= width-5 and 5 < y <= height-5
distance: min(Hamming(enrolled[40:48], probe[40:48]),
              Hamming(enrolled[40:48], probe[48:56]))
admission: distance <= configuration[5]
best/second replacement: strict distance decrease
```

`FUN_1800749c0` supplies configuration word 5 as `16`. Assembly at
`0x180076052..0x180076058` rejects only `distance > limit`, making 16 inclusive
and 17 the first rejected value. This resolves all five retained-count rows;
normal and official-record native projections produce the same six over-limit
pairs under the current hardcoded-23 translation.

The native translation now takes the tail-Hamming limit as a caller-supplied
profile value rather than embedding `23`. Profile-9/type-12 replay passes direct
boundaries 15/16 and 17/18 and excludes exactly the six previously admitted
distance-17/18 pairs. The unrelated ordinary cross-class descriptor helper
retains its existing limit.

## Confidence And Unresolved

- Confidence: high for the profile-9/type-12 loop, arithmetic boundaries,
  descriptor, threshold operand, and best/second updates.
- Unresolved: cross-profile configuration values and semantic field names.

## 2026-07-28 Inverse-Width Correction

This helper calls `FUN_1800689a0` unconditionally before its source scan. The
inverse therefore uses wrapped dword determinant semantics and copies the input
when that wrapped determinant is zero. Native's corresponding
`milan_match_cross_class_alternate_correspondences()` currently computes
`a*d-b*c` in `int64_t` instead.

For `[65536,0,1,0,65536,2]`, DLL semantics copy the input while the native
sibling-local arithmetic yields `[1,0,0,0,1,0]`. Ordinary profile-9 affines are
negative controls; the discrepancy affects determinant-wrap/extreme inputs.
The tail threshold, spatial bounds, ratio, and best/second behavior documented
above remain unchanged.

## 2026-07-28 Native Implementation

The sibling scan no longer carries a widened local inverse. It calls the shared
wrapped-dword implementation used by the primary/alternate matcher. Output pair
slots are initialized before inversion, so the native-only signed-division
overflow rejection returns zero pairs without a partial inverse or pair
publication. Determinant-wrap singular input follows the DLL's six-word input
copy; ordinary reached scans remain unchanged.

## Matrix Negative Control

This helper is not passed either `180x180` matrix. It computes both eight-byte
tail orientations directly, selects the lower distance, and compares it to
configuration word 5. This distinguishes the later guided sibling path from the
ordinary class-sibling `FUN_1800770e0` path, where producer eligibility and one
producer-selected layout are caller inputs.
