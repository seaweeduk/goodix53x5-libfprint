# FUN_180068bb0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180068bb0`, `0x180068bb0..0x180068f6b`.
- Role: construct a Q10 inverse affine and sample a byte matrix into a cropped
  destination with bilinear interior and valid-neighbor boundary handling.

## Call Graph

- Direct callers: `FUN_18006f390` and `FUN_180071510`.
- `FUN_18006f390` is the profile-9/type-12 overlap caller.
- The helper has no substantive callees.

## Recovered Signature

```c
void FUN_180068bb0(
  matrix_desc *source,
  matrix_desc *destination,
  const int32_t transform[6],
  int32_t source_rows,
  int32_t source_columns,
  uint32_t destination_rows,
  uint32_t destination_columns,
  int32_t border);
```

`FUN_18006f390` supplies the allocated descriptors' dimensions explicitly and
uses border zero for profile-9/type-12 overlap.

## Inverse Construction

For adjusted transform `[a,b,tx,c,d,ty]`, determinant construction is wrapped
dword arithmetic:

```text
det = int32(a*d - b*c)
```

For nonzero `det`, it computes signed 64-bit numerators and signed division
truncating toward zero:

```text
ixx = ( d * 0x40000) / det
iyx = (-c * 0x40000) / det
itx = ((ty*b - d*tx) * 0x400) / det
ixy = (-b * 0x40000) / det
iyy = ( a * 0x40000) / det
ity = ((c*tx - ty*a) * 0x400) / det
```

The destination loop uses wrapped dword accumulation:

```text
raw_x = int32(x*ixx + y*ixy + itx)
raw_y = int32(x*iyx + y*iyy + ity)
```

The coefficient array corresponding to those equations is
`[ixx,iyx,itx,ixy,iyy,ity]`.

When `det == 0`, the helper does not fail. It retains
`[1,0,0,0,1,0]`; these are literal Q10 accumulators, not Q10 unity. For the
profile-sized destination this maps nearly every sample to source pixel `(0,0)`.
The output sentinel remains in locations rejected by the boundary predicate.

## Sampling

Coordinates use arithmetic floor through signed right shift:

```text
x = raw_x >> 10
y = raw_y >> 10
fraction_x = raw_x - x * 0x400
fraction_y = raw_y - y * 0x400
```

Consequently each fraction is `0..1023`, including when `raw_x` or `raw_y` is
negative. A sample is outside the usable neighborhood when:

```text
x + 1 < border || x >= source_columns - border ||
y + 1 < border || y >= source_rows - border
```

The helper writes the destination's initial byte for that case. Under
`FUN_18006f390`, that byte is `0xff`.

When all four neighbors are usable, interpolation is:

```text
top    = p00 * (0x400 - fraction_x) + p10 * fraction_x
bottom = p01 * (0x400 - fraction_x) + p11 * fraction_x
value  = (top * (0x400 - fraction_y) + bottom * fraction_y + 0x80000) >> 20
```

At an edge, including floor coordinate `-1` with border zero, it ignores the
fractions and writes the unsigned integer average of only the usable cells in
the surrounding `2x2`. Division truncates toward zero.

For binary bitmap and validity planes, reached valid output values are `0` or
`1`; rejected samples are `0xff`. `FUN_180073540` also requires the warped
bitmap value to be below two, so an out-of-bounds `0xff` validity byte cannot
admit an out-of-bounds bitmap sample.

## Profile-9 Controls

| Fixture | Adjusted transform | Wrapped determinant | Inverse coefficients |
| --- | --- | ---: | --- |
| `static-0207-overlap` | `[235,65,-9358,-84,248,-2052]` | `63740` | `[1019,345,35141,-267,966,20375]` |
| `static-0259-overlap` | `[276,-31,-27,28,278,-1722]` | `77596` | `[939,-94,803,104,932,6261]` |
| `static-0320-overlap` | `[262,-33,-56,-1,229,139]` | `59965` | `[1001,4,140,144,1145,-620]` |
| `static-0376-overlap` | `[247,43,-96,-30,258,-5166]` | `65016` | `[1040,120,-3108,-173,995,20142]` |
| `production-full-0357 f38` | `[276,7,100,-26,255,-4123]` | `70562` | `[947,96,-788,-26,1025,16476]` |

The four static crops contain respectively `463`, `2270`, `2022`, and `531`
counted samples. They jointly exercise `252` valid edge-average samples, `74`
valid negative-floor samples, and `5034` valid bilinear samples. The reached
feature-38 crop adds 736 classified samples and byte-exact warped low-bitmap and
validity outputs. Exact negative-floor witnesses include:

```text
static-0320 local (0,0): raw (140,-620), floor (0,-1), fraction (140,404)
static-0376 local (3,1): raw (-161,21497), floor (-1,20), fraction (863,1017)
```

Both produce bitmap value `0` and validity value `1` through the edge-average
path. Every captured warped validity matrix is byte-exact against
`identify-match-overlap-matrix-4.bin`.

## Current Implementation Boundary

Current `overlap.c` uses a widened signed-64 determinant and rejects determinant
zero. The DLL uses the wrapped signed-dword determinant above and continues when
it is zero. A width difference is possible exactly when:

```text
int64(a)*d - int64(b)*c != sign_extend(int32(a*d - b*c))
```

The accepted primary affine producer independently gates every linear
coefficient to magnitude below `300`, which keeps its determinant within signed
dword range. The static controls and reached feature-38 low-bitmap fixture are
exact, including nonzero origins, negative floors, edge averages, fractional
bilinear samples, warped masks, class counts, scores, and decisions. No
profile-9/type-12 production witness reaches the width or singular-input
distinction.

## Confidence And Unresolved

- Confidence: high for determinant width, inverse numerators, signed division,
  coefficient order, dword accumulation, negative-floor behavior, validity
  bounds, sentinel, bilinear rounding, and edge averaging.
- Malformed and independently constructed singular transforms are outside the
  profile-9/type-12 producer-valid contract.
