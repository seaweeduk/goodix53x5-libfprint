# FUN_180046ef0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180046ef0..0x18004711a`.
- Role: choose the maximum-overlap geometric fallback candidate at capacity.

## Call Graph

- Caller: `FUN_1800469f0` when evidence flag `+0x644` is one and the primary
  candidate helper returned `-1`.
- Important callees: `FUN_180071370` and affine helpers `FUN_180068860` and
  `FUN_1800689a0`.

## Recovered Behavior

The helper walks every feature, considering only active `b5 != 0` features whose
state at `+0x124` is not 5. It obtains each feature's relation to the live
reference, composes it with evidence relation transform `+0x66c`, halves
translations in profile-9 mode, and measures the transformed in-bounds area.
The largest area wins; strict greater-than means an equal area keeps the earlier
feature index. The returned area is integer-truncated to a percentage of the
template domain.

The raw maximum starts at zero and the output index starts at `-1` at
`0x180046f4e..0x180046f5f`. Therefore a zero-area eligible feature does not
become the helper winner: every update requires `area > 0` as well as a strict
increase over the preceding maximum. Current production policy instead seeds
its internal raw maximum with `INT32_MIN`, so it tentatively selects the first
eligible zero-area feature. This is externally inert for profile 9 because area
is bounded to `0..2288`, zero converts to zero percent, and the caller rejects it
below 60 before publishing an action or selected index. Positive-area ranking,
ties, and every reachable action-3 result remain identical.

Raw area, not the truncated percentage, is the ranking key. Assembly compares
the `FUN_180071370` return against the retained raw maximum at
`0x1800470c3..0x1800470d0`; only after the scan does
`0x1800470e2..0x1800470ee` compute `best_area * 100 / domain_area`. A generic
translation that stores only per-feature integer percentages loses this order:
different raw areas can truncate to the same percentage, in which case the DLL
still selects the larger raw-area feature rather than the earlier feature.
The final `best_area * 100` and domain product are wrapped 32-bit operations
followed by signed division truncating toward zero; all percentage gates are
signed comparisons. Transform multiply/add intermediates, arithmetic-shifted
translations, footprint bounds, and area accumulation follow the same signed
dword policy. No saturation or overflow guard is present.

Production commit `a1e5384` carries raw area into policy selection and converts
only the selected winner to a percentage. The controlled `1848` versus `1849`
area tie-at-80-percent case selects the DLL's later, larger-area feature.
Independent 450- and 643-operation campaigns pass 1,093/1,093 selected
operations after the correction. Production commit `89dd6c43` closes the
remaining wrapped transform, area, product, and signed-percentage arithmetic.

`FUN_1800469f0` accepts this fallback at percentage at least 80. Percentages
60 through 79 are accepted only when template counter `+0x8cf8 > 1000`; 59 and
below are rejected. A successful flag-one fallback assigns transient selector
code 3, not final direct-replacement code 4.

## Evidence And Validation

- Candidate exclusions: `0x180046f90..0x180046fa9`.
- Relation direction/composition and half-mode adjustment:
  `0x180046faf..0x180047095`.
- Strict maximum selection: `0x180047095..0x1800470d0`.
- Zero maximum and `-1` index initialization: `0x180046f4e..0x180046f5f`.
- Integer percentage: `0x1800470e2..0x1800470ee`.
- Direct DLL controls prove exact fallback percentages and branch boundaries:
  79 with counter 1000 rejects, 80 succeeds, 60 rejects at counter 1000 and
  succeeds at 1001, and 59 rejects at 1001. Successful controls return internal
  code 3. Artifact:
  `<evidence-root>/derived/milan-native-capture-20260723/study-action-policy-v1/action-controls.jsonl`.

## Confidence And Unresolved

- Confidence: high for candidate eligibility, ranking, percentage arithmetic,
  and all strict boundaries from assembly and direct controls.
- No corrected natural row takes this fallback: all 286 observed capacity
  updates are code 4, which is assigned only by the primary flag-one path.
- The natural corpus does not reach code 3, but focused export/adapter controls
  below establish its external handling and positive persistence.
- Native now commits, packs, and exposes action 3 as a positive update.

## Native Ranking

Native retains raw geometric area per candidate, selects its strict maximum,
and computes the wrapped signed integer percentage only for the selected area
before applying the 80/60 and age gates. This preserves distinctions hidden by
equal truncated percentages.

## Caller And Persistence Closure

The unobserved lifecycle is now controlled. Successful reference and
nonreference fallback targets at 80%, plus an aged reference target at 60% and
counter 1001, all reach exported update 3. Reference targets preserve star
cardinality while composing every transform; nonreference targets clear
unrelated incident edges and reinstall one zero-leading reference edge.
`FUN_180044fc0` preserves 3 through an empty enabled queue, `templateStudy`
exports it, and production adapter/storage callers treat it as positive. The
native path now implements that commit/expose/persist behavior; code 3 is not
internal at the exported ABI boundary.
