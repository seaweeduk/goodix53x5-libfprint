# FUN_180070d90

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180070d90`, `0x180070d90..0x180070e3a`.
- Role: decode two compact policy classes from one packed scalar.

## Call Graph

- Callers include `FUN_18004ae70`, `FUN_180053680`, `FUN_180055a40`,
  `FUN_18005c3b0`, `FUN_18005d5f0`, `FUN_18005e5b0`, `FUN_180060fe0`, and
  `FUN_180062a30`.
- It has no callees.

## Recovered Behavior

The first output decodes packed bits `0..1`; the second decodes bits `8..10`:

```text
low class:  raw 0,1,2,3         -> 0,2,2,3
high class: raw 0,1,2,3,4,5,6,7 -> 0,1,2,4,5,5,0,0
```

The second mapping follows the intermediate `raw == 0 ? 0 : raw + 4` and the
shared class switch. All shifts, masks, and table boundaries are exact from the
body.

## Extraction-Class Use

`FUN_18004ae70` invokes this decoder immediately after `FUN_18006fb10`. It
places the decoded high output at `FUN_180048260` configuration `+0x0c` and the
decoded low output at `+0x10`. The low output is retained unchanged through the
extraction classifier and supplied as `EDX` to `FUN_180073820`. The high output
is only a seed: current class, auxiliary byte 2, prior high class, and hysteresis
may raise it before it is supplied as `R8D`.

## Matcher State Use

`FUN_180055a40` first decodes caller-provided match configuration `+0x48`.
Ordinary `FUN_18005edb0` calls place the probe's packed optional `c7` there;
queued `FUN_18005d330` calls force it to zero. The matcher then decodes each
enrolled feature's live `+0x150`, also packed tag `c7`. The late `FUN_180058700`
state is built from the accumulated high class, a separate caller scalar, and
the current accumulated-plus-feature high class capped at five.

On accepted row `39/1`, packed probe mode zero and all reached feature classes
produce late state `[0,0,0]`. On rows `3/1` and `31/1`, the corresponding state
is `[2,0,2]`. Native currently hard-codes the latter state for every match, so
the row-39 feature-7 mode-4 rejection is not an input-exact policy result.

## Confidence

- High for the complete packed-to-class mapping and profile-9/type-12 caller
  operands.
- Semantic names outside the matcher remain provisional.

## Native Implementation

The matcher now applies the complete low/high mappings generically. It
initializes the late context from probe `c7`, derives each reached feature state
as `[accumulated_high, probe_low, min(5, accumulated_high + feature_high)]`, and
retains the capped sum for later features when the current decoded feature high
class is above three. The former global `[2,0,2]` constant is removed.

Focused controls cover all 12 raw class values, probe-low propagation, capped
high-class accumulation, the existing `3/1` and `31/1` `[2,0,2]` paths, and the
new `39/1` `[0,0,0]` path. The latter now preserves feature 7, restoring its Q8
contribution and official score 41. No status predicate, affine penalty,
selection, contribution arithmetic, or other scoring policy changed.
