# FUN_180051af0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x180051af0..0x180051bc7`, strict adaptive threshold mask.

## Call Graph

- Auxiliary-state callers: `FUN_18004e8c0` and `FUN_18004dea0`.

## Exact Contract

With configuration `[limit, scale]`, average scores strictly between zero and
`limit`, rounded as `(sum + count / 2) / count`. If at least one sample was
admitted, set `threshold = max(average * scale >> 8, limit)`; otherwise threshold
remains zero. Clear the output, then write 255 exactly where `score > threshold`.

Both profile-9 callers use `[400, 512]`, hence a minimum threshold of 400 and a
scale of two. They count the resulting bytes and write context `+0x08` as 2 for
count strictly above 600, 1 for count strictly above 300, and 0 otherwise.
