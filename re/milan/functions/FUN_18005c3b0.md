# FUN_18005c3b0

## Binary And Body

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18005c3b0` / `0x18005c3b0-0x18005d2f3`.
- Inferred signature: `void f(metrics, config, inout_flag)`.
- Caller: `FUN_180058ee0`; callee: `FUN_180070d90`.

## Behavior

This is the type-12 continuation of `FUN_180058ee0`. It decodes packed mode
state, derives adjusted bitmap/detail combinations, dispatches on a clamped
count from 4 through 15, and clears the surviving flag on a large set of weak
candidate conditions. One early mode-dependent rejection also clears
`config+0x4c`.

## Evidence And Confidence

- All ordinary failure paths converge on `*inout_flag = 0`; mode state comes from `config+0x48`.
- The persistent type-12 trace records 29 entries from 47 `FUN_180058ee0`
  calls. This helper can clear `config+0x4c`; together with
  `FUN_180053680`, nine dispatcher calls transition mode `1 -> 0`.
- Confidence is high for type-12 veto behavior, low for reducing the threshold matrix safely.
- The profile-9/type-12 matrix and recognition-mode mutation are now translated
  in `goodix53x5-match-policy.c`; all 29 frozen continuation outputs and all
  nine `1 -> 0` match-level mode transitions reproduce exactly.
- Focused `139/1` evidence establishes the input bridge: the official
  `config+0x48` value is `0x200`, matching serialized live-feature optional tag
  `c7`. The pre-fix native decomposed-probe path dropped that tag and initialized
  `config+0x48` to zero, causing feature 2 to miss this helper's mode-dependent
  veto. This was an upstream scalar-preservation defect, not a threshold or
  translated-helper defect.
- The decomposed-probe boundary now preserves `c7`; focused `139/1` telemetry
  is exact at serialized, prepared, and `config+0x48` initialization boundaries
  (`512/512/512`). Feature 2 takes the translated mode-dependent veto and all
  three candidate flag/mode transitions match official without changing this
  helper's thresholds.

## Accepted-Score Residual 89/2

The repeated `89/2` feature-0 trace supplies a same-input type-12 continuation
counterexample outside the original 29-call gate. All 77 candidate words, input
flags `[0,0]`, quality/coverage `88/98`, packed mode `512`, and the initial
classifier output `[1,1]` are exact. Windows preserves flag60 through this
continuation; native clears it.

Ghidra review of the DLL bucket-6 body identifies the translation error. The
official rejection term is:

```text
detail < 226 && retained_count < 10 && coverage < 91
```

Native currently uses `retained_count < 16`. The row has detail/count/coverage
`224/11/58`, so only the widened native predicate rejects it. This is a generic
operand correction in the existing bucket-6 matrix, not a dispatcher, caller-
state, or contribution-threshold exception. Confidence is high from exact input
replay plus the DLL decompilation.

## Native Correction

The generic bucket-6 operand is now corrected to `retained_count < 10`; no other
matrix or matcher behavior changed. The focused native gate independently
verifies residual-v1 tree SHA-256
`560c6eee347e1ba68bb88e9465e1e486cd01ff2e74088982c6d744e1cc36a8df`,
then tests counts `9/10/11` as veto/survive/survive and the complete
authoritative `89/2` feature-0 input as final flags `[1,1]`.

The full immutable replay closes exactly one score, improving from `754/798` to
`755/798`, while decisions and candidate indices remain `798/798` exact and
impostor accepts remain zero. This function is no longer the next matcher
residual. The subsequent `FUN_180058700` mode-4 and transformed-area nonzero
status paths are now implemented and gated separately at `6/6` calls; they move
full score parity to `756/798` without changing decision/index parity or
impostor acceptance. Later retained-count work is complete. The final accepted
rows are `3/1` and `39/1` caller-state/context integration plus the separate
`74/1` affine-penalty ranking defect; none changes this helper.

## Identify Epoch 26 Common-Gate Grouping

Ghidra re-verification of `0x18005d1e1..0x18005d219` establishes one complete
term in the common post-switch survivor conjunction. With the helper's adjusted
values:

```text
noise      = metrics[12] + metrics[13] + metrics[14]
primary    = metrics[0]
filtered   = metrics[1]
detail     = metrics[5] - 2*noise
geometry   = metrics[11]
overlap    = metrics[4] - 2*noise
low_detail = metrics[8] - 2*noise
combined   = detail + low_detail
coverage   = metrics[9]
```

the exact DLL Boolean term is:

```text
primary > 10 ||
filtered > 16 ||
((detail > 210 || geometry > 16) &&
 (overlap > 229 || combined > 410 || coverage < 110))
```

This is not a flat OR. The assembly preserves both grouping levels:

- `0x18005d1e1..0x18005d1ea` accepts the term for `primary > 10` or
  `filtered > 16`.
- `0x18005d1ec..0x18005d1f8` evaluates the left nested group. If both
  `detail <= 210` and `geometry <= 16`, `0x18005d1f8` branches to the common
  clear sink.
- `0x18005d1fe..0x18005d213` evaluates the right nested group. If
  `overlap <= 229`, `combined <= 410`, and `coverage >= 110`,
  `0x18005d213` branches to the same clear sink.
- `0x18005d219` begins the next common survivor term. Satisfying this term does
  not independently accept the candidate; every surrounding conjunction term
  must also survive.
- The common failure sink at `0x18005d2ca..0x18005d2cc` writes zero through the
  third argument. On this caller path that argument is the `flag60` pointer.
  The helper does not clear `flag64` at this sink.

The current clean-room expression loses the nested AND and treats all five
inner comparisons as independent OR terms. That broadens the survivor set and
is a generic translation error, not an epoch-specific threshold adjustment.

The profile-9/type-12 witness is dump selector
`becea91e-eff1-497c-af41-67f4d1b6ce2e/identify/26`, gallery position 1,
physical feature 25. Its adjusted tuple is:

```text
primary/filtered/detail/geometry = 9/9/209/14
overlap/low_detail/combined      = 227/213/422
coverage                         = 105
```

The right nested group is true through `combined > 410` and `coverage < 110`,
but the left nested group is false. The DLL therefore clears `flag60`; the
flattened clean-room expression incorrectly preserves it. GDB forcing only this
official flag outcome caused all later candidate, score, lifecycle, after-match,
and after-study outputs to become byte-exact to the DLL, so no row-shaped
compensation is needed or authorized.

Production commit `84fc625` restores this grouping without changing any
threshold or surrounding term. Focused identify-26 replay and three complete
current-schema campaigns pass 1,402/1,402 selected operations against the
approved DLL.
