# FUN_18002e360

## Audit Scope

Production adapter caller of `identifyImage` at `0x180001ad0`. This note was
created before first Ghidra inspection during the independent methodology audit.

## Questions

- Which recognition flags, mode, calibration, raw frame, and metadata are used?
- Does the Wine identify call preserve production argument semantics?

## Audit Findings

- Production passes flags `0`, a context-derived recognition mode byte, the
  retained calibration/raw frame, retained `tCode`/DAC values, caller auxiliary
  data, and the configured anti-fake mode.
- The current oracle passes flags `0`, selectable recognition mode, the nonnull
  preprocessor auxiliary, full calibration workspace, live raw frame,
  production metadata tuple `0x79/0x7d/0xc6`, and anti-fake mode `1`.
- Standard identify-only raw replay explicitly exits and reinitializes
  preprocessing from the supplied setup frame. The focused safe-zero matcher
  diagnostic instead supplies the exact setup/live raw pair to the initial
  production preprocessor flow and does not first process a synthetic frame.
  Both validate a controlled fresh generation, not unknown
  sensor-ID-qualified persisted calibration or the production long-lived
  engine/preprocessor lifecycle.
- Candidate order and first-positive behavior match the production wrapper.
- `--identify-only` performs preprocessing with purpose `0`, unpacks candidates,
  and calls this export without creating an enrollment session.
- Immediately after this export returns, the oracle snapshots and serializes
  the retained probe at global `0x18024ebe8` before `templateStudy` can consume
  retained state.
