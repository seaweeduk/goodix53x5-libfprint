# FUN_18002e360

## Identity And Callers

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900.
- Address: `0x18002e360`; logged name `_CmpTemplateToCurrentFeatureSet_E`.
- Caller: `FUN_180033850`, shared by identify and verify.
- Callees include `templateUnPack`, `identifyImage`, and envelope validation
  `FUN_18002fe70`.

## Gallery Loading

The record buffer is privately copied after subtracting the configured trailing
size at `0x18019ba00`. Its first 16 bytes must equal the retained sensor ID at
`0x18019b9b8`; mismatch sets status `0x8011`. Envelope validation failure sets
`0x8005`; allocation failure sets `0x8001`.

At `0x18002e6b2..0x18002e6f5`, the packed template at copied record `+0x30`
is unpacked with length `record_size - 0x34` into public handle array
`0x18019bb20[index]`. The supplied unsigned index is not compressed or sorted.
The unpack return value is not tested here.

At `0x18002e6fa..0x18002e70c`, an index below unsigned `count - 1` returns
without matching. The final load invokes `identifyImage` once over the complete
array and supplied count at `0x18002e816`. The export performs extraction once
before its ordered matching loop. A failed record-envelope check returns before
unpacking or reaching this final-load gate.

## Identify Arguments

The export receives retained processed descriptor `0x180196c80`, caller
auxiliary data, handle array `0x18019bb20`, count, separate index and signed-score
outputs, and the caller's coverage/quality pair. Flags are zero. Recognition
mode comes from the caller's byte; raw/calibration inputs are retained at
`0x18019b980` and `0x18019c186`, with `tCode`/DAC words at
`0x18019c180/+2/+4`. The final argument is caller-configured anti-fake mode.

## Decision And Status

- Export status `0x83` maps to comparison status `3` before examining index.
- The matched index is tested as signed at `0x18002e890..0x18002e898`.
  A negative index checks coverage against byte `0x1800f2401`, then quality
  against byte `0x1800f2400`. Strictly below gives `0x8007` or `0x8008`;
  equality passes these checks. Otherwise status is `3` and match latch
  `0x18019b9b4` is cleared.
- A nonnegative index is copied to the caller and global `0x1800f2404`.
  The score test at `0x18002e9c6..0x18002e9ce` is signed `JLE`: only score
  strictly greater than zero sets status `2` and match latch to one. No second
  numeric acceptance threshold or best-score arbitration is applied.
- Nonpositive score with nonnegative index clears the latch and selects
  `0x8007` for low coverage, otherwise `0x8008`.

On success the temporary copied record supplies 32 envelope bytes from `+0x10`
to `0x18019b9d8`, eight bytes to `0x18019c178`, and its stripped length to
`0x18019b9f8`. In multi-gallery identify this is the final loaded record, not
necessarily the winning record. Public identity selection belongs to
`FUN_180020b30`; study selects the retained winner and handle index as described
in `FUN_18002ba60.md` and `FUN_18002bbf0.md`.

The private record copy is zeroed and freed on return. Normal function return
is zero; the caller consumes the separate comparison-status output.
