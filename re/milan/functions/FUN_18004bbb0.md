# FUN_18004bbb0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18004bbb0..0x18004bec1`; caller: `FUN_18004c100`.
- Callee: `FUN_1800496c0`.

## Recovered Behavior

It converts one refined, unique candidate into the pre-descriptor 56-byte
record and 24-byte auxiliary state. It obtains the orientation histogram peak,
interpolates and wraps orientation, records signed strength and coordinates,
and appends to the incoming caller count.

The selector test is `((param_10 - 9) & 0xfffffff6) == 0`, selecting the
multi-peak path exactly for `9`, `10`, `17`, and `18`. That path builds the
histogram without antipodal duplication, computes threshold
`(peak * 0xcccd + 0x8000) >> 16`, scans bins `0..35`, and accepts only bins
whose unsigned value meets the threshold and is strictly greater than both
wraparound neighbors. Each accepted bin is interpolated and appended; scanning
continues until the post-append count reaches `param_9` capacity.

The shipping profile-9/subtype-12 call instead supplies `param_9=300` and
`param_10=12`. It uses antipodal mode, bypasses both threshold and strict-
neighbor checks, interpolates the helper's single selected maximum
unconditionally, appends one record, and stops rather than scanning later bins.
This distinction is exact: a non-strict two-bin maximum is accepted in mode 12,
whereas the scan mode rejects a plateau and can continue to later strict peaks.

For subtype 12, `FUN_1800496c0` duplicates every weighted contribution into its
antipodal bin and selects the first maximum, so its returned bin before the
`+18` adjustment is in `0..17`; the adjusted interpolation index is therefore
in bounds at `18..35`. This function writes one 56-byte pre-descriptor record,
one `(strength,index)` rank pair, and one 24-byte auxiliary record, then updates
the caller-owned count. `FUN_18004c100` marks the converged integer pixel only
after this append returns. Current source has the same ownership and order in
`feature/materialization.c:125..180` and `212..230`.

The interpolated bin is narrowed to signed 16 bits, wrapped once into
`[0,0x47ff]`, and converted with signed truncating division as
`(((bin_q9 * 0x6488) / 36) >> 9) - 0x3244`. Subtype 12 adds `0x3244` when that
result is negative, yielding record orientation in `[0,0x3244)`. The foreground
predicate is the strict five-byte neighborhood sum `> 0x27f`; refined
coordinates narrow to 16 bits, while strength is negative absolute signed
32-bit strength. Current source uses 64-bit interpolation operands matching the
DLL's `RAX/RDX` arithmetic for every nonzero denominator.

The exact subtype-12 record writes at `0x18004bdc8..0x18004be37` are foreground
word `+0x00`, refined x/y words `+0x02/+0x04`, orientation word `+0x06`, and
signed strength dword `+0x08`. Auxiliary writes at
`0x18004be3a..0x18004be62` are integer x/y, fixed-point scale, histogram maximum,
selected-bin value, and a 16-bit selected-value-differs-from-maximum flag. The
reserved auxiliary word at `+0x16` is not written by the DLL and is not consumed
on this path; source initializes it to zero. The DLL also does not initialize
record `+0x0c..+0x37` here; every retained record gets class `+0x0c..+0x0f` and
descriptor `+0x10..+0x37` later. Source's full-record `memset` is therefore an
intentionally safer intermediate-state difference, not a final-record
difference.

At `0x18004bd96`, native `IDIV` uses denominator
`2 * previous + 2 * next - 4 * current`. The scan-mode strict checks prove it
nonzero; subtype 12 provides no such invariant. Direct DLL controls fault there
for an all-zero histogram and a positive three-bin wraparound plateau. Current
source mirrors the unguarded division at `feature/materialization.c:142..150`.
End-to-end reachability from production scale and gradient producers remains
unresolved; this is not a known DLL/source divergence.

Native strength negation is 32-bit two's-complement. Source's signed negation at
`feature/materialization.c:166..175` would be undefined for an independently
supplied `INT32_MIN` candidate, but `FUN_18004c5a0` emits bounded positive
strength on every accepted production candidate. This difference is
malformed-only for the recovered owner chain.

## Confidence And Unresolved

- Confidence: high for selector modes, threshold/neighbor predicates,
  append/count/capacity behavior, and sequence-72 role.
- Unresolved: end-to-end reachability of a subtype-12 zero-denominator plateau
  from one production frame through both scale and gradient producers.
