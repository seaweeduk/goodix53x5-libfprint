# FUN_18004bbb0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18004bbb0..0x18004bec1`; caller: `FUN_18004c100`.
- Callee: `FUN_1800496c0`.

## Recovered Behavior

It converts one refined, unique candidate into the pre-descriptor 56-byte
record and 24-byte auxiliary state. It obtains the orientation histogram peak,
interpolates and wraps orientation, records signed strength and coordinates,
and appends to the incoming caller count.

The shipping profile-9/subtype-12 call supplies `param_9=300` and
`param_10=12`. It uses antipodal mode, bypasses both threshold and strict-
neighbor checks, interpolates the helper's single selected maximum
unconditionally, appends one record, and stops rather than scanning later bins.
The selector `((param_10 - 9) & 0xfffffff6) == 0` is false for type 12;
the other selector branch is outside this contract.

For subtype 12, `FUN_1800496c0` duplicates every weighted contribution into its
antipodal bin and selects the first maximum, so its returned bin before the
`+18` adjustment is in `0..17`; the adjusted interpolation index is therefore
in bounds at `18..35`. This function populates the prefix of one 56-byte record,
one `(strength,index)` rank pair, and the meaningful 22 bytes of one 24-byte
auxiliary record, then updates
the caller-owned count. `FUN_18004c100` marks the converged integer pixel only
after this append returns. The current corresponding owner is
`feature/materialization.c:feature_materialize_candidate`.

The interpolated bin is narrowed to signed 16 bits, wrapped once into
`[0,0x47ff]`, and converted with signed truncating division as
`(((bin_q9 * 0x6488) / 36) >> 9) - 0x3244`. Subtype 12 adds `0x3244` when that
result is negative, yielding record orientation in `[0,0x3244)`. The foreground
predicate is the strict five-byte neighborhood sum `> 0x27f`; refined
coordinates narrow to 16 bits, while strength is negative absolute signed
32-bit strength.

The exact subtype-12 record writes at `0x18004bdc8..0x18004be37` are foreground
word `+0x00`, refined x/y words `+0x02/+0x04`, orientation word `+0x06`, and
signed strength dword `+0x08`. Auxiliary writes at
`0x18004be3a..0x18004be62` are integer x/y, fixed-point scale, histogram maximum,
selected-bin value, and a 16-bit selected-value-differs-from-maximum flag. The
reserved auxiliary word at `+0x16` is not written by the DLL and is not consumed
on this path. Antipodal equality means the selected value equals the returned
maximum, so the flag at `+0x14` is always zero for these type-12 producers.
The DLL also does not initialize record `+0x0c..+0x37` here; every retained
record gets class `+0x0c..+0x0f` and
descriptor `+0x10..+0x37` later. Neither untouched span contributes prior storage
contents to the completed record. See `FUN_180049b90.md` for rank-index-based
auxiliary lookup and final ownership.

At `0x18004bd96`, native `IDIV` uses denominator
`zext32(2*previous) + zext32(2*next) - zext32(4*current)`: each product wraps in
a 32-bit lane before the zero-extended 64-bit adds/subtracts at
`0x18004bd63..0x18004bd96`. The numerator is the signed-64 expression
`((int64(uint32(previous))-int64(uint32(next))) << 9) - (denominator >> 1)`, and
division is signed truncation toward zero. Accepted type-12 refinement bounds
radius to 12, and the directly reached gradient producer caps magnitude at
`0x3ffff`.
Exhaustive evaluation of every reachable scale value and the exact weight table
bounds the total unsmoothed contribution by `54648162` and any smoothed bin by
`20493060`; consequently every doubled/quadrupled term is below `2^32` and the
native lane products equal their widened mathematical values on this domain.

Successful ordinary interpolation requires a nonzero denominator; subtype 12
provides no unconditional nonzero invariant. Because mode 12 selects the first
global maximum in bins `0..17` before adding 18, a zero denominator requires
that first maximum to be bin 0 and smoothed bins `35,0,1` all equal the global
maximum. Antipodal symmetry
then gives the selected `17,18,19` plateau. A first maximum above zero cannot
have an equal previous bin, so there is no other well-formed mode-12 zero case.

The zero-denominator surface is not excluded by the accepted producer chain.
The DLL has no fallback orientation for it. Nonfaulting compatibility callers
must retain the producer range bounds and require nonzero interpolation
denominators for every candidate in every reached pass before invoking this
owner.

Native strength negation is 32-bit two's-complement. `FUN_18004c5a0` emits bounded
positive strength on every accepted production candidate; independently assigned
`INT32_MIN` is outside that producer domain.
