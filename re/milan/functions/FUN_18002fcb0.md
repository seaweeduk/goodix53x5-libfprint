# FUN_18002fcb0

## Identity And Call Graph

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18002fcb0..0x18002fd16`.
- Caller in scope: `_CheckUpdateTemplate_E` (`FUN_18002bbf0`) before packing a
  positive update.
- Callees: fixed-size copy helper `FUN_18008a9c0` and, only for flag zero,
  `FUN_180034220`.

## Behavior

Copies the 16-byte adapter identity/header from global `0x18019b9b8` to
`0x18019b9c8`. With flag zero it also initializes a 32-byte global block and
copies eight bytes into it. The production update caller passes flag one, so only
the fixed 16-byte prefix preparation occurs. It does not inspect action identity,
template contents, or persistence status and returns no error.

## Confidence

High for the production flag-one path from complete decompile. Semantic names of
the optional flag-zero block are outside this study scope.
