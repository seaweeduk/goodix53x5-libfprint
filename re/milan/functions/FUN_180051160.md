# FUN_180051160

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x180051160..0x1800514d8`, adaptive score-mask builder.

## Call Graph

- Auxiliary-path caller: `FUN_18004e8c0`.
- Flood helper: `FUN_180051f50` when the seed count exceeds 50.

## Exact Contract

Average scores strictly between zero and `config[5]`, rounded as
`(sum + count / 2) / count`. If no such score exists, leave the output clear.
Otherwise:

```text
seed = max(config[1] * average >> 8,
           min(5 * average, config[0]))
grow = max(config[2] * average >> 8, average, config[4])
mask[i] = score[i] >= seed ? 255 : 0
if seed_count > 50:
    flood score values at grow threshold
```

The profile-9 temporal configuration is
`[1600, 768, 384, 0, 700, 800]`. Linux already implements these calculations in
`milan_profile9_temporal_class3()`. The flood affects the class plane but not
the secondary histogram state or its fallback guard.
