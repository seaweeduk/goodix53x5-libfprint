# FUN_180072b60

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180072b60..0x180073054`.
- Sole caller: `FUN_18004ae70` at `0x18004b613`.
- Role: reduce the extraction validity matrix to the packed inline feature mask
  stored at live feature `+0x28` and serialized as tag `ce`.

## Profile-9/Type-12 Contract

The caller supplies the `104x88` one-byte validity matrix built during feature
extraction. This helper divides it into row-major `4x4` blocks. A full block is
one exactly when at least eight of its sixteen input bytes are nonzero. Generic
partial right or bottom blocks use the same inclusive-majority predicate,
`nonzero_count * 2 >= pixel_count`, but profile 9 reaches no partial block because
both dimensions are divisible by four.

The fixed result geometry is therefore `26x22`, or 572 bits. Blocks advance left
to right and then top to bottom. `FUN_1800741a0` at `0x180073025` packs that dense
plane least-significant bit first into 72 bytes: block index `i` is output byte
`i / 8`, bit `i & 7`. The final byte contains indices 568..571 in bits 0..3;
bits 4..7 are zero.

The helper initializes the live 200-byte generic mask field to `0xff` at
`0x180072bf0..0x180072c05`, but the packer overwrites the complete 72-byte
profile-9 prefix and neither profile-9 expansion nor serialization consumes the
remaining 128 bytes. The temporary 572-byte logical plane starts at zero, so
every clear block remains clear before packing.

Current source implements the reached contract in
`goodix_milan_feature_base_maps()` at
`drivers/goodix53x5/milan/core.c:1698-1725`. It uses the same fixed crop,
row-major block order, inclusive half threshold, LSB-first output, and exact
72-byte size.

## Boundaries And Confidence

- The caller owns the destination field and the source matrix; this helper does
  not allocate either one and returns no status.
- Fixed profile-9 arithmetic is bounded: 9,152 source bytes, 572 temporary
  bytes, and 72 published bytes. No partial-block or stack-capacity boundary is
  reached.
- Confidence is high from the complete body, the `FUN_18004ae70` call site, and
  the matching source implementation. The vendor semantic name of the input
  validity matrix remains unresolved.
