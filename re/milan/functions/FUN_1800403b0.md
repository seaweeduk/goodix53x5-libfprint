# FUN_1800403b0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800403b0`, `0x1800403b0..0x18004057a`.
- Role: decode the packed Milan tail object.

## Call Graph

- Caller: `FUN_18003e3a0`.
- Callee: memory copy.

## Recovered Behavior

The decoder consumes the length-delimited `94` object and dispatches tags
`a1..a8`. Tag `a1` copies its declared length, bounded to at most 200 bytes, to
live tail `+0x000`; the remaining tags map to `+0x0c8`, `+0x0cc`, `+0x10c`, and
`+0x50c..+0x518`. Valid production packing always supplies exactly 200 `a1`
bytes through `FUN_18003f290`.

Unpacking restores the order vector as template state. No probe, relation, or
feature traversal reconstructs it at this boundary.

The copy bound is not semantic order validation. Focused canonical controls
prove that a duplicate entry is accepted and later changes reachable matcher
behavior, while first entry 40 in a 40-feature template is accepted here and
later triggers an unhandled matcher read fault. Persisted-print admission must
require the first `feature_count` dwords to be a permutation of
`[0, feature_count)`; neither this decoder nor canonical repacking supplies that
guarantee. Exact evidence is maintained in
`../PERSISTED-ORDER-ADMISSION-AUDIT-20260809.md`.

## Confidence And Unresolved

- Confidence: high for tag mapping and copy bounds.
- Acceptance of a shorter `a1` block is decoder behavior, not authority for
  producing noncanonical templates.
