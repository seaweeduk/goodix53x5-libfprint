# FUN_18005edb0

## Binary And Body

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18005edb0..0x18005ef55`.
- Role: matcher-family dispatcher used by `identifyImage` and
  `identifytemplate`.

## Call Graph

- Callers: exported `identifyImage` and `identifytemplate`.
- Callees: `FUN_18005e230`, `FUN_180055a40`, and `FUN_180062a30`.

## Production Dispatch

The function preserves the live probe object from `RDX` and the unpacked
enrolled template from `R8`. `FUN_18005e230` builds the policy configuration
from the template, including subtype at `+0x3c`. Dispatch then reads the live
template's first word.

Types `9`, `10`, `17`, and `18` take `FUN_180062a30`. Type `12` instead passes
the second family mask and calls `FUN_180055a40` at `0x18005eec1`, with the
original probe in `RDX`, template in `R8`, and policy configuration in `R9`.
Thus profile-9's packed subtype 12 reaches `FUN_180055a40`; the target path does
not belong to the alternate type family.

## Evidence And Confidence

- Argument preservation and policy initialization: `0x18005edf0..0x18005ee09`.
- Family tests: `0x18005ee2a..0x18005ee52`.
- `FUN_180055a40` call setup: `0x18005eea9..0x18005eec1`.
- Confidence is high for argument provenance and type-12 dispatch.
- Other accepted type values in the second mask are outside this investigation.

The decision-half retrospective does not change this dispatch mapping. It does
supersede runtime-readiness claims downstream: ordinary subtype-12 calls reach
the missing final policy and the incorrect caller-state/finalization contracts
documented in `FUN_180055a40.md`.

## Tail-Admission Dispatch Applicability

The native ordinary class-sibling change is bounded to the proven type-12
`FUN_180055a40` family. Profile9/type12 initializes configuration
`[23,47,40,38,-1,16]` and supplies word 5 to the sibling consumer. Types
`9/10/17/18` still dispatch to `FUN_180062a30`; no value or helper in that family
was changed. Other second-mask types remain outside the recovered contract and
retain native defaults.
