# FUN_18005edb0

## Binary And Body

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900.
- Entry/body: `0x18005edb0..0x18005ef55`.
- Role: matcher-family dispatcher used by `identifyImage` and
  `identifytemplate`.

## Call Graph

- Callers: exported `identifyImage` and `identifytemplate`.
- Callees: `FUN_18005e230`, `FUN_180055a40`, and `FUN_180062a30`.

## Production Dispatch

The function preserves the live probe object from `RDX` and the unpacked
enrolled template from `R8`. `FUN_18005e230` builds the policy configuration
from the template through configuration word 17, including subtype at `+0x3c`.
The dispatcher writes recognition one to word 19 at `0x18005ee10`, copies live
probe `c7/+0x150` to word 18 at `0x18005ee1b`, and then reads the enrolled
template's first word for family dispatch.

Types `9`, `10`, `17`, and `18` take `FUN_180062a30`. Type `12` instead passes
the second family mask and calls `FUN_180055a40` at `0x18005eec1`, with the
original probe in `RDX`, template in `R8`, and policy configuration in `R9`.
Thus profile-9's packed subtype 12 reaches `FUN_180055a40`; the target path does
not belong to the alternate type family.

The probe handoff remains live rather than serialized. The dispatcher passes
the original feature object from `RDX`, including record pointer/count
`+0xf8/+0xf0`, stored partition boundary `+0x118`, inline mask `+0x28`, derived
mask matrix `+0x140`, and optional classification `c7/+0x150`. Packing the probe
through `FUN_180040a70` is not part of `identifyImage` matching and would discard
live-only record fields.

## Per-Gallery Reset

Type 0 also passes this dispatcher mask and reaches `FUN_180055a40`, with
its actual subtype and policy word 17 zero rather than type 12's one. The
shared entry point does not imply shared scoring or classifier paths. This
function receives no outer probe subtype to compare against the gallery.
See `../PROFILE0-MATCH-TEMPLATE-CONTRACT.md` for the type-0/type-12 policy,
queue, and storage-identity boundaries.

Each ordinary `identifyImage` iteration invokes this dispatcher with a fresh
stack-local configuration. `FUN_18005e230` fills the type-12 policy fields;
the dispatcher then supplies probe `c7` as word 18 and recognition state one
as word 19. The trigger-index slot, word 14, is not initialized by the ordinary
configuration builder; ordinary feature mode word 13 is one, so the
trigger-only comparison does not consume that slot. Candidate changes to the
consumed policy state cannot survive into the next gallery's invocation.

The shared evidence base is `0x18024e550`. Immediately before every dispatch,
`identifyImage` writes its operation-level anti-fake argument to
`0x18024ebe0`, which is evidence `+0x690`, at `0x180001e27`.
`FUN_180055a40` saves that seed locally before clearing all `0x698` evidence
bytes. It then initializes selected index `+0x648` to `-1`, the diagonals of
both selected and relation affines to `0x100`, continuation state `+0x68c` to
one, and score to zero. Previous-gallery selected feature, transforms, retained
entries, gates, and blocker results are not inputs to the next gallery.

Related current owners are `goodix_milan_match_info_result()` and
`milan_match_probe_result_internal()` in
`drivers/goodix53x5/milan/match/match.c`: the live-record input and non-owning
feature-view boundary. The native live-probe ownership contract is detailed in
`FUN_180055a40.md`.

## Instruction Boundaries

- Argument preservation and policy initialization: `0x18005edf0..0x18005ee09`.
- Family tests: `0x18005ee2a..0x18005ee52`.
- `FUN_180055a40` call setup: `0x18005eea9..0x18005eec1`.

## Type-12 Correspondence Configuration

`FUN_18005e230` initializes the type-12 correspondence subvector
`[23,47,40,38,-1,16]` at configuration `+0x18..+0x2c` (words `6..11`).
The final element is the tail limit 16 consumed by class-sibling construction
through `FUN_180057900 -> FUN_180075220 -> FUN_1800770e0`; it is word 5 of
that subvector, not word 5 of the enclosing policy. See `FUN_18005e230.md`
and `FUN_180057900.md` for initializer and consumer ownership.

## Score Return Ownership

For subtype 12 this dispatcher passes the caller's score pointer unchanged to
`FUN_180055a40` at `0x18005eec1` and returns that function's status. It does not
normalize a zero matcher score, synthesize a rejection reason, or call
`FUN_18005d5f0` itself. Terminal fallback admission belongs entirely to
`FUN_180055a40`: on its zero-score path, a live-probe auxiliary class of two or
greater bypasses that call. This bypass does not evaluate fallback's rejection
formula; the matcher retains zero unless its separate blocker-ratio branch
replaces the score.
See `FUN_180055a40.md` and `../MATCH-CONTRIBUTION-CONTRACT.md` for the ordered
score, evidence, queue and lifecycle publication contracts.
