# FUN_180079060

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180079060..0x1800792c2`.
- Role: construct the caller-retained 31-pair fallback workspace consumed by
  `FUN_18005d5f0`.

## Call Graph

- Direct caller: `FUN_180055a40`, call at `0x180056ff2`.
- Direct callee: `FUN_180075a60`, setup and call at
  `0x180079219..0x180079267`.
- `FUN_18005d5f0` later passes all 31 retained slots to `FUN_18005b900`.

## Matrix And Partition Contract

The function consumes enrolled and probe feature objects plus the caller-owned
`180x180` descriptor distance matrix produced by the primary path. Probes own
the candidate rows. A leading probe considers enrolled `[0,e_split)` and a
trailing probe considers `[e_split,e_count)`; no candidate crosses the stored
physical partition boundary. Each candidate distance is the unsigned byte at
`enrolled_index * 180 + probe_index`.

Best and second distances initialize to 192 and best indices to `-1`. Updates
are strict, preserving the earlier enrolled record on best and second ties.
Empty partitions and matrix values that do not improve 192 leave the probe
unset. The partitioned scan is at `0x1800790d8..0x180079219`.

## Selection And Publication

`FUN_180075a60` receives capacity 31, ratio configuration at caller
`configuration + 0x18`, and enrolled records for target-coordinate collision
checks. Its temporary orientation is `(probe,enrolled)`. The loop at
`0x180079267..0x1800792a0` swaps all 31 slots, including sentinels, into final
`(enrolled,probe)` orientation before writing the fallback workspace. The caller
invokes this function only when its fallback-enable condition succeeds and then
sets the parallel feature-enable word.

## Source Correspondence And Failure

Current `goodix_milan_match_relaxed_correspondences()` reproduces the partition,
tie, selection, and output order but recomputes descriptor distance from the
records instead of receiving the matrix. This is equivalent for a canonical
matrix produced from those records by `FUN_180076400`. The DLL also accepts a
synthetic-valid call with an independently inconsistent matrix; the current API
cannot represent that state.

Neither implementation allocates. The DLL has no pointer, count, partition, or
capacity status path, and malformed counts beyond its fixed 180-entry local
arrays can corrupt the native stack. Profile-9 callers cap record counts at 150
and validate stored boundaries before entry. The native relaxed helper itself
also relies on its validated caller rather than validating direct inputs.

## Confidence And Unresolved

- Confidence: high for matrix indexing, stored partitions, strict ties,
  selector arguments, capacity, output swap, and caller integration.
- Other profiles and configuration variants were not dynamically exercised.
