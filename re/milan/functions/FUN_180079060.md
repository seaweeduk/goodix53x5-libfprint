# FUN_180079060

## Identity

- Binary: `GoodixEngineAdapter.dll`.
- Binary SHA-256:
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180079060..0x1800792c2`.
- Role: construct the caller-retained 31-pair workspace consumed by
  `FUN_18005d5f0`.

## Call Graph

- Direct caller: `FUN_180055a40`, call at `0x180056ff2`.
- Direct callee: `FUN_180075a60`.

## Recovered Behavior

The helper consumes the enrolled and probe feature objects plus the descriptor
distance matrix already built by the primary caller path. It keeps the best and
second-best enrolled source for each probe record, independently across the two
record partitions. Distances initialize to `192` and indices to `-1`.

It then calls `FUN_180075a60` with capacity `31`, the caller configuration at
`configuration + 0x18`, the enrolled records for spatial collision checks, and
the per-probe best/second arrays. The selector emits `(probe,enrolled)` pairs;
this helper swaps each result to `(enrolled,probe)` and writes all 31 slots,
including `-1,-1` padding, into the per-feature fallback workspace.

The caller invokes this helper only when its fallback-enable condition succeeds,
then sets the parallel feature enable word. `FUN_18005d5f0` later passes all 31
slots to `FUN_18005b900`.

## Evidence

- Partitioned best/second scan: `0x1800790d8..0x180079219`.
- Capacity-31 selector call: `0x180079219..0x180079267`.
- Output pair swap: `0x180079267..0x1800792a0`.
- Caller workspace and enable write: `0x180056f95..0x180056ff2`.
- A passive `12/2` control showed that feature 2 is enabled and all 31 workspace
  pairs are exact against native construction. Its Windows affine-filter output
  is count `5` and transform `[253,-12,451,-20,256,1217]`; the following mode-1
  transform classifier returns `1`, so `FUN_18005d5f0` skips it before
  `geometry_seen`.

## Confidence

- High for the profile-9/subtype-12 partition scan, selector arguments,
  capacity, output ordering, and caller integration.
- Other profiles and configuration variants were not dynamically exercised.
