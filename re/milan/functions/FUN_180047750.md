# FUN_180047750

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180047750..0x18004781b`.
- Sole code caller: `FUN_180047820`.
- Role: build the per-pixel retained-class agreement mask used by extraction
  classification.

## Contract

```c
void FUN_180047750(
  const uint8_t *current_mask,
  uint8_t *output,
  int32_t rows,
  int32_t columns,
  int32_t retained_plane_count,
  uint8_t class_value);
```

For every nonzero current-mask pixel, the function counts retained planes whose
pixel equals `class_value`. Retained planes begin at `DAT_1801ce3e0` and use
stride `0x4c90`. It emits `0xff` exactly when

```text
matching_count > floor(retained_plane_count / 2) + 1
```

The only caller invokes it after proving the retained count is at least three
and passes the constant count 3. Consequently the production profile-9
predicate is `matching_count > 2`: all three retained planes must agree. This is
three-frame unanimity, not a two-of-three majority. Pixels outside the current
mask always remain zero.

## Evidence And Confidence

- Plane iteration and class comparison: `0x180047791..0x1800477df`.
- Strict final threshold: `0x1800477e1..0x180047801`.
- Confidence is high from the complete loop and exact caller operands.
