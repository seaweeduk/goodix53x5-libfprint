# FUN_1800405a0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800405a0..0x1800406f2`.
- Role: build the 256-dword table used by the outer template CRC.

## Call Graph

- Callers: complete decoder `FUN_18003e3a0` at `0x18003e468` and complete
  encoder `FUN_18003eaf0` at `0x18003ef60`.
- Callees: none.

## Recovered Behavior

The helper builds a reflected CRC-32 table from polynomial `0x04c11db7`. The
encoder and decoder initialize the running value to `0xffffffff`, consume each
payload byte through this table with a right shift, and complement the result.
This is CRC-32/ISO-HDLC, whose reflected implementation polynomial is
`0xedb88320`.

The outer encoder's loop is `0x18003ef75..0x18003efa0`. It covers exactly the
declared payload bytes beginning at packed offset 10, after every header,
feature, relation, graph, and tail byte has been emitted. The decoder's matching
loop is `0x18003e46d..0x18003e4a6`. The envelope tags, stored CRC, and payload
length at offsets `0..9` are outside the CRC domain.

The complete framing contract is maintained in
`../OUTER-TEMPLATE-CODEC.md`.

## Confidence

- Confidence: high from both complete call sites, table construction, and exact
  CRC agreement with valid type-12 payloads.
