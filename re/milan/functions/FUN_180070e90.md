# FUN_180070e90

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180070e90`, `0x180070e90..0x180070fbf`.
- Role: derive affine scale, rotation, and orthogonality diagnostics.

## Call Graph

- Direct callers include `FUN_18005a3c0` and `FUN_180057900`.
- Direct callees: `FUN_180074940`, `FUN_180074290`, and `FUN_180074980`.

## Recovered Behavior

It returns average column length, a folded `0..90` degree rotation, and absolute
normalized column dot product. Candidate words `12..14` do not use those three
outputs uniformly: word 12 tests unsigned `average_scale - 234 > 47`, equivalent
to average scale outside inclusive `[234,281]`; words 13 and 14 compare absolute
normalized dot product against `0x147b` and `0x28f6`. The folded rotation is not
the word-12 penalty operand.

Native had the two arguments to its locally transposed `feature_atan2()` in the
wrong order. Correcting that call removed multiple transform/flag differences.
The candidate penalty now computes exact average scale as
`(floor(sqrt(a*a+c*c)) + floor(sqrt(b*b+d*d))) / 2` and applies the unsigned
word-12 boundary above. Folded rotation remains a separate diagnostic and is
not a candidate penalty operand.

For feature 2's alternate affine `[307,-7,-23556,9,277,5759]`, Windows computes
average scale outside `[234,281]`, applies one four-point penalty, and ranks
detail `195` as `191`. Native now computes exact primary/alternate scales
`276/292`, penalty pairs `0,0/1,0`, and ranks them `193/191`, selecting the
primary like Windows. Focused boundaries `233/234/281/282` produce scale
penalties `1/0/0/1`.

## Confidence And Unresolved

- Confidence: high for arithmetic, output order, and all three candidate-facing
  thresholds.
- The native correction is implemented and covered by exact boundaries and
  authoritative row `74/1`; do not describe word 12 as an angle or rotation
  threshold.

The complete type-12 audit directly covers average-scale boundaries
`233/234/281/282`, Q16 dot boundaries `0x147a/0x147b` and
`0x28f5/0x28f6`, negative/nonidentity coefficients, and every equal-rank
outcome. No type-12 behavior remains unresolved.

## FUN_18005a3c0 Dependency Unit 1

This helper has no validity/status input and therefore must receive only a fully
defined affine. `FUN_18005a3c0:0x18005a756` violates that precondition when the
alternate selector produces fewer than three pairs. Native fail-closed handling
belongs in the caller: skip this call for `alternate_valid = false`; no zero or
poison affine diagnostic is a parity target.

## FUN_18005a3c0 Dependency Unit 2

Scale, orthogonality, strong-orthogonality, and the derived penalized rank now
travel in the same native owned object as the affine, overlap result, and owner
count. The primary diagnostics at `0x18005a546` and alternate diagnostics at
`0x18005a756` are never synchronized through parallel fields after ranking.
Strict rank ties copy nothing and retain every primary-owned field. The helper's
arithmetic and thresholds are unchanged.

## FUN_18005a3c0 Dependency Unit 5

An alternate selected count `>=3` can make `FUN_18005b900` deterministically
write affine zero when robust estimation finds no model. The DLL still calls this
helper at `0x18005a756`; zero produces defined diagnostics but carries no model
validity. A strict rank win may publish those diagnostics with failure-zero.
Native intentionally suppresses this call when explicit alternate model validity
is false. This differs from unit 1 only in that the DLL input bytes are defined
zero rather than uninitialized; neither is a valid affine candidate.
