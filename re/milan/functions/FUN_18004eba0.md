# FUN_18004eba0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18004eba0..0x18004ee77`, broken-level promotion classifier.

## Call Graph

- Caller: `FUN_18004ef70`.
- Substantive callee: `FUN_1800505e0`.

## Exact Behavior

The second argument is the eroded profile mask produced by `FUN_18004ee80`.
Every nonzero byte is canonicalized to one, then `FUN_1800505e0` produces
exclusive left, right, up, and down run lengths for the 88x108 image.

For each pixel define:

```text
all4 = left >= 6 && right >= 6 && up >= 6 && down >= 6
opposite_pair = (left >= 6 && right >= 6) || (up >= 6 && down >= 6)
strong = normalized_live > 3800

strict_count += strong && all4
combined_count += (!eroded_mask && all4) || (strong && opposite_pair)
```

The return policy, where `reference_count` is the active contrast-mask count,
is:

```text
if strict_count * 10 > reference_count: return 2
if (strict_count + combined_count) * 10 > reference_count: return 1
return 0
```

`combined_count` can include a strict pixel, and the second test deliberately
adds `strict_count` again. All comparisons are strict. `FUN_18006b290` promotes
every base mode below 8 to 8 for return 2, and every base mode below 6 to 6 for
return 1.
- `FUN_18006b290` promotes every base mode below `8` to `8` for return `2`, and
  every base mode below `6` to `6` for return `1`.

## Evidence

- Binary mask and directional planes: entry through `0x18004ed29`.
- Raw threshold and directional predicates: `0x18004ed50..0x18004ede7`.
- Final strict/combined tenth tests: `0x18004edfc..0x18004ee2b`.

## Confidence

High for the complete arithmetic and return policy. Vendor names remain
unknown.

## Unresolved

- The second focused capture retains strict/combined/reference counts for the
  five remaining rows. They are `13/26/9194`, `0/0/7861`, `0/0/8950`,
  `0/0/9472`, and `0/0/9476`; this function returns zero in every case and does
  not promote their modes.
