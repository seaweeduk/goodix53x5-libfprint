# FUN_18005b900

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18005b900`, `0x18005b900..0x18005bc51`.
- Role: robustly fit and post-filter an affine from selected correspondence pairs.

## Call Graph

- Direct callers include `FUN_18005a3c0` and `FUN_180057900`.
- Relevant callees: `FUN_180077e20`, `FUN_180057140`, and `FUN_180076af0`.

## Recovered Behavior

It materializes enrolled/probe coordinates, orientations, and original record indices for every valid pair. `FUN_180077e20` selects a robust affine and inlier mask. Crucially, `FUN_180057140` then removes orientation-inconsistent inliers before the function counts them. Only after that filtering, when more than three remain and the robust residual exceeds `0x4000`, `FUN_180076af0` attempts least-squares refinement using the filtered mask.

The caller's `R9` value is workspace capacity, not the selected-pair count.
Primary and alternate type-12 calls pass `42` and terminate populated entries
with `-1,-1`. Diagnostic v3 passed all 42 slots to native affine fitting, so its
reported `69/76` and `31/35` affine totals included invalid padded indices.
Diagnostic v4 derives the real count from the sentinel and proves affine and
post-orientation-mask parity for `76/76` primary and `35/35` alternate calls
when supplied the official pairs.

After native alternate pair construction was partition-bounded, the supplied
pairs are themselves exact `35/35`; affine and mask parity remains `35/35`.

Native previously counted and refined the pre-orientation mask. Reordering those operations to match this function improves the frozen candidate-dispatch boundary from `0/14` to `2/14` exact rows and from `4/26` to `6/26` exact calls.

The accepted-score retained-count reference adds seven exact supplied-input
controls (`16/1 f2`, `21/1 f1`, `51/1 f5`, `95/1 f6`, `99/1 f5`, `104/1 f3`,
and `161/1 f0`). For every target, native reproduces the official primary and
alternate final filtered counts and post-orientation masks when supplied the
official records and selected pairs. Their normal-path count differences first
surface in two `FUN_18005a3c0` promotion outputs and five `FUN_180057900`
sibling outputs; they are not evidence for another affine-filter arithmetic
change here.

The focused row `16/1` construction reference supersedes the earlier broad
localization for that row. Native's sole extra live record creates pair
`(0,extra)` and is a primary inlier, changing the primary affine and filtered
count from official `14` to native `15`. The normalized alternate selected set
is likewise the official set plus `(0,extra)`, but the changed fit/orientation
mask removes four official inliers and adds three native inliers, yielding
official/native alternate counts `22/21`. This is expected downstream behavior
from unequal exact record inputs, not an affine-filter arithmetic defect.

The final accepted-score residual adds `74/1` feature 2 as another supplied-
input control. Native exactly reproduces Windows primary count/affine/mask
`5/[274,-15,-20387,17,279,5051]` and alternate count/affine/mask
`6/[307,-7,-23556,9,277,5759]`. The row diverges only when
`FUN_18005a3c0` ranks those exact outputs, so no robust-fit, refinement, or
orientation-mask change is authorized here.

## Confidence And Unresolved

- Confidence: high for helper ordering, returned count, and refinement gate.
- Type-12 unresolved behavior: none. The all-model-rejected mask/affine behavior
  is closed below.

## Evolved-Gallery Units 7-9 Controls

The sequence-197 selected calls add two same-input controls without identifying
a defect here. For `197/1 f39`, native reproduces primary count/affine
`9/[236,43,17613,-48,241,-5792]` and alternate count/affine
`9/[238,45,17491,-46,241,-5847]`. For `197/2 f6`, it reproduces
`14/[241,81,-11234,-96,241,-2546]` and
`17/[234,84,-11101,-92,233,-2263]`. Selected pair arrays and masks are also
exact from the captured official inputs.

The first differences occur in `FUN_18005a3c0` after these results return:
equal-count alternate ranking for `197/1`, and ranked-transform ownership at the
forward-overlap promotion call for `197/2`. No robust-fit, orientation-filter,
least-squares, residual, or inlier-count change is authorized by either row.

## Complete Type-12 Failure And Tie Contract

The 2026-07-27 direct synthetic audit closes the former all-model-rejected
question. Robust fitting is called only when compacted pair count is greater
than argument 5; type 12 passes 2. Counts 0, 1, and 2 return zero without
writing the supplied affine. At count at least 3, `FUN_180077e20` initializes
residual `0x190000` and writes affine zero if no valid model survives. This
wrapper, not the estimator, owns zero initialization of the private inlier mask.

Model ranking is strict larger inlier count, then strict lower rounded mean
residual; an exact count/residual tie keeps the earlier triangle. Orientation
filtering precedes the returned count. Least-squares refinement requires final
count greater than 3 and robust residual greater than `0x4000`, and replaces
only for strict lower residual with no fewer inliers. Direct controls cover
untouched 0/1/2 output, identity, collinear no-model zero, translated Q8, and
orientation removal. No type-12 behavior remains unresolved.

## FUN_18005a3c0 Dependency Unit 1

The untouched-output behavior for selected-pair counts 0, 1, and 2 is valid
callee behavior but unsafe caller input. In the alternate call at
`FUN_18005a3c0:0x18005a6d2`, the supplied local has no prior definition, so the
caller must treat the returned zero count as `alternate_valid = false` and skip
all affine consumers. This helper does not own candidate rejection or status.
Primary positive counts at alternate entry remain independently valid because
they can occur only after this helper fitted an affine from at least three pairs;
orientation filtering may then reduce the returned count to one or two.

## Dependency Unit 5 Complete Failure Handoff

The returned count is not model status. Exact families are:

| Compacted pairs / estimator result | Returned count | Supplied affine | Private mask/residual |
|---|---:|---|---|
| `0..2` | 0 | untouched identity/zero/poison | estimator not called |
| `>=3`, no accepted model | 0 | six zeros | mask zero, residual `0x190000` |
| accepted model, orientation retains `0..2` | `0..2` | accepted affine unchanged | post-filter mask, robust residual |
| accepted model, retained `>3`, residual `<=0x4000` | retained | accepted affine | refinement skipped |
| accepted model, retained `>3`, residual `>0x4000`, refinement fails | retained | robust affine unchanged | count/mask unchanged |
| accepted refinement | retained | refined affine | wrapper count still the pre-refinement retained count |

`FUN_180057140` owns only mask/pair removal and count reduction; it never
invalidates or replaces the affine. `FUN_180076af0` is transactional at the
six-word output: singular normal equations, non-improving residual, or fewer
refined inliers leave the robust affine byte-for-byte unchanged. Neither helper
emits an error. Native must carry estimator model validity independently of the
post-orientation count so a valid model filtered to zero is not conflated with
no model.

Ghidra reports 32 blocks and 15 conditional branches here. Type 12 excludes only
the feature-index-`>=50` table arm and capacity panic `0x18005bc4d`; all semantic
failure arms apply. Persistent controls run zero, identity, `ff/a5/3c` initial
affines, every failure family, retained counts `0..4`, and nearby identity/
translated success twice byte-identically.

## Native Implementation

`MilanMatchAffineState` now separates selected count, estimation attempted,
accepted-model validity, orientation-filtered count, residual, and affine.
Selected counts below three remain unattempted and invalid. Count at least three
is valid only when the robust helper accepted a model; orientation-filtered zero
does not clear that validity. The type-12 target preserves the caller transform
for the unattempted case and publishes robust failure-zero for attempted
no-model. Native unit-5 controls freeze those ownership distinctions.
