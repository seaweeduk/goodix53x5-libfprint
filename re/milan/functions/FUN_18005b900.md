# FUN_18005b900

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18005b900`, `0x18005b900..0x18005bc51`.
- Role: robustly fit and post-filter an affine from selected correspondence pairs.

## Call Graph

- Direct callers include `FUN_18005a3c0`, `FUN_180057900`, and fallback
  `FUN_18005d5f0`.
- Relevant callees: `FUN_180077e20`, `FUN_180057140`, and `FUN_180076af0`.

## Recovered Behavior

It scans exactly argument-4 pair slots and compacts each slot whose first index
is nonnegative, preserving input order. It does not independently validate the
second index. For each compacted pair it materializes unsigned-16-bit enrolled
and probe Q8 coordinates, signed-16-bit orientations, and both original dword
indices into 42-entry locals. Argument 3 selects the enrolled feature table;
argument 4 is scan capacity, not compacted count; argument 5 is the strict
minimum, passed by primary/alternate callers as 2.

Compacted count at most argument 5 returns zero without calling the estimator
and without writing the supplied affine. Otherwise the wrapper zero-initializes
its private 42-byte mask and residual scratch, then calls `FUN_180077e20` with
argument 6 and mode 1. It replaces both compacted indices by `-1` for every zero
robust-mask byte, calls `FUN_180057140`, mirrors any newly negative enrolled
index back into the mask, and only then counts retained mask bytes. Thus
orientation filtering precedes the returned count and refinement. When retained
count is at least 4 and robust residual is strictly above `0x4000`, it calls
`FUN_180076af0` with the post-orientation mask. Refinement can replace only the
six affine words; returned count, mask, and robust residual remain unchanged.

The caller's `R9` value is workspace capacity, not the selected-pair count.
Primary and alternate type-12 calls pass `42` and terminate populated entries
with `-1,-1`.

The type-12 fallback call at `FUN_18005d5f0:0x18005d760` instead scans 31
slots, passes configuration word 2 as argument 5, and passes literal 4 as
argument 6. `FUN_18005e230 -> FUN_18005ed10` initializes configuration word 2
to 5. The signed comparison at `0x18005bab9..0x18005babf` therefore returns
zero for at most five populated fallback pairs, before any affine fit or
post-filter. This is distinct from the fallback caller's later requirement
that the returned filtered count exceed four: five survivors can arise only
from a larger input workspace. The supplied affine remains at the caller's
identity initialization when the compacted-input gate fails.

## Confidence And Unresolved

- Confidence: high for compaction, helper ordering, returned count, output
  ownership, and the refinement gate.
- Unresolved: whether naturally produced profile-9 records can exercise the
  synthetic recognition edge arithmetic difference described in
  `FUN_180077e20`. Its absence from retained natural affine outputs in the
  audited corpus does not establish unreachability.

The wrapper has no null, index, or local-capacity validation. A nonnegative first
index with a negative/out-of-range second index dereferences malformed record
storage. More than 42 compacted pairs overrun fixed locals before downstream
bounds handling. During post-orientation mirroring, compacted index 42 invokes
the non-returning bounds handler at `0x18005bc4d`. Primary/alternate type-12 calls
scan 42 sentinel-terminated slots and fallback scans 31; they compact at most
their respective capacities, use feature indices `0..49`, and point at records
bounded by their feature objects, so these
are malformed-only/native-crash surfaces.

Valid synthetic records expose the estimator's nonuniform third-edge order and
wrapped 5:6 products through this wrapper. With three pairs and neutral
orientations, the observed count/affine differences cannot be attributed to
orientation filtering or refinement. The detailed arithmetic and output
families belong to `FUN_180077e20`.

Canonical profile-9 robust estimation, orientation filtering, affine validity,
and least-squares refinement matched the DLL. Current `milan-dev` still uses one
uniform triangle-distance helper and signed C ratio products; it does not yet
contain the pending one-file production patch that defines the DLL's nonuniform
edge order and wrapped products.

## FUN_18005a3c0 Dependency Unit 1

The untouched-output behavior for selected-pair counts 0, 1, and 2 is valid
callee behavior but unsafe caller input. In the alternate call at
`FUN_18005a3c0:0x18005a6d2`, the supplied local has no prior definition, so the
caller must treat the returned zero count as `alternate_valid = false` and skip
all affine consumers. This helper does not own candidate rejection or status.
Primary positive counts at alternate entry remain independently valid because
they can occur only after this helper fitted an affine from at least three pairs;
orientation filtering may then reduce the returned count to one or two.

## Dependency Unit 5 Complete Failure Handoff

The returned count is not model status. Exact families are:

| Compacted pairs / estimator result | Returned count | Supplied affine | Private mask/residual |
|---|---:|---|---|
| `0..2` | 0 | untouched identity/zero/poison | estimator not called |
| `>=3`, no accepted model | 0 | six zeros | mask zero, residual `0x190000` |
| accepted model, orientation retains `0..2` | `0..2` | accepted affine unchanged | post-filter mask, robust residual |
| accepted model, retained `>3`, residual `<=0x4000` | retained | accepted affine | refinement skipped |
| accepted model, retained `>3`, residual `>0x4000`, refinement fails | retained | robust affine unchanged | count/mask unchanged |
| accepted refinement | retained | refined affine | wrapper count still the pre-refinement retained count |

`FUN_180057140` owns only mask/pair removal and count reduction; it never
invalidates or replaces the affine. `FUN_180076af0` is transactional at the
six-word output: singular normal equations, non-improving residual, or fewer
refined inliers leave the robust affine byte-for-byte unchanged. Neither helper
emits an error. Native must carry estimator model validity independently of the
post-orientation count so a valid model filtered to zero is not conflated with
no model.

Type 12 excludes the feature-index-`>=50` table arm and malformed capacity panic
`0x18005bc4d`; all semantic failure families above remain relevant. Native code
must separately represent whether estimation was attempted and whether the
robust estimator accepted a model, rather than deriving either state from the
post-orientation count or affine contents.
