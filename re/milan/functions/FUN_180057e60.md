# FUN_180057e60

## Binary And Body

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180057e60` / `0x180057e60-0x1800586d4`.
- Inferred signature: `void f(metrics, config, flag_a, flag_b)`.
- Caller: `FUN_180053680`; no callees.

## Behavior

This is a final type-12 veto in the `FUN_180053680` branch. It derives adjusted
count/detail combinations, applies count-indexed rejection matrices, and clears
both flags for weak geometry/coverage combinations. A final compound boundary
test can also clear both outputs after the switch.

## Evidence And Confidence

- Entry immediately returns when the controlling flag is zero; every rejection writes zero to both outputs.
- The persistent profile-9/type-12 trace records 18 entries from 39
  `FUN_180053680` calls and preserves the resulting flag pair for each call.
- Confidence is high for veto semantics, low for a compact equivalent implementation.
- The native profile-9/type-12 threshold translation now reproduces all 18
  frozen calls and resulting flag pairs exactly.

## Identify Epoch 26 Bucket-9 Clear

When the corrected first-veto path clears only `flag60`,
`FUN_180053680:0x180053e79..0x180053e8b` calls this helper with
`flag60/flag64 = 0/1`. For feature 25 the clamped primary bucket is 9. The DLL
bucket-9 survivor matrix at `0x180058047..0x1800580a5` includes these three
ordered conjunction terms:

```text
(filtered > 22 || detail > 199 || combined > 390 || geometry > 22) &&
(filtered > 15 || detail > 203 || combined > 398 || geometry > 15) &&
(filtered > 16 || overlap > 227 || detail > 216 || coverage > 130)
```

The witness `filtered/detail/combined/geometry/overlap/coverage` tuple is
`9/209/422/14/227/105`. It satisfies the first two terms but fails every arm of
the third term; notably, the overlap comparison is strict `> 227`, so overlap
exactly 227 does not survive. The `JLE 0x1800583f5` edge at `0x1800580a5`
reaches `0x1800583f5..0x1800583f8`, which writes zero through both output
pointers. This is the final owner of the DLL `flag60/flag64 = 0/0` tuple.

This downstream clear is reachable only because `FUN_18005c3b0` first clears
`flag60`. Preserving both flags at first veto triggers the dispatcher's early
return and bypasses this helper entirely.
