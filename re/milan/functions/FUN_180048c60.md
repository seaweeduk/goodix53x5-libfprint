# FUN_180048c60

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180048c60..0x180048f7c`; caller: `FUN_18004c5a0`.

## Recovered Behavior

It computes the three signed gradient and six Hessian terms for x/y/scale
refinement from unsigned-16 scale planes `s-1..s+2`. The center response is
`int16(2 * (G_(s+1)-G_s))`: the subtract/double and store wrap in a 16-bit lane
at `0x180048cae..0x180048ccd`. The x/y/s gradients and the three diagonal
Hessian terms are formed in signed 32-bit lanes and accepted only when
`abs(value) < 0x8000`; the three mixed terms use the stricter
`abs(value) < 0x2000`. Equality at either bound rejects. The writes and checks
occupy `0x180048cd1..0x180048f57` and return one only after all nine terms have
passed.

The exact term order is gradient `[x,y,s]` and Hessian
`[xx,yy,ss,xy,ys,xs]`. The center is written unconditionally first. Every
subsequent term is range-checked before its own store. Failure leaves all
earlier stored terms intact and all later destinations untouched. Current
owner: `feature/extrema.c:feature_build_refinement_derivatives`.

Let `D_s(x,y) = G_(s+1)(x,y)-G_s(x,y)` without narrowing, and let `C` be the
signed-16 doubled center. Gradient terms are twice the opposing x, y, and
scale differences. Spatial diagonal terms are four times
`D_s(p-axis) + D_s(p+axis) - C`; the scale diagonal is four times
`D_(s-1)(p) + D_(s+1)(p) - C`. Each mixed term is the four-corner difference
across its two axes, with positive same-sign corners and negative opposite-sign
corners, without an additional multiplier. Hessian storage orders y/scale
before x/scale. The production caller supplies scale `1..3` and one-pixel-
interior x/y, including after any published refinement move.

See [refinement](FUN_18004c5a0.md) for failure mutation and offset solving.
