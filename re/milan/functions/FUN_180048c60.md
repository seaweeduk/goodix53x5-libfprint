# FUN_180048c60

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180048c60..0x180048f7c`; caller: `FUN_18004c5a0`.

## Recovered Behavior

It computes the three signed gradient and six Hessian terms for x/y/scale
refinement from unsigned-16 scale planes `s-1..s+2`. The center response is
`int16(2 * (G_(s+1)-G_s))`: the subtract/double and store wrap in a 16-bit lane
at `0x180048cae..0x180048ccd`. The x/y/s gradients and the three diagonal
Hessian terms are formed in signed 32-bit lanes and accepted only when
`abs(value) < 0x8000`; the three mixed terms use the stricter
`abs(value) < 0x2000`. Equality at either bound rejects. The writes and checks
occupy `0x180048cd1..0x180048f57` and return one only after all nine terms have
passed.

The exact term order is gradient `[x,y,s]` and Hessian
`[xx,yy,ss,xy,ys,xs]`. Current source reproduces the formulas, strict ranges,
and wrapped center at
`drivers/goodix53x5/milan/feature/extrema.c:75..151`. The source's narrowing
cast for the center relies on the target compiler's two's-complement behavior;
all checked terms have already been proven representable before narrowing.
The production caller supplies a scale in `1..3` and one-pixel-interior x/y,
so all four scale-plane and spatial-neighbor reads are in bounds. A direct
out-of-range candidate is an unsafe malformed input in both implementations.

## Confidence And Unresolved

- Confidence: high for derivative formulas, term order, widths, strict range
  checks, and profile-9/type-12 reachability.
- Unresolved: none on this path.
