# FUN_180040980

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180040980`, `0x180040980..0x180040a6c`.
- Role: compute the encoded size of one live feature object.

## Call Graph

- Caller: `FUN_18003f510`.
- Callees: none.

## Recovered Behavior

The function sums the three optional matrix sizes, packed inline mask size,
type-dependent record block, fixed anti-fake/scalar storage, and optional tag
`c7`. Production type 12 is in the bitset selecting 32-byte records; the generic
fallback uses 28-byte records, and type 10 has one additional optional block.

With the required profile-9 owners populated, the exact type-12 sum is:

```text
feature 95 header                         5
three 52x44 bitmap elements       3 * 316
26x22 inline ce element                  77
b3 record-count element                   5
bf present framing + 0x1abc payload    6850
b4 record-block header                    5
records                         32 * count
mandatory b5..be,c0 scalars              55
optional c7                    5 when nonzero
```

This is `7945 + 32 * record_count + (c7 != 0 ? 5 : 0)`, exactly matching
`FUN_180040a70`'s returned end pointer for valid completed type-12 features.

The relevant optional-field boundary is exact at
`0x180040a59..0x180040a63`: live feature dword `+0x150 != 0` adds five bytes.
This matches `FUN_180040a70`, which emits one `c7` tag plus one dword only in
that case. The ordinary scalar run is already part of the fixed size, so `bb=0`
does not remove tag `bb` or change feature size.

Direct controls preserve size when `bb` changes to zero and add exactly five
bytes when `c7` changes from zero to nonzero. Enrollment-2 sequence 34 is the
discriminating replacement: native imported probe `c7=256` and produced target
feature-element size 11182, while the DLL preserved target `c7=0` and produced
11177. Clearing only native `c7` removes the five-byte size difference; one
independent `ba` lifecycle byte remains until the replacement helper is modeled.

## Confidence And Unresolved

- Confidence: high for the `c7` size predicate and separation from mandatory
  scalar `bb`.
- Integer-overflow behavior of the full generic size formula is not needed by
  the bounded profile-9 objects and was not independently controlled.

The native `bb` normalization milestone does not change this size function or
replacement ownership. Mandatory `bb` remains size-neutral when zero. The exact
next task remains preserving target-owned optional `c7` during replacement;
normalization parity does not close that independent five-byte boundary.
