# FUN_180042c30

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180042c30`, `0x180042c30..0x180042ec8`.
- Role: top-level enrollment-stage update and relation-graph orchestration.

## Call Graph

- Direct caller: exported `enrolAddImage` at `0x180001450`.
- Direct callees: `FUN_180041ac0`, `FUN_1800422e0`, `FUN_180042610`, `FUN_180042840`, `FUN_180047550`, `FUN_18007a040`, and `__security_check_cookie`.

## Recovered Behavior

The function validates enrollment state and feature status, then takes one of two paths. The first stage calls `FUN_180042610`; later stages deep-copy the incoming feature with `FUN_1800422e0`, assign its lifecycle ordinal, derive direct pair relations through `FUN_180041ac0`, and route those relations through `FUN_180042840`. It initializes the enrollment progress output, maintains the feature-index array, and calls finalization helpers when the configured stage count is reached.

Evidence addresses include the first-stage branch at `0x180042d8b`, copy/setup around `0x180042dc0`, direct relation construction at `0x180042e23`, graph processing at `0x180042e82`, and completion calls before `0x180042ec8`.

After each accepted enrollment stage it writes identity indices to order slots
`[0, feature_count)` at template `+0x87e8` and `UINT32_MAX` to the remaining
configured-capacity slots. Study append later changes this gallery order under a
different finalization contract.

The independent order audit parses the immutable batch pre-transition as exact
identity `[0,1,2,3,4,5,6,7]`. This separates enrollment initialization from the
later study finalizer and excludes filesystem, allocator, or serializer ordering.

## Confidence And Unresolved

- Confidence: high for control flow and enrollment role; exact for the called graph stages used by the native parity implementation.
- Confidence is high that enrollment supplies the identity-prefix input to the
  production study sort; audit report SHA-256 is
  `06fda76c874dd0aa554835c5f69ba9d9289f461aa0bc046901f8be430ba7c435`.
- Unresolved: semantic names of the status codes and every field in the enrollment context.
- Native combiner closure: when an established enrollment graph routes a new
  relation through a connected non-reference feature, the temporary relation
  context must carry both `graph_reference_index` and `graph_established=1`.
  Omitting the latter caused enrollment 2 to fail at stage 6 after the packed
  metadata semantic refactor; preprocessing and extraction were exact.
- Validation: `./re/milan/scripts/run-native-safe-zero-enrollment-tests.sh verify`
  reproduces both frozen eight-stage v2 lifecycles. The non-campaign graph-state
  control rejects unset/invalid state and admits the established route.
