# FUN_180042840

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180042840`, `0x180042840..0x180042ab4`.
- Role: integrate direct pair relations into the enrollment relation graph.

## Call Graph

- Direct caller: `FUN_180042c30`.
- Direct callees: `FUN_180041f60`, `FUN_180042180`, `FUN_180042ac0`, `FUN_180043110`, `FUN_180046780`, `FUN_180071370`, and `__security_check_cookie`.

## Recovered Behavior

It partitions candidate prior features by their active marker at feature `+0x110`. If active candidates exist, `FUN_180041f60` routes the strongest relation through the current runtime graph anchor. If none exist and no anchor has yet been selected, `FUN_180042180` may establish one. Inactive candidates are either scored without graph mutation by `FUN_180042ac0` or bridged through the established anchor by `FUN_180043110`. It then computes enrollment coverage/progress metadata.

The runtime graph-anchor index is read and updated at context `+0x87d8`;
graph-established state is at `+0x87e4`. These are packed as `f2/f5`. The fixed
header word `fb` is not a second persisted reference; it is the independent
queue transaction counter at live `+0x8e04`. The old phrase "persisted
reference-feature field" for `fb` is superseded.

## Confidence And Unresolved

- Confidence: high for graph partitioning, anchor selection, and bridge routing.
- Unresolved: full semantics of the coverage value packed into the high byte of `param_3[7]`.

## Native Initial-Graph Correction

The action-2 hardware replay exercises the `no active candidates && graph unset`
branch with a strongest prior other than feature 0. Ghidra at
`0x1800428d5..0x1800428f0` confirms that this branch passes the complete direct
candidate list to `FUN_180042180`; it does not filter for a preselected root.
Only established graphs route through `FUN_180041f60`.

Native now follows that split. With the exact action-2 single-feature inputs,
all stage relation arrays and graph reference/established fields match Windows:
`0/0/0/0/0/1/1/2` relations, first anchor 1 at stage 6, and the second routed
edge at stage 8. The action-7 control remains exact at every stage.

## Same-Call First-Anchor Bridge

The canonical safe-zero enrollment-2 stage-3 mismatch exposed a distinct
fallthrough omitted by the former native combiner. After `FUN_180042180`
returns at `0x18004291c`, this function rebuilds the inactive candidate list
from the complete admitted list. A successful anchor has already changed both
the new feature and selected prior to active and set `+0x87e4=1`, so remaining
inactive candidates dispatch to `FUN_180043110` at
`0x180042966..0x180042982` in the same call. Failure to establish the anchor,
or an inactive new feature, instead dispatches to non-mutating
`FUN_180042ac0` at `0x180042966..0x180042992`.

For the exact stage-3 input, direct relations to priors 0 and 1 are both
admitted with counts 22 and 27. `FUN_180042180` selects prior 1 as reference,
then the same-call bridge admits prior 0 and writes the zero-leading reference
edge. Windows therefore serializes ordered relations
`[1,0,229,-111,23515,111,229,-10968]` and
`[3,27,255,53,-12879,-56,251,5978]`; the old native path serialized only the
second. Current native replay is exact for every defined stage field in both
safe-zero lifecycles and the independent action-2, two action-7, and action-9
sequences.
