# FUN_180058700

## Binary And Body

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180058700` / `0x180058700-0x180058ed1`.
- Inferred signature: `int32_t f(probe, metrics77, subtype, transform6, state3, status_counter, inout_flag60)`.
- Caller: `FUN_180055a40`; callees: `FUN_1800624c0`, `FUN_1800628f0`, and `FUN_180071370`.

## Behavior

This is a late per-feature rejection/policy stage. It classifies the transform
against mode and threshold classes, combines those classes with count,
detail/coverage, context quality, and packed mode scalars, and can clear the
caller-owned flag. A final transformed-overlap-area calculation can also reject
the flag. Returning one increments the supplied counter; ordinary accepted
status is zero.

## Complete Reachable Profile-9/Type-12 Contract

Recovered and directly controlled in MATCH-PATH-COVERAGE unit 16 on 2026-07-26.
This section supersedes the historical statements below that only mode 4 and the
area path were known. It is complete for the production profile-9/subtype-12
caller contract; it does not claim the subtype-11 branch or arbitrary invalid
direct-call states.

### Exact Arguments

The call at `FUN_180055a40:0x180056313..0x180056360` supplies:

```text
arg1 RCX: live probe object
arg2 RDX: current 77-word candidate metrics
arg3 R8D: enrolled-template subtype, exactly 12
arg4 R9: current six-word Q8 affine transform
arg5 [rsp+0x20]: state[3]
arg6 [rsp+0x28]: address of the match-level cumulative status counter
arg7 [rsp+0x30]: address of caller flag60 at caller rsp+0x60
```

The callee reads probe columns/rows at `+0/+4`, record/context scalar at `+0xf0`,
coverage at `+0x11c`, and quality at `+0x120`. Candidate words used here are
`m0,m1,m5,m8,m9,m10`. `abs(m0-m1)` is computed before any policy branch.
The naming is DLL-semantic: native preprocessing variables reach these fixed
coverage/quality positions in swapped source-name order, as previously proven.

Caller state for the current feature is:

```text
state0 = accumulated packed-c7 high class
state1 = packed probe low/auxiliary class
state2 = min(5, prior accumulated high class + current feature high class)
```

When the current feature high class is above three, the next feature's
accumulated class becomes `max(prior_high, current_feature_high)`, not `state2`.
Valid high-class decoding therefore produces state0 `0..5`; state2 is `0..5`.
Valid low classes are `0,2,2,3`, so state1 can be
`0,2,3`, never `1`. This makes the margin's auxiliary-one arm statically
unreachable even though the helper contains it. The subtype argument is always
12. Negative state, state over 5, and the subtype-11 special branch are likewise
unreachable from this caller.

### Transform Predicates

`close_mode(transform, state0, 12)` uses inclusive absolute identity deltas.
Subtype-12 mode vectors are:

```text
mode 1/2/3: [25,21,1792,21,25,1792]
mode 4:     [38,38,3072,38,38,3072]
mode 5:     [45,40,3840,40,45,3840]
```

The four inclusive `FUN_1800628f0` classes are:

```text
class 0: [4,6,256,6,4,256]
class 1: [10,10,512,10,10,512]
class 2: [20,20,1280,20,20,1280]
class 3: [30,30,2560,30,30,2560]
```

All vectors compare `abs(transform-[256,0,0,0,256,0]) <= limit` component by
component. Direct controls cover both signs at every limit and one unit outside.

### Ordered Pre-Area Policy

The following order is exact. `status` starts at zero; a later predicate may run
after an earlier producer, but the first producer names the semantic branch.
Every numeric comparison in these tables is strict as written.

1. If the transform is close in current `state0` mode:

| State | Status predicate |
|---|---|
| `state0>=4` | unconditional |
| `state0 in {2,3}` | `m0>6 && m10<66 && coverage<85 && ((m5<201 && m8<215) || (m5<215 && m8<210))` |
| `state0==1` | `m0>6 && m10<66 && coverage<85 && ((m5<210 && m8<200) || (m5<205 && m8<210))` |

2. For `state0>1`, independently in this order:

```text
mode5-close && m5<201 && m8<190 -> clear flag60
mode4-close && m5<210 && m8<207 -> status=1
coverage<61 && m0>6 && class N close -> broad class table
```

The broad class metric maxima are class 0 `<240/<237`, class 1 `<235/<236`,
class 2 `<226/<228`, and class 3 `<216/<218` for `m5/m8`.

3. For `state0>0`, `coverage<61`, and subtype not 11, the tighter class table is
class 0 `<234/<233`, class 1 `<230/<233`, class 2 `<216/<221`, and class 3
`<206/<208`, also requiring `m0>6`.

4. For every reachable nonnegative state, apply these universal predicates in
order:

```text
class3 && coverage<76 && m5<205 && m8<200 -> clear flag60

class0 status:
  coverage<72 && (
    (m5<211 && m8<215) ||
    (coverage<31 && m5<218 && m8<220) ||
    (coverage<16 && m5<223 && m8<220))

class1 status, first true tuple:
  coverage<76 && m5<205 && m8<202
  coverage<79 && m5<202 && m8<201
  coverage<78 && m5<198 && m8<196  [dominated by preceding tuple]
  coverage<83 && m5<195 && m8<192
  coverage<32 && m5<218 && m8<220
  coverage<37 && m5<201 && m8<206

class2 status, first true tuple:
  coverage<65 && m5<212 && m8<203
  coverage<84 && m5<194 && m8<189
  coverage<82 && m5<202 && m8<193
  coverage<33 && m5<212 && m8<215
  coverage<78 && m5<198 && m8<197

class3 status, immediate nonzero return path:
  coverage<76 && m5<194 && m8<195
  coverage<17 && m5<199 && m8<199
```

The class1 third tuple is code-reachable as a comparison but cannot be the first
producer because it is a strict subset of the preceding tuple. It is excluded
from the reachable semantic-branch count by static domination, not by lack of a
natural sample.

5. If no status is set, one final profile/type-independent producer runs before
area eligibility:

```text
state0>=4 && probe_f0<50 && quality>80 && m9<50 -> status=1
```

The following subtype-11 producer at `0x180058c65..0x180058c9e` is unreachable:
the sole caller passes subtype 12. It is not silently omitted from the whole-
function graph and is outside the bounded implementation contract.

### Area Entry And Returns

With status still zero, area evaluation is admitted by the first true arm:

```text
state0>=4
state2>=4
state0>=2 && state1>=1
state0>=2 && m5<230 && m8<215 && coverage<=60
state0>=2 && m5<215 && m8<215 && coverage<87
state0>=1 && m5<208 && m8<214 && coverage<85
state0>=0 && m5<=195 && m8<=195 && coverage<=85
state0>=0 && m5<=200 && m8<=203 && coverage<=50
state0>=0 && m5<=205 && m8<=208 && coverage<=40
abs(m0-m1)<7 && coverage<46 && quality<45 && m0>5
```

Failure of the final arm returns zero in short-circuit order at the first of:
`abs(m0-m1)>=7`, `coverage>=46`, `quality>=45`, or `m0<=5`. These are four
distinct reachable zero-return branches. They preserve flag60 and the status
counter unless an earlier mode5/class3 clear already occurred.

For admitted area, margin is:

```text
state0 0..1                                      -> 10
state0 2..3 and m5<197,m8<195                   -> 20
state0 2..3 and state1>1                        -> 15
state0 2..3 and state1==1                       -> 13 [caller-unreachable]
state0 2..3 and state1==0                       -> 10
state0 4..5                                      -> 20
state0>5                                         -> 10 [caller-unreachable]

if m0<9 && quality<65: margin=max(margin,15)
margin += max(quality-trunc(probe_f0*100/120),0)/10
```

All division truncates toward zero. `FUN_180071370` receives
`rows,columns,source_rows,source_columns = 88,104,88,104`. Let `A` be its area
and `T=9152`:

```text
A*100 > (100-margin)*T -> status=1
A*100 > (95-margin)*T  -> clear flag60
```

Both are strict. At margin 10, status is zero/one at areas `8236/8237`; flag60
is preserved/cleared at `7779/7780`. A status-area hit necessarily also clears
flag60, while the interval between the two thresholds is a reachable clear-only,
status-zero path.

### Side Effects And Caller Continuation

The function never reads or writes flag64. It only clears flag60; it never sets
or normalizes it. Direct controls prove incoming flag60 values `0`, `1`, and the
dispatcher-bypass value `2`: zero paths preserve the value unless a clear
predicate fires. Every status-one return increments the caller-owned cumulative
counter exactly once, including when its initial value is nonzero. Status zero
does not increment it.

At `FUN_180055a40:0x180056365..0x180056367`, status one skips
`FUN_1800553f0`, Q8 contribution, winner/evidence updates, direct-positive `be`,
fallback staging, and every remaining operation for that feature. The existing
flag64 stack value is therefore not consumed. Status zero continues to
`FUN_1800553f0` with flag64 unchanged and the possibly cleared flag60. The two
caller dispatcher bypasses (`flag60==2 && accumulated state0<=1`) still call
this helper and follow the same status/continuation rule.

### Branch Count And Direct Evidence

The bounded profile-9/type-12 contract has 55 reachable semantic branches:

| Family | Reachable branches |
|---|---:|
| Status producers, including area | 31 |
| Flag60 clear producers | 3 |
| Area-entry arms | 10 |
| Ordered early zero returns | 4 |
| Reachable margin/floor/quality-delta arms | 7 |
| Total | 55 |

The direct gate covers `55/55`. It executes 185 complete policy calls and 219
direct transform-helper controls against the pinned DLL, `404/404` total. The
policy controls include every reachable producer/entry/return/margin arm,
strict scalar equality boundaries, inclusive area-entry boundaries, mode/class
limits on both signs, area cutoffs, cumulative counters, and flag60 values.
Predictions were generated from the assembly contract before DLL execution; no
sequence rows or thresholds were fitted to results.

Artifacts:

`<evidence-root>/derived/milan-native-capture-20260723/match-unit16-fun-180058700-contract-v1/`

- Tree SHA-256: `482461878b6cc519ac466c23edb895e8baba919d7d909427b22687bb402ed426`.
- Controls SHA-256: `c1f115a8f5a97f445a557a3113f587d1b31762896f8bfee8f0e4a4ed00de9877`.
- DLL results SHA-256: `8081bf1f903a01c434cb7f9f6bc80e09e8381b3276cdd7f60d925684965c55a8`.
- Prediction/results projection SHA-256:
  `5e51c22926c1fd827a989568b55d79fd7f68670a59e72450ce1dd1d95e4756ae`.

The prior natural evidence attached to the immutable 798-row authority and the
evolved chronology directly froze only status-zero, mode4-status, transformed-
area status, area-entry, and geometry cases (26 aligned calls in the widest
role/geometry projection). It did not provide branch-resolved examples for the
current-mode families, broad/tighter classes, universal class tables,
high-state producer, mode5/class3 clear-only paths, all ten independently
selected area-entry arms, all four ordered zero returns, or all valid margin
states. Those are direct-control-only in this unit. This is an evidence-scope
statement: the immutable `798/798` output gate is retained, but its stored rows
are not retroactively labeled as branch traces.

### Native Implementation Closure

`goodix_milan_matcher_policy_apply_status()` now implements the complete ordered
caller-reachable contract above. The prior mode-4-only special case was removed;
shared subtype-12 mode/class predicates feed one direct pre-area state machine,
which then retains the existing ordered area-entry ladder, reachable margin
arithmetic, `104x88` transformed area, strict area decisions, flag clearing,
and common status-counter sink.

Subtype 11, negative and over-5 states, auxiliary class 1, and the dominated
class-1 tuple remain caller-excluded and are identified in source comments.
No row identities, empirical thresholds, flag64 mutation, or downstream matcher
compensation were introduced.

`run-native-fun-180058700-contract-tests.sh` verifies the immutable unit-16 tree
and replays the same 404 inputs through native code. Native output is byte-exact
to frozen DLL output at SHA-256
`8081bf1f903a01c434cb7f9f6bc80e09e8381b3276cdd7f60d925684965c55a8`,
covering `55/55` semantic branches and area boundaries `8236/8237` and
`7779/7780`. Immutable matching remains score/decision/candidate-index exact
`798/798`, accepted scores `318/318`, and impostor accepts zero. The evolved
fixed-gallery gate is now direct-mask exact `798/798`, closing its former e2/86
residual. Full chronology was not rerun.

## Validation

- A prior focused native translation reproduced the captured return status for
  all 60 final-policy calls but reproduced final flags for only `17/60`. Its
  temporary log was lost on power failure; the persistent diagnostic below
  supersedes it as evidence.
- Therefore status behavior is evidenced, but this helper alone is insufficient to reproduce the caller's final tuple.
- The persistent dispatch diagnostic correlates 58 actual `FUN_180053220`
  invocations to this caller stage. `FUN_180058700` returns status `0` for all
  `58/58`; its status counter and `rsp+0x60` output are frozen per call. Two
  additional caller final-policy calls bypass `FUN_180053220` and are kept out
  of the dispatch table rather than attached to a stale record.
- Confidence is high for return/counter behavior and medium for argument roles.
- Unresolved: complete semantic names for context fields, not their production
  positions or observed interaction with `FUN_180053220`.
- The narrow native policy preserves `rsp+0x60`, status zero, and counter zero
  for all 60 frozen final-policy calls. General behavior outside this diagnostic
  remains deliberately unimplemented.

## Row 79/2 Exclusion

The focused `79/2` late-policy diagnostic proves this helper is not the first
divergence. The exact caller gate at
`FUN_180055a40:0x1800562b3..0x180056313` clears both flags before this call.
The official post-call checkpoint records status `0`, status counter `0`, and
`rsp+0x60 = 0`; the later final tuple remains `[0,0]`. Native's frozen-cohort
no-op is incomplete because it omits the preceding caller gate, not because
this helper needs a row-specific rejection. Do not add the `79/2` geometry
ladder here.

## Accepted-Score Late Residuals

The compact accepted-score admission reference adds two exact-input cases not
covered by the earlier 58-call dispatcher cohort. On `3/1` feature 5 and `31/1`
feature 3, official/native retained count and metric 4/5 are exactly
`23,232,212` and `22,230,202`; dispatcher flag64 is one on both sides. The
official helper returns status `1`, increments its status counter to `1`, clears
or preserves flag60 as zero, and does not reach a final contribution tuple.
Native's frozen-cohort no-op retains a contributor. Full 77-word inputs,
transforms, quality/coverage, dispatcher flags, and recognition mode are exact
on both rows. These classify as same-input helper-status work after the 87-row
caller-state correction, not as dispatcher, contribution-predicate, Q8, or
  candidate changes. Ghidra confirms that status one converges on the helper's
  counter increment and return-one sink.

## Exact-Input Status Resolution

The two calls converge on the same sink but do not take the same predicate.
The exact profile-9/type-12 coding contract is:

```text
mode4_status =
  state[0] > 1 &&
  all(abs(transform[i] - [256,0,0,0,256,0][i]) <=
      [38,38,3072,38,38,3072][i]) &&
  metrics[5] < 210 && metrics[8] < 207

if (!prior_status && survived_earlier_zero-return_guards) {
  margin = overlap_margin(state, metrics, context_f0, quality)
  area = transformed_in_bounds_area(108, 88, transform)
  area_status = area * 100 > (100 - margin) * 108 * 88
  clear_flag60 = area * 100 > (95 - margin) * 108 * 88
}

status = mode4_status || area_status
if (status)
  ++*status_counter
```

All comparisons shown are exact. `FUN_1800624c0` accepts equality at each
mode-4 transform limit; the metric and area comparisons are strict. For the
area path relevant here, the margin calculation is:

```text
state_mode < 2                         -> base 10
state_mode in [2,3] and m5<197,m8<195 -> base 20
state_mode in [2,3], state_aux > 1    -> base 15
state_mode in [2,3], state_aux == 1   -> base 13
state_mode in [2,3], state_aux == 0   -> base 10
state_mode in [4,5]                   -> base 20
otherwise                             -> base 10

if m0 < 9 && quality < 65:
  base = max(base, 15)
margin = base + max(quality - trunc(context_f0 * 100 / 120), 0) / 10
```

The final division is signed integer division. Coverage at context `+0x11c`
participates in earlier policy families but not this margin; quality is context
`+0x120`. `context_f0` is context `+0xf0`. The three caller state words are
`iStack_16c48`, `iStack_16e80`, and the current outer scalar.

Exact target paths:

- `31/1` feature 3 has state `[2,0,2]`, transform
  `[255,-26,-2863,29,247,-1857]`, and absolute identity deltas
  `[1,26,2863,29,9,1857]`. Every delta is within the inclusive mode-4 limits,
  `metrics[5]=202 < 210`, and `metrics[8]=206 < 207`. It sets the status latch
  at `0x1800589d1..0x1800589fd`. Its area `7461` is below the margin-10 area
  boundary and is not consulted because the earlier status is already one.
- `3/1` feature 5 also has state `[2,0,2]` and a mode-4-close transform, but
  `metrics[5]=212` fails the strict `<210` term. No earlier class term sets
  status. Its exact high-signal early-return guard does not enter because
  `metrics[5] > 214 || metrics[8] > 214 || coverage > 86` is false for
  `212/212/81`. The area margin is 10: base 10, `context_f0=115`, quality 99,
  `trunc(11500/120)=95`, and `max(99-95,0)/10=0`. `FUN_180071370` returns
  area `8908`; `890800 > 90*9504 = 855360`, so
  `0x180058e63..0x180058e7a` sets status one. It also clears flag60 because
  `890800 > 85*9504 = 807840`.

The four nearest same-row status-zero controls use the same state `[2,0,2]`:

| Row/feature | Area | Mode-4 term | Area term | Why zero |
|---|---:|---|---|---|
| `3/1` f3 | 7754 | false | `775400 <= 855360` | transform outside mode-4 limits |
| `3/1` f6 | 3064 | false | `306400 <= 855360` | transform outside mode-4 limits |
| `31/1` f2 | 858 | false | `85800 <= 855360` | transform outside mode-4 limits |
| `31/1` f5 | 7793 | false | `779300 <= 855360` | `abs(t1)=44>38`, `abs(t2)=4832>3072` |

At margin 10 the strict integer boundaries are status zero at area `8553` and
status one at `8554`; flag60 is preserved at `8078` and cleared at `8079`.
Mode-4 metric boundaries are pass at `metrics[5]=209` / `metrics[8]=206` and
fail at `210` / `207`.

Status one reaches `0x180058e9e`, increments the caller-owned counter, and
returns one. On `3/1`, flag60 changes `1 -> 0`; on `31/1` it remains zero. The
caller branch at `0x180056365..0x180056367` skips `FUN_1800553f0`, score
contribution, and the rest of that feature. The counter remains one for later
status-zero features in both rows.

## Frozen Status Controls

The passive repeated official control root is:

`<evidence-root>/references/milan-native-capture-20260723/windows-fun-180058700-status-v1/`

- Tree SHA-256:
  `e1c3f6048a67dfc56527637cbe528abb94fe879367c48571f43c66393f04e3f0`.
- Primary/repeat six-call projection, byte-identical:
  `29f780a00e636eeb96bcf71e8a63742f45e7a2ac9c4dae5eb2b88552fb3317bc`.
- Boundaries/summary/provenance SHA-256:
  `bb7dea244da935979982994470f531614b47d5b4eb98537d28c9ff7dde328e6d`,
  `30d5b44befe7eb2f1fd8ac754c9cb58ee8839276652b29854fc5e537c7e58cd6`,
  and `3facc787040691ea45077038895f1447aba7649ca93866e5769441d9ddd6328c`.

Two independent sealed-prefix captures preserve frozen processed bytes and
after-match templates, use no forced branch or diagnostic feedback, call no
study, and repeat exactly. The verifier re-hashes residual-v1 at
`560c6eee347e1ba68bb88e9465e1e486cd01ff2e74088982c6d744e1cc36a8df`,
pins source/DLL/prefix provenance, checks 17 strict boundaries, and rejects five
artifact mutations. Confidence is high for these exact-input predicates,
arithmetic, and side effects. This does not claim a complete translation of all
other threshold families in the 203-block helper.

## Native Implementation

The two recovered nonzero-status paths are implemented in
`goodix53x5-match-policy.c` without translating any other helper family. The
profile-9/type-12 caller supplies state `[2,0,2]`, probe record count, quality,
coverage, and a match-level status counter. Status one increments the counter
and makes the caller skip the later clear-only policy stage, rescue-record
retention, Q8 contribution, evidence, and selection for that feature. The area
path independently clears flag60 at its strict lower area boundary.

`run-native-fun-180058700-status-tests.sh` first verifies the unchanged status-v1
tree above, then replays all six calls through the production helper. Return
status, cumulative counter, and flag60 are exact `6/6`; both target paths and all
four status-zero controls match. Native boundary tests cover all six inclusive
mode-4 transform limits, strict metric limits, strict status/flag area limits,
and the documented margin arithmetic (`17` controls total).

Full immutable replay improves exact score parity from `755/798` to `756/798`,
split `375/399` and `381/399`. Decisions and outer candidate indices remain
exact `798/798`; genuine accepts remain `162/200` and `156/200`, and impostor
accepts remain `0/398`. Both status calls now take the official path, but only
`31/1` becomes whole-row score-exact; `3/1` retains an independent accepted-Q8
component difference after feature 5 is correctly skipped. This is same-input
profile-9/type-12 parity only, not a complete `FUN_180058700` implementation or
runtime/hardware/study/persistence validation.

The later seven-row retained-count classification does not include `3/1`.
Feature 5's status rejection remains exact here; its remaining accepted-Q8
difference is a separate upstream component and must not be treated as an
eighth retained-count row or repaired by weakening this status predicate.

## Row 39 Caller-State Counterexample

The final accepted-score residual reference adds a non-input-exact feature-7
counterexample. Windows reaches this helper with packed probe policy mode zero,
state `[0,0,0]`, count `34`, metric 5/8 `203/0`, coverage `71`, and transform
`[257,18,996,-14,253,376]`. State mode zero disables the mode-4 term. The
helper's earlier state-zero guard then returns zero at the strict
`coverage > 45` check, before transformed-area status. Feature 7 contributes Q8
term `207` and relatches final score `41`.

Native instead passes the global constant `[2,0,2]`; its independently built
low metric 8 is `205`, so the close-transform mode-4 term rejects the feature.
The first actionable mismatch is the caller's state construction, not a new
`FUN_180058700` threshold. `FUN_180055a40` derives the real state by decoding
probe and enrolled-feature `c7` through `FUN_180070d90`.

This does not weaken the exact `[2,0,2]` paths for `3/1` and `31/1`. It proves
that the current native constant is valid only for those supplied calls and
must not be generalized to all profile-9/type-12 rows.

The caller now derives the three-word state through the documented packed `c7`
decoder. Focused replay preserves all six prior status calls and 17 helper
boundaries, while row `39/1` reaches this helper with `[0,0,0]` and returns zero.
The helper itself is unchanged; no new row-specific guard or threshold was
added. The separate `3/1` context-scalar role mismatch remains accepted-score
work.

## Row 3 Context-Scalar Resolution

The separate mismatch is now exact. The official feature-5 call supplies
`context_f0/coverage/quality = 115/81/99`, while current native preprocessing
reports quality/coverage `81/99` and the caller passes those variables directly
to this helper's quality/coverage positions. Native consequently consumes
coverage `99`, triggers the strict `coverage > 86` early zero-return guard, and
leaves flag60 one. Supplying the official semantic roles to the unchanged helper
consumes quality/coverage `99/81`, reaches the already-proven area path, and
returns status/counter/flag60 `1/1/0` instead of `0/0/1`.

The focused repeated native root is
`native-accepted-score-row3-context-v1`, tree SHA-256
`de945c60c7e8ebbb1b1cc84ebfde94c59d13c8ba4b8e894b14968f74e04458b9`.
It proves that skipping feature 5 leaves contributor/count/Q8 tuples
`0/9/55, 1/29/177, 2/10/61, 3/15/91, 6/5/30, 7/20/122`, relatches
`0,1,2,7`, prefix `536/6`, and exact score `34`. No helper threshold or
arithmetic change is justified. The generic correction belongs solely at the
caller's scalar-role bridge.

The caller-side bridge is now implemented as coverage followed by quality. The
helper remains unchanged. Focused role-order and production row-3 replay pass at
status/counter/flag60 `1/1/0` and score `34`; all six prior status calls and 17
helper boundaries remain exact.

The full gate preserves all `798/798` decisions and outer indices and has zero
impostor accepts, but the global caller correction exposes four accepted-score
differences that were previously exact coincidences. Accepted-score parity is
`314/318`, and the independent rejection-score cohort remains 33 rows. This does
not authorize changing this helper to compensate for those newly visible rows.

## Global Scalar-Role Provenance And Area-Entry Residual

The repeated non-forced official/native root is:

`<evidence-root>/references/milan-native-capture-20260723/windows-native-fun-180058700-role-provenance-v1/`

Its tree SHA-256 is
`c68d47a6a976fd6a70542b2730cd91a478ab57a34a113b11c93ef2972047865b`.
Two independent sealed-prefix official captures and two fresh native processes
repeat exactly. No branch is forced, diagnostics feed no data into matching,
study is not called, and capture instrumentation does not change production
matcher behavior. Verification passes with six rejected drift mutations:

```sh
/usr/bin/timeout --foreground 295s bash re/milan/scripts/run-accepted-score-status-role-provenance-tests.sh verify
```

Assembly fixes the scalar roles globally. `FUN_180055a40:0x180056313..0x180056360`
always passes the probe object as argument one. This helper then loads
`probe+0x120` at `0x180058771` and `probe+0x11c` at `0x18005877a`; there is no
path- or configuration-dependent role selection. Across `3/1`, `15/2`, `73/1`,
`97/1`, and `117/1`, all `26/26` aligned calls reproduce the same mapping:

```text
probe+0x11c = native preprocessing_quality = helper coverage role
probe+0x120 = native preprocessing_coverage = helper quality role
```

Therefore the caller's global scalar swap is correct and must be retained. The
root contains `26/28` official/native calls, `22/26` exact non-scalar inputs,
and `18/26` exact status/flag/counter tuples. The exact-input first differences
are:

| Row/feature | Official | Native | Cause |
|---|---|---|---|
| `15/2` f1 | status `0`, flag `1 -> 1` | status `0`, flag `1 -> 0` | missing pre-area return |
| `73/1` f5 | status/counter `0/0` | `1/1` | wrong hardcoded area geometry |
| `97/1` f0 | status `0`, flag `1 -> 1` | status `0`, flag `1 -> 0` | wrong hardcoded area geometry |
| `117/1` f5 | status/counter `0/0`, flag `1 -> 1` | `1/1`, flag `1 -> 0` | missing pre-area return |

The probe matrix is `104` columns by `88` rows. `FUN_180071370` therefore runs
over rows/columns `88/104`, not native's hardcoded `108/88`. Corrected areas for
the four calls are `8239`, `8102`, `7659`, and `8579`. With total area `9152`
and margin 10, status begins at `8237` and flag clearing at `7780`; this exactly
explains `73/1` and `97/1`.

For state-one `15/2` and `117/1`, the official block at
`0x180058ca0..0x180058d88` reaches the return-zero sink before area evaluation.
The block combines state class, metrics 5/8, coverage, and then strict checks on
primary/filtered-count difference, coverage, quality, and primary count. Native
currently preserves only one nested high-signal condition and consequently
evaluates area on both calls.

The next implementation contract is one bounded area-entry correction: use the
live `88x104` matrix geometry and reproduce the preceding assembly-proven
eligibility/return gate before invoking transformed-area status or flag policy.
Do not change scalar roles, area percentages, mode-4 predicates, contribution,
Q8 arithmetic, selection, rescue, rejection scoring, study, persistence, or
runtime behavior as compensation. This is exact same-input profile-9/type-12
evidence only, not hardware, empirical-accuracy, study, persistence, or Windows
service-lifecycle validation.

## Native Area-Entry Correction

The bounded area-entry contract is implemented generically. Native transformed
area now uses destination/source rows and columns `88/104`, matching the live
profile-9 matrix descriptor. The historical `108x88` areas and boundaries in
the earlier status-v1 sections remain frozen observations of the old analysis
projection and are superseded for native area behavior.

When no earlier status term is set, overlap is evaluated only if the exact
`0x180058ca0..0x180058d88` eligibility chain admits it:

```text
state0 >= 4 || state2 >= 4
|| (state0 >= 2 && state1 >= 1)
|| (state0 >= 2 && m5 < 230 && m8 < 215 && coverage <= 60)
|| (state0 >= 2 && m5 < 215 && m8 < 215 && coverage < 87)
|| (state0 >= 1 && m5 < 208 && m8 < 214 && coverage < 85)
|| (state0 >= 0 && m5 <= 195 && m8 <= 195 && coverage <= 85)
|| (state0 >= 0 && m5 <= 200 && m8 <= 203 && coverage <= 50)
|| (state0 >= 0 && m5 <= 205 && m8 <= 208 && coverage <= 40)
|| (abs(primary - filtered) < 7 && coverage < 46 &&
    quality < 45 && primary > 5)
```

Failure of that chain returns zero before area calculation and preserves
`flag60`. Area status, flag clearing, margin arithmetic, and the earlier mode-4
status remain in their original order and retain their strict comparisons.
Quality and coverage remain in the unconditional DLL roles proven by evidence
tree `c68d47a6a976fd6a70542b2730cd91a478ab57a34a113b11c93ef2972047865b`.

The implemented focused gate reproduces that tree, rejects six mutations, and
reports calls `26/26`, scalar roles `26/26`, and status/flag/counter tuples
`26/26`. Test-only controls reproduce exact areas `8602`, `8239`, `8102`,
`7659`, and `8579` for row `3/1` plus `15/2`, `73/1`, `97/1`, and `117/1`.
State-one rows `15/2` and `117/1` return before area with flags preserved;
geometry rows `73/1` and `97/1` and retained row `3/1` reproduce official side
effects. Full immutable replay reaches accepted-score parity `318/318`, full
score parity `765/798`, decision/index parity `798/798`, and impostor accepts
`0/398`. The remaining 33 scores are the independent rejection-score cohort.

## Independent Production Geometry Verification

Independent review confirms the claim specifically for the authoritative
production profile-9, subtype-12 path. Descriptor order and helper order differ
and must be stated explicitly: the live probe stores `columns=104` at `+0x00`
and `rows=88` at `+0x04`; this function calls `FUN_180071370` with
`rows,columns,source_rows,source_columns = 88,104,88,104`.

Assembly proof is direct:

- `0x180058e03` reloads the live probe argument.
- `0x180058e0f` loads `probe+4` (rows) into `EBX`; `0x180058e12` loads
  `probe+0` (columns) into `ESI`.
- `0x180058e37..0x180058e5e` sets `RCX=rows`, `RDX=columns`, `R8D=rows`, and
  `R9D=columns` before calling `FUN_180071370`.
- `0x180058e6d..0x180058e70` multiplies the threshold by `ESI` then `EBX`.
  The actual denominator is therefore `104 * 88 = 9152`, not `108 * 88` and
  not a constant from another sensor path.

Provenance back to the exports is also exact. `ppp_param_init(9)` selects table
row `[1,0,0,800,88,108,12]`; `enrolStartEx` packs it as `0x36160061`;
`FUN_180037c80` decodes subtype/rows/raw-columns `12/88/108` and applies the
subtype-12 `108 -> 104` match normalization. The live probe independently takes
the same subtype-12 normalization in `FUN_18004ae70 -> FUN_180070690`.
`FUN_18005edb0` dispatches subtype 12 to `FUN_180055a40`; its alternate family
handles types `9`, `10`, `17`, and `18`.

Artifact and passive-trace cross-checks:

- Original official-DLL enrollment templates 1 and 2, SHA-256
  `bb5b7eb26f4cfb368d5909e8c9b970a607b808c958c5e0eccec32141fd734f1e`
  and `425163904b034fad912715f46968428297f1845942c663ea6b254876b92ac547`,
  both decode to top-level subtype/rows/columns `12/88/104`. Their safe-zero
  derivatives preserve those fields at SHA-256
  `e43d91b7f7cbf41cb0d7e1af63e96addb9a6e6d8e1a3ab9c14c53775a7099b03`
  and `4ce1798591feb378e9d27fa2a5d30a137e0745cd7b80f33fd6ba40ce60b7943b`.
- Passive role-provenance tree
  `c68d47a6a976fd6a70542b2730cd91a478ab57a34a113b11c93ef2972047865b`
  spans rows `3/1`, `15/2`, `73/1`, `97/1`, and `117/1`, including both
  enrollment templates. Its 26 official calls repeat exactly with no forced
  branch, feedback, study, or matcher change.
- Recomputing the documented transforms over `88x104` yields areas `8602`,
  `8239`, `8102`, `7659`, and `8579`. At margin 10, strict status begins at
  `8237` and flag clearing at `7780`. The repeated implemented gate reproduces
  all `26/26` status/flag/counter tuples and rejects six drift mutations.
- The complete immutable official reference still verifies all 798 rows at tree
  `1b1e4db33812b9506a4d9e7ab238196b299df9a2fbde675c6f044215c6c94327`.

No new capture was necessary: the existing passive trace covers both enrollment
templates, while assembly fixes the live dimensions, argument order, and exact
denominator. Confidence is high. The current native constants
`MATCH_ROWS=88`, `MATCH_COLUMNS=104` are correct and production behavior should
remain unchanged.

## Caller High-State Retrospective

The helper contract above remains valid for every supplied `state[3]`, but the
native caller's transition after the call is not DLL-exact. Assembly at
`FUN_180055a40:0x180055de6..0x180055e0c` computes current `state2` as the capped
sum, then, only for feature high class above three, updates accumulated high to
the greater of prior and feature high. Native
`goodix_milan_matcher_late_context_derive()` stores the capped sum instead.

For prior high two and feature high four, both sides call this helper with
`[2,0,5]`; DLL next state0 is four while native next state0 is five. The
`404/404`, `55/55` direct gate proves this function for supplied states, not the
caller's cross-feature transition.

The caller transition now keeps the current capped state unchanged and stores
`max(prior_high, feature_high)` for later features. Controls cover `2 + 4` and
repeated `4 + 4`, both with current state2 five and later state0 four. The
callee remains byte-exact for all `404/404` direct controls and all `55/55`
reachable semantic branches.
