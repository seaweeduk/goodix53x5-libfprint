# FUN_180048980

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180048980..0x180048af9`; caller: `FUN_18004cc70`.

## Recovered Behavior

It normalizes and thresholds descriptor accumulators, then dispatches the packed
descriptor layout selected by the caller mode. It is downstream of candidate
count selection and is byte-exact in both sequence-72 direct controls.

For `count` samples, it accumulates unsigned 64-bit energy from each sample
reinterpreted as unsigned 32 bits. `FUN_180074980` returns floor `sqrt(energy)`;
threshold is `(root * 0x3333) >> 16`. Each normalized signed 16-bit sample is
`floor(sqrt(sample))` below threshold or `floor(sqrt(threshold))` otherwise,
using `FUN_180074940`. Type-12 accumulators stay nonnegative; unsigned
reinterpretation is nevertheless part of the generic contract.

Profile-9/type-12 fails the special selector
`((type - 9) & 0xfffffff6) == 0`. Mode zero therefore calls `FUN_180081550`:
it forms 32 Hadamard signs over each of four 32-sample blocks, combines those
four partials through the `4x4` sign matrix, and sets four 32-bit words only for
strictly positive sums. Intermediate block sums narrow to signed 16 bits, which
is modulo-equivalent to current source's final per-block narrowing. The helper
clears record bytes `0x10..0x27`; `FUN_180081be0` then sets the word at `+0x20`
where a sample at indices `1,5,...,125` is strictly above the lower median
`sorted[63]`.

Mode one consumes the 32 central samples from cells `(1,1)`, `(2,1)`, `(1,2)`,
and `(2,2)`. `FUN_1800818e0` sets the Hadamard word at record `+0x28` for
strictly positive sums and clears `+0x28..0x37`; `FUN_180081be0` sets `+0x2c`
for samples strictly above lower median `sorted[15]`. Current implementations
are `feature/materialization.c:398-530`.

## Confidence And Unresolved

- Confidence: high for normalization widths, floor roots, clipping, lower
  medians, strict comparisons, type-12 Hadamard layouts, clearing, and record
  offsets.
- Unresolved: packing variants unreachable from type 12.
