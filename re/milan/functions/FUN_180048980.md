# FUN_180048980

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180048980..0x180048af9`; caller: `FUN_18004cc70`.
- Descriptor-owned leaves are unsigned roots `FUN_180074940`
  (`0x180074940..0x18007497a`) and `FUN_180074980`
  (`0x180074980..0x1800749bf`), first encoding `FUN_180081550`
  (`0x180081550..0x1800818d9`), second encoding `FUN_1800818e0`
  (`0x1800818e0..0x1800819f3`), median-bit owner `FUN_180081be0`
  (`0x180081be0..0x180081d23`), and signed lower-median selection
  `FUN_180081a00` (`0x180081a00..0x180081bdf`).

## Recovered Behavior

It normalizes and thresholds descriptor accumulators, then dispatches the packed
descriptor layout selected by the caller mode. It is downstream of candidate
count selection and is byte-exact in both sequence-72 direct controls.

For positive `count`, native energy uses `ceil(count/4)` unrolled groups and
therefore reads four unsigned dwords per group; reached counts 128 and 32 are
exact multiples of four. Each unsigned dword is squared to 64 bits and energy
addition wraps modulo `2^64`. `FUN_180074980` returns unsigned floor
`sqrt(energy)`; threshold is the low dword of `(root*0x3333)>>16`.
`FUN_180074940` returns unsigned floor square root for each dword. Native compares
the sample and threshold as unsigned, storing `sqrt(sample)` only when sample is
strictly below threshold and otherwise storing `sqrt(threshold)`; every result
narrows to signed 16 bits.

Current source computes energy for exactly `count` entries and uses one 64-bit
root implementation for both widths, which is intentionally safer and identical
at the reached multiple-of-four counts. Its clipping comparison is signed
`int32_t`, unlike native. `FUN_180049120` proves reached samples are
`0..11,213,479`, so this cannot alter profile-9 output. A direct negative-sample
control produced different native/source mode-one bytes
`777777770f0000000000000000000000` versus
`333333330c0000000000000000000000`; this is an unreachable synthetic internal
state, not a production defect and does not warrant a source correction.

Profile-9/type-12 fails the special selector
`((type - 9) & 0xfffffff6) == 0`. Mode zero therefore calls `FUN_180081550`:
the predicate is true exactly for types `9`, `10`, `17`, and `18`, so type 12
takes the generic branch. `FUN_180047610` (`0x180047610..0x1800476c6`) has
already rebuilt the caller's `128x128` signed-16-bit Sylvester table with exact
sign `(-1)^popcount(row&column)`; exhaustive direct comparison found no table
mismatch. `FUN_180081550` forms rows `0..31` over each of four 32-sample blocks,
with every product/add wrapping in a signed-16-bit partial. That is
modulo-equivalent to current source's int32 accumulation followed by per-block
narrowing. It combines partials in signed dwords through rows
`++++`, `+-+-`, `++--`, and `+--+`, and sets four little-endian dword bits only
for strictly positive sums. It first clears record bytes `0x10..0x27`.

`FUN_180081be0` accepts this reached median layout because
`width*stride=32*4 <= count=128` and `count<=128`. It copies all 128 signed
words, and `FUN_180081a00` selects index `(count-1)>>1=63`, the lower median,
using signed comparisons. It then ORs output bit `i` only when sample
`1+4*i` is strictly greater than that median. Ties stay zero. The resulting
dword is at record `+0x20`; cleared `+0x24..+0x27` remains zero.

Mode one consumes the 32 central samples from cells `(1,1)`, `(2,1)`, `(1,2)`,
and `(2,2)` in that order. `FUN_1800818e0` clears `+0x28..0x37`, computes 32
signed-dword Hadamard sums over all 32 samples, and sets the little-endian dword
at `+0x28` only for strictly positive sums. Median layout `32*1 <= 32` selects
signed lower median index 15; `FUN_180081be0` ORs bit `i` at `+0x2c` only when
sample `i` is strictly greater, leaving ties and `+0x30..+0x37` zero. A focused
tie control with 16 sevens and 16 eights emitted `ffff0000` for the strided first
layout and zero for an all-seven second layout. Current implementations are
`feature/materialization.c:398-530`.

Native has no failure result. Nonpositive/non-multiple-of-four counts, count
above 128, incompatible median dimensions, invalid mode, null storage, or a
missing/uninitialized Hadamard table expose skipped writes, logical overreads,
uninitialized transform input, or stack corruption. They are malformed direct-
call surfaces: the only production owner supplies initialized table state,
counts 128/32, modes 0/1, and layouts `(32,4)/(32,1)`. Current on-demand parity
signs and exact-count loop deliberately avoid the mutable-table dependency and
unrolled overread without changing reached bytes.

## Confidence And Unresolved

- Confidence: high for normalization widths/wrapping, both roots, unsigned
  clipping, production nonnegative reachability, complete Hadamard signs and
  partial widths, lower medians/ties, clearing, and every record offset.
- Unresolved: packing variants unreachable from type 12.
