# FUN_1800422e0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800422e0`, `0x1800422e0..0x18004260a`.
- Role: bounded deep copy of an owned feature object.

## Call Graph

- Direct callers: `FUN_180042610` and `FUN_180042c30`.
- Direct callees: `FUN_180003800`, `FUN_180068660`, `FUN_18008a9c0`.

## Recovered Behavior

The helper deep-copies the owned feature matrices into the destination's
existing owners, copies the inline mask and enrollment metadata, caps the
primary `0x38`-byte record count to the caller-supplied maximum, copies optional
secondary records, normalizes the tagged field at `+0x14c`, and allocates or
copies the `0x1abc`-byte anti-fake block. Destination pointer ownership is
retained rather than copied verbatim.

The scalar copy is intentionally selective. It copies enrollment fields at
`+0x110..+0x124`, `+0x12c..+0x138`, and `+0x150`, but it does not write packed
`bb/+0x128` or the internal overlap/neighbor count `+0x148`. Those two values
therefore remain owned by the destination physical slot. In
`FUN_180042c30`, later-stage setup subsequently replaces `b6/+0x114`, clears
`ba/+0x124` and the qword at `+0x130`, and writes ordinal `bc/+0x12c`; it still
does not write `+0x128` or `+0x148`. Reusing a slot after deletion consequently
preserves that slot's existing `bb` and overlap/neighbor values until a later
normalization call owns them.

Evidence includes matrix copies beginning at `0x1800422ed`, the bounded record
copy around `0x1800424b0`, explicit scalar stores that omit dword indices
`0x4a/+0x128` and `0x52/+0x148`, and optional anti-fake allocation/copy at
`0x180042585`.

## Confidence And Unresolved

- Confidence: high for copied ownership and the destination-slot retention of
  `+0x128/+0x148`.
- Unresolved: names of several copied metadata words and the optional block's internal layout.
