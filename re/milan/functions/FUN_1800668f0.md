# FUN_1800668f0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x1800668f0`, profile-aware auto-calibration and working-frame
  builder.

## Call Graph

- Callers: normal live path in `FUN_18006d540` and conditional post-render
  update in `FUN_18006c510`.
- Direct substantive callees: `FUN_180063e30`, `FUN_1800672e0`, and
  `FUN_180067500`; allocation/copy/free helpers provide temporary storage.

## Behavior

- Consumes the normalized live frame plus the initialized generation workspace,
  allocates temporary gain/calibration planes, and writes the working frame at
  workspace `+0x13244`.
- Profile 9 takes the specialized branch selected by the packed profile bitmask;
  it scales temporary live/setup terms by three before the downstream
  calibration stages.
- For subtype 12, `FUN_180067500` forms the first update source as
  `max(9000 + 3 * (normalized_setup - live), 0)`. The zero clamp applies after
  adding the 9000 baseline; moving it inside the subtraction is not equivalent.
- `FUN_180067500` always supplies that subtype-12 source to final working-frame
  construction. Its strict greater-than-90% mask-count decision opens
  frame-derived temporary-gain creation. The later temporal gate controls
  ratio-plane creation, both auxiliary-map updates, and the subsequent
  `FUN_180067050` calibration-map/workspace-count commit. A temporal-rejected
  frame can retain its frame-derived temporary gain for current rendering while
  skipping those persistent mutations. A mask-rejected frame is rendered from
  the same source through the previously accumulated application and auxiliary
  gain maps, not through bare unity and not from generic
  `max(setup - live, 0)`.
- Output construction depends on several persistent workspace maps and scalar
  controls initialized before the live call. The setup plane alone is not the
  complete input state.
- The normal call and the conditional post-render call alias the same
  calibration workspace. The post-render call occurs after candidate selection,
  so it can change later calls without changing the already selected bytes.
- The normal call optionally receives a two-pointer output pair. For subtype 12,
  the first plane is the signed-baseline source divided by the secondary gain
  plane; the second is `8000 * 8192 / application_gain`, with the zero-divisor
  fallback preserving the DLL's 16-bit truncation. `FUN_18006c510` consumes
  these planes for quality rerenders.
- `FUN_180067500` receives the temporary primary divisor at descriptor slot 0,
  persistent auxiliary/application maps at slots 1/2, the persistent ready flag
  at slot 5, the temporary secondary divisor at slot 6, and the secondary
  auxiliary map at slot 7. The caller's final write loop consumes slot 0; the
  optional rerender path consumes slot 6. The two outputs therefore do not share
  one fused application/auxiliary product.
- Before that call, `FUN_1800672e0` receives initialization global
  `0x1801efbf8`, the calibration workspace beginning at `0x180219670`, and
  auxiliary-map sample counter `0x1801efbf4`. `FUN_180067500` then receives the
  same auxiliary counter directly as argument 4. The counter is not an alias of
  the initialization global.
- Slot 5 always points to process-global RVA `0x1efbfc`. The two LEA/data xrefs
  at `0x180066a65/0x180066a6c` are the global's only direct references in the
  program; this function passes it through without initialization or reset.
- The post-render call deliberately passes a null optional-output pair. It
  reuses the normal call's map descriptor, cloned workspace/mask, normalized
  live frame, profile configuration, and setup/calibration context.
- Normal and nested calls therefore alias the same three persistent gain maps
  and process-global auxiliary counter. They do not own the same mask object:
  the normal update uses the ordinary update workspace, while the nested call
  uses the hole-filled/recounted clone prepared by `FUN_18006bce0`. The mask
  and temporal-stability gates are evaluated separately on each call.
- For subtype 12, the strict greater-than-90% mask predicate first opens the
  update path. `FUN_180067500` then calls the separate `FUN_180064170`
  temporal-stability gate at `0x180067ac6`; a zero result branches at
  `0x180067acd` past ratio-plane allocation and both `FUN_180063a80` calls. The
  resulting null ratio pointer also bypasses `FUN_180067050`. Only when both
  gates accept does the primary update at `0x180067b46` precede the secondary
  update at `0x180067ba9` and the later calibration-map/workspace-count commit.
- The normal call observes the retained workspace sample count; the nested call
  observes forced value 5. Only the normal call can take `FUN_1800672e0`'s
  profile-9 low-count application-map unity reset for counts `1..3`.

## Evidence

- Persistent workspace inputs and output plane setup:
  `0x180066916..0x180066a24`.
- Profile-9 scaled branch: `0x180066a31..0x180066aa8`.
- Subtype-12 signed-baseline source in `FUN_180067500`:
  `0x180067500..0x1800678eb`.
- Calibration chain: calls at `0x180066ac0`, `0x180066ae5`, and
  `0x180066bce`.
- Final working-frame write loop: `0x180066c0b..0x180066d08`.
- For subtype 12 the write loop uses a 14-bit numerator and rounded division:
  application-mask zero copies the source; otherwise it writes
  `round(source * 16384 / primary_divisor)`, with the DLL's 16-bit zero-divisor
  fallback.
- Optional reciprocal plane: `0x180066b80..0x180066bc9`.
- Primary/secondary optional working writes:
  `0x180066c0b..0x180066cf8`.
- Descriptor construction and exact persistent aliases:
  `0x180066a28..0x180066a83`; call at `0x180066c0c`.
- Scalar setup call: `0x180066b0b..0x180066b41`; auxiliary-counter argument to
  `FUN_180067500`: `0x180066bff`.

## Confidence

High that production live output depends on initialized generation state, that
mask-rejected subtype-12 frames retain the signed-baseline source and accumulated
gain application, and that normal and post-render calls alias the same workspace
subject to independent mask and temporal-stability gates.

## Unresolved

- Exact clean-room meanings and initialization rules for every workspace plane
  consumed by `FUN_1800672e0` and `FUN_180067500`.
