# FUN_1800608b0

## Identity

- DLL: `GoodixEngineAdapter.dll`.
- Address/body: `0x1800608b0..0x180060ad5`.
- Role: compute a final score from a compact candidate-metric vector and match
  configuration. It is distinct from the post-loop `FUN_18005d9e0` rescue.

## Callers And Callees

Known code callers:

- `FUN_180055a40`, call at `0x180057029`.
- `FUN_18005d5f0`, call at `0x18005d94e`.

The function has no direct internal callees.

## Recovered Interface

```c
int32_t FUN_1800608b0(const int32_t metrics[15],
                      const int32_t *configuration);
```

The configuration supplies the template type at `+0x3c` and an alternate-policy
flag at `+0x44`. The function takes two pointers and returns a signed dword.

## Behavior

The function consumes the following words from the 15-word prefix. Unlisted
words are ignored by this function.

| Word | Meaning at this boundary | Direct retained-vector owner |
|---:|---|---|
| 0 | primary count | `FUN_18005a3c0`, with the bounded word-0 increment/clamp in `FUN_180057900` |
| 1 | effective retained count and score numerator | primary/alternate promotion in `FUN_18005a3c0`, sibling updates in `FUN_180057900`, then `max(word1,word2)` at `FUN_180055a40:0x180055ef5..0x180055f08` |
| 4 | selected overlap score | caller recomputation at `FUN_180055a40:0x180056012..0x18005601d`, optionally replaced with the post-admission affine by `FUN_180072860` |
| 5 | selected overlap detail | caller recomputation at `0x180056017..0x180056028`, optionally replaced on strict better detail by `FUN_180072860` |
| 8 | low-bitmap detail | `FUN_180070c70 -> FUN_18006fc80`, called at `0x180056267` |
| 9 | scaled overlap coverage | caller recomputation and `configuration[12]` scaling at `0x18005602d..0x180056048`, optionally replaced with the post-admission affine by `FUN_180072860` |
| 11 | selected transformed-record geometric percentage | `FUN_180074e80` under primary/alternate/sibling ownership, optionally replaced by `FUN_180072860` |
| 12 | affine scale penalty | final `FUN_180070e90` result stored at `0x18005604d..0x180056053` |
| 13 | affine orthogonality penalty | final `FUN_180070e90` result stored at `0x180056053..0x180056073` |
| 14 | strong orthogonality penalty | final `FUN_180070e90` result stored at `0x180056069..0x18005607e` |

The other prefix words are word 2 sibling count, word 3 reserved zero, words
6/7 the other low-bitmap metrics, word 10 transformed-record topology, and are
not read here. Words 15..20 and 66..70 exist in the enclosing 77-word candidate
but are outside this compact input.

The fallback caller builds a separate zeroed 15-word vector at
`FUN_18005d5f0:0x18005d68d..0x18005d94e`: words 0/1 are the retained fallback
count, word 4 the retained overlap, word 5 its adjusted detail, words 6..8 come
from `FUN_180070c70`, word 9 is winner-carried scaled coverage, and words 12..14
are recomputed affine penalties. Its word 11 remains zero for the profile-9
type-12 call. This vector is not the earlier 77-word direct candidate.

In this fallback vector, words 0 and 1 are equal, and type-12 configuration
word 16 is zero from `FUN_18005e230` through ordinary `FUN_18005edb0` dispatch.
Consequently word 5 equals the raw overlap detail even though its retained
owner is the adjusted detail. `FUN_180070c70` changes only words 6..8, not
word 5. The final score's words-12..14 penalty remains active under word 17;
word 16 zero does not disable that separate penalty.

The fallback wrapper scans at most 31 populated pairs, and filtering cannot
increase that count. Its accepted type-12 count is therefore in `8..31`.
With denominator 42, the score range is `19..73`; the fallback caller's
positive cap at 100 cannot change these ordinary bounded outputs.

### Type And Penalty Dispatch

Template type is read as an unsigned dword from configuration `+0x3c`.
Denominator is 42 exactly for types `12`, `13`, `16`, and `22`, the set bits of
`0x413000`; every other value, including 23 and arbitrary values above 22, uses
31. Types 11 and 21 first compute, with dword wrap:

```text
penalty = metric12 + metric13 + metric14 + (metric0 < 5)
detail = metric5 - 3 * penalty
low_detail = metric8 - 3 * penalty
combined = detail + low_detail
```

The alternate-policy branch at configuration `+0x44` has priority over the
ordinary type-11/21 branch. Profile-9/type-12 configuration supplies template
type 12 and nonzero alternate policy.

### Alternate Policy

If `metric0 < metric1`, subtract two from both detail words before testing.
Accept exactly when all three signed conditions hold:

```text
metric1 >= 8
detail + low_detail - 4 * (metric12 + metric13 + metric14) >= 417
metric9 >= 96
```

The type-11/21 penalties happen before this mutation; the explicit four-point
penalty uses only words 12..14. Input metrics are never modified.

### Ordinary Type 11/21 Policy

With alternate policy zero, type 11 or 21 accepts when at least one complete
path is true. `combined` is the post-penalty sum above:

```text
metric0 >= 5 && metric1 >= 8 && combined >= 425 && metric11 >= 42
metric0 >= 3 && metric4 >= 235 && metric9 >= 120 && combined >= 400
metric0 >= 6 && metric9 >= 61 && combined >= 415 && metric11 >= 41
```

This is the exact grouping of `0x180060a4f..0x180060a98`; equality at every
shown threshold passes. Successful scoring always uses denominator 31 and the
special numerator rounding constant 15.

### Ordinary Generic Policy

Every other type with alternate policy zero accepts when any one of these six
complete paths is true:

```text
metric1 >= 5 && detail >= 226 && low_detail >= 176 && metric9 >= 91
metric1 >= 5 && detail >= 218 && low_detail >= 176 && metric9 >= 121
metric1 >= 7 && detail >= 216 && low_detail >= 176 && metric9 >= 121
metric1 >= 8 && detail >= 209 && low_detail >= 191 && metric9 >= 106
metric1 >= 9 && detail + low_detail >= 416 && metric9 >= 41
metric1 >= 8 && detail + low_detail >= 426 && metric9 >= 41
```

These paths are the exact compound grouping at
`0x180060998..0x180060a3c`, not independently accumulated weak flags.

### Score Arithmetic And Trust

After a successful alternate/generic path:

```text
q = ((denominator >> 1) + (metric1 << 8)) / denominator
score = (q * 100) >> 8
```

The type-11/21 ordinary path instead uses
`q = ((metric1 << 8) + 15) / 31`. Shifts, additions, and multiplication wrap as
32-bit dwords; division is signed and truncates toward zero; the final shift is
arithmetic. Rounding therefore occurs before percentage scaling. No metric is
range-checked, so wrapped penalty sums and accepted malformed values are
consumed as-is and can produce negative or otherwise non-policy scores.

Failure returns zero. There is no status output, score cap, or context mutation.
Null metrics or configuration pointers fault immediately in the DLL. Production
callers provide nonnull vectors with small caller-produced values; malformed
null/extreme behavior is not production-reachable.

## No-Direct Caller Finalization

At `FUN_180055a40:0x180057005..0x180057038`, this helper validates a retained
winner only when no direct score has already been latched, contributor count is
positive, packed mode is zero, and caller blocking state is zero. Acceptance
evidence at `+0x684` and rejection evidence at `+0x688` are not predicates for
this block. The helper's numeric result is used only as a zero/nonzero gate
at `0x180057031..0x180057038`; it is never published as the final score.

After a nonzero result, the caller visits physical feature indices in ascending
order and increments every nonzero contributor slot at
`0x18005703a..0x18005706d`. For type 12 it then sorts gallery order through
`FUN_1800607b0` and publishes the all-contributor aggregate at
`0x180057089..0x180057095`:

```text
score = (wrapped_dword(q8_sum * 100) / contributor_count) >> 8
```

Division is signed and truncates toward zero; the final shift is arithmetic.
This helper itself does not mutate lifecycle state. Publication and lifecycle
updates do not require a selected feature, so the selected feature can remain
`-1`. The ranked vector's ownership and replacement predicates belong to
`FUN_180055a40.md`. The post-loop `FUN_18005d9e0` rescue is a separate path and
does not call this helper.

For ordinary `FUN_18005edb0` dispatch, the zero packed-mode gate requires the
entire live probe `c7` dword to be zero, not merely its decoded low class.
`FUN_180070d90` therefore initializes the accumulated high class to zero.
At `FUN_180055a40:0x180055e00..0x180055e0c`, an encountered feature changes
that accumulator only when its decoded high class exceeds three, in which case
the accumulator becomes at least four and cannot satisfy the later compact
entry gate. The only remaining accumulator mutation is the single latched
increment at `0x180055e21..0x180055e2e`. Consequently an ordinary invocation
reaching this compact boundary has accumulated high class zero or one, never
two or three. Histogram class is independently constrained below two.

These bounds make the final clear-only `FUN_1800553f0` stage a flag-preserving
operation on this route: all its writes require accumulated high class at
least two. That helper's fourth argument is the histogram class, not the
decoded packed low class; see `FUN_1800553f0.md`. The caller's separate
post-dispatch packed-mode geometry veto also requires a positive raw word and
is excluded here. Neither stage supplies a candidate-zero producer for this
ordinary compact-finalization route.

The post-dispatch optional-classifier veto at
`FUN_180055a40:0x18005629a..0x1800562b3` cannot provide the successful compact
winner either. The second `FUN_180054ec0` call supplies its optional flag; for
ordinary type-12 configuration, that flag requires raw word 5 below 205.
The caller clears both flags only when the optional flag is nonzero and word 8
is below 175. Their unpenalized sum is therefore at most 378, already below
417. Affine penalties and the primary-below-retained adjustment only lower it.
No later stage rewrites these metric words before contribution or compact
validation. See `FUN_180054ec0.md` for the optional flag's complete predicate.

The strong policy bypass also cannot supply such a winner: its match flag two
implies candidate flag one, the final high-class-zero/one helper preserves both,
and a late-status-one return skips contribution entirely. A surviving bypass
therefore relatches a positive direct score when blocker count remains zero.
The successful no-direct winner must instead survive an actual dispatcher call
with candidate flag zero, satisfy the metric-only contribution arm, and pass
the compact predicate on that same unchanged vector. The dispatcher controls
both flag outputs and its mutable recognition mode; compact validation does not
recompute or replace any of them.

### Classifier-Origin Split For A Compact-Eligible Contributor

For ordinary zero-selector configuration, let `P=word0`, `R=word1`,
`S=word4`, `D=word5`, `L=word8`, `C=word9`, and
`H=word13+word14`. The caller's penalty stores at
`FUN_180055a40:0x18005604d..0x18005607e` make words 12..14 Boolean, with
word 14 implying word 13; hence `H` is in `0..2`. Refinement preserves these
penalties. Compact success and metric-only contribution require
`R>=8`, `S>=208`, `D>=196`, `C>=96`, and
`D+L-4*(word12+H)-4*(P<R)>=417`.

These predicates partition the first two dispatcher classifiers:

- For `R>=12`, initial classifier `FUN_18005c0c0` necessarily sets the
  candidate output. Its retained-index overlap threshold is at most
  `200+4=204`, strictly below `S`. Its optional topology bonus only increases
  the indices, whose broad tables are nonincreasing.
- For `8<=R<=11` and `P>=5`, either that classifier sets candidate, or
  `FUN_180054930` does. If initial classification fails, its retained-index
  detail ceilings are `[211,208,205,203]`. With `C>=96`, fallback's only
  penalty is `5*H`. The ordinary detail thresholds `[185,175,170,170]` pass
  because `D-5*H>=186`. Compact's sum bound forces the penalized low detail
  above fallback's corresponding `[199,193,185,185]` thresholds. Coverage
  also passes. Thus rejection by both classifiers is impossible in this arm.
- Remaining candidate-zero output from both classifiers requires `P<=4`
  and `R` in `8..11`. In that arm topology cannot add the initial-classifier
  count bonus. Assuming both incoming flags zero, the exact additional
  conditions are the following table plus the compact/contribution predicates:

  | R | D at most | S at most | Fallback rejects exactly when |
  |---:|---:|---:|---|
  | 8 | 211 | 217 | `D-5*H < 204` |
  | 9 | 208 | 214 | `D-5*H < 204` |
  | 10 | 205 | 211 | `D-5*H < 203` |
  | 11 | 203 | 209 | `D-5*H < 200` |

The decisive initial comparisons are `0x18005c2df..0x18005c322`;
fallback table selection, penalty and broad comparisons are
`0x180054ac5..0x180054b87`. Strict fallback cannot promote `P<=4,R<=11`.
The post-classifier veto is skipped on this all-zero arm, and late eligibility
`FUN_1800572e0` has no primary bucket below five. The dispatcher therefore
preserves both zeros and recognition mode on this arm. A compact-eligible
candidate outside it must first acquire candidate-positive state and subsequently
lose it in the post-classifier veto, then avoid re-addition by late eligibility.

This is a necessary classifier/consumer partition, not a joint bitmap/record
producer construction. In particular, a low final primary count must be traced
through the earlier `FUN_180057900` bonus: topology greater than 30 together
with distance below 10 increments primary before this policy boundary.
Post-admission refinement can replace topology and distance without repeating
that bonus. The count, bitmap, and transformed-record owners must therefore
retain their actual mixed-stage provenance; see `FUN_180053220.md`,
`FUN_180057900.md`, and `FUN_180072860.md`.

For the high-retained subdomain `R>=23`, this partition can be tightened further.
Write `A=word12+word13+word14`, `B=D+L-4*A`, and `G=word11`.
Compact eligibility gives `B>=417`; metric contribution and Boolean penalties
give `D-2*A>=190` and `S-2*A>=202`. Under packed mode zero, every
`FUN_180053680` / `FUN_180057e60` post-classifier rejection apart from the
following two is excluded by these bounds or its retained-count upper bound:

```text
G < 6
P <= 6 && G < 10 && S-2*A < 210
```

The first is the signed geometry floor at `0x180053a0b..0x180053a14`; the
second is the primary-bucket floor at `0x180053a3a..0x180053a50`, before
the bucket switch. The second floor does not have a retained-count ceiling.
Thus a high-retained compact vector can lose candidate state only at these
record-geometry floors, and still has to avoid later eligibility re-addition.

The producer correlation for those floors belongs to the actual last
`FUN_180074e80` output owner. With its caller count `N` and in-border probe
count `V`, `G=trunc(100*N/V)` for `V>0` and `G=0` for `V=0`.
Ordinary probe capacity bounds `V<=150`. Consequently, for nonzero `V`,
`G<6` requires `N<=8` and `100*N<6*V`; `G<10` requires `N<=14` and
`10*N<V`. These are bounds on that stage's `N`, not on retained `R`.
For `R>=23` with no refinement replacement, a primary-count repair after
alternate promotion is the possible small-`N` owner: `FUN_18005a3c0`
can retain the promoted alternate count while recomputing the record block
with original primary count at `0x18005a913..0x18005a94f`. Initial topology
admission excludes `V=0` on that unrefined route. A refinement replacement
uses `N=R`, so these low-geometry floors would instead require its `V=0`
case. They cannot be justified by replacing native mixed-stage metrics with
`N=R` everywhere. See `FUN_180074e80.md` and `FUN_180072860.md`.

More specifically, on the unrefined high-retained route, original primary
count `N` is greater than four because the primary repair itself requires it.
The `G<6` arm therefore restricts that original count to `5..8`. The second
floor additionally requires final primary at most six, restricting the original
count to five or six. If the repair's topology exceeds 30 and its average
distance is below 10, the sibling finalizer adds one to both primary and
retained counts while leaving the record percentages unchanged; otherwise no
bonus is added. The large promoted retained count skips sibling construction.
This gives `P=N+bonus` and
`R=min(42,promoted_alternate_count+bonus)` on this route.

On this high-retained route, an admitted unrefined repair necessarily retains
the **primary affine** in the earlier rank comparison. The pre-promotion record
call at `FUN_18005a3c0:0x18005a885` uses the alternate affine and count. Repair at
`0x18005a93b` changes the count to original primary but uses the ranked affine.
In `FUN_180074e80`, caller count participates only in geometric percentage at
`0x18007520a..0x180075211`; valid count, matched count, topology and average
distance depend on records and affine, not caller count. If alternate won rank,
repair therefore leaves topology below 30. The promoted count is at least 22
on the `R>=23` route (the later bonus adds at most one), so sibling construction
is skipped at `FUN_180057900:0x180057a02..0x180057a05`; its bonus also requires
topology strictly above 30. Caller admission then rejects at
`FUN_180055a40:0x180055f16..0x180055f1c`, before refinement or policy.
Thus an admitted repair requires alternate topology `<30`, primary-affine
topology `>=30`, and a primary-retaining overlap rank. Different count and
transform owners remain essential: this excludes alternate-ranked repair, not
the primary-ranked mixed-owner route. See `FUN_18005a3c0.md` and
`FUN_180074e80.md` for the underlying calls and scan contract.

Neither unrefined geometry-floor arm can reach the primary-12 bucket in late
eligibility: original primary is at most eight and the count-only bonus adds
at most one, so final primary is at most nine. The late helper still matters in
its actual smaller bucket. In particular, final primary six and detail at most
205 exclude every strict and broad term in `FUN_1800572e0`, independently of
quality, topology and coverage: the weakest detail comparison for that bucket
is broad detail `>=206`. Thus a primary-six geometry-floor clear need not be
restored by late eligibility even with large bitmap coverage. This does not
assert that arbitrary independently chosen metrics have a common producer.

For the ordinary compact winner, retained count is at most 42 from candidate
construction, and acceptance requires at least eight. Its helper return is
therefore in `19..100`; the bounded numerator, penalty and percentage arithmetic
cannot wrap. Testing nonzero and testing positive are equivalent on this caller
domain. That range is the helper's validation result, not a bound derived from
the all-contributor average, whose other terms may have retained count below
eight.

Compact success writes the score and gallery lifecycle/order only. It does not
set acceptance `+0x684` as positive fallback does at `0x1800570ea`, nor does it
publish a selected index, direct affine, or routed affine. On the ordinary
zero-blocker route whose contributions all have final flags zero, those evidence
fields retain their initialized values: acceptance/rejection zero, selected
index `-1`, selected count/relation count zero, and identity direct/routed
affines, provided the preceding independent rescue made no replacement.
