# FUN_1800608b0

## Identity

- DLL: `GoodixEngineAdapter.dll`.
- DLL SHA-256:
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x1800608b0..0x180060ad5`.
- Role: compute a final score from a compact candidate-metric vector and match
  configuration. It is not the post-loop rescue used by safe-zero row `81/1`.

## Callers And Callees

Known code callers:

- `FUN_180055a40`, call at `0x180057029`.
- `FUN_18005d5f0`, call at `0x18005d94e`.

The function has no direct internal callees.

## Recovered Interface

```c
int32_t FUN_1800608b0(const int32_t metrics[15],
                      const int32_t *configuration);
```

The configuration supplies the template type at `+0x3c` and an alternate-policy
flag at `+0x44`. Names remain provisional, but the two pointer arguments and
integer return are confirmed by the entry and return instructions.

## Behavior

- Selects score denominator 42 for template types in mask `0x413000`, otherwise
  31.
- Reads candidate counts and quality fields from metric offsets `+0x00`,
  `+0x04`, `+0x10`, `+0x14`, `+0x20`, `+0x24`, `+0x2c`, and `+0x30..+0x38`.
- Applies separate policy branches when configuration `+0x44` is nonzero and
  for template types 11/21.
- Returns zero when the applicable count/quality gates fail.
- On success, returns the retained count scaled by the selected denominator and
  converted to an integer percentage.

Important evidence addresses are `0x1800608ec..0x180060900` for denominator
selection, `0x18006092a` for the alternate-policy split,
`0x180060998..0x180060a3c` for the generic gates, and
`0x180060a98..0x180060ab8` for type-11/21 scoring.

## Native Equivalent

`goodix_milan_index0_match_final_score()` in
`drivers/goodix53x5/goodix53x5-milan.c` is the current clean-room equivalent.
This review did not revalidate every branch against focused vectors.

## Diagnostic Evidence

The immutable 31-row safe-zero scalar diagnostic hooks this function entry. Its
`final_policy_input` is empty on every row, including the sole post-loop rescue
`81/1`. Therefore those captures cannot validate this helper and must not be
used to attribute `81/1` to it.

The later six-row route trace strengthens that exclusion: `81/1` reaches
`FUN_18005d9e0` at the caller's `0x180056caf` site and reaches neither this
function nor its other caller `FUN_18005d5f0`. The accepted feature 2 / score 19
result is therefore outside `goodix_milan_index0_match_final_score()`.

The focused winner-selection trace now supplies one exact positive generic
vector. On safe-zero row `157/2`, `FUN_180055a40` retains feature 7 metrics with
`[0,1,4,5,8] = [8,11,227,214,212]`, configuration type 12, and configuration
`+0x48 == 0`. This function returns `26` at caller return site `0x18005702e`.
The caller then independently computes Q8 aggregate `(67 * 100 / 1) >> 8 = 26`
and writes it at `0x180057095`. The final outer candidate is accepted even
though recognition evidence keeps matched feature `-1` and acceptance/rejection
flags zero. Evidence root and tree are
`windows-native-winner-selection-v1` and
`f6cfd549d49323093e01ffc3c5dbfa94c382af275c3e88f47d50cd2bd7b2c741`.

## Confidence And Unresolved

- High confidence in entry/body, callers, denominator selection, gate shape,
  and zero/nonzero score role from direct disassembly.
- Medium confidence in semantic names for the individual metric fields.
- One generic type-12 positive vector is now exact. Focused negative vectors and
  alternate-policy/type-11/21 vectors are still required before claiming full
  helper parity outside the recovered winner-selection contract.

## Native Winner Finalization

The focused `goodix53x5-match-selection.c/.h` state machine now invokes the
existing native equivalent only at the recovered no-direct boundary: a retained
winner and contributor exist, packed configuration `+0x48` is zero, selection
evidence does not block finalization, and no direct score was latched. A positive
validator result gates the all-contributor Q8 score; the validator's numeric
return is not substituted for that score.

Focused positive/zero tests pass. Row `157/2` is exact with validator return
`26`, Q8 `(67 * 100 / 1) >> 8 == 26`, outer acceptance, and internal feature
`-1`. This does not broaden parity claims beyond the one recovered generic
type-12 positive vector, and it does not implement the separate `81/1`
`FUN_18005d9e0` rescue.

## Match-Time Lifecycle Consequence

This helper never mutates `be`. When its no-direct validation returns positive,
`FUN_180055a40:0x18005703a..0x18005706d` performs the lifecycle mutation itself:
it visits physical feature indices in ascending order and increments every
nonzero contributor slot at `0x180057058`. The internal selected feature can
remain `-1`; immutable e2 sequence 157 proves score 26, selected `-1`, and the
sole aggregate event feature 7 `be:0->1`. A wrapper that requires a selected
feature before applying lifecycle mask `0x80` suppresses a real DLL mutation.

## Unit-18 Publication Integration

No-direct finalization now receives caller blocking state explicitly. After a
positive validator return it increments every nonzero physical contributor slot
in ascending index order, then publishes the wrapped Q8 sum/count score.
Validator output remains a gate rather than the published score, and selected
feature remains `-1`. Zero, one, many, configuration-block, caller-block, and
validator-zero controls match the frozen DLL outputs.

## No-Direct Evidence Predicate Correction

The integration statement above is incomplete. The DLL caller block
`FUN_180055a40:0x180057005..0x180057038` checks positive contributor count,
packed mode zero, caller blocking count zero, and this helper's positive return.
It does not read acceptance evidence `+0x684` or rejection evidence `+0x688`.

Native `goodix_milan_match_selection_finalize()` additionally requires both
evidence fields to be zero before calling the validator. A deterministic control
with one contributor, a valid retained winner, rejection evidence one, and a
positive validator records native `accepted=0` and zero validator calls; the DLL
block's static result is validator invocation followed by aggregate Q8
publication. Existing zero-evidence and natural row `157/2` controls remain
correct but do not cover this extra native veto.

Native finalization now invokes this helper without acceptance/rejection
evidence predicates. Evidence-one controls for both fields reach the validator
and publish aggregate Q8 when contributor, packed-mode, caller-blocker, and
positive-validator gates pass. Configuration, blocker, and validator-zero
controls still stop before publication. The helper's numeric return remains a
gate, not the published score.
