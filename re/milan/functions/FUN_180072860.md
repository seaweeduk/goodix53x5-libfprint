# FUN_180072860

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180072860`, `0x180072860..0x180072b28`.
- Role: build and rank a post-admission alternative transform, then replace the
  caller's selected transform and associated overlap/record metrics when its
  overlap detail is better.

## Call Graph

- Callers: `FUN_180055a40`, `FUN_180059900`, `FUN_180060fe0`, and
  `FUN_180062a30`.
- Callees: `FUN_180068700`, `FUN_180071d40`, `FUN_180073ba0`,
  `FUN_180073d10`, `FUN_180074e80`, `FUN_180077860`, and
  `__security_check_cookie`.

## Recovered Behavior

The helper lazily builds two class-partitioned record workspaces, obtains an
alternative affine through `FUN_180073ba0`, and evaluates it with
`FUN_180071d40`. It rejects overlap score `0x80` and otherwise replaces the
current candidate only when the current detail is unset (`0x80`) or the new
detail is greater. Replacement writes overlap score/detail, scaled coverage,
transformed-record fields, and the six-word output affine. Types 9 and 10 use
the special packed coverage path; other active types use ordinary scaled
coverage and `FUN_180074e80` record metrics.

In `FUN_180055a40`, the call at `0x18005618e` lies strictly between direct-stage
checkpoints `0x180056130` and `0x180056230`. The caller invokes it when any of
the initial overlap/flag conditions require an alternative candidate.

## Focused Evidence

For profile-9/type-12 row `142/1`, feature 1 is exact through primary state,
sibling state, affine, and caller admission. The first direct checkpoint has:

```text
affine [263,71,11464,-73,237,-3214]
state  [11,11,0,0,223,203,...,82,46,39,...]
record [41,19,46,39,14]
```

At the second checkpoint, after this helper, Windows has:

```text
affine [238,76,11949,-76,238,-3148]
state  [11,11,0,0,223,208,...,78,47,25,...]
record [44,21,47,25,13]
```

The subsequent low-bitmap call receives that replacement affine and produces
`[223,156,215]`. Native currently omits the replacement, retains the first
affine, and consequently computes `[221,150,213]`. Two independent passive
replays are byte-identical at:

`<evidence-root>/references/milan-native-capture-20260723/windows-safe-zero-pre-dispatch-admission-v1/`

## Confidence And Unresolved

- Confidence: high for ranking, replacement writes, call placement, and the
  `142/1` first-divergence boundary.
- Unresolved: exact native construction of the alternative affine for the full
  corpus and branch coverage for losing alternatives and type-specific metric
  paths.

## Native Contract-2 Investigation

The direct helper chain is now translated structurally: two radius-two signed
index maps (`FUN_180073d10`), two partitioned inverse-affine pair scans
(`FUN_180073e30`), and the fixed-point similarity fit (`FUN_1800732c0`) through
`FUN_180073ba0`. Replacement ranking and the type-12 overlap/record writes match
this function's assembly.

The pre-fix frozen `142/1` native input produced affine
`[239,75,12003,-75,239,-3257]`, overlap/detail `222/205`, and record metrics
`[44,22,50,26,13]`; Windows required `[238,76,11949,-76,238,-3148]`,
`223/208`, and `[44,21,47,25,13]`. After restoring the generic coverage-derived
record-selection limit, native extraction supplies the exact 135-record Windows
object and this unchanged helper produces the exact Windows replacement state.
The focused contract-2 gate is now exact for both rows, all five calls, and the
`142/1` direct stage without a matcher-side correction.

## Evolved-Gallery Units 7-9 Audit

The sequence-197 calls exclude this post-admission helper as the first source.
For `197/1 f39`, the DLL enters with the alternate affine already selected by
`FUN_18005a3c0`, while native enters with the primary affine; neither side
replaces its incoming transform here. For `197/2 f6`, both sides enter with the
same ranked primary affine despite different retained counts, and both produce
the same replacement affine `[242,91,-11910,-91,242,-3049]` and associated
record metrics. The unequal count survives because this helper does not own it.

Assembly confirms the relevant complete ordering: reject only score `0x80` at
`0x180072a0c`, retain a non-`0x80` current candidate on equal or lower detail at
`0x180072a17..0x180072a27`, and replace when current detail is unset or new
detail is strictly greater. These natural calls and the existing contract-2
fixtures prove the reached type-12 paths, while losing/tied synthetic-valid
coverage remains incomplete. Classification is therefore exact but unvalidated
context, not partial and not exact complete.

## 2026-07-28 Retrospective Correction

The exact-but-unvalidated-context classification is superseded by one concrete
ownership defect. This function and `FUN_180073ba0` read the enrolled and probe
stored partition boundaries at feature `+0x118` when building the two index maps,
collecting pairs, and rebasing the second partition. Native
`milan_refine_record_similarity()` instead derives both boundaries from a
leading run of mutable `foreground == 0` record values.

A fixed generated split-`2/2` input refines successfully; changing only those
record flags to contradictory inferred splits changes native refinement to
failure. The DLL supplied the unchanged stored boundaries would retain the
baseline ranges. The score-`0x80` rejection, strict-greater detail replacement,
equal/lower retention, type-12 metric writes, and shared wrapped inverse remain
structurally exact. The function is therefore partial due to pair-input
ownership, not ranking.

## 2026-07-28 Native Contract Completion

The native refinement call now receives validated stored boundaries and passes
them unchanged through both partition scans and index-map builds. The previous
foreground-derived split is removed. Direct controls cover agreement,
contradiction, physical reorder, both empty-partition directions, malformed
boundaries, and successful refinement. Replacement controls retain the recovered
ordering: reject alternative score `0x80`, replace an unset current score, retain
losing/tied detail, and replace only strict-greater detail. No type-12 field owner
or score computation changed.
