# FUN_180053220

## Binary And Body

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180053220` / `0x180053220-0x180053441`.
- Recovered signature for the production caller:
  `void f(int32_t metrics[77], int32_t quality, int32_t coverage,
  int32_t config[20], int32_t *flag_rsp_60, int32_t *flag_rsp_64)`.

## Call Graph

- Caller: `FUN_180055a40`.
- Callees: `FUN_18005c0c0`, `FUN_180054ec0`, `FUN_180054930`, `FUN_180058ee0`, `FUN_18005f150`, `FUN_180053680`, `FUN_180061a60`, `FUN_1800602e0`, and `FUN_1800572e0`.

## Behavior

This is the top-level per-feature policy dispatcher used before the later
`FUN_180058700` and `FUN_1800553f0` stages. `FUN_180055a40:0x180056295` passes
the current 77-word candidate object in `RCX`, live quality/coverage from
`probe+0x11c/+0x120` in `EDX/R8D`, the matcher configuration in `R9`, and
caller flags `[rsp+0x60]`/`[rsp+0x64]` as stack arguments 5/6. The diagnostic
serializes final tuples in the existing order `[rsp+0x64,rsp+0x60]`.

For type 12, the exact dispatcher order is:

| Stage | Predicate at entry to stage | Helper/output behavior | 58-call hits |
|---|---|---|---:|
| Initial | either flag is zero | `FUN_18005c0c0`; temporary outputs are ORed into the corresponding flags | 43 |
| Fallback initial | `flag_rsp_64 == 0` | `FUN_180054930`; updates `flag_rsp_60`, then ORs its second output into `flag_rsp_64` | 6 |
| First veto | `flag_rsp_60 != 0` | `FUN_180058ee0`, including type-12 continuation `FUN_18005c3b0`; clear-only | 47 / 29 continuation hits |
| Early return | both flags nonzero after first veto | skip the remaining dispatcher stages | 13 |
| Post-classifier veto | `flag_rsp_64 != 0 && flag_rsp_60 == 0` | `FUN_180053680`, including type-12 continuation `FUN_180057e60`; can clear both | 39 / 18 continuation hits |
| Late eligibility | `flag_rsp_60 == 0 && config+0x4c == 1` after prior helpers | `FUN_1800572e0`; temporary outputs are ORed into the flags | 26 |

`config+0x3c` is 12 in all calls. The match starts in recognition mode 1, but
the configuration is mutable across features: 47 calls enter with
`config+0x4c == 1`, 11 enter with zero, and nine mode-1 calls leave with zero.
Both `FUN_18005c3b0` and an early `FUN_180053680` rejection can clear this
field. The late-eligibility predicate therefore must read the post-helper mode,
not the invocation's original mode.

Observed input flag pairs `(rsp+0x60,rsp+0x64)` are `0/0` 35 times, `0/1`
eight times, `1/1` 12 times, and `2/1` three times. Dispatcher outputs are
`0/0` 35 times, `0/1` ten times, `1/1` ten times, and `2/1` three times.

## Evidence And Confidence

- Dispatch tests occur at `0x180053220-0x180053441`; the type field is read from `+0x3c` and recognition-mode-like state from `+0x4c`.
- Confidence is high for control flow and flag mutation, medium for semantic names and scalar argument roles.
- The non-forced 31-row trace is frozen at
  `<evidence-root>/references/milan-native-capture-20260723/windows-safe-zero-matcher-dispatch-diagnostic-v4/`,
  tree SHA-256
  `dd9249d44f89f8354a68a7f4236e4af1cbd23c75c684cbb4cec57ac1a5cc142b`.
  Its 58-call table SHA-256 is
  `33aef2a892b319bb6ded578f45c322d19725eba7ccb6c1f75b49f8b7a863cde4`;
  an independent repeat is byte-identical.
- Confidence is high for argument mapping, branch occupancy, helper output
  order, mutable-mode behavior, and final tuples. Human-readable names for all
  77 metrics and 20 configuration words remain incomplete.
- The exact profile-9/type-12 chain is now implemented in
  `goodix53x5-match-policy.c`. The focused native gate reproduces helper
  occupancy/output, mutable mode, and dispatcher finals for all `58/58` frozen
  calls, plus the caller's 60 final tuples/contributions. This does not extend
  the claim beyond the frozen diagnostic inputs.
- The subsequent 14-row candidate-state correlation does not add a same-input
  counterexample. All 26 feature-aligned official calls differ from native in
  their 77-word input before this function; official/native candidate counts
  also differ on every row. Dispatcher outputs are exact only `12/26`, mode
  exits `15/26`, contributions `17/26`, and Q8 terms `12/26`, but those are
  downstream observations under different inputs and do not invalidate the
  exact 58-call/60-final v4 policy gate. The next implementation work belongs
  before this dispatcher.

## Corrected Same-Candidate Interpretation

The focused admission/state diagnostic supplies the missing precondition for
`139/1`. Features `2,3,7` now have exact native and official 77-word candidate
inputs, order, incoming flags, quality `78`, and coverage `96`. The remaining
output difference is still not a same-input dispatcher defect: official
configuration `+0x48` is `0x200`, while the full native decomposed-probe path
initializes that policy word to zero after dropping serialized optional tag
`c7 = 0x200`. Direct policy replay with the complete official inputs remains
exact `58/58` calls and `60/60` final tuples.

For feature 2, the official nonzero packed mode activates the type-12 veto,
clears both flags, and changes mutable recognition mode `1 -> 0`. Native mode
zero leaves flags `[1,1]` and recognition mode `1`. The implementation contract
is therefore to preserve the packed live-feature scalar before invoking this
dispatcher, not to alter any helper threshold or branch.

## Resolved Row 79/2 Boundary

The focused late-policy diagnostic supersedes the six-row classification's
broad attribution of `79/2` to this dispatch/final-policy family. Candidate
count/order, all 77 metrics, initial flags, every reached helper output,
feature identity, recognition-mode entry/exit, and this function's final
`[rsp+0x60,rsp+0x64] = [1,1]` are exact. This function is not the divergence.

The first difference is the caller's post-dispatch type-12 geometry gate at
`FUN_180055a40:0x1800562b3..0x180056313`. Official `config[18]=512`, type `12`,
`metrics[0]=6`, and `metrics[11]=14` satisfy the third generic ladder term
`metrics[0] < 7 && metrics[11] < 16`, clearing both flags at `0x180056309`.
The pre-fix native path omits that caller gate and retains `[1,1]`. The repeated
trace and DLL byte slice are frozen at
`windows-native-row79-late-policy-v1`, tree SHA-256
`358c165687cf9c5e099ba0ade1d1391ae7efc240a738a550ac516c9eaa927bcc`.

The native caller gate is now implemented after this function returns. Direct
replay remains exact for all `58/58` frozen dispatcher calls and `60/60` caller
final tuples/Q8 contributions, confirming that no dispatcher helper threshold,
occupancy, output, or mutable recognition-mode behavior changed. Full immutable
replay closes `79/2`; the four remaining decision rows are `77/1`, `81/1`,
`157/2`, and `160/1`.

The accepted-score residual trace adds `89/2` feature 0 beyond the original
58-call gate. Dispatcher input, initial flags, packed mode, quality/coverage,
and initial-classifier output are exact. The first mismatch is specifically the
type-12 `FUN_18005c3b0` bucket-6 continuation beneath `FUN_180058ee0`: native's
widened retained-count operand clears flag60 while the DLL preserves it. See
`FUN_18005c3b0.md` for the exact `<10` versus `<16` condition.

## Geometry Provenance Boundary

Independent review confirms that this dispatcher identifies the same production
path but does not manufacture the later area dimensions. At
`FUN_180055a40:0x180056271..0x180056295`, it receives the current 77-word
candidate, live probe scalars `+0x11c/+0x120`, and policy configuration whose
`+0x3c` field is subtype `12`. After it returns, `FUN_180055a40` passes the live
probe object itself to `FUN_180058700`. That object carries width/height
`104/88`; the area helper receives rows/columns `88/104`.

Thus the dispatcher trace and mutable policy establish type-12 path identity,
while geometry provenance remains the live probe descriptor. There is no
cross-profile dimension lookup in this function.

## Evolved-Gallery Extra Positives Excluded

The e1/52 feature-37 and e2/55 feature-32 native positives do not expose another
dispatcher branch. Production skips both at
`FUN_180055a40:0x180055d8f..0x180055db9` because each gallery is full
(`feature_count == maximum_features == 40`), retained evidence is one, and the
current feature has `b5=1`. Native compared feature count with unrelated packed
registration count `781`, admitted both candidates, and then reached the
caller's `rsp60 == 2` bypass at `0x180056236..0x180056244`. Consequently neither
extra candidate calls this function in production or in the corrected direct
control.

The valid official candidates in the same rows still call this function with
complete 77-word state and follow the established helper sequence exactly:
e1/52 feature 39 exits `rsp60/rsp64 = 0/1`, as does e2/55 feature 36. Their
mutable recognition mode remains `1 -> 1`; subsequent late helpers preserve the
tuple. This function's thresholds, helper occupancy, output ORs, and mutable
mode are independent boundaries and require no change. See `FUN_180055a40.md`
and artifact root `evolved-direct-policy-v1` for the caller rule and controls.

## Caller Bypass Operand Correction

The bypass is owned by `FUN_180055a40`, not this dispatcher. At
`0x180056236..0x180056244`, the caller skips this function when `flag60 == 2`
and its mutable accumulated high class is at most one. Native
`goodix_milan_matcher_policy_evaluate()` currently tests
`policy->values[18] >> 8`, the immutable packed probe high field, instead.

A direct native control with packed high one and accumulated high two therefore
sets `GOODIX_MILAN_POLICY_CALLER_BYPASS`, while the DLL caller executes this
dispatcher. The existing 58-call target-chain replay remains valid for its exact
inputs; it does not prove this separate caller predicate.

The native policy entry now takes accumulated high class explicitly and uses it
for the `flag60 == 2` bypass. Packed-high/accumulated-high controls cover both
bypass and dispatch edges. The frozen target-chain replay remains helper-stage
exact `58/58`, with caller final tuples and contributions exact `60/60`; no
dispatcher helper predicate or mutable recognition-mode rule changed.
