# FUN_18004dea0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18004dea0..0x18004e0db`, fallback broken-level promotion
  classifier.

## Call Graph

- Caller: `FUN_18004ef70`, only while promotion outputs `+4` and `+8` are zero
  and the current history was not newly initialized.
- Substantive callees: `FUN_18004ef90`, `FUN_180051bd0`, `FUN_1800514e0`, and
  `FUN_180051af0`; allocation and free helpers own temporary planes.

## Exact Behavior

This function is called only inside the retained-count-greater-than-20 block,
after `FUN_18004e8c0`, when context `+0x04 == 0`, context `+0x08 == 0`, and the
retained reference was not initialized from the current workspace source on
this call.

It replaces context `+0x04` with
`FUN_18004ef90(calibration_map, qualified_mask, 9504, ..., 9000, ...)`.
The source is workspace `+0x04`, the same 9,504-word calibration map copied into
the retained reference when retained count is zero. The qualified mask is the
same support-age and eroded-valid mask used by `FUN_18004e8c0`.

It then runs `FUN_180051bd0`, `FUN_1800514e0`, and `FUN_180051af0` with
configuration `[400,512]`. The strict output count rewrites context `+0x08` as
2 above 600, 1 above 300, and 0 otherwise.

## Instruction Ranges

- Input setup and `FUN_18004ef90` output write:
  `0x18004dea0..0x18004dfc5`.
- Binary plane construction: `0x18004dfc5..0x18004e039`.
- Strict `300`/`600` promotion count and offset-8 write:
  `0x18004e039..0x18004e098`.
- Caller guard: `0x180050d57..0x180050d8b`.

## Confidence

High for the caller guard, source/mask provenance, output offsets, and strict
thresholds. Vendor names remain unknown.

## Exactness Boundary

- No untranslated helper in this function blocks auxiliary-byte exactness; the
  gradient, walk, and threshold helpers are documented separately.
