# FUN_18002d4b0

## Audit Scope

Production adapter caller of `enrolAddImage` at `0x180001450`. This note was
created before first Ghidra inspection during the independent methodology audit.

## Questions

- Which image, calibration, raw frame, and sensor metadata fields are passed?
- Does the Wine enrollment call preserve production argument semantics?

## Audit Findings

- Production passes the processed image, caller auxiliary object, quality and
  coverage output, retained calibration workspace, retained raw frame, and the
  retained `tCode`, `dacHigh`, and `dacLow` values.
- The current oracle passes a nonnull preprocessor auxiliary, the full
  calibration workspace, live raw frame, and production metadata tuple
  `0x79/0x7d/0xc6`. Its default-init/save/load sequence does not reproduce an
  unknown sensor-ID-qualified persisted calibration state or Windows service
  lifetime.

## Rollback Gates

- `enrolAddImage == 0x83` destroys the whole enrollment session through
  `FUN_18002bea0` and reports adapter error `0x8009`; any other nonzero result
  reports `0x8010` without the one-image rollback.
- After a successful mode-0 insertion, one-image rollback requires all three
  gates: caller cancel flag `param_8 == 0`, caller used count `*param_4 > 2`,
  and a strict bad-capture predicate. Before seven images that predicate is
  `DAT_1800f240c < session[+0x14]`; from seven onward it additionally requires
  `DAT_1800f240d < session[+0x10]`.
- Only that conjunction calls `enrolDeleteImage` and reports `0x8008`. Thus a
  graphless stage accepted by `FUN_180042c30` is not automatically rolled back;
  graph state is not one of the adapter gates.
- Evidence: result split `0x18002d686..0x18002d6d2`; strict threshold branches
  `0x18002d790..0x18002d80f`; rollback call `0x18002d818`.
