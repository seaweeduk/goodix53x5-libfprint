# FUN_18007a850

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18007a850`, `0x18007a850..0x18007abf2`.
- Role: recover relation-graph components and predecessor/root state.

## Call Graph

- Callers: `FUN_180079ef0` and `FUN_18007ac00`.
- Callees: `FUN_1800689a0`, `memset`, and the security-cookie helper.

## Recovered Behavior

It initializes two 50-entry output arrays to `-1`, walks the live triangular
matrix through each feature's `b6` base, and assigns component and predecessor
state. Strong traversal requires a leading value greater than two; a later pass
can attach still-unassigned vertices through any defined relation with
`leading>=0`. Reverse-index reads invert the stored affine before use.

The function demonstrates that zero-leading relations are defined graph edges in
the broader lifecycle even though they are not strong traversal edges. It does
not participate in the type-12 serializer's omission test, which independently
uses `leading>=0`.

## Confidence And Unresolved

- Confidence: medium-high for component outputs, thresholds, and matrix
  direction; decompiler type propagation is unstable but assembly bounds are
  consistent with 50 features.
- Source names for the two output arrays remain unknown.
