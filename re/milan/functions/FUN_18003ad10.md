# FUN_18003ad10

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18003ad10..0x18003afe7`.
- Caller: `FUN_1800392f0` at `0x1800395ce`.
- Callees: `FUN_18003d1a0`, `FUN_18003b6d0`, allocation, and free.

## Signature

```c
int32_t FUN_18003ad10(
  const uint16_t *residual,
  const uint8_t *mask,
  const uint8_t *primary_contrast,
  int32_t rows,
  int32_t columns);
```

## Behavior

The function builds the watershed class map through `FUN_18003d1a0`, retains
only class `2`, and thins that binary map in place through
`FUN_18003b6d0(..., 100)`. It then computes:

```text
maximum = max residual at rows 1..rows-2, columns 2..columns-3
          where class != 0xfe
difference_sum = sum abs(residual[p] - residual[q])
                 for every directional 8-neighbor class-2 pair
                 at rows 3..rows-4, columns 3..columns-4
adjacent_count = number of those directional pairs
denominator = adjacent_count * maximum
numerator = difference_sum * 1000
score = denominator == 0
          ? numerator
          : idiv32(wrap32(numerator + sar32(denominator, 1)),
                   signed32(denominator))
```

The final arithmetic block at `0x18003afab..0x18003afc8` keeps both products and
the rounding addition as dwords. The divisor half is an arithmetic `SAR`,
including negative odd wrapped divisors, before signed `IDIV` truncates toward
zero. A zero divisor returns the wrapped numerator.
Each undirected adjacent pair is normally counted twice because all eight
directions are visited from every retained pixel.

Production commit `362b892a` implements those exact wrap, reinterpretation,
`SAR`, and signed-division semantics. It safely returns failure for the sole
native-hostile `INT32_MIN / -1` form instead of reproducing the DLL fault. The
ordinary sequence-2 control remains `maximum=2291`, `difference_sum=42114`,
`adjacent_count=1435`, and score `13`; transient wrap and negative-odd-divisor
controls close the synthetic arithmetic boundary.

## Sequence-2 Evidence

The production call at `0x1800395ce` was captured before execution without
changing arguments or branches. Replaying this function directly with those
same buffers returns `13` and gives:

```text
maximum=2291
difference_sum=42114
adjacent_count=1435
numerator=42114000
denominator=3287585
quotient=12.810010995913
rounded score=13
```

Input SHA-256 values are residual
`805f78bb6da44a6010dfe6ea61b098439f65b793a7e5dca9e1700740d5c3f2da`,
mask `ee4e6832bc284dd137ee0df1710f3d3d1b0f69e347714c5b02cb694530b7a909`,
and primary contrast
`42fed78fdcb95ae0472910945be1eb94914af03e06292ff9b5d4fbe104d17d4b`.
The class and thinned-map SHA-256 values are respectively
`f6572c0d31ffdec8e4d31bda17efc37c8ecdbbf4f4df8af7686de4da2345c669`
and `02e95f7d7a806c4ab52d4a034b211f66bd65c2a2419c0c4df3bc43139cfc14c`.

Persistent evidence is under
`<evidence-root>/derived/milan-native-capture-20260723/normal-boundary-score-diagnostic-v1/`.

## Confidence

High. The arithmetic is confirmed by disassembly and same-input DLL replay.

No profile-9/type-12 arithmetic behavior remains unresolved. Malformed
dimensions remain outside the native caller contract; the Linux implementation
retains its explicit dimension checks and hostile-division failure.
