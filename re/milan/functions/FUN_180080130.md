# FUN_180080130

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180080130`, `0x180080130..0x18008024e`.
- Role: validate an affine linear part against two fixed geometric bounds.

## Call Graph

- Direct caller on the recovered profile-9/type-12 path: `FUN_180077e20`.
- Direct callees: none.

## Recovered Behavior

It forms the 2x2 product associated with the affine linear matrix, derives its trace and discriminant, rejects negative values, and compares the resulting characteristic bounds against `param_2 << 9` and `param_3 << 9`. Enrollment estimation calls it with `401` and `163`. It returns one only when all strict determinant/discriminant checks pass.

Evidence is the complete leaf body at `0x18008015d..0x18008024d`.

## Confidence And Unresolved

- Confidence: high for exact integer policy and return value.
- Unresolved: whether the two bounds are named scale, singular-value, or deformation limits in source.

## Dependency Unit 5 Failure Contract

The robust estimator calls this leaf only after triangle, coefficient, and
inlier gates, with fixed bounds `401/163`. Return zero rejects the candidate but
does not directly clear estimator outputs. If no other model was accepted,
`FUN_180077e20` later writes final affine zero and leaves residual at
`0x190000`; if a prior model existed, validator rejection preserves it. A direct
validator-reject control records one call and final wrapper count/affine `0/zero`.
