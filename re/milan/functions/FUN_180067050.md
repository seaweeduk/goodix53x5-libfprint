# FUN_180067050

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x180067050`, persistent calibration-map commit.

## Call Graph

- Sole caller: `FUN_180067500` at `0x180068377`.
- Calls matrix smoothing only when the subtype permits it; subtype 12 excludes
  that call.

## Behavior

- Receives the already-rescaled u32 ratio plane from `FUN_180063a80`, the u16
  calibration and application planes, a pointer to the workspace u32 sample
  count, and the packed configuration (width at byte 0, height at byte 4,
  subtype at byte 24).
- Scans every sample, including borders. Profile-9 subtype 12 uses runtime base
  8000 and strict absolute signed deviation `< 6400`. The additional `< 8192`
  admission while the entry count is below 30 requires the subtype bit in
  `0x412000`; bit 12 is clear, so subtype 12 never uses that relaxation.
- An accepted sample becomes
  `(floor((n + 1) / 2) + old * n + candidate) / (n + 1)`.
- Increments the persistent count once even if every sample is rejected and
  caps it at 400. The full-map sum includes retained samples. After the new
  count exceeds the configured threshold, a nonzero rounded full-map mean
  causes every application sample to become
  `(calibration * 8192 + floor(mean / 2)) / mean`, narrowed to u16. A zero
  mean leaves the application plane unchanged.
- `FUN_1800672e0` supplies base 8000, deviation 6400, and count threshold 3
  for the profile-9 subtype-12 configuration with packed word 5 equal to zero.
- Post-normalization smoothing requires both the new count at most 200 and a
  clear subtype bit in `0x473112`. Bit 12 is set, so subtype 12 publishes the
  unsmoothed normalization at every count. No kernel setting or image content
  can enable smoothing on this subtype path.

## Evidence

- Smoothing-exclusion bit test: `0x1800670ab..0x1800670cb`.
- Relaxed-admission subtype test: `0x1800670cf..0x1800670e3`; count and
  deviation conditions: `0x180067119..0x180067132`.
- Strict admission and rounded running average: `0x180067136..0x18006716c`.
- Count increment, rounded full-map mean, cap, and publication predicate:
  `0x18006718e..0x1800671c8`.
- Application-map normalization: `0x1800671e0..0x1800671fa`.
- Count-200 and subtype smoothing bypass: `0x1800671fc..0x180067211`.
