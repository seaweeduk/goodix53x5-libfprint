# FUN_1800749c0

## Identity

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800749c0`, `0x1800749c0..0x180074b90`.
- Role: build the later affine-guided sibling correspondence set.

## Call Graph And Behavior

`FUN_180057900` is the observed direct caller. The function initializes
best/second arrays, invokes `FUN_180075dc0` over the two foreground ranges
`[0,boundary)` and `[boundary,count)`, then calls `FUN_180075a60` with capacity
`42` for type 12 (`31` otherwise). The selected pair workspace is subsequently
filtered by `FUN_18005b900`.

For profile 9/type 12 its configuration is `[23,47,40,38,0,16]`. The final
word, not word 0, is the affine-guided tail-distance admission limit. Thus
`FUN_180075dc0` admits tail distance `<=16`; `FUN_180075a60` then applies ratio
`best*40 < second*38` and its normal spatial/capacity policy.

The five retained-count rows prove the distinction: native's hardcoded `23`
admits six pairs at distance `17/18`; removing exactly those pairs reproduces
all official selected-pair, affine-filter, and sibling-admission counts. See
`FUN_180057900.md` and the external sibling-retained-counts-v1 reference.

## Native Implementation

The native affine-guided path now receives the existing profile matcher
configuration and passes configuration word 5 to its tail scanner. For profile
9/type 12 this is `16`; the separate ordinary cross-class descriptor limit
remains `23`. Focused replay verifies inclusive acceptance through 16, rejection
at 17 and 18, and exact final retained counts `9,6,8,9,5` for all five rows.
No ratio, spatial-selection, affine-filter, promotion, or sibling-admission
behavior changed.

## Confidence And Unresolved

- Confidence: high for the profile-9/type-12 helper sequence, ranges, capacity,
  configuration operands, and retained-count application.
- Unresolved: semantic names and cross-profile values of the six configuration
  words.

## 2026-07-28 Retrospective Correction

Both calls to `FUN_180075dc0` receive explicit stored partition ranges. The
first uses starts zero and ends from enrolled/probe `+0x118`; the second uses
those boundaries as starts and the corresponding record counts as ends. Native
re-derives the boundaries from record `foreground` bytes before entering its
guided scan, so the helper chain is partial for valid contradictory metadata,
record reorder, and flag mutation even though the ordinary threshold and
selection controls remain exact.

The DLL scan also calls `FUN_1800689a0` for its inverse. Native's equivalent
duplicates that operation with a widened determinant. See
`MATCH-CANDIDATE-RETROSPECTIVE.md`.

## 2026-07-28 Native Implementation

The guided native builder now consumes the same explicit stored enrolled/probe
boundaries as the class-filtered builder and delegates inversion to
`milan_match_inverse_affine_s32()`. The two physical ranges, tail limit 16,
ratio, order, capacity, and robust filter remain unchanged. Ordinary identity,
wrapped-singular copy, and signed-division-overflow fail-closed controls pass.

## Matrix Negative Control

This guided sibling builder does not receive the eligibility/distance or layout
matrices used by `FUN_180075220`. Its two `FUN_180075dc0` calls independently
recompute normal and shifted tail distance after geometry checks. The shared
profile value 16 is therefore a common configuration limit, not evidence that
this path consumes or persists either matrix.
