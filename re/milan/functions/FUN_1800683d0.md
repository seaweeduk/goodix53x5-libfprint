# FUN_1800683d0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800683d0`, `0x1800683d0..0x180068659`.
- Role: solve a six-word affine from three source and three destination points.

## Call Graph

- Direct caller on the recovered profile-9/type-12 path: `FUN_180077e20`.
- Direct callee: `__security_check_cookie`.

## Recovered Behavior

It loads three Q8 point pairs, shifts destination coordinates by ten bits for intermediate precision, solves the two affine rows through determinant divisions, narrows division results to signed 32-bit values, then shifts linear terms down by two and computes translations. Degenerate determinants use `INT_MAX` before later shifts rather than returning failure.

Evidence is the determinant and first row at `0x1800684bd..0x180068576`, followed by the second row through `0x18006864c`.

## Confidence And Unresolved

- Confidence: high; signed narrowing and translations are required for exact frozen affine output.
- Type-12 unresolved behavior: none. Degenerate determinants produce the
  documented `INT_MAX`-derived coefficients; `FUN_180077e20` rejects them at its
  coefficient-consistency gates and publishes final no-model affine zero only at
  the estimator epilogue. This solver itself has no status and never owns the
  caller-visible failure affine.
