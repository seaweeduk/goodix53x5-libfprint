# FUN_180074ba0

## Identity

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180074ba0`, `0x180074ba0..0x180074e79`.
- Role: generic affine-guided alternate correspondence scan.

## Call Graph And Behavior

`FUN_180076850` calls this helper twice for type 12. A range descriptor supplies
source begin/end, target begin/end, image dimensions, and geometry thresholds;
the two calls cover the two foreground-class partitions without cross-class
matches. The helper inverts the primary affine, enforces strict inverse
six-pixel borders, forward component differences at most `0x1500`, inclusive
forward far borders, and strict near borders. It then consumes a precomputed
descriptor-distance byte matrix to update best/second candidates.

Native `milan_match_alternate_correspondences()` now preserves the two disjoint
foreground ranges and applies the same geometry arithmetic within each range.
Corrected diagnostic v4 reproduces alternate pair arrays `35/35`, superseding
the former `24/35`; alternate affine and post-orientation masks remain exact
`35/35`. No selector, affine, policy, or threshold behavior changed.

## Confidence And Unresolved

- Confidence: high for type-12 branch role, partition contract, matrix
  orientation, and spatial gates.
- Type-12 unresolved behavior: none. The range fields and bounds are fixed
  below; source-level names are nonsemantic.

The range fields are operationally complete: width, height, enrolled begin/end,
and probe begin/end. Inverse borders are strict six-pixel bounds; forward
component delta is inclusive `0x1500`; forward near border is strict and far
border inclusive. Empty/single/multiple stored partitions plus identity,
singular-copy, and negative inverse controls close the type-12 contract.

## FUN_18005a3c0 Dependency Unit 6

Ghidra operand-level recovery fixes every scan width and order. Inverse-point
products/additions and the two ordered `+0x80; SAR 8` stages are dword operations
at `0x180074c60..0x180074cb1`. Forward products, translation shifts, additions,
first `SAR 8`, wrapped signed residual comparison, and pixel rounding are
`0x180074d21..0x180074dd8`. In particular, residual magnitude is not C `abs`:
the subtract/reverse-subtract sequence remains wrapped dword arithmetic even for
`INT32_MIN`.

Native now reproduces those dword operations explicitly and consumes caller
supplied half-open stored ranges. Invalid ranges or the checked inverse-overflow
case return failure before pair output. Controls cover half-step and dword
overflow boundaries, negative values, strict/inclusive borders, empty and dual
partitions, and physical output order. No descriptor, ratio, collision,
capacity, robust-fit, rank, or promotion rule changed.

## Matrix Consumption

The precomputed descriptor input is the producer's `180x180` distance matrix,
indexed `enrolled_index * 180 + probe_index`. Unlike `FUN_1800770e0`, this
helper consumes the stored distance value after its affine geometry gates; it
does not receive the separate layout matrix. Cross-partition cells remain
`0xff` because the caller invokes this helper over the same two stored physical
ranges. Ownership stays with the enclosing matcher stack frame.
