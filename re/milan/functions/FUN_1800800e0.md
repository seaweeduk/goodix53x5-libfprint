# FUN_1800800e0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800800e0`, `0x1800800e0..0x180080128`.
- Role: apply a six-word Q8 affine to one signed-dword coordinate pair.

## Call Graph

- Direct callers: `FUN_180046040`, `FUN_18006f390`, and `FUN_180071a70`.
- `FUN_18006f390` is the profile-9/type-12 overlap-geometry owner.

## Recovered Contract

For input `(x,y)` and transform `[a,b,tx,c,d,ty]`, the helper writes:

```text
out_x = int32(a*x + b*y + tx + 0x80) >> 8
out_y = int32(c*x + d*y + ty + 0x80) >> 8
```

Each multiply and addition is dword arithmetic. The final right shifts are
arithmetic. Input and output may alias; `FUN_18006f390` transforms all four
corners in place.

For the fixed profile-9 overlap rectangle, the input order is top-left,
top-right, bottom-right, bottom-left:

```text
(0,0), (51,0), (51,43), (0,43)
```

No overflow distinction is reached by the accepted profile-9/type-12 affine
controls. The dword rule remains part of the native contract for independently
constructed inputs.

## Confidence And Unresolved

- Confidence: high for operation order, dword width, rounding, signed shift,
  aliasing, and the profile-9 corner order.
- Unresolved behavior in other callers is outside the profile-9/type-12 scope.
