# FUN_180076af0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180076af0`, `0x180076af0..0x1800770de`.
- Role: least-squares affine refinement over a retained inlier mask.

## Call Graph

- Direct caller: `FUN_18005b900`.
- No material external callees beyond runtime support.

## Recovered Behavior

It builds Q8 normal equations from masked source/target coordinates, solves the two affine output rows through integer cofactors and signed rounded division, then rescans all supplied pairs with squared-distance `<0x64000`. The refined affine replaces the input only when its mean residual is lower and its retained count is at least the input-mask count.

## Confidence And Unresolved

- Confidence: high for matrix construction, replacement predicate, and residual gate.
- Type-12 unresolved behavior: none. Exact wrap and narrowing behavior is
  recorded below; inputs outside the bounded feature domain are not valid.

The complete audit records arithmetic rather than assuming mathematical
overflow freedom: normal-equation/cofactor operations are wrapping signed
64-bit, normalization adds `0x80` before arithmetic `>>8`, signed coefficient
division rounds by half denominator and truncates through `IDIV`, and outputs
narrow to dword. Production coordinates are bounded unsigned 16-bit. No
type-12 behavior remains unresolved.

## Dependency Unit 5 Failure Contract

This helper has no status and commits only the complete six-word refined affine.
Denominator zero returns without writing. A candidate also leaves the input
unchanged unless refined residual is strictly lower and refined inlier count is
at least the input-mask count. The wrapper neither changes its retained count nor
publishes refinement status. Four singular direct calls preserve zero and three
poison patterns exactly; a wrapper gate control proves calls occur only for
retained `>3` and residual `>0x4000`.
