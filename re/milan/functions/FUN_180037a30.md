# FUN_180037a30

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180037a30..0x180037b05`.
- Role: remove the last live gallery feature and repair graph/count state.

## Call Graph

- Callers: exported `enrolDeleteImage` and `FUN_180041570`.
- Callee: `FUN_180047550`.

## Behavior

The helper rejects null or empty galleries. It reads the rejected feature's
relation-row base from `b6/+0x114`, resets every relation slot from that base to
the old logical relation count to unset identity, rewinds relation count
`+0x24`, and decrements feature count `+0x1c` at
`0x180037a59..0x180037abe`. The rejected feature object itself is not cleared;
it is merely outside the decremented live count.

If the removed physical index equals `f2/+0x87d8`,
`0x180037ac1..0x180037af5` clears `f5/+0x87e4` and every remaining feature's
active `b5/+0x110`. It does not choose a replacement reference and leaves raw
`f2/f3/f4` unchanged. If the removed feature is not `f2`, the helper preserves
`f2/f5`, prior-feature active markers, and all surviving relation slots,
including graph activation, reference, and relation mutations introduced while
the rejected feature was tentatively live.

The helper always calls `FUN_180047550` at `0x180037af7..0x180037afa`, after
relation/count rewind and any reference-deletion clearing. Type 12 passes that
wrapper's type gate and enters `FUN_180047120`, which recomputes every surviving
feature's packed `bb/+0x128` and internal overlap/neighbor count `+0x148` from
surviving masks and the retained graph. Both fields are initialized for every
live feature before active-feature residual processing; they are not restored
from pre-insertion values. The removed physical feature lies outside the
decremented live count and is not normalized.

Graphless deletion does not require a valid reference index. Constructor state
keeps `f2/+0x87d8 == -1`, `f5/+0x87e4 == 0`, and every live `b5/+0x110 == 0`;
this helper preserves that `f2` value and still invokes normalization. Each
inactive survivor takes `FUN_180047120`'s initialized completion path, producing
`bb=2288` and `+0x148=0` without indexing `f2`. Reference lookup belongs only to
active-feature processing. `FUN_180037a30` ignores the normalizer wrapper's
return and returns success after the call.

The helper does not restore anti-fake pair scores at feature `+0x1ab4`, does not
rewrite order array `+0x87e8`, and does not touch `fa/fb`, session overlap
metrics, session used/progress, or preprocessing state. Consequently the
deleted order position remains stale, pair-score updates on prior features
remain committed, and session count/progress rewind belongs to
`enrolDeleteImage`.

## Evidence And Confidence

- Relation/count rewind: `0x180037a59..0x180037abe`.
- Reference deletion branch: `0x180037ac1..0x180037af5`.
- Type-12 normalization call: `0x180037af7..0x180037afa`.
- Unconditional success after normalization: `0x180037aff..0x180037b05`.
- Confidence: high for the complete enrollment rollback mutation boundary.

## Remaining Uncertainty

The semantic vendor name for the anti-fake score at `+0x1ab4` remains
unresolved; its retention boundary is resolved.
