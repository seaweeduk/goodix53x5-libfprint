# FUN_18004ef70

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: dispatch stub `0x18004ef70..0x18004ef8e`, with the profile-9
  implementation at `0x1800508a0..0x180050f11`; profile broken-level
  classifier and state coordinator.

## Call Graph

- Caller in this milestone: `FUN_18006b290`.
- Substantive callees include `FUN_18004ee80`, `FUN_18004d510`,
  `FUN_18004e8c0`, `FUN_18004dea0`, `FUN_18004eba0`, and `FUN_18004ff40`.

## Behavior

- Builds profile-specific broken-level maps, maintains process-global history,
  writes the class image consumed by `FUN_18006b290`, and returns multiple
  state flags that can promote metadata mode independently of class counts.
- Profile 9 uses threshold groups including `800/768/384/20`,
  `240/640/448/20`, and `350/400`; these are classifier internals, not direct
  replacements for `FUN_18006b290` mode thresholds.
- The profile-9 invocation order relevant to the three auxiliary bytes is:
  import retained state once; clear the class plane; prepare `difference` and
  eroded-valid inputs with `FUN_18004ee80`; initialize/update retained reference
  state unless the selected-plane entry value is 2; run `FUN_18004d510` to write
  primary state `+0x00`; when retained reference count exceeds 20 run the
  secondary path that writes `+0x04` and `+0x08`; run `FUN_18004eba0` to write
  directional state `+0x0c`; serialize retained state.
- More precisely, secondary state remains zero while retained reference count
  is at most 20. Above 20, `FUN_18004e8c0` writes it from retained reference. A
  zero result is replaced by `FUN_18004dea0` from the calibration map only
  when context `+0x08` is also zero and this call did not just initialize the
  retained reference.
- Native retained-reference initialization occurs when its count is zero and
  workspace sample count is greater than one: copy the 9,504-word calibration
  map, set retained count to `min(sample_count, 21)`, fill every support-age byte
  with that count, and mark the call newly initialized. Otherwise values 0/1 of
  the selected-plane entry value enter `FUN_1800512c0`; value 2 skips both
  initialization and update. Exported `preprocessor` supplies entry 0 for
  ordinary WBF enrollment and identify calls and entry 2 only when exported
  argument 7 equals 1.
- Classifier context offsets are `+0x00` primary histogram state, `+0x04`
  secondary histogram state, `+0x08` secondary count class, `+0x0c`
  directional state, `+0x10` selected-plane entry value, `+0x14` subtype,
  `+0x18` active contrast-mask count, and `+0x20` class-plane pointer.

When retained reference count exceeds 20 and both secondary outputs remain
zero, `FUN_18004dea0` reruns histogram and count classification against the
calibration map. A positive count class can subsequently promote metadata mode
0 to mode 5 in `FUN_18006b290`.

## Instruction Ranges

- Profile dispatch: `0x18004ef70..0x18004ef8e`. Profile constants and workspace
  setup begin at `0x1800508a0` through the first `FUN_18004ee80` call.
- Class-map/state pipeline spans the remainder of the function through
  `FUN_18004ff40`.

## Confidence

High for ownership, output role, call ordering, and context offsets; medium for
individual state semantics.

Non-profile-9 classifier variants remain out of scope.
