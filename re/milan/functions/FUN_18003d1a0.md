# FUN_18003d1a0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18003d1a0..0x18003d248`.
- Caller: `FUN_18003ad10`.
- Callee: `FUN_18003c680`.

## Signature

```c
void FUN_18003d1a0(
  const uint8_t *image,
  const uint8_t *mask,
  uint8_t *classes,
  int32_t columns,
  int32_t rows);
```

## Behavior

The initial classification is:

```text
image < 26   -> class 2
image >= 231 -> class 1
otherwise    -> class 0
mask == 0    -> class 0xfe, overriding the image class
```

It then calls `FUN_18003c680(image, classes, columns, rows)` to assign the
remaining in-mask zero pixels by watershed propagation. `FUN_18003ad10` later
uses final class `2` as its boundary topology and excludes class `0xfe` from its
maximum-residual scan.

## Evidence

- Threshold comparisons are at the entry loop (`0x1a` and `0xe7`).
- Mask override writes `0xfe` before the watershed call.
- Sequence-2 same-input output SHA-256 is
  `f6572c0d31ffdec8e4d31bda17efc37c8ecdbbf4f4df8af7686de4da2345c669`.

## Confidence

High for the threshold and mask rules; high for the active call chain.

## Unresolved

- The vendor terminology for classes `1`, `2`, `0xfd`, `0xfe`, and `0xff` is
  unknown.
