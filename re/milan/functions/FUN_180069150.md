# FUN_180069150

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x180069150..0x180069345`, masked row-transition
  metric used for profile-9 candidate selection.

## Call Graph

- Profile-9 caller: `FUN_18006e7f0`, once for primary and, after primary
  reaches the selection threshold, once for refined.
- No substantive callees.

## Behavior

- The frame is an unsigned byte plane. Every nonzero mask byte admits the
  corresponding frame byte to a 32-bit valid sum and increments the valid
  count. The replacement for excluded pixels is
  `valid_sum / (valid_count + 1)`, using integer division.
- The first and last rows containing a nonzero mask byte define an inclusive
  active-row interval. Each row in that interval replaces excluded pixels with
  the value above, sums all columns, and stores the Q8 row average
  `(row_sum << 8) / columns`.
- It sums absolute differences between adjacent Q8 row averages. When the
  active interval spans more than one row, the return value is
  `(((rows - 1) * difference_sum) / (last_row - first_row)) >> 8`.
  Division occurs before the final right shift. All production operands are
  nonnegative, so these operations round down.
- An empty mask and a mask confined to one row both return zero. The production
  profile-9 selection mask contains only `0` and `255`, but this helper treats
  every nonzero byte as admitted.

## Evidence

- Valid sum/count and excluded-pixel replacement:
  `0x1800691ad..0x1800691e8`.
- First and last active rows: `0x1800691ef..0x180069252`.
- Inclusive Q8 row averages: `0x180069252..0x1800692c7`.
- Adjacent differences and normalized return: `0x1800692cc..0x18006931b`.

## Confidence

High for the profile-9/type-12 arithmetic and row domain.

## Unresolved

- The vendor's semantic name for the metric is unknown.
