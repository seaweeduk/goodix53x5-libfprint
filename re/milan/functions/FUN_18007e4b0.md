# FUN_18007e4b0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18007e4b0`, gradient-based image-mask builder.

## Call Graph

- Caller in this milestone: `FUN_18006d540` immediately before
  `FUN_18006e7f0`.
- Feature-extraction caller: `FUN_18004ae70` at `0x18004b25b`; its output is
  the second matrix passed to `FUN_180048260` at `0x18004b4ba`.
- Final-quality caller: `FUN_180044530` through `FUN_180044300`.
- Substantive callees include `FUN_18007be70`, `FUN_18007d530`,
  `FUN_18007e790`, and `FUN_18007e960`.

## Behavior

- Applies the fixed five-tap Gaussian, asymmetric Sobel passes, half-L1
  gradient magnitude, and a reflected `15x15` box filter already recovered for
  index-0 quality coverage.
- Writes the requested mask byte where the filtered gradient is strictly above
  the caller's threshold and zero elsewhere.
- Returns `floor(selected_count * 0x10000 / pixel_count)` with signed-32
  count/numerator/division. The fixed 9,504-pixel product is in range.
- The profile-9 preprocessing caller uses the primary contrast image, threshold
  `100`, and mask value `255`. `FUN_18006e7f0` uses this generated mask for both
  candidate metrics and correlation; it does not use the valid or profile
  rendering masks for subtype 12.
- Final image quality calls the same filter on the selected 108x88 output at
  threshold `120`. The comparison is strict `filtered > 120`. That caller needs
  the returned raw Q16 coverage and does not request a mask write.
- The profile-9/type-12 extraction call writes byte `1` for every selected
  `104x88` validity position.

For the final-quality call, the byte image is separably filtered by the fixed
five coefficients, asymmetric Sobel X/Y values are converted to
`floor(abs(x)/2) + floor(abs(y)/2)`, and each reflected 15x15 sum is multiplied
by 291 and shifted right 16. If `S` pixels are strictly above 120, this function
returns `R = floor(S * 65536 / 9504)` and the quality combiner publishes
`floor(R * 100 / 65536)` as coverage. Consequently coverage 4 and 6 correspond
to ordinary nonzero gradient sets, not failed rendering.

## Instruction Ranges

- Filter pipeline: function entry through the `FUN_18007d530` call.
- Strict threshold and optional mask write: final loop before cleanup.
- Profile-9 call setup at `0x18006db36..0x18006dba5` passes threshold `100`,
  mask value `255`, and the generated image data to `FUN_18006e7f0`.
- Final-quality setup at `0x18004466c..0x180044682` passes threshold `120` and
  no output matrix.
- Profile-9 table entry at `0x1800e46b4` has the bit-2 selector clear, so
  `FUN_18006e7f0` retains this generated mask rather than replacing it with the
  profile rendering mask.

## Confidence

High for the profile-9 preprocessing and extraction call-site provenance,
dimensions, mask bytes, and thresholds.

## Unresolved

- Other callers and threshold variants are outside this milestone.
