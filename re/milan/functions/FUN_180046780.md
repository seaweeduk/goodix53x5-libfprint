# FUN_180046780

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180046780..0x1800469ed`.
- Role: measure probe mask area left uncovered by active gallery features other
  than one proposed replacement target.

## Call Graph

- `FUN_180045530` calls it for full-capacity primary study-candidate validation.
- `FUN_180042840` also uses it during type-12 enrollment graph integration.
- Relevant callees are `FUN_180047580`, `FUN_180071370`, `FUN_180072600`,
  `FUN_180068660`, `FUN_180068700`, `FUN_180068860`, `FUN_1800689a0`, and
  `FUN_180068b70`.

## Contract

The helper clones the probe mask at live feature `+0x140`. It walks the live
gallery and skips:

- features with `b5/+0x110 == 0`;
- the proposed replacement target;
- features with `ba/+0x124 == 5`.

For each remaining feature it obtains the relation from the live reference at
template `+0x87d8` to that feature. A feature above the reference has its stored
feature-to-reference affine inverted; a feature below the reference uses its
stored reference-to-feature affine directly; equality retains identity. The
lookup reads the six affine dwords without testing the slot's leading rank, so
an unset slot contributes its initialized identity affine.

The helper composes this reference-to-feature affine after the supplied
probe-to-reference evidence transform, yielding probe-to-feature. In profile-9
half-resolution mode it arithmetic-right-shifts both translations by one. It
rasterizes the transformed `52x44` rectangular footprint with
`FUN_180071370` and removes covered positions from the cloned probe mask through
`FUN_180072600`.

`FUN_180047580` counts the remaining set mask bits. A count below 20 returns as
zero. `FUN_180045530` compares this result with candidate live `+0x128`; its
separate aged-template `+20` adjustment occurs after this helper returns.

## Arithmetic

Affine composition in `FUN_180068860` sign-extends dword coefficients, computes
each pair of products and their wrapped sum in 64-bit registers, arithmetic-
right-shifts the sum by eight, and stores the low dword. Translation addition is
a low-dword add after that shift.

`FUN_180071370` then uses low-dword multiply/add for transformed coordinates,
wrapped dword footprint bounds and area accumulation, and signed bounds
comparisons. Its line-intersection divisions are signed and truncate toward
zero. Profile-9 translation halving is an arithmetic right shift. The returned
residual and the caller's wrapped `+20` result are compared as signed dwords.

## Address Ranges

- Clone/setup: `0x1800467b0..0x180046820`.
- Active/target/state exclusions: `0x180046850..0x180046871`.
- Relation direction and composition: `0x180046877..0x18004693f`.
- Half-resolution clipping and removal: `0x180046941..0x180046998`.
- Final residual and cleanup: `0x1800469ac..0x1800469c2`.
