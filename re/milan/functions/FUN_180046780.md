# FUN_180046780

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180046780..0x1800469ed`.
- Role: measure probe mask area left uncovered by active gallery features other
  than one proposed replacement target.

## Call Graph

- Callers: `FUN_180045530` for full-capacity study candidate validation and
  `FUN_180042840` for type-12 enrollment graph integration.
- Important callees: `FUN_180047580`, `FUN_180071370`, `FUN_180072600`, and
  affine/matrix helpers `FUN_180068660`, `FUN_180068700`, `FUN_180068860`,
  `FUN_1800689a0`, and `FUN_180068b70`.

## Recovered Behavior

The helper clones the probe's packed mask at live feature `+0x140`. It then
walks the gallery and skips inactive `b5 == 0` features, the proposed
replacement target, and features whose state at `+0x124` is 5. For every other
feature it obtains the canonical relation to the live reference at template
`+0x87d8` in the reference-to-feature direction. For `feature > reference`, the
stored feature-to-reference affine is inverted; for `feature < reference`, the
stored reference-to-feature affine is copied; equality retains identity. It then
composes that reference-to-feature affine after evidence probe-to-reference
transform `+0x66c`, producing probe-to-feature, halves translations in profile-9
mode, clips the transformed footprint through `FUN_180071370`, and removes it
from the cloned probe mask through `FUN_180072600`.

The exact lookup/direction block is `0x180046877..0x180046916`. It reads only the
six affine dwords at slot `+4` and never tests the leading rank. An omitted slot
therefore supplies its decoder-initialized identity affine. Current source's
`goodix_milan_template_reference_transform(..., reverse=1)` has the same
reference-to-feature result and missing-edge identity, while rejecting a
graph-unestablished state before lookup.

`FUN_180047580` returns the remaining active-bit count, except that counts below
20 normalize to zero. This is the value compared with candidate live `+0x128`
by `FUN_180045530`; the separate `+20` aged-template adjustment occurs in the
caller.

The profile-9 policy arithmetic remains dword-exact through this path. Composed
transform coordinates use wrapped multiply/add intermediates, translation
halving is signed arithmetic shift, footprint bounds and removed-area
accumulation wrap as 32-bit values, and the final residual comparison is signed.
The caller's aged residual `+20` wraps before its signed comparison. Production
commit `89dd6c43` preserves these rules in the clean-room footprint and policy
helpers.

`FUN_180042840:0x1800429bf` reuses the same primitive after enrollment graph
integration when the graph is established and the newly added feature is
active. It supplies the accumulated new-feature-to-reference transform, excludes
the final feature index, stores the residual at enrollment output `+0x20`, and
multiplies it by four in half-resolution mode. This second caller confirms that
the helper consumes only the supplied live graph, probe mask, transform, and
excluded index; it has no hidden study-policy state.

## Evidence And Confidence

- Clone/setup: `0x1800467b0..0x180046820`.
- Active/target/state exclusions: `0x180046850..0x180046871`.
- Relation direction and composition: `0x180046877..0x18004693f`.
- Half-mode translation adjustment, clipping, and removal:
  `0x180046941..0x180046998`.
- Final residual count and cleanup: `0x1800469ac..0x1800469c2`.
- Enrollment caller: `FUN_180042840:0x1800429a3..0x1800429d2`.
- Confidence: high for data flow, exclusions, direction handling, and returned
  metric. Exact source-level names for the mask and residual metric are unknown.
