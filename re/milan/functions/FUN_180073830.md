# FUN_180073830

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180073830`, leaf function.
- Callers include `FUN_180055a40` and `FUN_18004ae70`.

## Behavior

The function reads three consecutive bytes and zero-extends them into three
32-bit output words. It has no branch and no meaningful return value. In
`FUN_18004ae70`, the source is the preprocessor auxiliary workspace passed to
`identifyImage`. The three words become `FUN_180048260` configuration `+0x14`,
`+0x08`, and `+0x18` respectively: current class/mode seed, state/ring selector,
and one-step high-class promotion input. The same bytes later have independent
matcher context uses, including `FUN_18005b440`.

Current source calls byte 0 `primary_histogram_state`. That operational name
does not imply packed-`c7` low-class ownership: the packed low class is decoded
separately from entry metadata by `FUN_180070d90` and never comes from this
three-byte leaf.

In `FUN_180055a40`, the source is the live probe's `+0x158` pointer and the
three dword outputs begin at `RBP+0x1f8`. The first output is copied to
`RSP+0x68` at `0x180055cf6..0x180055d08`. It becomes state word 1 for
`FUN_180058700`, gates queue eligibility as `< 3` at `0x180056d21`, and gates
no-direct/fallback entry as `< 2` at `0x180056d91`. It is distinct from both
packed-`c7` low class at `RBP+0x1ac` and the matcher status counter at
`RBP-0x54`.

For expanded identify selector
`1826743d-e6ff-452d-b0da-7e2caf6ce7fe/identify/19`, the live triplet is
`[2,0,1]`, while packed `c7=0x200` decodes to low/high `0/2`. The first byte's
value two causes the terminal zero return before `FUN_18005d5f0`; no
`FUN_180058700` call occurs for either gallery row.

## Confidence

Confidence is high from the complete five-line leaf body and caller assembly.
Vendor semantic names for the byte fields remain unresolved.
