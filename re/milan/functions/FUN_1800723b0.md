# FUN_1800723b0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800723b0`, `0x1800723b0..0x1800725f2`.
- Role: compute forward overlap of expanded feature-validity masks.

## Call Graph

- Direct caller: `FUN_18005a3c0`.
- Relevant callees: `FUN_1800721a0` and `FUN_180068b70`.

## Recovered Behavior

It expands both packed masks, forward-maps every active source pixel through the Q8 affine, and counts destinations active in the second mask. Mode zero uses full resolution; nonzero mode halves affine translations, returns four times the raw count, and preserves the percentage. Percentage rounding is `(area/2 + count*256)/area`.

## Confidence And Unresolved

- Confidence: high; row 207 is exact at percent/count `50/1772` before caller scale `246/256`.
- Unresolved: none for the candidate promotion input.

## Caller Transform Ownership

The sequence-197 audit resolves an important caller contract. At
`FUN_18005a3c0:0x18005a819..0x18005a840`, argument 3 is the caller's currently
ranked output transform. It is not necessarily the alternate transform that
produced the larger retained count. Thus `197/2 f6` correctly evaluates the
retained primary affine, yielding raw percentage/count `108/3868` and scaled
percentage `103`; the larger-count alternate affine must not be substituted.

Current native computes the exact helper arithmetic but supplies the alternate
affine unconditionally at this call site, producing scaled `107` and crossing
the separate promotion predicate. The helper itself remains exact; the defect is
caller transform ownership.

The complete direct controls cover both rank owners independently of count, all
five caller promotion terms, and every strict boundary. The call always uses the
currently ranked affine and mode 1; its percentage is then scaled by signed
dword `scale*percent >> 8`. No target-facing behavior remains unresolved.

## FUN_18005a3c0 Dependency Unit 1

This helper is not directly reached by the below-three alternate family:
`FUN_18005b900` returns alternate filtered count zero, alternate-path entry
proves primary count at least one, and the strict larger-count gate at
`0x18005a806` fails. A poisoned alternate rank can still taint the ranked affine
used by later units, so unit 1 must prevent rank publication before this helper
or any later ranked-affine consumer can run.

## FUN_18005a3c0 Dependency Unit 3

The native call site now consumes `MilanMatchForwardOverlapResult`, initialized
from the complete unit-2 ranked result. Its affine, rank owner, and associated
owner count are copied together before this helper runs. For a larger alternate
that loses or ties rank, this therefore calls with the primary affine and retains
the primary owner-count association; for an alternate rank winner it calls with
alternate state. The helper's raw percentage/count and scaled percentage never
take ownership from the separate local alternate topology result.

Direct identity, translated, nonidentity, negative, and packed-mask edge
controls observe all three predicted primary-ranked forward changes and no
alternate-ranked change. Equal/lower-count and invalid alternates suppress the
call. No helper arithmetic changed.

## FUN_18005a3c0 Dependency Unit 4

The unit-3 owner stream is unchanged. Unit 4 consumes its scaled percentage only
after strict larger alternate count, then combines it with local-alternate
topology/count in the five exact promotion terms. It does not repurpose the
forward owner count as retained count or repair count. Low-dword signed scaling
by 246 is now explicit in native code, including negative/wrapping controls;
ordinary helper arithmetic and affine direction are unchanged. All 20 adjacent
promotion-boundary controls pass, and sequence `197/2 f6` retains raw/scaled
`108/103` and rejects promotion exactly.
