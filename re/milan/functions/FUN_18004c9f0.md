# FUN_18004c9f0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18004c9f0`, `0x18004c9f0..0x18004cac2`.
- Role: convert the descriptor portion of one live type-12 record to its packed
  representation in place.

## Call Graph

- Sole code caller: `FUN_180040a70` at `0x180040cf9`.
- Callees: only the compiler security-cookie check.

## Transform

The input record has two eight-byte descriptor halves at `+0x10..+0x17` and
`+0x18..+0x1f`. For each byte index `j`, let `a` and `b` be the corresponding
bytes and define:

```text
ab = (a & 0xf0) | (b & 0x0f)
ba = (b & 0xf0) | (a & 0x0f)
order = [0, 1, 1, 0, 1, 0, 0, 1]
```

The helper writes `[ba, ab]` when `order[j] == 0`, otherwise `[ab, ba]`, to
the consecutive pair at `+0x10 + 2*j`. It preserves the dword at `+0x20` and
zeros the omitted dword at `+0x24`; it does not change coordinates,
orientation, foreground, strength, or the eight-byte tail at `+0x28`.

`FUN_180040a70` copies each 56-byte live record to a stack temporary, calls this
helper, then emits transformed bytes `+0x10..+0x1f`, original bytes
`+0x20..+0x23`, and original bytes `+0x28..+0x2f`. Record bytes `+0x24..+0x27`
remain outside the 32-byte packed representation.

## Decoder Asymmetry

The type-12 branch of `FUN_18003d6c0` copies packed descriptor bytes `+4..+19`
directly to live bytes `+0x10..+0x1f`; it does not call this helper or invert the
nibble transform. This is the DLL's live-object convention after unpack, not a
claim that this encoder is idempotent. Current source uses the explicit inverse
when decoding so a packed record can be decoded and re-encoded byte-for-byte,
while fresh extracted records use the transform above.

## Confidence

Confidence is high from the complete helper body and its sole call site in the
type-12 serializer branch.
