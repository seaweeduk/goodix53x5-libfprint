# FUN_180068860

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180068860`, `0x180068860..0x180068996`.
- Role: compose two six-word Q8 affine transforms and normalize the linear part.

## Call Graph

- Direct callers include `FUN_180041f60`, `FUN_180043110`, and numerous matching/study helpers.
- Direct callee: `FUN_1800687c0`.

## Recovered Behavior

The six signed words are the Q8 homogeneous matrix
`[[a,b,tx],[c,d,ty],[0,0,256]]`. For arguments `first=A` and
`second=B`, the output direction is `A after B`, or matrix product `A*B`:

```text
r0 = sar64(B0*A0 + B3*A1, 8)
r1 = sar64(A0*B1 + B4*A1, 8)
r2 = sar64(B2*A0 + B5*A1, 8) + A2
r3 = sar64(B0*A3 + B3*A4, 8)
r4 = sar64(A3*B1 + A4*B4, 8)
r5 = sar64(A3*B2 + A4*B5, 8) + A5
```

Every source word is sign-extended from 32 bits. Each multiply is signed
64-bit, each pairwise add wraps at 64 bits, and `sar64` is an arithmetic right
shift, so a negative fractional value rounds toward negative infinity. The
shift result narrows to its low signed 32 bits; translation addition is then a
32-bit wrapping add. There is no saturation or overflow check. The six raw
words are written before `FUN_1800687c0` projects the linear four words to a
normalized rotation; translations are not changed by normalization.

All source words are loaded before output writes, so output may alias either
input. Any null input or output produces no write.

Evidence is the complete function, especially composition at `0x180068899..0x18006897f`.

Direct controls against the pinned DLL cover identity on both sides,
noncommuting translation/rotation order, negative values, exact positive and
negative half steps (`0/-1` after `sar`), wrapped 64/32-bit intermediates,
aliasing, and two normalized hops. All are exact against the width-controlled
model. The producer control also proves sequence-16 enrollment transforms:

```text
compose([256,-4,-5208,4,256,-29266],
        [205,-76,21295,69,251,-188])
  = [243,-81,16089,81,243,-29122]

compose([252,46,-13736,-46,252,-12480],
        [205,-76,21295,69,251,-188])
  = [255,-31,7192,31,255,-16492]
```

Artifacts are under
`<evidence-root>/derived/milan-native-capture-20260723/relation-transform-arithmetic-v1/`.

## Confidence And Unresolved

- Confidence: high; matrix order, every width/wrap point, signed shift,
  normalization timing, aliasing, and all six outputs are direct-control exact.
- Unresolved: no unresolved behavior on exercised inputs.

## Native Implementation

The relation module now performs pairwise products/additions through unsigned
modulo-`2^64` bit patterns, narrows the arithmetic shift at the recovered point,
wraps translations at 32 bits, normalizes every hop, and loads both inputs before
writing output. Frozen controls are exact `30/30` across the combined arithmetic
contract, including both composition alias directions and overflow cases.
