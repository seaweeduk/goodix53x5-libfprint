# FUN_180042610

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180042610`, `0x180042610..0x180042738`.
- Role: initialize the first enrollment feature and its identity relation.

## Call Graph

- Direct caller: `FUN_180042c30`.
- Direct callee: `FUN_1800422e0`.

## Recovered Behavior

It deep-copies the first feature into the next template slot, writes an unset relation record with affine identity `[256,0,0,0,256,0]`, advances relation and feature counts, copies selected feature metadata, initializes enrollment-owned lifecycle fields, conditionally preserves packed scalar `c0` at live `+0x14c`, and increments progress by 100.

The exact first-stage writes are: relation slot 0 becomes unset identity and
live relation count `+0x24` becomes 1; copied `b7/+0x118` and `b8/+0x11c` are
retained; `ba/+0x124`, `bc/+0x12c`, `bd/+0x130`, and `be/+0x134` become zero;
feature count increments last. `bb/+0x128` is not copied by `FUN_1800422e0` and
is not written here, so a newly allocated production destination retains zero.
The source extractor supplies zero `b5/b6`; this helper does not independently
clear those two fields.

Packed scalar `c0/+0x14c` survives only when bit 0 is set and
`(value & ~1) > 1000`; otherwise it is zeroed. Current packed-singleton
reconstruction is exact for natural extractor output but must not infer that an
arbitrary saved singleton's lifecycle or `bb` values are native first-stage
inputs. Optional `c7/+0x150` is a separate extractor-classification field and is
copied unchanged.

The unset identity record establishes the generic state distinction used later:
identity affine does not make a relation defined; `leading=-1` is the omission
sentinel. Append and retained refresh change a slot to `leading=0` when they
materialize an edge.

Evidence is visible across `0x18004262e..0x180042738`; the identity relation is written at template `+0x1b8 + relation_index * 0x1c`.

## Confidence And Unresolved

- Confidence: high for exact first-stage field ownership and write order.
- Unresolved: precise meaning of the tagged `+0x14c` field.
