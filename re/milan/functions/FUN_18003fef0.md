# FUN_18003fef0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18003fef0..0x180040305`.
- Role: post-decode live-template reconciliation and allocation shrink.

## Call Graph

- Sole code caller: `FUN_18003e3a0` at `0x18003e891`.
- Callees: `FUN_180040700`, `FUN_180037b10`, `FUN_180068860`,
  `FUN_1800689a0`, `FUN_180047ae0`, `memset`, and the security-cookie helper.

## Profile-9/Type-12 Behavior

The helper reconciles decoded and context-derived geometry/count limits, frees
surplus preallocated feature owners, rewrites live feature ordinals, restores a
validated order prefix, and reconciles graph state before later normalization.
For type 12 it reads `f2/+0x87d8` and `f5/+0x87e4`, but never reads `f3/f4` or
queue state/counter.

When graph state is nonzero and the decoded reference reaches the current count
boundary, it counts active `b5` features. Missing or singleton active state
clears `f5` and sets `f2=-1`; otherwise it uses `f2` to route/combine graph
relations. The path does not provide a general range check before every indexed
feature access.

## Evidence And Confidence

- Graph-state test and reconciliation: `0x18003ff94..0x1800400c5`.
- Surplus owner cleanup and count/order rewrite:
  `0x1800400d0..0x1800401dc`.
- Unit-6 direct controls show valid stale `f2=3,f5=0` survives, while an
  out-of-range stale reference causes an access fault during the complete fresh
  unpack tail. Exact fault ownership between this helper and the immediately
  following normalizer is not isolated.
- Confidence: high for field accesses and ownership; medium for source-level
  intent of every legacy reconciliation branch.

## Remaining Uncertainty

The optional decode context is a nullable unsigned feature-allocation cap
consumed through `FUN_180040700`; null selects the subtype default. Legacy
type-specific record normalization is outside profile-9/type-12 scope.
