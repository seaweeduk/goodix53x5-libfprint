# FUN_18003fef0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900.
- Entry/body: `0x18003fef0..0x180040305`.
- Role: post-decode live-template reconciliation and allocation shrink.

## Call Graph

- Caller: `FUN_18003e3a0` at `0x18003e891`.
- Callees: `FUN_180040700`, `FUN_180037b10`, `FUN_180068860`,
  `FUN_1800689a0`, `FUN_180047ae0`, `memset`, and the security-cookie helper.

## Profile-9/Type-12 Limits

`0x18003ff1d..0x18003ff82` loads decoded maximum-feature and record-limit fields
and calls `FUN_180040700` to select their live values. Type 12 selects table
entry zero at `0x180040800`, which dispatches to `0x180040765`: both record
limits are literal 150. The optional decode context caps feature capacity only;
it does not lower either record limit. Null context preserves the decoded
maximum feature count through `0x1800407ba`, rather than replacing it with the
subtype's default feature capacity.

`0x18004022a..0x180040240` writes the reconciled maximum feature count to `+0x20`
and both record limits to `+0x14/+0x18`. This runs whether or not the decoded
live count needed shrinking. Every ordinarily unpacked type-12 candidate
therefore supplies the same 150-record configuration to identify extraction
and replacement, independently of its encoded record-limit words.

## Count And Graph Reconciliation

Only when decoded live count exceeds the selected maximum does the helper free
surplus feature owners, rewrite remaining feature ordinals, restore the order
prefix, and reconcile graph state. It reads `f2/+0x87d8` and `f5/+0x87e4`, not
`f3/f4` or queue state/counter.

With nonzero graph state and reference at or above selected maximum minus one,
it counts exact-one `b5` features in the retained prefix. Missing or singleton
active state clears `f5` and sets `f2=-1`; otherwise it reanchors through the
last active prefix feature. The path does not provide a general range check
before every indexed feature access.

- Graph-state test and reconciliation: `0x18003ff94..0x18004010e`.
- Ordinals, surplus-owner cleanup, count and order rewrite:
  `0x18004010e..0x180040228`.
- Type-12 record-angle adjustment and per-record mode-zero transformation:
  `0x180040243..0x1800402b9`; these do not change feature record counts.
