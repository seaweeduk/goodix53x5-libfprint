# FUN_18007be70

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18007be70`, typed image resize/filter dispatcher.

## Call Graph

- Profile-9 contrast caller: `FUN_18006cbc0` with selector 10.
- Profile-9 auxiliary-map caller: `FUN_180063a80` at `0x180063c50` with
  selector 9.
- Additional extraction caller: `FUN_18004ac40`.
- Substantive callees: `FUN_18007d800`, `FUN_18007c610`, descriptor resize and
  allocation cleanup helpers.

## Behavior

- Validates two 16-bit image descriptors, resizes the destination descriptor if
  needed, constructs a selector-specific filter context, executes it, and
  restores descriptor types. Profile 9 contrast passes selector 10 and
  same-sized images.
- The profile-9 `FUN_180063a80` path passes selector 9. Its symmetric Q16 kernel
  is `7869,15328,19142,15328,7869` (table entry `0x1800f2b50`). The weighted
  separable backend uses reflect-101 borders, unsigned accumulation, and
  truncating `>>16` horizontal and vertical passes.

## Evidence

- Descriptor validation and resize: function entry through the
  `FUN_180068660` calls.
- Filter construction/execution: `FUN_18007d800` then `FUN_18007c610`.
- Selector-9 construction call: `0x180063c21..0x180063c50`.

## Extraction Selectors

In the extraction path, the numeric selector is a Gaussian filter ID resolved
through `FUN_18007d800`/`FUN_18007dd20`. `FUN_18004ac40` supplies IDs `0..5`
for the ordinary pass and `100..105` for the sequence-72 low-count retry.

## Confidence

High for dispatch/lifecycle and extraction filter-ID propagation; medium for
the standalone semantics of selector 10 in the contrast path.

## Unresolved

- Exact vendor name and standalone kernel represented by selector 10.
- Generic matrix backend details outside the documented paths.
