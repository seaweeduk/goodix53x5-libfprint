# FUN_18006d540

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry: `0x18006d540`, internal live-preprocessing routine called by exported
  `preprocessor`.
- Substantive callees include `FUN_180064bb0`, `FUN_1800668f0`,
  `FUN_18006b290`, `FUN_18006c510`, and `FUN_18006e7f0`.

## Behavior

- Exported purpose argument 2 reaches this function as argument 10 and is
  forwarded as the first argument of `FUN_18006c510`.
- Any nonnull auxiliary argument is cleared with `memset(aux, 0, 0x4c98)` on
  every live call before the profile-qualified auxiliary branch.
- Packed subtype 12 is included in the auxiliary branch mask.
- Before candidate selection, profile 9 builds a third mask from the primary
  contrast image through `FUN_18007e4b0` with strict threshold `100`. Packed bit
  2 is clear, so `FUN_18006e7f0` uses this generated mask rather than the
  profile rendering mask.
- `FUN_180069820` receives the copied canonical u16 input before setup or live
  processing. For packed subtype 12 it clamps to 12 bits without applying
  `4095 - sample`. Profile 9 selects validation for both calls: setup uses the
  strict 85% threshold while ready is zero and can return `0x29bb`; initialized
  live uses 15% and can return `0x29aa`. Both successful calls copy only the
  outer edge and retain invalid second-from-edge samples.
- Caller `preprocessor` at `0x180002ab0` builds packed chip info from initialized
  profile globals and passes it as argument 5. The subtype selector recovered
  here is not the source descriptor's separate `0x10` format byte.
- Setup calls (`calibrated == 0`) run `FUN_180064bb0` and return before live
  rendering. Live calls therefore depend on the initialized `0x3048c`-byte
  generation workspace; a reset state plus setup frame is not equivalent to a
  production `preprocessor_init` call.
- On the live path, workspace `+0x1cb64` is cleared and passed as the output of
  `FUN_18006b510`, making it the primary contrast plane. A second candidate
  plane lives at `+0x217f4`. The selected-plane scalar is workspace `+0x26484`.
  `FUN_18006e7f0` initially publishes primary to the exported result and can
  replace it with refined. At function exit, scalar zero makes `+0x1cb64` the
  destination of a copy from the finalized result; nonzero makes `+0x217f4`
  the destination. This synchronizes the selected workspace after post-render
  processing; it does not republish a candidate over the result.
- The copy occurs after metadata encoding. When scalar zero selects primary,
  retained primary contrast `+0x1cb64` is therefore byte-identical to the
  finalized exported image, including its encoded first-row and mask bits. This
  retained plane is the later extraction and anti-fake auxiliary input.
- The selected-plane scalar at `+0x26484` is read into a local before this call
  invokes `FUN_18006e7f0`. The selector then overwrites the
  workspace scalar, but `FUN_18006b290` receives the saved entry value and emits
  its low byte as auxiliary byte 1.
- The exported `preprocessor` writes zero to that scalar at `0x180002be5` before
  every ordinary call into this function. Only exported argument 7 equal to 1
  replaces it with 2. WBF enrollment hardcodes argument 7 to zero at
  `0x18002cd4a..0x18002cd7b`. Identify forwards `_PreProcessor_E` argument 10 at
  `0x18002cefa..0x18002cf3a`, but the sole adapter path
  `EngineAdapterAcceptSampleData -> FUN_180032040 -> FUN_18002c810` hardcodes
  that value to zero at `0x18002004a`. Consequently an ordinary
  profile-9/type-12 call saves zero and emits auxiliary byte 1 as zero, even
  when the preceding call left selected-plane output 1 in the workspace.
- The saved value also enters classifier context `+0x10`. Value 2 suppresses
  both retained-reference initialization and `FUN_1800512c0` update for that
  call; values 0/1 permit the same normal lifecycle. Value 1 is a possible
  selector output but is not an ordinary exported-call input; value 2 requires
  the special exported flag.
- Exported `preprocessor` passes null as this function's optional argument 12.
  `FUN_18006e7f0` can write a selection metric to that object's `+0x28` for a
  nonnull internal caller, but no such metadata object exists on the production
  live path. Exported quality at the separate result descriptor's `+0x28` is
  written later by `preprocessor`, not by selection.
- `FUN_1800392f0` always consumes primary contrast `+0x1cb64` for boundary-score
  classification, independently of the selected-plane scalar. The primary
  contrast plane and selected/exported output are distinct owners when refined
  output is selected.
- For subtype 12, the normal `FUN_1800668f0` call allocates two temporary
  16-bit planes and passes their pointer pair as argument 5. The pair reaches
  `FUN_18006c510` as argument 3 and is freed immediately after that helper
  returns. The nested post-render `FUN_1800668f0` call passes `NULL` for the
  pair, so it cannot replace the planes while they are being evaluated.
- The cloned mask structure produced by `FUN_18006bce0`, not the earlier normal
  update mask, is argument 6 of `FUN_18006c510` and becomes the aliased workspace
  of the nested update.
- Return `0x7531` from `FUN_18006c510` writes 2 to caller result field 0 and sets
  bit 3 of result field 9 after downstream quality processing.
- `FUN_18006b290` can independently return `0xc351` after complete rendering
  when its severe mode-9 predicate fires. `FUN_18006d540` preserves that raw
  status at `0x18006ddd6..0x18006dde4`, still computes quality/coverage, and
  synchronizes the finalized result into the selected workspace before
  returning.
- `FUN_180044900` writes preliminary quality and coverage at
  `0x18006dd45..0x18006dd5b`. The caller then evaluates its late stateful
  `0x7531` condition, selects the pending status with `0xc351` taking priority,
  and finally zeros quality when signed coverage is at most 5. Coverage is not
  changed. Optional result publication and selected-workspace synchronization
  follow the gate, so neither nonzero status nor zero quality prevents that
  final image-state publication.
- Setup validation `0x29bb` and live validation `0x29aa` return before rendering
  and quality. Stateful `0x7531`, helper metadata `0x7532`, and severe
  `0xc351` are evaluated without changing the final coverage gate.
- The exported result and fixed candidate workspaces are disjoint in this
  caller. Their copies use overlap-safe `FUN_18008a9c0`, but no production
  selection or final synchronization depends on aliasing.

## Instruction Ranges

- Normal optional-plane allocation and call: `0x18006da1a..0x18006dae6`.
- Exported null argument 12: `0x180002c1c..0x180002c57`.
- `FUN_18006c510` argument construction: `0x18006dbbd..0x18006dc1f`.
- Status propagation: `0x18006dc22..0x18006dc35` and
  `0x18006dd5b..0x18006dd6d`.
- Final quality/status policy: `0x18006dd45..0x18006ddf2`.
- Final result-to-selected-workspace synchronization:
  `0x18006ddfc..0x18006de1d`.
