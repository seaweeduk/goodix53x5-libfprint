# FUN_180047820

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180047820..0x180047ad2`.
- Profile-9 caller: `FUN_180048260`, twice per extraction classification.
- Role: retain the largest current connected components and, once the
  three-plane ring is full, publish spatially large components that are stable
  in all retained planes.

## Contract

```c
void FUN_180047820(
  uint8_t *current_mask,
  int32_t rows,
  int32_t columns,
  uint8_t class_value,
  uint8_t *history_classes,
  int32_t retained_plane_count);
```

The first stage labels nonzero `current_mask` pixels with four-connectivity,
allows labels 1 through 71, selects the three largest labels whose sizes are
strictly above 500, and rewrites `current_mask` to `0xff` only for those labels.

When `retained_plane_count < 3`, the function stops after that current-mask
rewrite. At count 3 it additionally:

1. Calls `FUN_180047750` to require each retained slot to contain
   `class_value` at every admitted current-mask pixel.
2. Labels that stable mask with four-connectivity and labels 1 through 31.
3. Selects the three largest labels whose sizes are strictly above 350.
4. Writes `class_value` into `history_classes` only where that destination is
   currently zero.

`FUN_180048260` first calls it with class 1, then with class 2. Class 1 therefore
wins when stable masks overlap. The current-mask output is separately used to
form the plane later appended to retained history.

## Callees

- `FUN_1800807a0`: bounded four/eight-connected component labeling; this caller
  uses four-connectivity, zero border exclusion, and maximum-label fields 70 or
  30, yielding representable labels 1..71 or 1..31.
- `FUN_1800802c0`: selects up to three largest labels strictly above the supplied
  size floor while temporarily zeroing selected sizes.
- `FUN_180047750`: intersects the current mask with class agreement across the
  retained extraction planes.

## Evidence And Confidence

- Current labeling and strict-500 selection:
  `0x1800478ea..0x180047999`.
- Full-ring gate and retained-plane filter:
  `0x180047999..0x1800479c5`.
- Stable-mask labeling and strict-350 selection:
  `0x1800479c5..0x180047a99`.
- Confidence is high from complete caller and callee assembly. Vendor class
  names remain unknown.
