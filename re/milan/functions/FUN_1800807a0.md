# FUN_1800807a0

## Identity And Relevant Contract

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry: `0x1800807a0`.
- Profile-9 extraction caller: `FUN_180047820`.
- Role: bounded connected-component labeling with per-label pixel counts.

The function initializes every output label to `-1`, scans the configured
interior, writes label zero for background, and flood-fills nonzero pixels using
either four or eight configured neighbors. Label counts are written through the
caller's count array. The return is the highest materialized label count/index
used by `FUN_1800802c0`.

`FUN_180047820` supplies connectivity 4, zero row/column border exclusion, and
a maximum-label field of 70 for the current mask or 30 for the stable-history
mask. Because labeling starts at 1 and the stop comparison admits one final
label, these calls represent labels 1..71 and 1..31 respectively. Queue storage
is bounded at `0x992` coordinates.

Confidence is high for the caller-relevant configuration, label bounds, and
output ownership. Other generic configurations are outside this path.
