# FUN_180042ac0

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180042ac0`, `0x180042ac0..0x180042c2a`.
- Role: score disconnected candidate relations without mutating the graph.

## Call Graph

- Direct caller: `FUN_180042840`.
- Direct callees: `FUN_180071370`, `FUN_180071d40`, and `__security_check_cookie`.

## Recovered Behavior

The helper scans the supplied inactive list in order at
`0x180042b27..0x180042be8`. For each prior it passes the stored direct transform
to `FUN_180071d40`; the returned overlap must be strictly greater than
`param_3[6]`. Passing candidates are ranked by
`uncovered = rows * columns - FUN_180071370(...)`. A candidate replaces the
winner only when its uncovered area is strictly smaller at
`0x180042bc3..0x180042bd5`, so equal areas retain the first passing list entry.

The initial minimum is the full `rows * columns` area and the initial selected
prior is literal index zero, not `candidates[0]`. Consequently, if no candidate
passes overlap, or no passing candidate improves on full area, output
`param_3[8]` is full area and progress names prior zero even when zero was not in
the supplied list. An empty list has the same deterministic outputs and does not
dereference the list.

At `0x180042bee..0x180042bfd` it writes the minimum uncovered area to
`param_3[8]` and adds `0x100 + ((selected_prior & 0xff) << 8)` to
`param_3[7]`. It does not mark any feature active, change graph metadata, or
rewrite any relation. The overlap and area helpers return scalar metrics rather
than status values, so this function has no failure or rollback path.

## Current Source Mapping

The current combiner computes overlap for every admitted direct candidate at
`drivers/goodix53x5/milan/enrollment/template.c:293..303`, but it has no
equivalent graph-progress/uncovered-area output. If the graph remains unset, or
is established but none of the admitted priors is active, no relation or graph
metadata is published from these inactive candidates. This matches the DLL's
template mutation; only the DLL enrollment-progress side channel is absent from
the combiner API.

## Confidence And Unresolved

- Confidence: high for strict gates, tie/default ownership, output arithmetic,
  and non-mutation from assembly and direct controls.
- Unresolved: source name of the uncovered-area metric.
