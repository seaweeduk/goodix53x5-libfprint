# FUN_18004e8c0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18004e8c0..0x18004eb90`, broken-level secondary
  promotion classifier.

## Call Graph

- Caller: `FUN_18004ef70`.
- Substantive callees: `FUN_18004ef90`, `FUN_180051bd0`, `FUN_180052b80`,
  `FUN_180051160`, `FUN_1800514e0`, and `FUN_180051af0`; allocation and free
  helpers own temporary planes.

## Exact Behavior

This path is entered only when retained reference count is greater than 20. Its
source is the 88x108 retained `profile9_history_reference`. Its byte mask is
zero at the outer three rows/columns and one elsewhere only when both the
eroded-valid mask is nonzero and:

```text
profile9_reference_age[i] * 100 >= profile9_history_count * 50
```

It first calls `FUN_18004ef90(source, qualified_mask, 9504, ..., 9000, ...)` and
writes the resulting 0/1 state to classifier context `+0x04`.

It then uses `FUN_180051bd0`, `FUN_180052b80`, and `FUN_180051160` with temporal
configuration `[1600,768,384,0,700,800]` to OR class 3 into the class plane.
This is the path already represented by `milan_profile9_temporal_class3()`.

Finally, `FUN_1800514e0` and `FUN_180051af0` with configuration `[400,512]`
form a separate strict score mask. Its count writes context `+0x08` as 2 above
600, 1 above 300, and 0 otherwise.
- The `+0x08` count state does not become one of the three auxiliary bytes, but
  a nonzero value suppresses the fallback replacement of a zero `+0x04` state.

## Evidence

- Input setup and `FUN_18004ef90` output write:
  `0x18004e8c0..0x18004ea0a`.
- Class-3 plane update: `0x18004ea27..0x18004eaae`.
- Secondary binary plane construction: `0x18004eaae..0x18004eb15`.
- Strict `300`/`600` promotion count and offset-8 write:
  `0x18004eb15..0x18004eb59`.

## Confidence

High for inputs, arithmetic, output offsets, strict thresholds, and class-plane
mutation; vendor names remain unknown.

## Exactness Boundary

- `FUN_180051f50` flood details are not needed for exact auxiliary bytes: the
  secondary state is written before flooding, and the flood does not affect
  context `+0x08` or the fallback guard.
