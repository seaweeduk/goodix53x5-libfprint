# FUN_180074e80

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180074e80`, `0x180074e80..0x18007521c`.
- Role: compute transformed-record validity, match percentages, and descriptor distance.

## Call Graph

- Direct callers include `FUN_18005a3c0`, `FUN_180057900`, and
  post-admission refinement `FUN_180072860`.
- Relevant callee: `FUN_180078fd0`.

## Recovered Behavior

### Type-12 Spatial And Ratio Contract

The enrolled feature supplies destination width and height at offsets 0 and 4,
record count at `+0xf0`, and its borrowed 56-byte record array at `+0xf8`.
The probe supplies its own count and record array at the same offsets. Coordinates
at record offsets 2 and 4 are loaded as unsigned 16-bit Q8 values. The affine
products and translation shifted by eight use signed 64-bit intermediates;
division by 256 rounds half away from zero before narrowing to a dword
(`0x180074f40..0x180074fdb`).

For a nonpositive raw coordinate, the qword magnitude calculation is
`(128-raw)>>8`, followed by dword negation: `NEG EBX` at `0x180074f85` and
`NEG R10D` at `0x180074fd8`. The sign is therefore applied modulo `2^32`,
including a narrowed magnitude of `0x80000000`. That case remains
`INT32_MIN` and fails the subsequent positive lower-border check. With arbitrary
signed-dword affine words and unsigned-word coordinates, the raw qword products,
translation and rounding are bounded within signed64; no small-coefficient
premise is needed for those qword operations.

A transformed probe record is valid exactly inside the inclusive lower border
`6*256` and exclusive upper borders `(width-7)*256`, `(height-7)*256`.
For each valid probe, the enrolled scan compares only flag bits `0..1` and
requires each absolute coordinate difference to be at most 512. It keeps the
first strictly nearest record, retaining the earlier physical index on a
squared-distance tie (`0x180075021..0x180075082`). A hit requires squared
distance strictly below 262144. Enrolled hits are not consumed: multiple probe
records may select the same enrolled record.

For type 12 a hit always increments matched count. Its distance contribution
is the smaller Hamming distance between enrolled bytes `40..47` and probe bytes
`40..47` or `48..55`; there is no descriptor-distance rejection after the spatial
hit. The sum is divided by matched count with signed truncation, or remains zero
without hits. With valid count `V`, matched count `M`, and caller count `N`,
the percentages are `trunc(100*M/V)` and `trunc(100*N/V)`, both zero if `V=0`
(`0x1800751d5..0x180075211`). Only the first ratio is bounded by 100: `N` belongs
to the caller's correspondence stage, is not recalculated from this spatial
scan, and the second ratio is not clamped. Neither ratio uses bitmap support
or raw image coverage as its denominator.

## Complete Output And Caller Ownership

Every call first zeroes all eleven output words at
`0x180074ee7..0x180074f03`. Valid count, matched count, topology percentage,
geometric percentage and average distance occupy words `66..70` in the
candidate; words `71..76` remain zero for type12. Callers copy the two
percentages to words10/11 without a type12 minimum-percentage clamp.

In `FUN_18005a3c0`, a larger alternate invokes this helper at
`0x18005a885` with the local alternate affine and alternate count, independently
of the bitmap-ranked affine. The forward-mask result was computed from the
ranked affine; promotion combines those separately owned results.
After promotion, the repair at `0x18005a93b` runs exactly when the original
primary count is greater than4 and published topology is below30. It uses the
then-ranked affine and original primary count and replaces the complete record
block and copied percentages. An alternate-ranked repair therefore has
`alternate.affine/primary.count`; a primary-ranked repair has
`primary.affine/primary.count`. Without either call the caller's initialized
record fields remain intact. See `FUN_18005a3c0.md` for rank and promotion.

Post-admission `FUN_180072860` invokes this helper only when its alternative
passes bitmap/detail publication. It supplies the alternative affine and the
unchanged incumbent candidate word1, replacing all eleven record words and
the copied percentages. It does not derive a new retained count from this
helper. See `FUN_180072860.md` for the strict publication predicate and
subsequent classification. The current arithmetic owner is
`goodix_milan_match_record_metrics_internal` in `match/geometry.c`.
