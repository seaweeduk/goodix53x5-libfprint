# FUN_180057900

## Identity

- Binary: `GoodixEngineAdapter.dll`, SHA-256 `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180057900`, `0x180057900..0x180057e56`.
- Role: construct and rank the immediate sibling candidate path after `FUN_18005a3c0`.

## Call Graph

- Direct caller: `FUN_180055a40`.
- Relevant callees: `FUN_180075220`, `FUN_18005b900`, `FUN_1800749c0`, `FUN_180071d40`, `FUN_180070e90`, and `FUN_180074e80`.

## Recovered Behavior

For low current counts it builds two class-partitioned correspondence sets through `FUN_180075220`, filters each with `FUN_18005b900`, and keeps the larger affine result. When at least three pairs survive it builds another affine-guided set through `FUN_1800749c0`. A better sibling may populate candidate word `2`, replace overlap metrics and the selected transform using the same penalized-detail ranking as the primary path, and initialize the complete words `66..76` block through `FUN_180074e80`; only words `66..70` can be nonzero for type 12.

Afterward, when word `10` exceeds `30` and word `70` is below `10`, words `0` and `1` are incremented and clamped to the type-specific capacity (`31` or `42`).

### Exact state transitions for type 12

- If either incoming word 0 or word 1 is `>8`, sibling construction is skipped;
  equality at eight still enters it.
- The larger of the two class-filtered results replaces word 0 only when it is
  strictly greater. A tie retains the incoming word.
- The affine-guided count must be strictly greater than the selected
  class-filtered count before ranking or admission is considered.
- Transform replacement is independent of sibling-count admission. It requires
  overlap score `>128` and strictly greater wrapping-dword penalized detail,
  using words 12 and 13 as the incumbent penalties. A score of 128 or a rank tie
  retains the incoming transform and metrics.
- A sibling that passes the separate strength predicate writes word 2, words
  10/11, and the complete `66..76` producer block. For type 12, words 71..76
  remain zero.
- The final topology bonus uses strict word `10 > 30` and word `70 < 10` tests.
  It increments words 0 and 1 independently, then clamps each to 42. Values at
  30 or 10 do not increment; a value already at 42 remains 42.

## Confidence And Unresolved

- Confidence: high for candidate-state writes, ranking, count increment, and capacity clamp.
- Unresolved: semantic role of the two class-partitioned searches and exact native parity of their correspondence builders.

## Focused Remaining Divergence

On `139/1` feature 7, primary count, overlap metrics, transformed-record fields,
and the initial affine `[232,91,3095,-60,244,-5265]` align, but Windows primary
word 13 is one while native leaves it zero. Windows then selects sibling affine
`[240,81,3332,-62,246,-5410]`; native retains the primary affine. This is the
narrow sibling-ranking/flag case for the remaining candidate-construction
contract. `142/1` feature 1 does not diverge here: its primary and sibling state
are exact, and its first mismatch is the later `FUN_180072860` caller
refinement.

## Native Contract-1 Completion

Native sibling construction now preserves the primary affine penalty flags in
the penalized-detail comparison. When the sibling affine wins, overlap metrics,
transform, and words `12..14` are replaced together.

This makes `139/1` feature 7 exact: primary word 13 is one, sibling affine
`[240,81,3332,-62,246,-5410]` wins, and the selected sibling flag becomes zero.
The complete contract-1 primary/sibling stage gate is `5/5`. The stricter exact
semantics expose a separate `14/2` under-admission outside this contract; it must
be correlated rather than hidden with a relaxed predicate.

## Exact Sibling Admission Predicate

The repeated non-forced `14/2` capture at `0x180057d47` and the corresponding
accept-branch checkpoint at `0x180057d86` resolve the later predicate. A sibling
is admitted only when:

```text
alternate_count > selected_count
and average_affine_column_length in [234, 281]
and abs(normalized_affine_column_dot_q16) < 10486
and (
  topology_percent > 40 and matched_record_count > 23
  or topology_percent > 45 and matched_record_count > 11
  or topology_percent > 75
  or alternate_count > 16
)
```

Folded rotation and geometric percentage are captured diagnostics but are not
operands of this predicate. For `14/2` feature 5, Windows has sibling count `5`,
record metrics `47/25/53/10/15`, and affine diagnostics `266/5/2982`; it admits
through `53 > 40 && 25 > 23`. Native substitutes geometric percentage `10` for
matched count and rejects the sibling, making candidate word `2` the first
difference (`5` official, `0` native). Confidence is high for this exact
profile-9/type-12 predicate and its `14/2` application; broader profile behavior
is not claimed.

## Native Predicate Correction

The generic native sibling strength test now uses `matched_count` rather than
`geometric_percent` in both topology branches. No row identity or fitted
threshold is involved. Focused replay of `14/2` feature 5 admits count `5` and
matches the official sibling state `77/77`; the full candidate contract-1 and
contract-2 gates retain their previous results.

## Accepted-Score Residual 15/1

The repeated passive accepted-score residual trace is exact for `15/1` feature
0 through the complete primary 77-word state and affine
`[257,-10,3355,-4,249,-13265]`. The first difference is this sibling path:

```text
Windows sibling count/state  [4,4,0,...], affine [251,15,1555,-4,252,-13417]
native sibling count/state   [4,4,7,...], affine [251,15,1590,-3,252,-13556]
```

Windows consequently stops before dispatcher admission, while native promotes
retained count 7 and dispatches the feature. This is a new sibling-construction
input outside the earlier focused predicate gate; it does not justify changing
the caller admission threshold.

## Accepted-Score Retained-Count Cohort

The repeated retained-count reference localizes five of the seven overlapping
accepted-score count differences to this sibling stage. Primary state is exact
on every row; the first unequal state is:

| Row/feature | Official after sibling | Native after sibling | Final count |
|---|---|---|---|
| `21/1 f1` | `[7,7,9]` | `[7,7,10]` | `9/10` |
| `51/1 f5` | `[6,6,0]` | `[6,6,7]` | `6/7` |
| `99/1 f5` | `[7,7,8]` | `[7,7,9]` | `8/9` |
| `104/1 f3` | `[7,7,9]` | `[7,7,10]` | `9/10` |
| `161/1 f0` | `[5,5,0]` | `[5,5,7]` | `5/7` |

Official-record same-input replay proves the preceding primary and alternate
pair selection and `FUN_18005b900` filtering exact for all five. This is one
shared sibling-state investigation surface. It is distinct from `15/1 f0`,
whose different sibling input/affine was already isolated above.

## Generic Retained-Count Contract

The compact repeated reference is frozen outside Git at:

`<evidence-root>/references/milan-native-capture-20260723/windows-native-sibling-retained-counts-v1/`

Its tree SHA-256 is
`ec6a4c69ddc35faa9fdea4aa2b37ccf1037fb428df6db33918ffe2b5eb795613`.
Rows, contract, summary, and provenance SHA-256 values are respectively
`7bb7a14346d5d0ba74bb052bc56ce4f5cd53c1e9144cd5c4031db01250e7ddc6`,
`0ae3d7943ea465a0b118c0ab130a99113ab948f6a730b01760f4c16e3fe387ac`,
`feefb0a083d4ffea90d05b6082ee04d1cce418ac816b1cc83530417f36a87293`,
and `cb3f65d8c8ec46c492514d388e0253b5f057fd3a82a65565d12926896371d85b`.
Two independent official captures and two fresh native projections repeat
exactly; the verifier rejects five drift mutations.

The first unequal operation is the affine-guided `FUN_1800749c0` descriptor
admission in `FUN_180075dc0`, not a record-stream difference or later affine
filter. Windows compares the minimum of the two eight-byte tail Hamming
distances against configuration word 5, which is `16` for profile 9/type 12.
Native uses `23`. Its six extra pairs are:

| Row/feature | Native-only pair(s) | Tail distance(s) |
|---|---|---|
| `21/1 f1` | `(102,113)` | `18` |
| `51/1 f5` | `(107,111)` | `18` |
| `99/1 f5` | `(5,12)`, `(75,64)` | `17`, `17` |
| `104/1 f3` | `(36,11)` | `17` |
| `161/1 f0` | `(20,42)` | `18` |

At each extra source, the official selector receives best/second `192/192`
and indices `-1/-1`, proving rejection before ratio and spatial selection.
Filtering native pairs at the inclusive `<=16` boundary yields official pair
counts `16,25,24,21,8`, filtered counts `9,6,8,9,7`, and admitted sibling
words `9,0,8,9,0`; therefore the complete after-sibling states become exactly
`[7,7,9]`, `[6,6,0]`, `[7,7,8]`, `[7,7,9]`, and `[5,5,0]`.

The narrow coding contract is to source the affine-guided tail limit from
configuration word 5 (`16` here). Keep the existing foreground-partition
scans, coordinate gates, best/second updates, ratio, spatial de-duplication,
capacity, `FUN_18005b900` filter, `filtered_count > selected_count` promotion,
and sibling admission predicate unchanged. This task did not change production
behavior.

## Native Implementation Result

The coding contract is implemented using the existing profile configuration.
The post-implementation gate re-hashes the frozen reference tree, tests
tail-Hamming distances 15/16 accepted and 17/18 rejected, rejects five reference
mutations, and reproduces selected-pair counts `16,25,24,21,8`, filtered counts
`9,6,8,9,7`, sibling words `9,0,8,9,0`, and final retained counts
`9,6,8,9,5`. The full immutable score gate improves from `756/798` to
`760/798` while decisions and outer indices remain exact `798/798` and impostor
accepts remain `0/398`.

## Sequence-72 Negative Control

The selected sequence-72 candidates are unchanged across this function. E1
feature 17 enters and leaves with words `[10,10]`; e2 feature 34 enters and
leaves with `[17,17]`. The word-10/word-70 conditional increment and sibling
promotion paths are negative controls for the `21/23` and `38/40` score
residuals. Unequal primary counts already exist because native/official live
probe inputs contain `54/48` records. No sibling count or threshold change is
authorized for these rows.

## Evolved-Gallery Units 7-9 Audit

Sequence 197 is a downstream negative control for this function. On `197/1
f39`, the DLL and native sibling paths each add the same one-count topology bonus
and preserve their respective incoming affine; the selected-affine difference
already exists on return from `FUN_18005a3c0`. On `197/2 f6`, both sides leave
the incoming primary/retained state unchanged, so official `14/14` and native
`14/17` remain different while the affine remains exact.

The existing sibling predicate, replacement ranking, record metrics, and
capacity clamp are therefore not sources of either sequence-197 residual. The
function is exact for these reached contexts, but losing/tied synthetic-valid
coverage remains incomplete; this result supports an exact-but-unvalidated-
context classification rather than a broader complete claim.

## FUN_18005a3c0 Dependency Unit 1 Handoff

This function may assume that its incoming selected affine and words
`4,5,9,12..14` derive only from a valid primary or valid alternate overlap call.
For a below-three alternate, unit 1 requires the incoming state to equal the
defined primary-only state; no invalid alternate affine or diagnostic may reach
this sibling. The sibling remains free to construct and select its own fully
defined result and continues to own word 2. It must not inspect any private
invalid-alternate scratch field.

The native unit-1 implementation establishes that handoff with explicit
candidate validity. A below-three alternate reaches this function with the
primary-only affine/count/metrics, freshly initialized remaining words, and
word 2 still zero. No sibling predicate, replacement rank, count, affine,
record metric, admission, or status behavior changed.

## FUN_18005a3c0 Dependency Unit 5 Handoff

This sibling is called after primary insufficient-pair count zero, primary
attempted no-model count zero, and valid-model/post-orientation count zero. Their
affine bytes differ and only the last has model validity. Native may permit this
function to recover from an invalid primary only through a sibling construction
that is independently fitted and explicitly valid without reading the invalid
guide. If that independence is not established for a branch, fail closed rather
than score caller identity or estimator failure-zero. Alternate no-model is
suppressed before this handoff, so the incoming state remains primary-only.

## 2026-07-28 Retrospective Correction

The earlier exact-but-unvalidated-context classification is superseded. Complete
body review identifies three native gaps in this sibling family.

First, after choosing the larger of the two class-filtered results,
`0x180057b1e..0x180057b22` writes that selected count to candidate word 0 when
it exceeds the incoming value. This occurs before the `>=3` guided-alternate
gate and survives a missing, losing, tied, or rejected guided result. Native
updates only its detached `primary_count` scalar; it does not update the
`GoodixMilanMatchCandidate` later consumed by admission and dispatch. A fixed
generated control obtains selected/guided `4/4` and observes scalar 4 with
candidate words `0,0,0`.

Second, both `FUN_180075220` and `FUN_1800749c0` consume stored feature
partition boundaries at `+0x118`. Native sibling construction re-derives both
boundaries from a leading run of mutable `foreground == 0` record values. This
is outside `FUN_18005a3c0` dependency unit 6, which correctly threaded stored
metadata only through the primary/alternate builders.

Third, `FUN_1800749c0 -> FUN_180075dc0` uses `FUN_1800689a0`'s wrapped-dword
inverse. Native's guided sibling loop duplicates the inverse with a widened
64-bit determinant. Ordinary reached affines remain negative controls, but a
determinant-wrap singular input is observably different.

These generic rules were recovered without sequence or feature predicates.
Sequence-100 feature 33/23 is consistent with this surface but is not closure
evidence because its frozen secondary/predicate/caller artifact hit counts are
zero. See `MATCH-CANDIDATE-RETROSPECTIVE.md` and external evidence root
`match-candidate-retrospective-v1/`.

## 2026-07-28 Native Contract Completion

Native now passes each feature's validated stored partition boundary into both
sibling builders. Immediately after the two class-filtered sets select their
winner, a strictly greater selected count is written to candidate word 0 before
the guided `>=3` gate. Guided sibling count remains word-2-owned, and all rank,
record, bonus, and clamp owners are unchanged.

Fixed-seed controls cover selected counts below, at, and above three; guided
absence, loss, tie, and win; and strict word-0 ties with complete 77-word
ownership. The evolved sequence-100 features now reach after-sibling state
`[4,3,0]` rather than losing selected word 0, but remain unadmitted with topology
16. This closes the documented publication/partition/inverse contracts without
closing the separate retained-count producer gap.

## Sibling-To-Caller Completion Audit

The complete body has 39 Ghidra blocks, 317 instructions, 13 calls, and 25
actual conditional instructions. Type 12 fixes capacity at 42. Direct DLL
controls cover selected counts 0..5, second-set ties, strict word-0 publication,
guided loss/tie/win, rank replacement independent of admission, score 128,
scale 233/234/281/282, dot 10485/10486, all four strength arms, topology bonus
boundaries, capacity clamp, and the count-at-nine construction skip. Thirty-eight
calls pass twice byte-identically.

This audit also corrects the remaining sequence-100 producer localization.
`FUN_180075220` does not independently recompute descriptor eligibility or tail
layout. Its `FUN_1800770e0` calls consume two matrices supplied to this function
by the caller alongside the feature objects. Current native sibling construction
does not receive those matrices and recomputes pair state locally.

For sequence 100, enrollment-1 feature 33 and enrollment-2 feature 23 have exact
primary state `[3,3,0]`. Production class pair sets contain `4/7` pairs and
filter to `0/3`; native constructs `7/10` and filters to `0/4`. Production then
uses the selected affine `[274,-3,684,20,237,12763]` to construct 16 guided
pairs, which filter to five and publish `[3,3,5]`. Native's different selected
affine produces no guided result and leaves `[4,3,0]`.

Supplying the production pair sets directly to native produces the exact
production filtered counts and affines, and supplying the production selected
affine to native guided construction reproduces the exact ordered 16-pair set.
The first unequal value is therefore the class-pair set, not robust fitting,
guided scanning, sibling admission, caller admission, or contribution. Matrix
production and lifetime are not yet completely controlled, so no implementation
change is authorized by this localization alone.

## Matrix-Contract Closure

The formerly open producer and lifetime are now controlled. This function
borrows the same two `180x180` matrices initialized in `FUN_18005a3c0` and
filled by `FUN_180078a70 -> FUN_180076400` for the current gallery feature. It
passes both unchanged to `FUN_180075220`. The later guided
`FUN_1800749c0 -> FUN_180075dc0` path is separate and recomputes its tail
distance with the same profile limit 16 rather than consuming the matrices.

Native's exact gap is ordinary class-sibling tail admission 23 instead of 16.
The sequence-100 six-pair delta is entirely at distances 17..19 and matches the
frozen generic prediction. A narrow configuration-word-5 correction is now
authorized under `re/milan/MATCH-MATRIX-CONTRACT.md`; ranking, robust fitting,
guided construction, admission, policy, and lifecycle behavior remain outside
that change.

## Matrix Tail-Admission Implementation

The existing caller-supplied `sibling_tail_hamming_limit` now reaches both the
ordinary `FUN_180075220` equivalent and the already-correct guided
`FUN_1800749c0` equivalent. For profile9/type12 the value is 16. The ordinary
17..23 superset is removed without changing guided scanning, selected-count
publication, ranking, strength admission, topology bonus, count clamps, or any
candidate word owner. Sequence 100 now publishes the production guided count 5,
admits middle features 33/23, and closes both chronology scores at 27.
