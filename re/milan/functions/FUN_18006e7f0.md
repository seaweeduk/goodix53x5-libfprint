# FUN_18006e7f0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18006e7f0..0x18006ea99`, processed-image candidate
  selection helper.

## Call Graph

- Sole caller: `FUN_18006d540`.
- Direct callees: `FUN_180069150`, `FUN_180069350`, `FUN_18006d430`,
  `FUN_180081d30`, and copy helper `FUN_18008a9c0`.

## Behavior

- Copies the primary rendered candidate from workspace `+0x1cb64` to the
  exported output and initially marks candidate zero selected.
- If the primary mask metric reaches the caller's threshold, renders the
  alternate/refined candidate from working frame `+0x13244` into workspace
  `+0x217f4`, evaluates correlation and mask metrics, and conditionally replaces
  the output with that candidate while marking candidate one selected.
- Packed subtype 12 selects bit 12 of mask `0x473100`, so its transition
  thresholds are `1800` and `3600`, with an upper boundary at `5400`. The
  earlier `2000`/`4000` native assumption used the default bands.
- For profile 9, let `C` be the primary metric, `R` the refined metric, and
  `T=800` the caller threshold. Native applies these regions in order:

  | Primary region | Required refined metric | Replacement rule |
  | --- | --- | --- |
  | `C < T` | refined is not rendered or scored | keep primary |
  | `T <= C < 1800` | `(T * 205) >> 8` | replace only when `R` and correlation are strictly below the required metric and `235`, respectively |
  | `1800 <= C < 3600` | `((((C - 1800) * T * 179) / 1800) + T * 205) >> 8` | replace only when `R` is strictly below the required metric |
  | `3600 <= C < 5400` | `(((C - 3600) * T * 2) / 1800) + ((T * 384) >> 8)` | replace only when `R` is strictly below the required metric |
  | `5400 <= C` | not used | replace with refined unconditionally |

- Multiplications, division, addition, and shifts occur in the displayed
  order with signed 32-bit intermediates. Profile-9 production values are
  positive, so division and right shifts round down. `R` equal to its required
  metric keeps primary; correlation exactly `235` also keeps primary. Exact
  primary boundaries `800`, `1800`, and `3600` enter the next scoring stage,
  while `5400` enters unconditional replacement.
- When the optional internal diagnostic object is nonnull, its field `+0x28`
  initially receives `C`. If primary survives the `1800 <= C < 3600` region,
  the field is overwritten with `C >> 1`; if primary survives
  `3600 <= C < 5400`, it is overwritten with `C >> 2`. Refined selection and
  the outer regions leave the original `C` there.
- Exported `preprocessor` passes null for that optional object at
  `0x180002c1c..0x180002c57`. The writes above are therefore dormant on the
  production live path and are unrelated to the exported descriptor's quality
  byte at its own `+0x28`. This helper's production-owned outputs are the image
  and current selected-plane scalar; it does not write returned quality,
  coverage, or preprocessing status.
- This selection occurs before `FUN_18006d540` calls purpose-aware
  `FUN_18006c510`. Purpose can affect returned status and a post-render
  calibration update, but it does not select the current processed image.
- Its selected-plane output pointer is workspace `+0x26484`. `FUN_18006d540`
  snapshots that word before this call, so extraction auxiliary byte 1 reports
  the prior live call's selection. Final result-to-workspace synchronization
  uses this call's newly written selection.
- Both candidate workspaces and the exported result are distinct allocations
  in the production caller. The copy helper `FUN_18008a9c0` itself has memmove
  overlap semantics, but the complete scoring operation assumes that copying
  primary to the result does not overwrite either candidate before correlation
  and refined scoring.

## Evidence

- Primary metric and initial output copy: `0x18006e867..0x18006e8b7`.
- Default and profile-specific transition thresholds:
  `0x18006e8bf..0x18006e914`.
- Alternate render and metric calls: `0x18006e914..0x18006e9a9`.
- Candidate selection bands and replacement copy:
  `0x18006e9ac..0x18006ea4c`.
- Selection-owned diagnostic attenuation: `0x18006ea53..0x18006ea89`.
- Null production diagnostic argument: `preprocessor` at
  `0x180002c1c..0x180002c57`.
- Sole call from `FUN_18006d540` precedes its `FUN_18006c510` call.
- Focused diagnostic sequences `204`, `307`, and `309` distinguish the subtype
  12 `1800`/`3600` bands from the former default-band native implementation.
- The alternate render receives the `FUN_18006a4c0` mask structure. Its
  coverage field is the pre-hole-fill admitted percentage, not the separate
  generic valid-mask percentage. Sequence `236` is the distinguishing case:
  `94%` admitted versus `98%` generic-valid selects masked row centering and
  makes the captured pre-contrast source exact.
- Profile 9 has the packed bit-2 selector clear. Candidate scoring therefore
  uses the separate `FUN_18007e4b0` mask generated from the primary contrast
  image with strict threshold `100`, rather than either rendering mask.
- A strict natural operation with setup/live SHA-256
  `657470a6f17296ee5901e145d1693391f13ff3cf664c78b16be32fc491e7f40a` /
  `45b39fa626f8b8b750a9771d1684db3855b73041a08da6e439f0fea881d690c4`
  has `C=250`. Native returns before refined rendering and correlation, keeps
  primary, and finalizes SHA-256
  `812a441d87099af09e326ef74aaa09ec3bc0ff3209a257d081549fa8c76e860f`.
- A real raw12 pair with setup SHA-256
  `4500fb6ac48461ad2cd242fcf53b423037bc0b53fb1e6a20323084a4806e67ae`
  and live SHA-256
  `44bfe8b1334f0a97756d5443d4ad005148bafef537c7bbdd7e0717c02ae52592`
  exercises first-band replacement: `C=1461`, `R=544`, correlation `233`.
  Breakpoint captures and current source agree on primary SHA-256
  `3e2017fcdbd5597cb3411c1f8d15943d0c69853780e0789b949700564ef307a2`,
  pre-post-render refined/selected SHA-256
  `bb728d28e2e3722e5eb9b8b08d7dedae27a5efc39db53f715af076c470c7ef44`,
  and all-`255` selection-mask SHA-256
  `28d131f2551758d75b9c3ed9bddf3d99b245cb5da0b9ca66f7477ec94f6fe52d`.
  The finalized exported image is
  `277a7617ab6bee098fd2e9561b9d04cd52715c30dc7626deb53b70c4fd4c5a3e`.
- A second real raw12 pair with setup/live SHA-256
  `622689264d7c419f5be427039ee53480aab375a337af383a5011a480f410fa63` /
  `b35571b3ab3123a079502944e307c53e7996baf8492964d6206b39d7794845ba`
  exercises the correlation override: `C=959`, `R=363`, correlation
  `246`. Native and current candidate planes, mask, primary selection, and
  finalized image are byte-exact; the final SHA-256 is
  `60a0b1a88eb1e3d4a5c9bd43a00d6b4f055155a99c14c0570a5bbba7640a7fb8`.

## Confidence

High for candidate ordering, workspace offsets, subtype-12 thresholds,
arithmetic, ties, diagnostic writes, and purpose ordering. Medium only for the
vendor's semantic names for the metrics.

## Unresolved

- Vendor names and high-level interpretation of each metric returned by
  `FUN_180069150` and `FUN_180081d30`.
- Non-profile-9 threshold variants are outside this milestone.
- Post-call workspace snapshots contain the finalized result in the selected
  workspace, not necessarily its pre-selection candidate. Both pre-selection
  candidates must be observed before `FUN_18006e7f0` returns when their
  distinction matters.
