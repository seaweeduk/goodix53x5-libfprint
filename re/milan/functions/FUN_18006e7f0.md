# FUN_18006e7f0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18006e7f0..0x18006ea99`, processed-image candidate
  selection helper.

## Call Graph

- Sole caller: `FUN_18006d540`.
- Direct callees: `FUN_180069150`, `FUN_180069350`, `FUN_18006d430`,
  `FUN_180081d30`, and copy helper `FUN_18008a9c0`.

## Behavior

- Copies the primary rendered candidate from workspace `+0x1cb64` to the
  exported output and initially marks candidate zero selected.
- If the primary mask metric reaches the caller's threshold, renders the
  alternate/refined candidate from working frame `+0x13244` into workspace
  `+0x217f4`, evaluates correlation and mask metrics, and conditionally replaces
  the output with that candidate while marking candidate one selected.
- Packed subtype 12 selects bit 12 of mask `0x473100`, so its transition
  thresholds are `1800` and `3600`, with an upper boundary at `5400`. The
  earlier `2000`/`4000` native assumption used the default bands.
- The alternate/refined candidate replaces primary when its metric is below
  the active scaled threshold and, in the first band, correlation is below
  `235`.
- This selection occurs before `FUN_18006d540` calls purpose-aware
  `FUN_18006c510`. Purpose can affect returned status and a post-render
  calibration update, but it does not select the current processed image.
- Its selected-plane output pointer is workspace `+0x26484`. `FUN_18006d540`
  snapshots that word before this call, so extraction auxiliary byte 1 reports
  the prior live call's selection while final image copying uses this call's
  newly written selection.

## Evidence

- Primary metric and initial output copy: `0x18006e80e..0x18006e869`.
- Default and profile-specific transition thresholds:
  `0x18006e86b..0x18006e8b8`.
- Alternate render and metric calls: `0x18006e8bd..0x18006e91d`.
- Candidate selection bands and replacement copy:
  `0x18006e91d..0x18006ea0b`.
- Sole call from `FUN_18006d540` precedes its `FUN_18006c510` call.
- Focused diagnostic sequences `204`, `307`, and `309` distinguish the subtype
  12 `1800`/`3600` bands from the former default-band native implementation.
- The alternate render receives the `FUN_18006a4c0` mask structure. Its
  coverage field is the pre-hole-fill admitted percentage, not the separate
  generic valid-mask percentage. Sequence `236` is the distinguishing case:
  `94%` admitted versus `98%` generic-valid selects masked row centering and
  makes the captured pre-contrast source exact.
- Profile 9 has the packed bit-2 selector clear. Candidate scoring therefore
  uses the separate `FUN_18007e4b0` mask generated from the primary contrast
  image with strict threshold `100`, rather than either rendering mask.

## Confidence

High for candidate ordering, workspace offsets, subtype-12 thresholds, and
purpose ordering. Medium for semantic names of the individual mask and
correlation metrics.

## Unresolved

- Vendor names and exact high-level interpretation of each metric returned by
  `FUN_180069150` and `FUN_180081d30`.
- Non-profile-9 threshold variants are outside this milestone.
- The focused diagnostic snapshots candidate workspaces after
  `FUN_18006d540` may copy the selected output back over one workspace. They
  therefore prove final selected bytes but do not preserve both pre-selection
  candidates for every row. A profile-mask substitution fixed rows 318/351 but
  regressed 50 verification rows and two enrollment frames, so it was rejected.
