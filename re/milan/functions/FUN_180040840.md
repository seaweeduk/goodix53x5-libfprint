# FUN_180040840

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180040840`, `0x180040840..0x1800408f9`.
- Role: encode an optional owned byte blob.

## Call Graph

- Sole code caller: `FUN_180040a70` at `0x180040c7a`.
- Callees: none.

## Contract

The effective contract is:

```c
uint8_t *FUN_180040840(uint8_t *output, uint8_t tag, const void *blob);
```

The helper always emits `tag`. A null blob emits a zero presence byte and
returns `output + 2`. A nonnull blob emits presence byte one, little-endian
length `0x1abc`, and an exact copy of the following `0x1abc` source bytes; it
returns `output + 0x1ac2`.

For profile-9/type-12 feature serialization, the caller passes tag `bf` and live
feature pointer `+0x160`. A completed `identifyImage` probe has that owner
populated by `FUN_1800392f0`, so its canonical form is exactly:

```text
bf 01 bc 1a 00 00 [0x1abc opaque bytes]
```

The helper performs no content transformation and computes no checksum over the
blob. The absent two-byte form exists at the generic helper boundary, but is not
size-compatible with `FUN_180040980`'s completed profile-9 feature contract.

## Confidence

Confidence is high from the complete leaf body and the sole serializer call.
