# FUN_180039fc0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900.
- Entry/body: `0x180039fc0..0x18003a142`; sole caller `FUN_1800392f0`.
- Callee of note: `FUN_18003b380`.

## Recovered Behavior

Inputs are a `uint16_t` residual, byte mask, signed rows and columns, and two
output dword pointers. The builder supplies `88x108`, writes the first output
to object `+0x12c8` and the second to `+0x12c4`.

The first scan includes every nonzero-mask pixel. With sum `S` and population
`N`, it writes `N == 0 ? 0 : (S + (N >> 1)) / N`. Residuals load unsigned;
the accumulator and rounded division are signed dword operations. At the fixed
geometry even `9504 * 65535` fits a positive signed dword.

`FUN_18003b380` then produces a full-size high-pass plane. It builds a dword
integral image of the unsigned residual and, for each pixel, takes the rounded
mean of the inclusive radius-five square clipped to the image. The four
rectangle-sum arms distinguish zero/nonzero left and top boundaries. Each
output is the low word of `residual - local_mean + 0x2000`. All integral sums
and local rounded numerators fit positive signed dwords at this geometry.

The second scan visits rows `4..rows-5`, columns `4..columns-5`, selecting only
nonzero-mask centers. Its sum `T` is the absolute difference of the **unsigned**
high-pass word from `0x2000`. For population `K`, the second output is
`K == 0 ? 0 : ((T + (K >> 1)) * 3) / K`. Multiplication occurs **before**
division; this is not three times an already-rounded quotient. At `88x108`,
at most 8000 terms of at most 57343 keep that rounded numerator below signed
dword overflow.

The mask does not restrict the local-mean neighborhood, only the two aggregate
selection scans. The input residual and mask are not changed. The temporary
high-pass plane is freed after both outputs; its integral-image allocation is
freed inside `FUN_18003b380`. Allocation failures are unchecked. No candidate
record or count is read or modified.
