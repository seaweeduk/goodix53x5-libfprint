# FUN_18003a9b0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18003a9b0..0x18003ad04`.
- Caller: `FUN_1800392f0`.
- Callees: `FUN_18003aff0`, `FUN_180039c10`, allocation, and free.

## Signature

```c
void FUN_18003a9b0(
  uint16_t *residual,
  int32_t rows,
  int32_t columns,
  int32_t chip_type_unused,
  int32_t *threshold_out);
```

## Behavior

The function builds a 4096-bin histogram from interior residual values below
`4095`, accumulates from the high end, and selects the first level whose upper
tail exceeds `rows * columns / 50`. It stores that level in `threshold_out`.

Pixels at least `threshold * 11 / 10` are then examined with a 3x3 neighborhood.
The nine roughness values come from `FUN_18003aff0`, which clamps each requested
center independently to interior `x/y` coordinates. When their lower median
from `FUN_180039c10` satisfies `median * 5 <= center * 2`, this function builds a
second nine-value set directly around the original flat residual pointer and
replaces the pixel with its lower median. This replacement load does not reuse
the clamped center: its offsets are exactly `[-columns-1, -columns,
-columns+1, -1, +1, +columns-1, +columns, +columns+1, 0]`. Interior column-zero
pixels therefore take their left neighbors from the preceding flat row, and
interior last-column pixels take their right neighbors from the following row.
Other pixels remain unchanged.

The caller passes its extracted chip type (12 on this path) as the fourth
argument. That argument is not read by this body: `R9` is overwritten before use.
The roughness helper sums nine absolute unsigned-word differences, including
the zero self-difference. Its maximum is `8*65535 = 524280`; multiplying a
roughness median by five or center roughness by two remains within a signed
dword. The median selector mutates its nine-dword scratch; the center roughness
is saved before that call at `0x18003abee`. With nine entries the returned
order statistic is index four. Replacement likewise narrows the selected
nonnegative dword to a word, with no change in value.

## Caller Geometry And Bounds Contract

- `FUN_1800392f0` is the sole code caller at `0x18003952e`. It passes rows in
  `EDX` from packed bits `14..22` and columns in `R8D` from packed bits `23..`;
  profile 9 supplies exactly `88x108`. The residual allocation at
  `0x1800394a0..0x1800394ad` is exactly `88 * 108 * 2 = 19,008` bytes, and
  `FUN_18003a150` fills it immediately before this call.
- Histogram construction visits only rows `1..86`, columns `1..106`, but the
  tail-count floor is full geometry `(88 * 108) / 50`. Correction then scans
  every pixel in row-major order and mutates the residual in place, so later
  neighborhoods can observe earlier replacements.
- The native replacement offsets are all within the allocation exactly when
  flat pixel index `p` satisfies `p >= columns + 1` and
  `p + columns + 1 < rows * columns`. This admits physical column-zero and
  last-column row-wrap adjacency away from the allocation ends. It excludes
  all first-row pixels, row 1 column 0, all last-row pixels, and the
  second-last-row last column.
- The DLL provides no replacement-neighborhood fallback at the allocation
  ends. Defined execution requires either skipping correction there or failing
  the roughness replacement predicate; successful replacement is scoped to the
  in-allocation indices above.
- The native function assumes nonnull storage and dimensions at least `3x3`.
  Its signed `rows * columns` arithmetic supplies both allocation-independent
  loop bounds and the `/50` threshold floor. For `88x108` that floor is 190;
  the selected tail must contain at least 191 interior samples. Histogram
  bin 4095 stays zero, so the threshold is in `0..4094` and the word-narrowed
  cutoff is in `0..4503`. The histogram is computed only once, before any
  replacement, and is freed on return.

## Instruction Anchors

- Native full-frame correction loop: `0x18003ab11..0x18003acce`.
- Roughness neighborhood calls and predicate: `0x18003ab60..0x18003ac17`.
- Unconditional direct flat replacement loads: `0x18003ac19..0x18003ac78`.

The complete filtered residual is handed to `FUN_18003b0b0` for candidate
production and to the later residual statistics and descriptor consumers in
`FUN_1800392f0`. Those consumers do not reconstruct the pre-filter plane.
