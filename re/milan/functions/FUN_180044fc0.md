# FUN_180044fc0

## Identity And Interface

- Body: `0x180044fc0..0x1800452d9`.
- Role: profile-9/type-12 study dispatcher and transaction finalizer.
- Production caller: `templateStudy` at `0x180001f70`, passing mode one.

```c
int32_t FUN_180044fc0(
    void *gallery,
    void *probe,
    const int32_t evidence[422],
    int32_t study_result[2],
    int32_t mode);
```

The gallery is the matched live object at global `0x18024e548`; evidence is
the unchanged `0x698`-byte block at `0x18024e550`. Local action starts at zero
and selected index at `-1`. Mode zero disables selection, mode two enables
selection without replacement, and mode one enables both. Other nonzero modes
take the mode-one branch. The export supplies one.

The ordinary function return is always zero. When the result pointer is nonnull,
`0x1800452af..0x1800452b7` writes `[action, primary_selected_index]`. Queued
continuation can supersede the action but never replaces that primary index.
There is no recoverable helper rollback.

## Primary Sequence

The type-12 branch at `0x180045049..0x1800450aa` executes:

```text
if signed retained_count (+0x640) > 0 and retained_flag (+0x644) == 1:
    FUN_180046430(gallery, evidence)
    if feature_count (+0x1c) == maximum_features (+0x20):
        FUN_180047550(gallery)
if action_gate (+0x688) != 0 and selection_enabled:
    if probe.quality (+0x11c) > 15 and probe.coverage (+0x120) > 65:
        primary_selected_index = FUN_1800469f0(..., replacement_enabled)
```

Retained refresh and exact-capacity normalization precede every action gate.
Count zero or flag other than exactly one skips both. Neither this dispatcher
nor its selector consumes strict relation metric count `+0x668`; selected
direct affine `+0x650` and relation affine `+0x66c` remain distinct evidence.

Retained refresh does not test graph state. For an unestablished graph it can
replay against the previous reference before selection. A successful append may
then establish the matched reference and replay positive-count evidence again
inside `FUN_1800469f0`, independently of the first call's flag predicate.
See that selector and `FUN_180046430.md` for their mutation contracts.

## Queue Ordering

At `0x1800450af..0x1800450e2`, a nonnegative primary selected index and queue
state `+0x8e00 == 0` enter `FUN_18005d330` on the same live gallery. The third
argument is original evidence `+0x694`. Its separate follow-up-status output
starts at zero. The helper return value is ignored; only follow-up status
exactly five changes the local action to five (`0x1800450e7..0x1800450f7`).
Otherwise the original primary action survives unchanged.

After all continuation, `0x1800450fb..0x180045149` tests exact feature-count /
maximum equality and state zero. If both hold it writes state one, destroys
all twenty queue owners through `FUN_180037b10`, and resets every rank to `-1`.
This shutdown is action-independent and precedes the late enqueue path. An
append that reaches capacity therefore still runs continuation before shutdown;
shutdown is not performed between queued selectors.

At `0x180045150..0x18004519b`, late enqueue requires all of:

- Selection mode enabled.
- Primary selected index exactly `-1`.
- Original evidence action gate `+0x688` exactly one.
- Queue state zero.
- Probe coverage `>65` and quality `>15`.

It calls `FUN_1800462a0` with the current probe, gallery, twenty owner pointers,
and ranks. It does not run continuation or return action five. Queue insertion
and matcher admission are separate contracts owned by `FUN_1800462a0` and
`FUN_180055a40`.

The type-12 branch never reads or modifies queue counter `+0x8e04` (packed
`fb`); shutdown changes state `+0x8e00` (packed `fa`) only. Bodies and ranks
are transient. The later counter-based shutdown branch is not type 12.

## One-Time Finalization

After primary selection, all queued work, shutdown, and late enqueue, original
evidence `+0x684 != 0` enters `0x1800451a0..0x1800451c6`. Type 12 satisfies
bitset `0x413002`; it calls `FUN_1800607b0` once, then increments dword
`gallery+0x8cf4` once. The increment wraps modulo `2^32` without carry into
`+0x8cf8`. The finalizer does not test the action. Gate zero skips both order
and tail, without undoing earlier mutations.

Selector-owned normalization is earlier and separate: primary retained refresh
at capacity can normalize before policy, while a capacity-reaching append and
actions 3/4 normalize inside the selector before any later queued rematch.
The queued caller enters that selector directly without the dispatcher prepass.
There is no per-primary or per-queued order/tail finalization.

## Action And Publication Domain

For this path, initialized zero plus selector assignments `1`, `2`, and
`4 - geometric_fallback_boolean`, plus the sole queued override `5`, give the
complete action output domain `0..5`. No action-six assignment exists here.
Codes one through four are primary selector actions; five means at least one
queued selector returned a nonnegative index. Empty/no-positive continuation
preserves the positive primary action, never synthesizing action zero.

Generic live full-capacity/state-zero inputs can supersede primary 2/3/4 with
five. A gallery that reaches capacity through this dispatcher is shut down
before persistence, so subsequent ordinary transactions on that gallery cannot
enter continuation while its persisted state remains one.

`templateStudy` first tests the same original `+0x688` word. Gate zero at that
export skips this dispatcher entirely, including prepass, queue work, order,
and tail, while still consuming the original probe. Once entered, action-zero
paths can mutate transient gallery/queue state. This function never packs;
positive adapter publication and action-zero discard belong to `templateStudy`
and its adapter callers. See `FUN_18005d330.md`, `FUN_18002ba60.md`, and
`FUN_18002bbf0.md`.
