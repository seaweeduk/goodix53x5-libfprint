# FUN_180044fc0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry: `0x180044fc0`; relation-relevant instruction ranges are recorded below.
- Role: production `templateStudy` policy and lifecycle dispatcher.

## Signature

Windows x64 signature recovered from `templateStudy` and the decompiler:

```c
int32_t FUN_180044fc0(
  uint32_t *matched_template,
  void *probe_object,
  const int32_t recognition_evidence[422],
  int32_t study_result[2],
  int32_t mode);
```

The export passes mode `1`; `study_result[0]` is the update/action code and
`study_result[1]` is the selected feature/detail index. The function returns
zero after policy execution.

## Call Graph

Known caller:

- `templateStudy` at `0x180001f70`.

Known callees:

- `FUN_18005d330`: matcher/evidence refresh path.
- `FUN_1800469f0`, `FUN_180046430`, `FUN_1800462a0`: study/relation helpers.
- `FUN_180047550`, `FUN_18007b310`: template lifecycle/update helpers.
- `FUN_1800607b0`, `FUN_180054830`: template-type policy helpers.
- `FUN_180037b10`, `FUN_180068b70`: cleanup.
- `FUN_1800607b0`: profile-9/type-12 accepted-study order finalization.
- `FUN_180054830`: sibling order finalization for other template families.
- `memset`, `__security_check_cookie`.

## Layout

- Entry: `0x180044fc0`.
- The export keeps the matched enrolled template in global `0x18024e548`.
- Recognition evidence is the `0x698` block at global `0x18024e550` populated
  by `identifyImage`.
- Row 202 evidence has matched index `7`, score `50`, relation count `0`, and a
  zero relation-leading value, but Windows still appends.
- The native append consumes a seven-word relation where word zero is allowed
  to be zero and words one through six carry the affine transform.

## Pseudocode

Recovered profile-9/type-12 path:

```text
initialize action to zero and selected index to -1
derive enabled/update mode from mode argument
for non-legacy template types:
    optionally refresh retained state from evidence +0x640/+0x644
    if evidence[+0x688] != 0 and mode is enabled and probe dimensions pass:
        selected = FUN_1800469f0(template, probe, evidence, &action, update_mode)
        optionally refresh matching evidence/state
    run template lifecycle finalization and acceptance bookkeeping
store action and selected index in study_result
return 0
```

For the production profile-9/type-12 path, the exact primary action gate at
`0x180045078..0x1800450aa` is ordered after retained refresh and requires:

1. evidence `+0x688 != 0`;
2. study mode enabled (`templateStudy` passes mode 1);
3. probe quality `+0x11c > 15`;
4. probe coverage `+0x120 > 65`.

All comparisons are strict. Type 11 uses coverage threshold 55; type 12 uses
65. Failure leaves action zero and selected index `-1`; it does not enter
`FUN_1800469f0`. The later accepted-evidence `+0x684` finalizer is independent
of this action gate.

Neither this policy nor its call to `FUN_1800469f0` checks evidence relation
count `+0x668`.

The previous inference from row 202 to packed-relation omission is superseded.
Evidence count zero proves append eligibility is independent of `+0x668`, but it
does not create a live edge. Relation creation is downstream: retained refresh
can write reference-star slots, and append writes its slot only when matched
feature `b5` is active. The serializer then emits every reference-star slot with
`leading>=0`, including zero.

For production type 12, accepted evidence `+0x684 != 0` reaches
`FUN_1800607b0` at `0x1800451c1` after the primary append and any queued
follow-up processing. The helper sorts template order state at `+0x87e8` before
`templatePack`. The nearby `FUN_180054830` call is the opposite-generation
comparator and is not the type-12 path.

Before testing `evidence+0x688`, probe quality/coverage, or selecting an update
action, the nonlegacy path tests `evidence+0x640 > 0` and
`evidence+0x644 == 1` at `0x180045049..0x18004505b`. Only that conjunction calls
`FUN_180046430` at `0x180045063`. The call activates every feature indexed by the
retained-evidence array and refreshes its relation state. It is therefore a
pre-action study mutation, not a match-time mutation and not conditioned on the
eventual update code. The supplied campaign observes the conjunction only on
three update-1 append rows; no update-0 counterexample occurs.

Assembly fixes the complete dispatch condition: after the non-legacy study
block, `evidence+0x684` must be nonzero; template type must be at most 22 and its
bit must be set in `0x413002`. Type 12 satisfies that bitset and calls
`FUN_1800607b0`; a failed bit test calls `FUN_180054830`. This finalization is
not conditioned on the returned persistence/update code.

## Constants

- Recognition evidence size: `0x698` bytes.
- Evidence matched index: `+0x648`.
- Evidence score: `+0x64c`.
- Evidence relation count: `+0x668`.
- Evidence relation transform: `+0x66c` through `+0x680`.
- Exported study mode: `1`.
- Official append update code: `1`.
- Official replacement update code: `4`.
- Static flag-zero replacement update code: `2`.
- Internal flag-one geometric-fallback code: `3`.
- Type-12 outer quality/coverage gates: `>15` and `>65`.
- Type-12 order finalizer call: `0x1800451c1`.
- Accepted-evidence gate: `0x1800451a0..0x1800451a7`.
- Type bitset selection: `0x1800451ad..0x1800451bc`.

## Artifacts

- Row 202 match result:
  `re/milan/private/real-learning-profile9-20260722/replay-full/probe-0202/identify-self-match-result.bin`
- Row 202 serialized probe:
  `re/milan/private/real-learning-profile9-20260722/replay-full/probe-0202/identify-self-probe-template.bin`
- Row 202 after-match template:
  `re/milan/private/real-learning-profile9-20260722/replay-full/probe-0202/identify-self-template-after-match.bin`
- Row 202 after-study template:
  `re/milan/private/real-learning-profile9-20260722/replay-full/probe-0202/identify-self-template-after-study.bin`
- Native replay gate:
  `re/milan/scripts/run-native-study-replay-test.sh`

## Validation

- Native and official row-202 match index/score are `7/50`.
- Native row-202 after-match defined template fields are exact after the generic
  tag-`be` lifecycle increment.
- Windows returns study status `0`, update code `1`, and appends.
- Native now treats nonnegative relation counts as valid construction results;
  row 202 reaches append action `1`.
- The corrected row-202 identify-only oracle produces the same official
  after-match and after-study templates as the earlier combined oracle, but the
  pre-study retained probe serialization has SHA-256
  `eaa29ebcb86bd3d1ab5e2bdb43ef58524a0e1fb565c29f969be6d72d2fee1124`.
- The prior enrollment-generated substitute has SHA-256
  `92ba44d3c96608ed4d49af4bdc1fb9a15cc175109a8f814eb99d198e3c6ad41d`
  and is not authoritative study input.
- Independent verdict: high confidence that this is the production
  profile-9/type-12 caller path, not a Wine call-order artifact. Eight direct
  helper processes also reproduce the result with production-first and
  tie-first invocation order. Audit report SHA-256 is
  `06fda76c874dd0aa554835c5f69ba9d9289f461aa0bc046901f8be430ba7c435`.
- Native matching now exposes the ordered retained-evidence indices and the
  separate `+0x644`-equivalent flag at their actual final direct-positive
  evidence boundary. `goodix_match_study_feature` validates that state and
  applies only the `b5` activation before dispatching the unchanged append or
  replacement path. It does not derive the list from the lifecycle mask,
  selected feature, or campaign row identity.
- Corrected enrollment-1 sequence 3 now has exact `b5` and type-12 order. The
  bounded native matrix also has exact relation count six, slots
  `[1,2,7,22,29,37]`, and all defined relation values.
- Static and frozen-transition analysis resolves official count six as live
  reference-star slots `[1,2,7,22,29,37]`; no branch consumes evidence
  relation count. Native materialization now follows that rule and packs once.
- Enrollment-1 sequence 5 is the discriminating natural gate control: accepted
  score 26, probe quality/coverage `85/97`, feature count `11/40`, and retained
  flag `+0x644 == 1`, but evidence `+0x688 == 0`; action remains 0.
  Enrollment-2 sequence 5 has the same quality/coverage and count but
  `+0x688 == 1`, so it appends with action 1.
- Across all corrected 798 rows, the ordered outer policy partitions into 405
  not accepted, 43 accepted evidence-gate-zero, 64 append, and 286 direct
  code-4 capacity replacement rows. Fourteen synthetic DLL controls establish
  strict boundaries independently of those rows.
- Native matching now publishes its exact `+0x688`-equivalent outer evidence as
  `study_action_gate`. The native policy projection is update exact `798/798`,
  including sequence-5 `0/0` and `1/1`; the immutable matcher remains exact
  `798/798`. The projection uses the pinned action ledger and contains no row or
  target constants.

## Confidence And Unresolved

- Confidence: high for the production profile-9/type-12 call path, retained
  refresh predicate, relation-count exclusion, and order-finalizer dispatch.
- The exact vendor name of evidence gate `+0x688` remains unknown; its action
  admission role and producer state are resolved.
- The function itself always returns zero and has no rollback path. Ordinary
  ownership and update-0 discard semantics are resolved; helper allocation
  crashes remain process failures rather than recoverable statuses.
- Corrected natural accepted update-zero rows make no replacement mutation and
  do not advance persistence. Controlled update 0 proves accepted-evidence order
  generation can still mutate transient live state, which callers discard.
- Exact source-level name for the `+0x640` entry array; its layout, count,
  feature-set semantics, and `+0x644` caller gate are resolved.

## Rare-Action Dispatcher Closure

Assembly at `0x180045098..0x1800450f7` proves that action 3 from
`FUN_1800469f0` is retained in the local result. A selected index `>=0` enters
`FUN_18005d330` only while queue state `+0x8e00` is zero. The helper does not map
3 to 4: if it reports follow-up status 5, the dispatcher overwrites the action
with 5; otherwise 3 survives. A direct code-3 control with queue state zero and
all 20 queue entries empty returns external 3.

Accepted-evidence gate `+0x684` then invokes `FUN_1800607b0` and increments
`+0x8cf4` without testing action. Thus code 2, code 3, and rejected update 0 all
reach final ordering when accepted evidence is present. Finally
`0x1800452aa..0x1800452b7` copies the local action and selected index unchanged
to the caller. No dispatcher error path or rollback exists; it always returns
zero. Confidence is high from assembly and eight controlled export calls.

## Complete Type-12 Queue Lifecycle

For type 12, a primary selected index `>=0` invokes `FUN_18005d330` only while
state `+0x8e00 == 0`. Follow-up status 5 unconditionally overwrites the local
primary action, so primary codes 1, 2, 3, and 4 all have the same possible
external supersession. The helper does not expose its inner queued action; the
only external result is 5. Status zero preserves the original action exactly.

That statement is the generic live-object contract, not equal canonical
reachability for all four primaries. Action 1 is below-capacity append and can
run with state zero, including the append that reaches capacity in the current
transaction. Actions 2, 3, and 4 are selected only when the gallery is already
at maximum capacity. A canonically evolved type-12 gallery reached that count
through an earlier dispatcher transaction which set state one and persisted
`fa=1`, so its later replacement primaries cannot enter `FUN_18005d330`.
Superseding 2/3/4 therefore requires a structurally accepted but noncanonical
full-gallery/state-zero object, such as a direct control or externally supplied
template. The assembly still requires the generic implementation to handle it;
it does not establish ordinary production frequency for those combinations.

If feature count equals maximum capacity after primary/follow-up processing,
the dispatcher sets state to 1, deep-frees all 20 cache owners, and resets all
ranks to `-1`. A direct full-capacity update-0 control proves this transition as
state/allocated/occupied `0/20/0 -> 1/0/0`. It occurs before the late enqueue
path and is independent of action identity.

If no primary index was selected, mode is enabled, evidence `+0x688 == 1`
exactly, state is 0, coverage is `>65`, and quality is `>15`, the dispatcher
calls `FUN_1800462a0` to enqueue the current probe for future continuation in
the same live object. This late path does not itself return 5 because there is no
primary selected index to trigger follow-up processing.

The state-1 threshold/counter logic at `0x18004525c..0x1800452a5` belongs only
to template types `9/10/17/18`; type 12 takes the earlier nonlegacy branch.
Although scalar `+0x8e04` is packed for every type, type-12 queue shutdown is
feature-capacity driven and does not increment that counter.

Instruction search finds the only non-codec writes to `+0x8e04` in this
dispatcher at `0x180045273/0x18004527c`, inside the later state-1 branch for
types `9/10/17/18`. Type 12 takes the earlier branch and never reads or writes
the counter. Nonzero and `UINT32_MAX` therefore survive a type-12 transaction
unless a caller explicitly replaces them.

Capacity is always the exact comparison live `+0x1c == +0x20`. The resulting
type-12 shutdown writes queue state `+0x8e00=1`; it does not synthesize a
separate capacity field. A unit-6 direct `8/9 -> 9/9` append proves action/index
`1/8`, owner cleanup `20->0`, `fa 0->1`, and fresh-unpack disabled state.

### Independent Type-12 Queue-Gate Recheck (2026-08-04)

- Primary queue consumption is reached only after selector return index is
  nonnegative and queue state is zero at `0x1800450af..0x1800450e2`; status 5
  supersedes the primary action via `CMOVZ` at `0x1800450f0..0x1800450f7`.
- Exact-capacity shutdown is action-independent at
  `0x1800450fb..0x180045149`. The distinct late producer requires enabled mode,
  selected index `-1`, exact gate `+0x688==1`, queue state zero, coverage `>65`,
  and quality `>15` at `0x180045150..0x18004519b`. It is not the match-time
  producer in `FUN_180055a40` and cannot itself return action 5.
- Accepted finalization follows both queue paths at `0x1800451a0..0x1800451c6`.
  Type 12 is selected by bitset `0x413002` and calls `FUN_1800607b0`; the sibling
  `FUN_180054830` branch is not ours. The later state/counter policy at
  `0x18004525c..0x1800452a5` and matcher family `FUN_180062a30` belong to live
  types `9/10/17/18`; they must not be copied into type-12 behavior.
- Confidence is high from disassembly, graphless action controls, and the
  approved action-5 control whose after-study SHA-256 is
  `c971239885adccdcaece80b31dc9e5e3abea6b0cc428b1b82307cb05fe12b181`.

## Study-Path Coverage Audit

The current action-5 matrix is
`re/milan/ACTION5-FRESH-AUDIT-20260818.md`. Accepted-evidence finalization is
action-independent. Native now reproduces the direct
coverage-`-11` action-0 order and `+0x8cf4` transition in a non-publishable
diagnostic state; the adapter and native wrapper persist neither transient
object.

The native ordinary wrapper now preserves the initialized action 0/index `-1`
contract for accepted positive no-direct matches. It validates the probe and
returns success without producing or persisting an update. Immutable
enrollment-2 sequence 157 is exact at study status/action/persistence `0/0/0`,
and generic direct, aggregate-selected-`-1`, rejected, and malformed controls
retain their distinct outcomes. The queued late-enqueue path remains a separate
predicate and still returns action 0.

At capacity, this function invokes `FUN_180047550` after retained refresh and
before `FUN_1800469f0`. The exact conjunction at
`0x180045049..0x180045073` is signed retained count `>0`, retained flag
`+0x644==1`, and exact live `feature_count==maximum_features`. Zero count or
flag zero skips both refresh and this normalization. The wrapper receives only
the same live gallery pointer and normalizes every feature, not only retained or
candidate features.

The retained-refresh call is also part of the now-complete first-graph contract.
It does not test `f5`. Therefore `count>0 && flag==1 && f5==0` first replays the
ordered entries against stale `f2` here, before the action gate. A successful
below-capacity append then enters `FUN_1800469f0`, which overwrites `f2` with the
matched index, creates the append anchor, and replays the list a second time
because old `f5` was zero. The selector's second call checks only positive count,
not flag. Direct controls prove that collapsing these calls loses an observable
stale-reference edge when the retained list contains the future reference.

For the controlled accepted type-12 calls, this function then preserves action
and selected index `1/n`, performs queue shutdown only when append reaches
capacity, calls `FUN_1800607b0` exactly once under `+0x684!=0`, and increments
`+0x8cf4` exactly once. The 39-to-40 control returns selected 39, action 1,
queue state `0->1`, and tail `31->32`. Evidence:
`<evidence-root>/derived/milan-native-capture-20260723/study-first-graph-unit3-v1/`.

Native unit 3 now preserves both caller phases exactly. The append wrapper runs
the flag-one stale-`f2` prepass before graph establishment, and the selector path
runs positive-count post-anchor replay independently of the flag. All 13 frozen
controls are packed-byte exact, including 39-to-40 queue shutdown and one
order/tail finalization. No row-specific branch was added.

## Full-Capacity Retained Normalization Closure

Backlog unit 4 is complete in static and synthetic-valid DLL RE. The primary
type-12 sequence is:

```text
if retained_count > 0 && retained_flag == 1:
    FUN_180046430(gallery, evidence)
    if feature_count == maximum_features:
        FUN_180047550(gallery)
then apply +0x688/mode/quality/coverage gates
then call FUN_1800469f0(gallery, probe, evidence, &action,
                        replacement_enabled)
```

The selector later calls `FUN_180047120` for committed actions 3/4. Therefore a
full retained flag-one action 0 normalizes once before policy, actions 3/4
normalize twice (before policy and after replacement), flag-zero action 2
normalizes zero times, and flag-one/count-zero action 4 normalizes only once
after replacement. A retained 39-to-40 append fails the caller's exact capacity
test and normalizes once inside the selector after append reaches capacity.

The queue caller `FUN_18005d330` invokes `FUN_1800469f0` directly and has no
equivalent retained-refresh/pre-normalization phase. Its queued actions 3/4 keep
only selector-owned post-mutation normalization.

The stale-`bb` discriminator proves observable target selection. With identical
profile-9/type-12 live state, direct selector entry chooses feature 13 from the
supplied stale values. This dispatcher refreshes retained reference 0,
normalizes, and chooses feature 34 by the recovered lowest-`bb`, lowest-coverage,
highest-`+0x148` order. Explicit
`FUN_180046430 -> FUN_180047550 -> FUN_1800469f0` staging and an already-
normalized input both produce the complete dispatcher bytes at SHA-256
`5dc15026cc41963fb9342441757608d531ca0c2a488a7664ab6ced7a4faf4ec3`.

All full-capacity controls with initial queue state zero, including actions
0/2/3/4, execute the action-independent shutdown at
`0x1800450fb..0x180045150`: state becomes one, all 20 owners are deep-freed, and
ranks become `-1`. Accepted evidence still invokes `FUN_1800607b0` and increments
`+0x8cf4` once after that transition, including transient action 0.

Ten controls pass twice byte-identically under
`<evidence-root>/derived/milan-native-capture-20260723/study-capacity-normalization-unit4-v1/`.
`controls.jsonl` and `SHA256SUMS` SHA-256 values are
`77451907c40100ac3f7662598816ec2bf60db7fd4946d07d8f8e9ed6dd45f0a7`
and `058d542f2d8fccc552c94bf730b40e8b84cdc10a84fa35baeb08ac1868f638ef`.
The 798-row chronology has three retained-refresh calls, all below-capacity
appends; no natural row is predicted to change.

Native now models this caller boundary explicitly. Ordinary and queued-primary
study pass a dispatcher-prepass context; queued follow-up passes direct-selector
context. The primary full-capacity positive-count/flag-one path refreshes and
projects the same working gallery, normalizes all live features, and only then
derives policy. All ten frozen controls pass twice, the dispatcher/staged/
already-normalized discriminator is exact at SHA-256
`5dc15026cc41963fb9342441757608d531ca0c2a488a7664ab6ced7a4faf4ec3`,
and the direct selector still chooses feature 13. Native run-log SHA-256 is
`7f225aae7a1f493bbd339625ffbc940c24d3f89c0ddcfd765768a059bf12421e`.
Gate script/test source SHA-256 values are
`9b87a36bd42bc7c07d948e881e4aeef804b30afcaffb9fdda8cf881ebc01f46f`
and `23b252ae8bc377f6bdeaaccf070b566c4277285d9f9a3a7855f801a59789af67`.
The natural policy projection remains `798/798`; full chronology was not run.

Unit-6 native migration now derives every capacity branch in this dispatcher
from exact count/maximum equality. Packed `fa` controls only transient queue
allocation/shutdown and `fb` is carried unchanged. Full-enabled and below-full
disabled controls therefore take their recovered policy branch independently,
and queue shutdown is projected into the positive candidate regardless of the
later accepted order/tail gate.

## Action-5 Finalization Boundary

The queued object handoff is now exact. After `FUN_1800469f0` returns the primary
selected index, this dispatcher calls `FUN_18005d330` on the same live gallery.
The queue helper performs every queued matcher write and ordinary selector
mutation before control reaches `0x1800451a0`. Only then does accepted evidence
call `FUN_1800607b0` at `0x1800451c1` and increment `+0x8cf4` at
`0x1800451c6`. There is no per-primary or per-queued order/tail finalization.

The sequence-3 direct trace proves primary action `1`, follow-up status `5`, and
the `CMOVZ` override at `0x1800450f0..0x1800450f7`, followed by one order call
on count 11. The final handle still points to that gallery when `templatePack`
runs. Normalization is distinct: selector-owned `FUN_180047120` calls for a
capacity-reaching append or actions 3/4 remain inside primary/follow-up mutation
and must be visible to continuation before this final order boundary.

Native currently supplies the queued matcher a primary-only object that has
already crossed this boundary. Its packed payload hashes exactly to the DLL's
primary-only finalized result `081c3e...`, proving the gallery data is current
but its lifecycle phase is too late. Native also supplies serialized Q rather
than the DLL's full deep-owned live feature. The bounded implementation must
preserve full live Q and defer `FUN_1800607b0`, `+0x8cf4`, and final publication
packing until queue exhaustion while leaving selector normalization immediate.

## Complete Accepted Action-0 Finalization

Unit-5 Ghidra recovery and direct adapter controls correct one caller-level
assumption. Recognition evidence base is global `0x18024e550`, so evidence
`+0x688` is address `0x18024ebd8`. Exported `templateStudy` tests that exact word
at `0x180001fe3..0x180001fe9` before this function is called. On the production
adapter/export path, shared gate zero therefore skips this function entirely.
The export still consumes the retained probe, but no retained prepass, queue
handling, order, or tail mutation occurs.

This function's own control flow remains action-independent once entered. The
complete type-12 action-0 sequence is:

```text
optional retained refresh and exact-capacity normalization
strict quality/coverage policy attempt and selector
queued follow-up only after selected >= 0
exact-capacity queue shutdown
late enqueue only for selected == -1, gate == 1, state 0, and strict gates
accepted evidence +0x684 test
type bitset 0x413002 selects FUN_1800607b0 for type 12
INC dword ptr [gallery+0x8cf4]
publish action 0/index -1 to the caller
```

The finalizer block is `0x1800451a0..0x1800451cc`. It follows every action and
queue block and does not test local action at `RSP+0x38`. `FUN_1800607b0` runs
before the `INC`. The increment is exactly 32 bits, wraps modulo `2^32`, carries
nothing into `+0x8cf8`, and has one call site per dispatcher invocation. Direct
controls prove `0->1`, `7->8`, `32->33`, `100->101`,
`0x12345678->0x12345679`, and `UINT32_MAX->0` plus unstable tie order
`[1,3,2,0]`.

Dispatcher-entered external action 0 includes strict outer quality/coverage
rejection, later policy rejection, code-2 coverage `-11`, positive no-direct at
capacity/disabled queue, and positive no-direct below capacity with late enqueue.
Full state-zero calls shut the queue down before ordering. The late path deep-
copies Q and then orders/tails; it does not convert action 0 to 5 because queued
processing requires an earlier nonnegative primary index. There is no
queue-exhausted/no-positive action-0 family: an empty or no-positive queue is
reached only after primary action 1..4 and preserves that primary action.

`+0x684==0` skips order/tail after any earlier dispatcher mutations. Rejected
authentication is outside this function because the adapter does not call
`templateStudy` without its successful-match latch. `FUN_18005ef60` also has a
static call to this dispatcher, but Ghidra resolves no code caller for that
wrapper; its three data references are PE runtime/unwind records. It does not
override the proven production adapter/export ordering.

Ten direct profile-9/type-12 controls, including live size/pack traps, are under
`<evidence-root>/derived/milan-native-capture-20260723/study-action0-finalization-unit5-v1/`.
The controls and report SHA-256 values are
`2a5798ca8c303b608d5214a97d1fe05592425f985ea9ed51b4bbac68a82dc9d7`
and `85a8f262d0feb69daf56ea52289f1bc8ca9bfc84608340b3db8a99e44c46a45c`.
Confidence is high for the complete exported production path. Natural frequency
outside the supplied chronology and indirect use of `FUN_18005ef60` remain
unclaimed.

## Native Unit-5 Closure

Native now models this caller boundary with an explicit dispatcher-entered
transient state. Retained refresh and any full-capacity normalization are
captured from the working unpacked gallery; queue shutdown or late enqueue runs;
then the type-12 sorter and unchecked `uint32_t` tail increment run once. The
state is diagnostic-only and discarded without a `GBytes` candidate. Shared
`+0x688==0`, rejected auth, and `+0x684==0` preserve their recovered exclusions.
All ten controls pass twice at native run-log SHA-256 `715e6aa0...37797fb`.

## Identify-305 Relation Handoff

For frozen identify 305 this dispatcher performs no relation transform of its
own. It receives the matcher evidence after a mixed direct-then-rescue result,
passes the same block to `FUN_1800469f0`, and later performs ordinary action-4
finalization. The approved matcher state intentionally has selected
feature/affine 8/`[268,74,-16487,-74,246,18204]` while relation evidence remains
owned by direct-positive feature 33 at
`10/[254,-35,5792,35,254,-2496]`.

Historical source collapsed those owners before entering this function. Current
production commit `1b017754` publishes the accumulated active relation winner
before rescue returns, so this dispatcher now receives the approved independent
owners. No dispatcher gate, retained refresh, queue branch, or final order
operation changes the relation on this row. The complete handoff and correction
are documented in
`re/milan/IDENTIFY-305-STUDY-RELATION-ROOT-CAUSE-20260808.md`.

## Current Action-2/Action-3 Family Lifecycle Audit (2026-08-18)

An independent recheck against production source commit
`7e92c8f563f5d20da70b9c2fe36abbbe53198194` and the approved DLL supersedes the
stale native-handoff warning in `Action-5 Finalization Boundary`. Current native
queue study keeps one unfinalized gallery transaction, deep-owned transformed
live queue entries, per-physical-feature live record owners, and the live
`+0x148` overlap vector through queue exhaustion. It runs
`FUN_1800607b0`-equivalent ordering and the `+0x8cf4` transaction-tail increment
only in the final transaction pack.

The shared full-capacity lifecycle has an intentional action-family split:

```text
primary dispatcher only, when retained_count > 0 && retained_flag == 1:
    FUN_180046430
    project the changed relation star for native serialized handoff
    FUN_180047550 -> FUN_180047120

FUN_1800469f0 action 2:
    FUN_1800458c0
    clear target incident relations only when target != reference
    target b5 = 0
    no FUN_180045d40 and no FUN_180047120
    preserve the pre-existing live +0x148 vector

FUN_1800469f0 action 3 or 4:
    copy matched be
    FUN_1800458c0
    FUN_180045d40
    FUN_180047120 over the complete live gallery
    replace the live +0x148 vector with the recomputed values
```

The primary dispatcher reaches `FUN_18005d330` only for selected index `>=0`
and queue state `+0x8e00==0`. The queue helper calls `FUN_1800469f0` directly, so
it has no retained-refresh/full-capacity prepass. It consumes physical queue
slots `0..19`, preserves entries whose selector returns `-1`, removes and
reranks only entries with a nonnegative returned index, and appends that returned
index to its modulo-20 continuation ring. Consequently a queued selector after
action 2 must see stale-but-live `bb/+0x148`, while one after action 3/4 sees the
selector-owned normalized values. Reconstructing `+0x148` from packed masks at
each queued selector would be a synthetic-valid defect even when the primary
action or final packed result appeared correct.

All queued mutations complete before the single accepted-evidence finalizer at
`0x1800451a0..0x1800451c6`. Queue status 5 supersedes primary 2 or 3 exactly as
it supersedes 1 or 4; otherwise `0x1800452aa..0x1800452b7` preserves the primary
action. Canonically evolved type-12 full galleries have queue state one, so
2/3-to-5 requires a structurally valid but noncanonical full/state-zero object;
this is a valid generic contract, not established natural frequency.

Current native relation projection before each intermediate pack is a transport
requirement, not an extra DLL mutation phase: the DLL retains one live matrix,
whereas native queued matching reopens the serialized current gallery. Final
type-12 ordering remains deferred. Current native allocation, packing, and
validation failures discard the working candidate and report a learning error;
the DLL helpers have no rollback and the adapter ignores `templatePack` status.
That difference is an intentional safety boundary, not a successful-input parity
defect.

One synthetic-valid selector defect preceded these lifecycle phases at source
commit `7e92c8f5`. For flag-zero ranked-primary validation, DLL
`FUN_180045530` scans eligible features with `b5!=0` but counts the required
three zero-`bb` features with exact `b5==1` at
`0x180045620..0x180045645`. The older native policy used nonzero for both
predicates, allowing packed `b5=2` to enter action 2 where the DLL returned
action 0. Production commit `87a02ffb` changes only that count to exact one; the
focused witness, existing suites, and both standing campaigns pass. Known
producers remain Boolean, so the fixed distinction is synthetic-valid rather
than naturally reproduced. The witness and source comparison are in
`FUN_180045530.md` and `FUN_1800469f0.md`.
