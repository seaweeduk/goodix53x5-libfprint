# FUN_1800469f0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry: `0x1800469f0`; append and graph-establishment body ranges are recorded
  below.
- Role: select and apply the append/replacement study action.
- Scope: this is the authoritative profile-9/type-12 selector. Selector or
  replacement routines attributed to the alternate-profile `0x180062...` and
  `0x180064...` families do not own this study policy.

## Signature

Windows x64 signature recovered from `FUN_180044fc0` and the decompiler:

```c
int32_t FUN_1800469f0(
  int32_t *enrolled_template,
  void *probe_feature,
  const int32_t evidence_0x698[422],
  int32_t *study_action,
  int32_t replacement_enabled);
```

The return value is the inserted/replaced feature index or `-1`.

## Call Graph

Known callers:

- `FUN_180044fc0`, the `templateStudy` policy/dispatcher.
- `FUN_18005d330`, the evidence refresh path.

Known callees:

- `FUN_1800456b0`: append/slot selection helper.
- `FUN_180045530`, `FUN_180046ef0`, `FUN_180071370`: replacement eligibility
  and candidate selection helpers.
- `FUN_1800458c0`: feature replacement/copy helper.
- `FUN_180045d40`: relation materialization/update helper.
- `FUN_180046430`, `FUN_180047120`: lifecycle/finalization helpers.
- `FUN_180003800`: allocation.
- `__security_check_cookie`.

## Layout

- Entry: `0x1800469f0`.
- Evidence reads at entry:
  - matched feature index `+0x648`;
  - study/retained flag `+0x644`;
  - primary transform pointer `+0x650`;
  - relation transform pointer `+0x66c`.
- Evidence relation count `+0x668` is not read in the entry/append path.
- The below-capacity branch begins near `0x180046ad3` and writes append action
  `1` before calling `FUN_1800456b0`.
- Live feature offsets copied from the matched feature include lifecycle tag
  `be` at `+0x134` and retained-state field `+0x110`.

## Pseudocode

Recovered append path:

```text
matched_index = evidence[0x648]
study_flag = evidence[0x644]
primary_transform = &evidence[0x650]
relation_transform = &evidence[0x66c]
if matched_index == -1:
    return -1
copy selected anti-fake state from matched feature into probe
if feature_count < capacity:
    action = APPEND
    new_index = append_probe_feature(...)
    copy matched be and retained state into new feature
    if this is the first retained append:
        activate retained state on matched and new features
    if matched retained state is active:
        materialize relation(new_index, relation_transform)
    finalize lifecycle state
    return new_index
otherwise:
    evaluate replacement candidate and update feature/relation
```

The complete production type-12 capacity policy is:

```text
if replacement is disabled or probe quality <= 15:
    no update
if matched quality >= 60 and probe quality * 10 <= matched quality * 6:
    no update

candidate = FUN_180045530(...)
if candidate == -1 and evidence flag +0x644 == 0:
    require primary-transform area * 100 > domain area * 80
    candidate = matched feature
if candidate == -1 and evidence flag +0x644 == 1:
    candidate, percent = FUN_180046ef0(...)
    require percent >= 80 or (percent >= 60 and template +0x8cf8 > 1000)

if evidence flag +0x644 == 1:
    action = 4 for a primary candidate, 3 for a geometric fallback
    replace candidate and install its reference relation
else:
    require probe coverage >= matched coverage - 10
    action = 2
    replace candidate, clear its b5, and omit the flag-one relation install
```

For type 11 only, the capacity quality multiplier is 3 instead of 6. Equality
at the type-12 ratio rejects because the passing comparison is strict
`probe_quality * 10 > matched_quality * 6`. The flag-zero primary-transform
area comparison is also strict greater-than 80 percent. The code-2 coverage
comparison is inclusive at exactly matched coverage minus 10.

The complete policy arithmetic is signed dword arithmetic with modulo-`2^32`
wrap: both quality-ratio products, `FUN_180045530`'s residual `+20`, both
full-area products, coverage minus 10, footprint transform multiply/add
intermediates, footprint bounds and area accumulation, and `area * 100` before
percentage division. Comparisons are signed, translation halving uses arithmetic
right shift, and percentage division is signed truncation toward zero. Relevant
selector ranges include `0x180046cd4..0x180046cde`,
`0x180046d9b..0x180046dab`, and `0x180046e1c..0x180046e2c`; footprint ownership
is recorded in `FUN_180046780`, `FUN_180046ef0`, and `FUN_180071370`.

The flag-zero primary-transform fallback uses the full live template dimensions
at `0x180046d73..0x180046dab`: for profile 9/type 12 it calls
`FUN_180071370` over the full `104x88` domain with `evidence+0x650`, without
halving the translations, then requires `area * 100 > 104 * 88 * 80`. This is
intentionally different from `FUN_180046780` and `FUN_180046ef0`, which use the
half-resolution `52x44` domain and arithmetic-shift translations. Replacing the
full-domain test with a half-domain percentage is not generically equivalent at
subpixel and odd translation boundaries.

Production commit `5a411f0` restores the full-domain calculation. The controlled
transform `[240,-24,-3472,24,240,0]` now matches the DLL at area `7329/9152`
and selects action 2. Independent 450- and 643-operation campaigns pass
1,093/1,093 selected operations after the correction.

An independent direct-helper and selector control now discriminates that
boundary. With primary transform `[240,-24,-3472,24,240,0]`, the approved DLL's
`FUN_180071370(88,104,88,104,...)` returns `7329/9152`, strictly above 80
percent, while current `goodix_milan_study_replace()` arithmetic-shifts the two
translations and obtains `1813/2288`, below 80 percent. A full-capacity,
flag-zero, no-primary-candidate control therefore returns DLL action/index
`2/0`, while current policy returns no action at the geometry gate. This is a
generic same-input defect in current source. It is not evidence of natural
frequency: the maintained real corpora still contain no action-2 row, and the
control deliberately constructs the otherwise valid fallback state.

Flag handling also has an exact split. `FUN_180045530` treats zero as the
flag-zero validation family and any nonzero value as the uncovered-probe family;
candidate absence sends zero to the full-domain matched fallback and any
nonzero value to geometric fallback. The mutation branch at
`0x180046dce..0x180046e56`, however, assigns actions 3/4 only for exact flag one;
every other value takes the action-2 coverage branch. The production matcher
publishes only Boolean zero/one, so this distinction is dormant there, but a
generic policy API must either validate Boolean input or preserve the exact
comparisons.

Production commit `1a858825` preserves that split after fallback selection:
nonzero selects the validation family, but only `retained_flag == 1` selects the
action-3/4 mutation family. A retained-flag-2 control returns action/index
`2/1`. Production commit `89dd6c43` implements the wrapped arithmetic above; a
wrapped quality-ratio control returns action/index `4/1`.

`FUN_180045530` returns the matched feature immediately when its live `+0x128`
is zero. Otherwise it ranks active nonreference features by lowest `+0x128`,
then lowest coverage `+0x120`, then highest derived `+0x148`; a complete tie
keeps the earlier index. Flag-zero and flag-one candidate validation are
documented in that helper's note. `FUN_180046ef0` documents the separate
geometric fallback and transient code 3.

Evidence relation count `+0x668` is never read. The constructor always receives
the six-word transform at `+0x66c`.

Append relation behavior instead branches on live graph state. `FUN_1800456b0`
allocates only unset incident slots. If matched feature `b5` is one,
`FUN_180045d40` materializes the appended feature/reference slot from evidence
transform `+0x66c` with leading zero. If `b5` is zero, no append edge is created.
The separate first-graph fallback activates matched/new features and may replay
retained entries through `FUN_180046430`; none of these branches reads
evidence relation count `+0x668`.

## Constants

- Append action: `1`.
- Replacement actions observed in this function: `2` and `4`.
- Internal flag-one geometric-fallback action: `3`.
- Matched index offset: `0x648`.
- Study flag offset: `0x644`.
- Primary transform offset: `0x650`.
- Relation transform offset: `0x66c`.
- Live feature retained-state offset: `0x110`.
- Live feature tag-`be` offset: `0x134`.
- Anti-fake size: `0x1abc`.

## Artifacts

- Row 202 evidence:
  `re/milan/private/real-learning-profile9-20260722/replay-full/probe-0202/identify-self-match-result.bin`
- Row 202 after-match template:
  `re/milan/private/real-learning-profile9-20260722/replay-full/probe-0202/identify-self-template-after-match.bin`
- Row 202 after-study template:
  `re/milan/private/real-learning-profile9-20260722/replay-full/probe-0202/identify-self-template-after-study.bin`
- Native replay:
  `re/milan/scripts/run-native-study-replay-test.sh`

## Validation

- Row 202 is accepted at matched index `7`, score `50`.
- Evidence relation count is zero, but Windows reports append action `1`.
- The append branch does not read evidence `+0x668` before selecting and
  inserting the new feature.
- Native zero-count append now advances feature count `8 -> 9` and registration
  count `29 -> 37` while preserving the official three serialized relations.
- `FUN_1800456b0` copies live feature offset `+0x14c` but not optional `c7` at
  `+0x150`. The zero-initialized appended slot therefore packs without tag `c7`
  even when the immutable serialized probe carries a nonzero value.
- Native mirrors that append-only live-copy boundary. The feature-copy portion
  is exact for authoritative enrollment-1 sequence 2; its complete achievable-v2
  transition still requires the separate `FUN_1800607b0` gallery-order
  finalization documented in that function's note.
- The append copies the matched feature's current `b5` to the new feature at
  `0x180046b19..0x180046b25`, after `FUN_180044fc0` may have activated retained
  evidence entries through `FUN_180046430`. It therefore inherits the matched
  feature's post-refresh value, not the probe's extracted value and not a value
  derived from the selected index during packing.
- Normal-boundary enrollment 1 sequence 3 has matched feature `1` in retained
  evidence `[8,7,2,1]`. The pre-append refresh sets feature 1 `b5` to one, and
  append consequently creates feature 9 with `b5=1`. Across all 64 authoritative
  append rows, the appended value equals the matched feature's post-refresh
  value. Enrollment 1 sequence 2 is the discriminating flag-zero control:
  evidence contains matched feature 5, but `evidence+0x644 == 0`, so existing
  feature 5 and appended feature 8 both remain `b5=0`.
- The separate `template+0x87e4 == 0` branch at
  `0x180046c09..0x180046c31` establishes the first runtime graph anchor and sets
  only the matched and appended features to one. It cannot explain the
  sequence-3 activation of nonmatched features 2, 7, and 8.
- Native study now refreshes the matcher-supplied retained entries before this
  function runs. The existing append copier is unchanged and therefore copies
  matched feature 1's refreshed `b5=1` into sequence-3 feature 9. Flag-zero
  sequence 2 still copies `b5=0`, and count-zero sequence 6 still inherits the
  already-active matched feature's `b5=1`.
- `FUN_180047120` is lifecycle-sensitive, not an unconditional replacement
  helper. A below-capacity append initializes new tag `bb` to zero and preserves
  prior values; only the append that reaches maximum feature count calls the
  normalizer at `0x180046c8a`. The final branch calls it at `0x180046e8c` only
  for actions `>=3`, covering codes 3 and 4 but not code 2.
- Across the corrected chronology this partitions as 62 ordinary appends, two
  capacity-reaching appends, 286 normalized code-4 replacements, and 448
  no-update rows. Direct controls independently show code 3 normalizing sentinel
  `bb=100000` to 2288 and code 2 retaining 100000.
- The authoritative zero-evidence append is enrollment-1 sequence 2. Its matched
  feature remains `b5=0`, so the append allocates sentinel slots but does not call
  `FUN_180045d40`; relation count stays one. This replaces the prior false claim
  that a zero-leading slot was written and then omitted by packing.
- Corrected natural action validation covers all 798 rows: 405 not accepted,
  43 accepted but stopped by evidence `+0x688 == 0`, 64 appends, and 286
  flag-one primary replacements returning code 4. Each enrollment contributes
  `32/143` append/replacement actions; accepted evidence-gate-zero counts are
  21 and 22. No natural code 2 or internal code 3 occurs.
- Fourteen direct DLL controls prove the strict outer, ratio, code-2 coverage,
  and fallback percentage/counter boundaries independently of target rows.
  Analysis and controls are under
  `<evidence-root>/derived/milan-native-capture-20260723/study-action-policy-v1/`.

## Confidence And Unresolved

- Confidence: high for append eligibility, matched-`b5` edge gating,
  relation-transform ownership, and exclusion of evidence relation count.
- The source-level vendor name of live feature active/retained field `+0x110`.
- Codes 2 and 3 remain naturally unobserved, so empirical frequency,
  real-hardware reachability, and security performance remain unvalidated.
  Their live mutation, export, packing, and persistence semantics are resolved.
- Mutation helpers expose no recoverable failure result. Allocation failure can
  crash or leave partial state; no rollback path exists.

## Native Implementation

Native policy preserves the full-domain flag-zero fallback, raw-area geometric
ranking, dispatcher-versus-queued normalization boundary, sentinel-seeded
primary ranking, exact retained-flag split, and wrapped signed dword arithmetic.
The selector witnesses are action/index `0/unset` for a sentinel-equal primary
scan, `4/1` for wrapped quality-ratio admission, and `2/1` for retained flag 2.
These are generic input-contract controls, not claims of natural frequency.

## Complete Code-2/Code-3 Mutation Closure

The previous persistence uncertainty and native discard policy are superseded.
Assembly `0x180046dd5..0x180046e1a` assigns flag-one action as
`4 - fallback_taken`, so fallback is 3 and primary is 4. Both then copy matched
`be`, call `FUN_1800458c0`, and call `FUN_180045d40`. Action 3 also satisfies
the final `action >= 3` normalization call at `0x180046e68..0x180046e8c`.

For code 2, `0x180046e1c..0x180046e56` proves inclusive coverage, replacement,
and final `b5=0` without relation installation. Direct controls establish all
three reachable candidate sources: matched `+0x128==0`, a ranked primary
candidate, and matched fallback after strict primary-transform coverage `>80%`.
Replacement increments template `+0x8cf8` and target `+0x130`, moves target
`bc` to `feature_count-1` while decrementing later ordinals, preserves target
`be`, preserves `b6`, and clears every incident relation only for nonreference
targets. A reference target preserves its star. Code 2 then reaches type-12
ordering and external positive packing.

For code 3, nonreference replacement clears all incident edges and reinstalls
only the zero-leading reference edge; reference replacement composes every
defined star transform and preserves cardinality. It preserves `b5`, copies
matched `be`, applies the same counter/`bc` transition, normalizes, orders, and
is exported unchanged. Production callers persist 3. Queued follow-up 5 is now
recovered as a real intra-match queue re-match and ordinary selector mutation;
native runtime remains pending and must not fake the status override.

Artifacts: `<evidence-root>/derived/milan-native-capture-20260723/study-action-code2-code3-v1/`.

## Native Generic-Normalization Closure

Native now invokes the generic learned-feature normalizer after both code 3 and
code 4 relation mutation. Code 2 still bypasses it, and action 0 still returns
without packing a mutation. All three direct code-3 controls remain exact; the
286 natural code-4 official outputs plus two capacity appends are exact under
native normalization, `288/288`. Remaining sequence-36 `ba` and enrollment-2
sequence-34 `c7/ba` differences belong solely to replacement copy/lifecycle
ownership and were intentionally not changed.

## Full-Capacity Caller Context

The former capacity ordering gap is now completely recovered. The primary
caller `FUN_180044fc0` runs retained refresh and, only for exact full capacity,
`FUN_180047550 -> FUN_180047120` before entering this selector. The conjunction
is retained count `>0`, flag `==1`, and `feature_count==maximum_features`.
Consequently this function's primary capacity ranking consumes post-refresh,
normalized `bb/+0x148` for that family.

The selector's own call at `0x180046e68..0x180046e8c` remains after mutation for
actions 3/4. Full retained actions 3/4 therefore invoke normalization twice;
action 0 invokes it once before selection. Flag-zero action 2 can never satisfy
the caller conjunction and invokes it zero times. Count-zero flag-one action 4
invokes only this selector-owned post-mutation call. At count one below maximum,
the caller does not normalize; an append that reaches maximum invokes the
separate post-append call at `0x180046c77..0x180046c8a` once.

The other caller, `FUN_18005d330`, enters this selector directly for queued
follow-up. It performs no retained refresh or pre-selector normalization, so a
queued selector can consume stale `bb`; queued actions 3/4 still normalize after
their mutation before continuation.

The same direct-selector boundary applies to live `+0x148`, not only `bb`.
`FUN_180045530:0x1800455d0..0x1800455df` reads the existing live value as its
third ranking key. Only the primary dispatcher's optional
`FUN_180047550 -> FUN_180047120` prepass recomputes both `bb` and `+0x148` before
selection. The queued call at `FUN_18005d330:0x18005d4e8` invokes this selector
without that prepass, so it must retain the current live `+0x148` vector across
the primary mutation. Reconstructing overlap counts from masks inside every
policy derivation silently converts the queued direct-selector contract into a
normalizing prepass and can change tertiary ties after action 2, which mutates a
full gallery but intentionally bypasses `FUN_180047120`.

Direct controls discriminate the callers. Stale direct selector input selects
feature 13, while primary dispatcher and explicit
`refresh -> normalize -> selector` select feature 34 and pack byte-identically
at SHA-256
`5dc15026cc41963fb9342441757608d531ca0c2a488a7664ab6ced7a4faf4ec3`.
The normalized choice follows this function's existing candidate helper: lowest
`bb`, then lowest coverage, then highest `+0x148`; it is not a new exclusion or
row-specific rule. Evidence is under
`<evidence-root>/derived/milan-native-capture-20260723/study-capacity-normalization-unit4-v1/`.

Native implementation now preserves this distinction instead of placing the
new call in the selector. Primary replacement receives
`apply_dispatcher_prepass=1`; queued follow-up receives zero. The former runs
retained refresh, relation projection, and complete-gallery normalization before
`milan_study_policy_derive`; the latter consumes the supplied live `bb` and
relations directly. Separate pre/post normalization owner arrays preserve the
existing post-mutation call for actions 3/4 without replacing only the chosen
target. All ten controls and the exact `5dc150...ec3` discriminator pass twice.

## Complete First-Graph Append Contract

The old live graph flag is captured before any branch writes. After
`FUN_1800456b0` appends index `n`, copies matched `be`, and copies matched
post-caller-refresh `b5`, assembly `0x180046c09..0x180046c31` executes only when
old `f5/+0x87e4 == 0`:

```text
f2/+0x87d8 = matched_index
matched.b5 = 1
f5/+0x87e4 = 1
appended.b5 = 1
```

Control then falls through the ordinary active-matched test at
`0x180046c37`. Because first-graph setup forced matched `b5=1`, it always calls
`FUN_180045d40(template, appended_index, evidence+0x66c)`. The appended index is
the greatest live index, so the canonical slot is
`appended.b6 + matched_index`; leading is zero and the six evidence words are
stored directly in appended-to-matched direction. Identity is not omitted.

The canonical graphless matcher boundary makes that identity case concrete.
With `f5=0`, `f2=-1`, zero packed relations, and every enrolled `b5=0`,
`FUN_180055a40` leaves `evidence+0x66c` at its initialized identity because no
active relation winner can replace it. The selected affine remains separately
in the retained entry. `FUN_180046430` then computes
`X = compose(identity, inverse(retained))` and the reference-entry branch writes
the final append edge from that composition. Publishing the selected affine as
the initial anchor applies it twice and is not native behavior.

Packed profile-9/type-12 probes 4 and 6 validate this complete transaction.
Their final relations are respectively
`[0,256,8,3367,-8,256,8235]` and
`[0,253,-41,13386,41,253,-4256]`; raw payload SHA-256 values are
`ba2c7c94d96fc754fb571caaf5b13ba180d55574bb6bf87c2720a46d9a186c61`
and `5bbc07877d9ec78cfc4804e29e57e9247a5e6742ef015f47103daff4be87e992`.
Both are byte-identical to the approved DLL. A negative probe keeps action zero
and the graphless template unchanged.

Only after that anchor exists does `0x180046c51..0x180046c66` test the saved old
flag and evidence count. If old `f5` was zero and count `+0x640` is positive, it
calls `FUN_180046430` regardless of retained flag `+0x644`. Empty retained state
therefore stops after the anchor; any positive list is replayed in its supplied
order. Evidence relation count `+0x668` remains irrelevant.

The complete dispatcher has an additional phase. `FUN_180044fc0` calls the same
replay before action selection when `count>0 && flag==1`, even while `f5==0` and
before this function overwrites stale `f2`. Such calls therefore replay twice:
first against stale `f2`, then against the matched reference. This is observable,
not redundant. If the retained list includes the matched feature and stale
`f2!=matched`, the first pass can create a stale-reference/matched edge; the
second pass handles matched as reference and composes rather than clears that
edge. Stale references 0, 3, and 7 produce distinct controlled stars.

Append ownership remains ordinary and ordered before graph setup. For old count
`n` and old logical pair count `P`, new index is `n`, new `b6=P`, new `bc=n`,
`n` slots are initialized unset/identity, pair count becomes `P+n`,
`order[n]=n`, new `bb=0`, append counter `+0x8cfc` increments, and action/index
are `1/n`. If the new count reaches capacity, this function calls
`FUN_180047120`; otherwise it does not normalize. The caller later performs one
type-12 order finalization and one `+0x8cf4` tail increment when evidence
`+0x684!=0`.

Capacity here is exclusively live `feature_count/+0x1c` against configured
maximum `+0x20`; this selector never reads queue scalar `fa/+0x8e00` or counter
`fb/+0x8e04`. Queue shutdown after a capacity-reaching append is caller-owned in
`FUN_180044fc0`. The selector reads/writes graph `f2/f5` directly and never uses
`fb` as a reference.

With old `f5!=0`, the branch and fallback replay are skipped. Existing `f2` is
preserved; appended `b5` remains the matched copy, and an active matched feature
creates the append edge against existing `f2`, not necessarily against matched.
An inactive matched feature creates no edge. Positive retained count with flag
zero is inert in this established-graph negative family.

Thirteen profile-9/type-12 direct controls cover empty/multiple retained sets,
flag zero/one, identity/nonidentity transforms, stale reference equal/different,
retained matched/nonmatched entries below and above the reference, established
matched-reference/nonreference/inactive negatives, and a real 39-to-40 capacity
append. All pass twice identically. Evidence root:
`<evidence-root>/derived/milan-native-capture-20260723/study-first-graph-unit3-v1/`;
`controls.jsonl` SHA-256 is
`a9764e06ec5131f0d39a31ff796d236a10f9035d39496abc058726b4bfeb582d`,
and `SHA256SUMS` SHA-256 is
`11b8aa1d7ec10313ca4ab2255d32cdd4d44e2a14473cf08f069dc943860a762f`.

Native implementation now captures old `f5`, preserves the caller's stale-`f2`
prepass, performs ordinary append allocation before establishment, activates the
matched and appended owners, writes `f2/f5`, installs the leading-zero anchor,
and runs positive retained replay with a selector-local forced flag. All 13
packed outputs are byte-identical, including established negatives and the
39-to-40 payload SHA-256
`e2742965c1c879645318402b123b5d75583ba2f5071987239e8d93357fbc6559`.

Unit 6 removes the final native ownership collision around this selector.
Append versus replacement is selected solely from count/maximum; `fa` is queue
state and `fb` is preserved opaque counter state. Dispatcher completion and
accepted order/tail finalization are separate inputs, so capacity shutdown is
serialized even when the latter gate is zero, while queued direct-selector calls
remain unfinalized until the dispatcher owns the single final pack.

The selector itself reads no packed profile field. The dispatcher routes live
types 9, 10, 17, and 18 to the sibling selector; type 12 reaches this nonlegacy
path, uses outer coverage `>65`, normalizes at capacity, and selects
`FUN_1800607b0` for accepted ordering. The controls retain authoritative packed
profile 9/live type 12. The core branch may be shared by other nonlegacy types,
but their caller and finalizer contexts are not promoted by this result.

## Queued Ordinary-Mutation Contract

`FUN_18005d330` invokes this exact selector with the current gallery and queued
deep copy; there is no queue-specific mutation policy. A nonnegative return is
the sole queue-consumption predicate and the returned inserted/replaced index,
not the matcher-selected source index, is appended to the continuation ring.
Authoritative sequence 3 enters with matcher-selected feature 9, performs
ordinary append action 1, returns new feature index 10, and changes count
`10->11`.

Normalization remains part of this selector and is not deferred with final
ordering. A queued append that reaches capacity calls `FUN_180047120` before
return; queued actions 3/4 do likewise, while below-capacity append and action 2
bypass it. Thus a subsequent continuation re-match sees every committed feature,
relation, scalar, and required normalized `bb` mutation, but it must see the
pre-`FUN_1800607b0` order/tail state.

## Independent Canonical-Graphless Controls (2026-08-04)

- The approved DLL accepted an eight-feature profile-9/type-12 gallery with
  `f5=0`, `f2=-1`, zero packed relations, and all `b5=0`. Probe 4 returned
  matcher status/score/index `0/45/4`, evidence action/finalization gates `1/1`,
  then study status/action `0/1`; count became 9, `f5=1`, `f2=4`, and only
  matched/appended `b5` became active. After-match and after-study SHA-256 are
  `1d5e7c346ad9691a59ab5ca9de29908ad5ef9f3e759b82380e7592866ddb4990`
  and `ba2c7c94d96fc754fb571caaf5b13ba180d55574bb6bf87c2720a46d9a186c61`.
- Probe 6 independently returned `0/42/2` then action 1, establishing `f2=2`;
  after-match/study SHA-256 are
  `965eef27ca4891af010059f6c158e66bea7cf14aeb6bf72317aff263136ba1e9`
  and `5bbc07877d9ec78cfc4804e29e57e9247a5e6742ef015f47103daff4be87e992`.
- Probe 8 is the action-gate control: matcher status/score/index is `0/23/3`,
  finalization/action gates are `1/0`, study returns `0/0`, and the graphless
  gallery remains byte-identical before/after study at SHA-256
  `4d4cdff2c17613d9b5f82bb6e727e4f69ba64ba2cc985c307135415023949c45`.
- Ghidra fixes first-graph writes at `0x180046c09..0x180046c31`, anchor creation
  at `0x180046c37..0x180046c4c`, and positive retained replay at
  `0x180046c51..0x180046c66`. These controls demonstrate acceptance and first-
  graph study; they do not authorize graph fabrication during enrollment,
  unpack, matching, or serialization. Confidence is high and repeat hashes are
  exact.

## Identify-305 Action-4 Consumer Boundary

Frozen identify 305 proves that this selector and its action-4 mutation are not
the source of the current/native relation mismatch. The selected replacement
target is feature 39 and the live reference is feature 0. Assembly
`0x180046dd5..0x180046e15` passes evidence `+0x66c` unchanged to
`FUN_180045d40`; that helper stores it directly in canonical slot 742 because
`39 > 0`. The approved transform is
`[254,-35,5792,35,254,-2496]`; current supplies
`[256,-16,3348,16,256,-2342]` after incorrectly replacing an earlier direct
relation winner with its aggregate-rescue-selected feature route.

The post-action call to `FUN_180047120` at `0x180046e8c` reads relations only and
recomputes feature residual scalars. It cannot alter slot 742. The 487055-byte
outputs differ only in slot 742 and outer CRC, so no action-4 selection,
replacement, normalization, or packing correction is warranted. The owner fix
is the matcher rescue-publication boundary documented in
`re/milan/IDENTIFY-305-STUDY-RELATION-ROOT-CAUSE-20260808.md`.

An ephemeral control publishing only the preserved feature-33 relation count 10
and affine before this unchanged selector produces the exact approved 487055-byte
SHA-256 `e1094cc9a9a08af3185bb8f41d4c86300eefd831cf9652600e4e84e6e1208f0a`.
