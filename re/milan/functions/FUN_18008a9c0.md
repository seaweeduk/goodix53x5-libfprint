# FUN_18008a9c0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x18008a9c0..0x18008b054`, optimized overlap-safe byte-copy
  helper.

## Selection Callers

- `FUN_18006e7f0` uses it to publish primary or refined into the result.
- `FUN_18006d540` uses it after post-render processing to synchronize the
  finalized result back into the selected candidate workspace.

## Behavior

- Arguments are destination, source, and byte count; the original destination
  is returned.
- Small copies load their source fragments before overlapping stores. Larger
  copies run backward when the destination begins inside the source range and
  otherwise run forward. The observable contract is `memmove`, not `memcpy`.
- The profile-9 selection path does not rely on overlap: primary, refined, and
  the exported result are distinct ranges. Overlap safety belongs to this leaf
  helper and does not make arbitrary aliasing of the complete selection scorer
  valid.

## Evidence

- Small-copy dispatch: `0x18008a9ca..0x18008ab0c`.
- Overlap test and backward path: `0x18008ab0d..0x18008ab1f` and
  `0x18008af70..0x18008b054`.
- Forward vector paths: `0x18008ab20..0x18008af6a`.

## Confidence

High for direction, overlap behavior, and profile-9 selection usage.

## Unresolved

- Other callers are outside this selection/output audit.
