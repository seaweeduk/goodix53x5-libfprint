# FUN_1800619a0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800619a0..0x180061a53`.
- Role: convert one direct probe-to-feature affine into the live
  probe-to-reference relation transform.

## Call Graph

- Callers: profile matcher workers `FUN_180055a40` and `FUN_180062a30`.
- Callees: `FUN_180068860`, `FUN_1800689a0`, and
  `__security_check_cookie`.

## Signature And Layout

```c
void FUN_1800619a0(
  void *const *feature_pointers,
  relation_record *relation_table,
  const int32_t direct_probe_to_feature[6],
  int32_t feature_index,
  int32_t reference_index,
  int32_t probe_to_reference[6]);
```

Each feature row base is signed dword `+0x114`. A relation record is 28 bytes:
one leading dword followed by six affine words. The canonical pair slot is
`feature[max(i,j)].row_base + min(i,j)` and stores the transform from the
higher feature index to the lower feature index.

## Recovered Behavior

The matrix direction is exact:

```text
if feature > reference:
    stored = relation[feature.row_base + reference]  # feature -> reference
    output = compose(stored, direct)                 # stored after direct
elif feature < reference:
    stored = relation[reference.row_base + feature]  # reference -> feature
    output = compose(invert(stored), direct)
else:
    output = direct                                  # literal 24-byte copy
```

It neither checks the relation leading word nor substitutes an identity for an
unset slot. Composition normalizes after this hop; the equal-index copy does
not normalize. There is no status return.

Evidence is branch order at `0x1800619c9..0x180061a30`, slot calculation using
`+0x114` and stride `0x1c`, and the compose/invert calls through
`0x180061a29`. `FUN_180055a40` calls it when an active direct candidate strictly
raises evidence count `+0x668`; input is that candidate's six-word affine and
output is evidence relation transform `+0x66c`.

## Validation

Direct controls exercise all three branches. The two corrected chronology
controls are:

- Enrollment 1 sequence 16: feature/reference `16/0`, stored slot 121
  `[256,-4,-5208,4,256,-29266]`, direct
  `[205,-76,21295,69,251,-188]`, output
  `[243,-81,16089,81,243,-29122]`.
- Enrollment 2 sequence 16: feature/reference `18/1`, stored slot 155
  `[252,46,-13736,-46,252,-12480]`, the same direct affine, output
  `[255,-31,7192,31,255,-16492]`.

A reverse-direction `2/7` control produces
`[256,-1,1761,1,256,-9862]`; an equal-index control copies the deliberately
unnormalized direct affine byte-for-byte. The width-controlled model also
reproduces `389/389` official chronological route constructions where
`+0x64c == +0x668`, and their `60/60` materialized append edges across both
enrollment chains.

## Confidence And Unresolved

- Confidence: high for signature, slot calculation, all matrix directions,
  compose/invert order, output field, and equal-index copy.
- Rows where strict direct winner `+0x64c` differs from strict active relation
  winner `+0x668` require producer-index telemetry to associate `+0x66c` with a
  direct affine; they were excluded rather than guessed.

## Native Implementation

The native relation route now delegates all three direction branches to the
single exact Q8 relation module. The frozen native gate reproduces all `389/389`
unambiguous official routes and both sequence-16 outputs; selected-match and
active-relation producer state remain independent.

## Identify-305 Natural Route

Frozen identify 305 exercises the ordinary feature-above-reference branch with
feature/reference `33/0`. Canonical slot 529 stores
`[256,22,-13165,-22,256,765]`; the direct-positive feature affine is
`[269,-46,19097,73,241,-1619]`. The call at `FUN_180055a40:0x180056f28`
therefore computes:

```text
compose([256,22,-13165,-22,256,765],
        [269,-46,19097,73,241,-1619])
  = [254,-35,5792,35,254,-2496]
```

This equals the approved action-4 candidate relation. A later aggregate rescue
selects feature 8 but does not call this helper and does not overwrite its
output field `+0x66c`. Current composition and normalization arithmetic are
exact. Production commit `1b017754` also preserves this independently published
owner across rescue, closing the former caller defect. The same preserved field
is authoritative when a later full-capacity selector reaches geometric action 3.
