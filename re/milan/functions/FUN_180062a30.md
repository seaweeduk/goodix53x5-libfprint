# FUN_180062a30

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180062a30..0x180063759`.
- Role: alternate full matcher worker; relation-relevant producer behavior is
  recorded here.

## Call Graph

- Direct callers: `FUN_18005d330`, `FUN_18005edb0`, and `FUN_18005ef60`;
  additional data references are at `0x180270978` and `0x18009ab44`.
- Relation-relevant callee: `FUN_1800619a0`; the function has many geometry,
  policy, allocation, and cleanup callees outside this focused trace.

## Recovered Behavior

The helper initializes the same 0x698-byte evidence layout as
`FUN_18005e3e0`, iterates ordered template features, computes candidate state,
and maintains two independent strict-count winners:

- selected direct feature/count/affine at `+0x648/+0x64c/+0x650`;
- active relation count/affine at `+0x668/+0x66c`.

When an accepted candidate's live `+0x110` is one and its count strictly exceeds
`+0x668`, it sets retained flag `+0x644`, updates `+0x668`, and calls
`FUN_1800619a0` with that candidate's direct affine, feature index, and live
reference. A separate strict comparison writes `+0x648/+0x64c/+0x650` even for
an inactive candidate. Thus `+0x650` is a valid observable input for `+0x66c`
only where the two stored counts are equal and the selected feature is the same
producer; the chronological audit deliberately uses that bounded cohort.

Evidence is initialization at `0x180062a60..0x180062b08`, active-relation write
and call at `0x1800631ee..0x180063241`, and selected direct write at
`0x18006324b..0x180063274`.

The strict count write at `0x18006320d` and six-dword route output at `+0x66c`
are independent fields. `FUN_1800619a0` receives output `+0x66c` at the call
`0x180063237`; it does not receive or inspect count `+0x668`. This worker belongs
to the alternate live types `9/10/17/18`, not the profile-9/type-12 dispatcher
path through `FUN_180055a40`.

## Confidence And Unresolved

- Confidence: high for the evidence layout, independent winner semantics,
  strict comparisons, relation producer call, and field association.
- The broader matcher policy is documented in its dedicated matcher notes and
  was not re-audited here. Rows with unequal strict winners still need explicit
  active-producer index telemetry for standalone relation reconstruction.
