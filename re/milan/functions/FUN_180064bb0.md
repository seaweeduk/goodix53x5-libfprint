# FUN_180064bb0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x180064bb0`, setup-frame initialization helper.

## Call Graph

- Caller: setup branch of `FUN_18006d540`.
- Direct callee: `FUN_180064710` for types `0x3e` and `0x3f` only.

## Behavior

- Argument 1 is the caller-owned, already clamped and outer-edge-normalized
  u16 setup frame. Arguments 6 and 7 supply its dimensions; their product is
  9,504 on the profile-9/type-12 path. Argument 5 receives the rounded mean.
- Accumulates unsigned u16 samples in a signed 32-bit sum, then stores
  `(sum + (count >> 1)) / count` using signed division. With normalized samples
  in `0..4095`, the sum is at most 38,918,880, so neither accumulation nor the
  half-count addition overflows. Rounding is nearest with half ties upward.
  All 9,504 samples, including the copied outer edges and corners, contribute.
- For profile-9 packed type `0x0c`, copies each setup sample into workspace
  plane `+0x9924` and adds `0x1bb7` (7095). It returns success after seeding
  this generation state. This branch requires argument 4 (packed bit 1) zero
  and selects type 12 through bit 12 of `0x473000`. Argument 3 does not affect
  this branch. Every output is in `7095..11190`; the mean is computed before
  adding that offset and remains in `0..4095`.
- The production source copy and workspace destination are disjoint. This
  helper does not modify the normalized source. The caller frees that source
  after setup and marks validation-ready when this helper returns 1.
- The related current owner is `goodix_milan_preprocess_make_setup_map`.

## Evidence

- Rounded setup mean: `0x180064bfc..0x180064c30` (signed `IDIV R14D`).
- Profile bitmask selecting the `0x1bb7` branch:
  `0x180064d59..0x180064e4c`.
- Destination workspace plane: `param_2 + 0x9924` at function entry.
