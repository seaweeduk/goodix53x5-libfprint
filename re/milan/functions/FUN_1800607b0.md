# FUN_1800607b0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800607b0`, `0x1800607b0..0x1800608a2`.
- Role: sort the profile-9/type-12 template study order vector.

## Call Graph

- Callers: `FUN_180044fc0` and `FUN_180055a40`.
- Callees: none.

## Recovered Behavior

The live order vector is 50 `int32_t` entries at template `+0x87e8`. This
helper selection-sorts only entries `[0, feature_count)`, where feature count is
read from template `+0x1c`. Each entry is a feature index. The descending key is:

1. feature `+0x110`, serialized tag `b5` (active state);
2. feature `+0x134`, serialized tag `be` (study generation); and
3. feature `+0x12c`, serialized tag `bc` (lifecycle ordinal).

Assembly comparisons are at `0x180060830`, `0x18006083d`, and `0x180060844`.
Swaps are at `0x180060868..0x180060875`. Counts zero and one return without a
write. Slots `[feature_count, 50)` are not inspected or changed.

All three comparisons replace the selected candidate only on strict greater-
than. Equal keys retain the first maximum encountered during that pass, but the
in-place selection swaps are globally unstable. The direct tie control with key
classes `[low,high,low,high]` produces `[1,3,2,0]`; a stable descending sort
would produce `[1,3,0,2]`.

For achievable-v2 enrollment 1 sequence 2, the nine keys produce exact order
`[4,0,8,5,7,6,3,2,1]`. This is the source of the eight defined-tail bytes that
native currently serializes from its different order
`[0,1,2,3,4,6,7,5,8]`.

## Independent Production Audit

The exact key tuples parsed from the v2 transition are
`[(1,0,0),(0,0,1),(0,0,2),(0,0,3),(1,0,4),(0,1,5),`
`(0,0,6),(0,0,7),(0,1,8)]`. Applying only the assembly comparator to identity
order reconstructs `[4,0,8,5,7,6,3,2,1]`.

That vector is exact in the original full-campaign batch and focused carry
artifact (`5f75afe60e7a6ef433a57f82203481baaaec5160fe42d320ba74a3beaf35b794`),
v1 primary/repeat/fresh-prefix artifact
(`09665a629d66b0791166263565748698d04a798b0dc5c7b7c488dbf1f640af8e`),
and v2 primary/repeat artifact
(`ef9805bf57ae44f3d4f690517ee1973706e707396a2161502f700360de776bf4`).
The current native artifact remains the expected negative control at
`eef0e7c19a9e16e3ea29e2eaa85bbe8a00841152c40c820f5b6a4fe12526019e`.

Eight fresh direct DLL invocations covered four allocation layouts, opposite
feature-address order, contiguous storage, heap churn, mode-dependent poison in
every non-key feature byte, and both production-first and tie-first call order.
Every run produced the production and tie vectors above and preserved a unique
poisoned suffix. The report and deterministic PE helper SHA-256 values are
`06fda76c874dd0aa554835c5f69ba9d9289f461aa0bc046901f8be430ba7c435`
and `bd6bd52182bb0b444769cc4f4de13cb80b4bbc58289b830b73dc7150f05a3969`.
Negative controls distinguish omitted sort, fully ascending sort, the ascending-
generation sibling, stable ties, and range overrun.

## Confidence And Unresolved

- Confidence: high that this is true production profile-9/type-12 behavior and
  not Wine allocator, pointer, or call-order behavior. The helper reads pointer
  locations only to obtain the three tagged scalar values; output entries are
  feature indices `0..8`, and neither the order nor keys contain anti-fake
  payload, one-past residue, or serialized pointers.
- The source-level vendor name is unknown. The field is called the study order
  vector according to its proven behavior, not a recovered symbol.

## Native Implementation

The production path now parses each prefix entry's tagged `b5/be/bc` scalars,
validates that `[0, feature_count)` is a feature-index permutation, and applies
the assembly-faithful strict-greater selection sort. The helper writes only that
prefix, so the remaining 50-entry suffix and tagged tail counters are unchanged.
Sensor type 12 uses this finalizer after append or replacement and before
packing; historical sensor type 0 behavior remains separate.

Focused controls reject an omitted sort, fully ascending order, stable tie
handling, and sorting beyond `feature_count`, while independently checking the
suffix. The achievable-v2 enrollment 1 sequence 2 after-study and
next-persistent artifacts are byte-exact at 105,783 bytes and SHA-256
`ef9805bf57ae44f3d4f690517ee1973706e707396a2161502f700360de776bf4`.

## Rare-Action Ordering

Direct exported controls prove this finalizer is action-independent once
evidence `+0x684` is nonzero. Code 2 runs after clearing target `b5`, placing the
replaced target after active features while honoring its new final `bc` ordinal.
Code 3 runs after preserving target `b5`, copying matched `be`, and assigning the
new `bc`; reference and nonreference controls produce the exact logged vectors.
An update-0 `coverage=-11` control still increments order generation `+0x8cf4`
and runs this helper, but the adapter does not pack or persist that transient
object.

## Action-0 Family And Tail Closure

Unit-5 recovery expands the single `coverage=-11` observation into the complete
caller contract. `FUN_180044fc0` calls this helper for every dispatcher-entered
type-12 result with accepted evidence `+0x684!=0`, regardless of action. Action 0
therefore reaches it after strict outer rejection, later selector rejection,
code-2 `-11`, positive no-direct at capacity/disabled queue, and positive
no-direct late enqueue. Shared evidence gate zero is excluded on the exported
production path because `templateStudy` tests the same `+0x688` word before the
dispatcher call.

The helper mutates only gallery order `+0x87e8`; it neither reads nor increments
tail `+0x8cf4`. The caller executes `INC dword ptr [RBX+0x8cf4]` immediately
after this helper returns at `0x1800451c6`. Thus order precedes tail, both occur
once after queue/action handling, and the 32-bit tail wraps modulo `2^32` without
carry. Direct controls include zero, nonzero, and `UINT32_MAX`, and the tie
control reproduces this helper's unstable `[1,3,2,0]` result while action remains
zero. Omission, stable sorting, wrong width, and double finalization are all
discriminated. Evidence and hashes are in
`study-action0-finalization-unit5-v1`; confidence is high.

## Native Action-0 Closure

The existing strict-greater selection sorter is reused on a non-serializable
transaction-owned state copied from the working action-0 gallery. Its caller
runs it only after capacity shutdown or late enqueue and then performs one
unchecked `uint32_t` increment. Native controls reproduce `[1,3,2,0]` and
`UINT32_MAX -> 0`; no action-0 pack or publication is produced.
