# FUN_180044150

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Address/body: `0x180044150..0x1800442fd`, masked central-gradient
  preparation.
- Sole caller: `FUN_180043d90`.

## Behavior

The function clears signed-32 horizontal-gradient, vertical-gradient, and
squared-magnitude planes. It visits only non-border pixels. A pixel is computed
only when all nine bytes in its 3x3 validity neighborhood are nonzero;
otherwise all three outputs remain zero.

For an admitted pixel:

```text
gx = image[x + 1] - image[x - 1]
gy = image[y + 1] - image[y - 1]
magnitude = gx * gx + gy * gy
```

The byte-input bounds make every fixed-profile signed-32 result safe. Evidence
is the complete body, especially `0x1800441e0..0x1800442c9`. Confidence is high.
