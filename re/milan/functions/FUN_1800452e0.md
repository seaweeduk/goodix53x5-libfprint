# FUN_1800452e0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800452e0`, `0x1800452e0..0x180045525`.
- Role: replace a feature's owned live state and invalidate its incident
  relations before study installs the replacement transform.

## Call Graph

- Sole caller: `FUN_1800458c0`.
- Callees: `FUN_180068660` and `FUN_180068700`.

## Recovered Behavior

The helper refreshes the selected feature's matrices and inline state, advances
replacement counters, maintains the `bc` ordering ordinals, and preserves the
feature's relation-row ordinal `b6` at `+0x114`. If the replaced feature is not
the live reference at template `+0x87d8`, every pair incident to that feature is
reset to `[leading=-1, affine identity]` using slot
`feature[max(i,j)].b6 + min(i,j)`. If the replaced feature is the reference, it
does not clear the reference star.

Target tag `ba` is live dword `+0x124`. At `0x180045400..0x180045412`, zero is
left unchanged and every nonzero value is replaced with literal 2. This is
target-owned replacement lifecycle state, not a probe scalar copy. The
authoritative code-4 corpus exercises the split on all 286 rows: 255 nonzero
targets become 2 and 31 zero targets remain zero.

`FUN_1800469f0` subsequently calls `FUN_180045d40` for update code 4, installing
or composing the selected replacement/reference relation. Across all 286 code-4
transitions in the two frozen lanes, packed star cardinality is unchanged and
exactly one serialized slot changes per transition.

The native code-4 path now preserves `b6`, clears nonreference incident slots,
installs the replacement/reference edge, and projects only the reference star.
A focused control requires unchanged cardinality and exactly one changed packed
slot. Native code 2 implements the recovered distinction: `FUN_1800458c0`
reaches this invalidation helper, then `FUN_1800469f0` clears selected `b5` and
does not call `FUN_180045d40`. Code 2 remains absent from all 798 corrected
natural rows; direct DLL controls below validate it.

## Confidence And Unresolved

- Confidence: high for incident-slot invalidation, preserved `b6`, exact `ba`
  promotion, and the replacement handoff.
- The semantic names of the matrix fields and replacement counters remain
  provisional.

## Reference And Nonreference Controls

Controlled code-2/code-3 replacements prove the complete branch split. A
nonreference target with two defined incident relations exits with zero for code
2 and one zero-leading reference edge for code 3 after the caller reinstall. A
reference target retains all 39 star records; code 2 leaves their bytes unchanged
while code 3's later install helper composes every affine. In every positive
case `+0x8cf8` and target `+0x130` increment once, target `bc` becomes 39, every
former ordinal above it decrements, and `b6` remains unchanged.

The helper captures the old target `bc/+0x12c` as an arbitrary signed dword; it
does not range-check it. At `0x180045420..0x18004543b`, every feature whose signed
`bc` is strictly greater than that captured value is decremented once with dword
wrap. `0x180045443..0x180045448` then rewrites the physical target's `bc` to
`feature_count-1`. Thus old `bc=-1` is accepted, every greater ordinal advances
through the wrapped decrement rule, and a 40-feature control exits with target
`bc=39`.

The same helper increments template `+0x8cf8` and target `bd/+0x130` as wrapped
dwords. Target `bd=INT32_MAX` therefore becomes `INT32_MIN`. Production commit
`a29d5d5f` implements these shared replacement scalar rules while retaining the
separate bounds check on the selected physical feature index.

Native controls now set target/probe `ba` oppositely and prove the ownership
split directly: target zero remains zero despite probe `7`, while target `7`
becomes literal `2` despite probe zero. Enrollment 1 sequence 36 closes
byte-exactly at SHA-256
`70fd52642d56e8579d9a2d506fe8215f42fdb9a720ff2c7ecb9c4b4805083bd2`.
