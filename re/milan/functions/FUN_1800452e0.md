# FUN_1800452e0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900.
- Entry/body: `0x1800452e0..0x180045525`.
- Role: replace selected-feature matrices and inline state, advance lifecycle
  scalars, and invalidate nonreference incident relations.
- Caller: `FUN_1800458c0`.
- Callees: `FUN_180068660`, `FUN_180068700`.

## Copy And Lifecycle

The selected physical feature is obtained from template `+0x28 + index*8`.
At `0x18004530c`, the helper loads signed configured maximum feature count from
template `+0x20`. This is the ordinal loop bound, not live count `+0x1c`.

Template `+0x8cf8` and selected `bd/+0x130` increment once, modulo `2^32`, before
matrix copying (`0x180045310..0x180045316`). Matrices at feature `+0x08`, `+0x10`,
and `+0x18` receive the supplied probe matrices. A null optional source clears
the corresponding destination pointer. A nonnull `+0x18` source allocates the
destination when absent before copying.

`0x180045378..0x1800453fc` copies exactly `0xc8` bytes of supplied inline data
to selected `+0x28..+0xef` and clears selected pointer `+0x20`. The helper leaves
`b5/+0x110`, relation-row base `b6/+0x114`, `be/+0x134`, and optional `c7/+0x150`
unchanged. Its caller owns additional record, mask, quality and anti-fake copies.

At `0x180045400..0x180045412`, selected `ba/+0x124 == 0` remains zero; every
nonzero value becomes literal two. This is selected-target ownership, not probe
ownership.

At `0x180045420..0x180045441`, the helper visits all configured slots and
decrements each signed `bc/+0x12c` strictly greater than the selected target's
old signed ordinal. The target never qualifies against itself, so its old ordinal
remains the comparison value throughout the loop. Decrements wrap as dwords.
`0x180045443..0x180045448` finally sets selected `bc` to configured maximum minus
one. Ordinary capacity replacement has live count equal to configured maximum.

## Relations

`0x18004544e..0x180045507` preserves the reference target's incident relations.
For a nonreference target it resets every incident slot to
`[-1,256,0,0,0,256,0]`. The canonical slot for distinct features `i,j` is
`feature[max(i,j)].b6 + min(i,j)`; the lower loop visits indices below the
target, and the upper loop is bounded by live count `+0x1c`.

`FUN_1800469f0` later invokes `FUN_180045d40` for actions 3/4 to install or compose
the selected reference relation. Action 2 omits that installation and clears
selected `b5` in the caller. Consequently, Action 2 leaves a reference target's
incident relations intact, but leaves every nonreference target incident slot at
the unset identity sentinel written here.

## Failure And Ownership

The helper is void and mutates the live object in place. No counter rollback or
recoverable allocation-failure branch exists. It preserves relation row bases;
it does not allocate a new physical feature or change feature count/reference.
