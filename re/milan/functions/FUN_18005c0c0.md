# FUN_18005c0c0

## Binary And Body

- Binary SHA-256: `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18005c0c0` / `0x18005c0c0-0x18005c3a7`.
- Inferred signature: `void f(metrics, scalar_a, scalar_b, config, out_a, out_b)`.
- Caller: `FUN_180053220`; no callees.

## Behavior

This mask-family initial classifier uses metrics `0,1,4,5,9` and configuration
words, including `config[1]` and `config[0x10]`. Four 22-entry threshold tables
classify adjusted count/detail values. It writes one broad eligibility flag and
a stricter second flag requiring the first flag plus scalar and coverage
thresholds.

## Evidence And Confidence

- The threshold tables and both output writes are contained in the function body.
- Production type-12 mapping is
  `(metrics[77], quality=probe+0x11c, coverage=probe+0x120, config,
  out_rsp_60, out_rsp_64)`. The helper is reached on 43 of 58 traced dispatcher
  calls when either input flag is zero; outputs are ORed into the corresponding
  caller flags.
- Confidence is high that this is a two-output classifier, medium for metric names.
- Output-to-tuple mapping is resolved; semantic flag names remain intentionally
  unresolved.
- Native profile-9/type-12 translation is now exact for all `43/43` frozen
  helper entries in the 58-call dispatch gate.
