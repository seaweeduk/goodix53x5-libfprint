# FUN_1800462a0

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x1800462a0`, `0x1800462a0..0x18004642e`.
- Role: select and process one deferred fallback feature candidate.

## Call Graph

- Callers: `FUN_180044fc0`, `FUN_180055a40`, `FUN_180062a30`, and
  `FUN_18007af90`.
- Callees: `FUN_180045a50`, `FUN_180071d40`, and the security-cookie helper.

## Recovered Behavior

The helper finds minimum and maximum rank entries in a caller-owned candidate
array, optionally gates the maximum through overlap, copies the selected fallback
feature, renumbers ranks when the maximum occupied rank is removed, and assigns
the selected slot the next rank.

Enrollment 1 sequence 2 succeeds through the primary append path, so this
fallback is not the source of its order-vector transition. It was inspected only
to bound the late `FUN_180044fc0` flow.

## Confidence And Unresolved

- Confidence: medium-high for selection and rank mutation.
- Exact source names for the candidate rank and overlap threshold remain unknown.

## Complete Type-12 Queue Contract

Ranks encode insertion age as a contiguous permutation `0..occupied-1`; `-1`
means empty. The helper finds the maximum rank (newest) and minimum rank/empty
slot. While queue state is 0 and an entry exists, it compares the incoming live
probe only with the newest entry through `FUN_180071d40`; result `>190` suppresses
the enqueue as an adjacent duplicate. Otherwise `FUN_180045a50` deep-copies the
probe into the selected slot.

Assembly `0x18004631f..0x1800463ca` fixes the comparison operand order as
incoming probe first and newest queued owner second, with an identity transform.
Current source passes those bitmap operands in the opposite order at
`drivers/goodix53x5/milan/match/study.c:46..49`, but consumes only the primary
equality score. At identity, swapping operands exchanges only mismatch classes
`01/10`; the score numerator uses `00+11` and the denominator is the same mask
intersection. The compared score and strict `>190` result are therefore exactly
invariant. This is not an action-1 queue-admission divergence.

If full with ranks `0..19`, the minimum-rank slot is overwritten, every occupied
rank is decremented, and the new entry receives rank 19. A controlled full queue
therefore transitions physical ranks `[0,1,...,19]` to
`[19,0,1,...,18]`. Before capacity, an empty slot receives `max+1`.

The helper assumes caller validation. State 1 skips only duplicate comparison;
an unguarded direct call can still copy. Malformed ranks `25/-2` produce rank 26
rather than an error. All production callers independently require state 0 and
valid fixed-capacity storage. Native code must validate state, nonnull owners,
rank range, uniqueness, and contiguity before mutation.

## Native Implementation

`goodix53x5-study-queue.c` implements the validated fixed-capacity state and
deep-copy contract. Its focused controls preserve the strict `>190` boundary,
full physical ranks `[19,0,1,...,18]`, and fail closed on malformed ranks rather
than reproducing this helper's unsafe direct-call behavior.

The structural state/rank contract is exact, and action-5 handoff recovery
narrows the payload requirement: a queued body must be a deep-owned full
prepared/live feature, not only an independently owned one-feature
serialization. The DLL sequence-3 copy retains 115 active 56-byte records and
nonserialized derived state. The historical 13,069-byte packed-Q callback failed
that contract; current source retains the complete transformed `GoodixMatchInfo`
and closes sequence-3 and the explicitly enqueued consumer controls.

## Unit-5 Late Action-0 Enqueue

The dispatcher calls this helper before accepted order/tail finalization when
primary selected index remains `-1`, shared evidence gate is exactly one, queue
state is zero, and type-12 quality/coverage pass their strict boundaries. The
helper deep-copies the live probe into an already allocated owner at
`gallery+0x8d10`, changes rank 0 from `-1` to `0`, and returns no action. The
dispatcher then runs `FUN_1800607b0`, increments `+0x8cf4`, and still exports
action 0/index `-1`.

A direct profile-9/type-12 control preserves the preallocated destination,
records occupancy `0->1`, rank 0, tail
`0x12345678->0x12345679`, and independent original-probe consumption by
`templateStudy`. Adapter action-0 cleanup later destroys the gallery and copied
queue owner without size, pack, publication, or persistence. A null destination
is not a valid empty slot: `FUN_180045a50` would return `-1` while this unsafe
helper could still assign the rank. The final fixture therefore preserves the
unpacked preallocation rather than treating rank `-1` as a null owner. Evidence
is under `study-action0-finalization-unit5-v1`; confidence is high for this
production caller boundary.

Native unit 5 now executes this order explicitly. `goodix_study_queue_enqueue()`
deep-copies the full `GoodixMatchInfo` before finalization, the diagnostic shows
occupancy `0->1`, rank zero, and an owner distinct from the original probe, and
only then does action-0 order/tail finalization run. The copied owner remains
queue-owned until transaction cleanup; no queue body is packed.

Unit 6 additionally binds this transient object to packed `fa/fb` at matcher and
study entry. State and the full 32-bit transaction counter must agree; owners and
ranks must form the canonical enabled or disabled structure before mutation.
Counter values never influence rank assignment and survive enqueue, consume,
disable, and final packing unchanged.
