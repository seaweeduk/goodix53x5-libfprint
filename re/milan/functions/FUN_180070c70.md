# FUN_180070c70

## Identity

- Binary: `GoodixEngineAdapter.dll`.
- Binary SHA-256:
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180070c70..0x180070d8f`.
- Role: rebuild low-bitmap overlap metrics for a retained transform and write
  them into a caller-provided compact metric workspace.

## Call Graph

- Direct callers: `FUN_180055a40` at `0x180056267` and `FUN_18005d5f0` at
  `0x18005d8fd`.
- Direct callees: `FUN_1800721a0` twice, `FUN_18006fc80`, `FUN_180068b70`
  twice, and `__security_check_cookie`.

## Recovered Interface

```c
int32_t FUN_180070c70(
  const image_desc *first,
  const image_desc *second,
  const int32_t transform[6],
  const int32_t mode[2],
  int32_t *metric_workspace);
```

Names remain provisional. The two descriptors, copied six-word transform,
two-word mode input, fifth stack argument, and integer return are established by
the body and both call sites.

## Behavior

The function copies the transform and reads both mode words. When `mode[0]` is
nonzero, each copied translation is halved by `INC; CDQ; SUB; SAR`, equivalent
on production values to signed `(value + 1) / 2` with truncation toward zero.
It expands descriptor views from `first+0x28` and `second+0x28` through
`FUN_1800721a0`, supplies their `+0x18` low bitmaps, expanded inline masks,
`mode[1]`, and the caller workspace to `FUN_18006fc80`, then releases both
temporary views. The return value is the core helper's return value.

At `FUN_180055a40:0x18005624a..0x180056267`, `first` is the selected enrolled
feature and `second` is the live probe. The transform maps probe/source into
enrolled/target coordinates. Thus the enrolled low bitmap and expanded enrolled
inline mask remain direct target-owned matrices; only the probe low bitmap and
expanded probe inline mask are warped and cropped. This direct call is
conditional: `FUN_180055a40:0x180056236..0x180056244` skips it, together with
the following `FUN_180053220` call, when `flag60 == 2` and signed accumulated
high class is at most one. Candidate words 6..8 then retain their caller-owned
zero initialization for the later `FUN_180058700` policy.

At the `FUN_18005d5f0` fallback boundary, the retained winner's transform and
feature are passed here only after the candidate loop has finished. The compact
workspace is subsequently completed with affine diagnostics and passed to
`FUN_1800608b0`. This recomputation does not select the fallback winner and does
not update the rejection `count_sum`, seen latch, maximum adjusted metric, or
winner-carried coverage.

The core distinguishes unavailable cropped images from zero classified support.
`FUN_18006f390` leaves its output owners null when the clipped rectangle has
nonpositive width or height. With both cropped owners present,
`FUN_18006fc80` calls `FUN_180073540` and returns one even if every class count
is zero; its three guarded denominator calculations then write zero to words
6..8. Zero shared mask support is therefore not the untouched-output failure
case. This wrapper does not mutate either input feature, bitmap, inline mask,
or the supplied affine.

## Evidence

- Transform/mode copy and half-translation:
  `0x180070c92..0x180070ce8`.
- Descriptor construction: `0x180070cec..0x180070d10`.
- Mask/view/core-helper argument assembly and call:
  `0x180070d15..0x180070d55`.
- Temporary cleanup and return: `0x180070d5a..0x180070d8f`.
- Fallback caller setup and follow-on affine diagnostics:
  `FUN_18005d5f0:0x18005d8de..0x18005d90f`.
- Direct caller low-bitmap site: `FUN_180055a40:0x180056267`.
- Existing probe-201 low-bitmap evidence records three exact calls with final
  metrics `220/142/212`, `217/129/209`, and `214/124/207`; native warped
  bitmaps, masks, classes, denominators, and metrics are exact for those calls.
- Reached `production-full-0357-overlap-internal-v3` feature 38 supplies mode
  `[1,0]` and transform `[276,7,7368,-26,255,-8248]`. The copied transform is
  `[276,7,3684,-26,255,-4123]`; the negative even translation therefore proves
  the native add-one-before-halving rule rather than ordinary division by two.
  Current source returns DLL-exact low metrics `209/126/198` from the real
  feature elements.

## Confidence And Open Questions

- Confidence is high for the body, both callers, direct callees, transform/mode
  handling, workspace role, cleanup, and its position after fallback winner
  selection.
- `mode[0]` owns packed-mask scale and translation halving. The profile-9
  captures supply `mode[0]=1`; `mode[1]=0` is forwarded to the core warp block.
  Exact broader names for `mode[1]` and unrelated workspace fields remain
  provisional.
- Direct controls now confirm `FUN_18006fc80` writes candidate words 6..8 only
  after both required temporary views succeed, uses zero for zero denominators,
  and applies half-up Q8 rounding otherwise. Both null-output failure branches
  return zero with all three words untouched. This wrapper propagates that
  return. The fallback caller ignores it and retains its zero-initialized words
  on failure; final acceptance belongs to `FUN_1800608b0`.
