# FUN_18005d5f0

## Identity

- Binary: `GoodixEngineAdapter.dll`.
- Binary SHA-256:
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x18005d5f0..0x18005d9de`.
- Role: select a retained fallback candidate, build its compact metric vector,
  and delegate final percentage scoring to `FUN_1800608b0`.

## Call Graph

- Sole direct caller: `FUN_180055a40`.
- Direct callees: `FUN_18005b900`, `FUN_1800608b0`, `FUN_1800624c0`,
  `FUN_180070c70`, `FUN_180070d90`, `FUN_180070e90`, `FUN_180071d40`,
  `memset`, and `__security_check_cookie`.

## Recovered Interface

```c
int32_t FUN_18005d5f0(
  uint32_t *score,
  void *match_context,
  const uint32_t *candidate_inputs,
  const uint32_t *template_features,
  int32_t *configuration,
  const int32_t *candidate_enabled,
  void *optional_diagnostics);
```

Names remain provisional, but the output score, configuration, candidate loop,
and status return are established by the caller and decompiled data flow.

## Behavior

The function rejects an empty live feature object at context `+0xf0`, writes a
zero score, and returns `0x80000006`. Otherwise it iterates the template feature
pointer array in ascending physical slot order and skips candidates whose
parallel enable word is zero. It does not read the lifecycle-order vector at
context `+0x87e8`; that vector belongs to the separate aggregate rescue. For
each enabled candidate it:

- obtains and filters correspondence evidence through `FUN_18005b900`;
- rejects transforms for which `FUN_1800624c0` returns one;
- computes overlap/detail state through `FUN_180071d40` and `FUN_180070e90`;
- requires overlap greater than `configuration[0] + 207` and a primary metric
  greater than 195;
- ranks survivors by an adjusted quality metric, then correspondence count and
  scaled coverage.

After selecting the best survivor, it recomputes the retained overlap through
`FUN_180070c70`, fills a compact 15-word metric vector, and calls
`FUN_1800608b0` at `0x18005d94e`. A positive result is capped to 100 and written
to the caller score. A zero result becomes a negative bit-coded rejection reason
formed exactly as:

```text
-((count_sum < 6) << 2 |
  (geometry_seen == 1 && best_adjusted_metric < 208) << 1 |
  (geometry_seen == 1 && best_coverage < 128))
```

The count sum includes every fallback candidate that passes the overlap/detail
gate. The adjusted metric is the best ranked overlap detail after affine
penalties. All comparisons are strict.

## Clean-Room Proximity Correspondence

The call to `goodix_milan_match_transform_proximity()` at
`drivers/goodix53x5/milan/match/match.c:1770` is the clean-room counterpart of
this function's `FUN_1800624c0` check. Both follow affine filtering and precede
overlap/detail computation. Native first requires filtered count greater than
four and then calls the classifier at `0x18005d770..0x18005d7e7`; source calls
the pure classifier before its combined count/classifier rejection at
`match.c:1773`. This call-order difference does not change candidate state. It
is the only clean-room proximity call and does not implement the separate
direct-admission call in `FUN_180055a40`.

For packed raw high classes 0 through 4, the local fallback decoder selects the
same native modes `0,1,2,4,5`. Raw class 5 is an additional static difference:
the source expression selects 6, while `FUN_180070d90` selects mode 5. The
current helper also overwrites mode-4/5 type-12 limits with the alternate mode-1
table. Neither difference is selected by the complete current natural authority,
whose 643 replayable probes contain only raw high classes 0, 1, and 2. The exact
643-operation current-versus-DLL report is documented in `FUN_1800624c0.md`;
these fallback differences remain no-change without a natural output witness.

## Evidence

- Candidate correspondence, transform, and overlap chain:
  `0x18005d700..0x18005d850`.
- Best-candidate ranking and retained-state copy:
  `0x18005d850..0x18005d8a7`.
- Recompute and compact final score: `0x18005d8a7..0x18005d966`.
- Negative rejection-reason encoding: `0x18005d978..0x18005d9bd`.
- The four final component inputs are live at `0x18005d9a2`: geometry-seen at
  stack `+0x78`, best adjusted metric in `R14D`, best coverage at frame `+0x24`,
  and count sum at stack `+0x60`.
- The immutable six-row route trace records no `FUN_18005d5f0` entry for
  `81/1`; this function is not that row's post-loop rescue.
- The later repeated aggregate trace at
  `windows-native-aggregate-rescue-v1` confirms `FUN_18005d9e0` receives eight
  ordered records, accepts the two eligible records, and writes feature 2 / score
  19 directly. No `FUN_18005d5f0` or `FUN_1800608b0` input is involved.

## Confidence And Open Questions

- Confidence is high for the candidate loop, ranking order, delegation to
  `FUN_1800608b0`, positive score behavior, exact negative-bit formula, and
  exclusion from `81/1`.
- Confidence is medium for semantic names of individual metric/configuration
  words.
- Full fallback candidate construction and finalization are implemented and
  validated below.

## Safe-Zero Rejection Component Diagnostic

The compact non-forced reference is:

`<evidence-root>/references/milan-native-capture-20260723/windows-safe-zero-score-components-v1/`

Its 11 rows cover one representative for every distinct rejected score pair and
both genuine/impostor labels for dominant `-4/-7`. Primary and independent
repeat projections are byte-identical. The tree SHA-256 is
`59936df20004c45429ee8d405e7714f3e9ba8f218d0b4fc287f8218bdd1a1508`;
each projection hashes to
`fe1b31087c35e7876cb5043859c42e5b906ab2a768a6f18f469f7ad9b908597e`.

All captured component tuples reproduce the official score through the formula
above. The current 33 divergences split into adjusted+coverage `23`,
count+adjusted `2`, count+coverage `2`, count-only `2`, coverage-only `3`, and
adjusted-only `1`. Direct negative-value remapping would therefore be wrong;
native must reproduce this function's fallback candidates and four accumulators.
Instrumented processed and after-match outputs remain exact, no branch is
forced, no diagnostic state feeds matching, study is disabled, and no full
campaign was rerun.

## Complete Fallback Accumulator State Machine

The interrupted research capture was recovered intact at:

`<evidence-root>/references/milan-native-capture-20260723/windows-safe-zero-fallback-accumulators-v1/`

Frozen identities:

- Complete six-file manifest/tree:
  `44ddc062e6ee6f15a9c8553902ae5dacef84308f47b7b239c24a5fc95180074d`.
- Primary and independent-repeat 33-row projections, byte-identical:
  `fd9343c39fc19ba84cddfd6686bb1fb854cb5cf0bbc9995b530536ff41b674ec`.
- State machine:
  `2d737cd130346cd9b5cd6ca7f3430092b18eb6a7d4c2d5b9c7ea9e1648a99ecf`.
- Summary/provenance:
  `add1e967b7b6cb75308aae7a53bc215e4ce966007dae1780b3c878e33e36911d` /
  `03f94de0a08e823f363ad0fd0099468f33a38ec7f5e2f076cfb26905efd52c38`.
- Function slice `0x18005d5f0..0x18005d9df`, 1,007 bytes:
  `3359aec76706d77d6d903db7d1820ad73275551fa14095ed06c52f687acae2d2`.

All 33 rejected-score divergences were run in ascending physical feature-slot
order. The caller supplies one retained enable word and one 31-pair fallback
workspace per physical feature. This function does not form the rejection
components from the earlier direct 77-word candidate. For each enabled feature
it applies this exact state machine:

```text
filtered_count = filtered affine count from FUN_18005b900
if filtered_count <= 4:
    continue
if FUN_1800624c0(transform, decoded_mode, template_type) != 0:
    continue

geometry_seen = 1
(overlap, raw_detail, coverage) = FUN_180071d40(...)
coverage = coverage * configuration[12] >> 8

if overlap <= configuration[0] + 207 or raw_detail <= 195:
    continue

adjusted = raw_detail
if configuration[16] != 0:
    adjusted -= 4 * (uint32(average_scale - 234) > 47)
    adjusted -= 4 * (absolute_dot_q16 >= 0x147b)

count_sum += filtered_count
best_adjusted_metric = max(best_adjusted_metric, adjusted)

if (adjusted, filtered_count, coverage) is strictly lexicographically
   greater than (winner.adjusted, winner.count, winner.coverage):
    winner = current feature/count/adjusted/coverage/transform
```

Initial state is `geometry_seen=0`, `count_sum=0`,
`best_adjusted_metric=0`, and a zero winner with feature `-1`. Exact winner ties
retain the lower physical feature slot. `best_adjusted_metric` is updated
independently for every gate pass. Coverage is not a maximum: the terminal
coverage is the coverage carried by the retained lexicographic winner. Because
adjusted metric is the first rank field, the winner's adjusted value and the
independent maximum agree, but their update roles must remain separate.

If at least one candidate passes the overlap/detail gate, the retained winner is
recomputed through `FUN_180070c70`, packed into the 15-word vector, and offered
to `FUN_1800608b0`. Only when that helper does not produce a positive score does
the negative reason use the four accumulators above.

Strict boundaries are established by assembly and natural controls:

- filtered count must be `> 4` and the transform classifier must return exactly
  zero;
- overlap must be `> configuration[0] + 207`; row `373/2` has equality `207`
  and does not accumulate;
- raw detail must be `> 195`; row `139/1` has equality `195` and does not
  accumulate;
- scale penalty is the unsigned outside-`[234,281]` test; row `393/2` at `282`
  sets it, while dot penalty is inclusive at `0x147b`;
- adjusted `208` and count sum `6` clear their reason bits; row `52/1` proves
  both equality boundaries;
- coverage `128` clears its reason bit; the comparison is strict `< 128` at
  `0x18005d996..0x18005d99f`;
- a seen candidate that fails the later gate still sets adjusted and coverage
  reason bits from zero state, while no seen candidate emits only the count bit;
- row `146/1` proves that counts from all three gate passes sum to `37`, while
  coverage `202` remains attached to the first retained winner rather than the
  later coverage `103` or a separately accumulated value.

Trace occupancy is two rows with no geometry candidate, 24 with geometry but no
gate pass, six with one pass, and one with three passes. The official terminal
state reproduces all six differing-component cohorts: adjusted plus coverage
`23`, count plus adjusted `2`, count plus coverage `2`, count only `2`, coverage
only `3`, and adjusted only `1`. The exact formula closes all `33/33` frozen
scores.

Evidence addresses are initial state `0x18005d65d..0x18005d676`, enabled and
filtered-count gates `0x18005d71b..0x18005d76a`, transform/seen boundary
`0x18005d770..0x18005d7e7`, overlap/detail and penalty block
`0x18005d7ec..0x18005d84d`, strict winner and accumulators
`0x18005d850..0x18005d89e`, retained recomputation/final validator
`0x18005d8d9..0x18005d964`, and negative bits
`0x18005d978..0x18005d9b9`.

Confidence is high for the complete profile-9/subtype-12 state machine, update
order, strict boundaries, and all 33 terminal results. Other template types and
configuration modes are outside this trace.

## Narrow Native Coding Contract

Replace only the current direct-candidate rejection shortcut at
`goodix53x5-milan.c:7457..7466`. Preserve, per template feature, the caller's
fallback enable word and 31-pair workspace, then run one purpose-specific
fallback finalizer after direct selection and aggregate rescue fail. That
finalizer must use the existing affine filter, transform classifier, overlap,
affine diagnostic, retained-winner recomputation, and final-score primitives in
the order above; update `count_sum`, independent maximum adjusted metric, and
winner-carried coverage only at the documented boundaries. Initialize all four
reason inputs exactly as above and retain the existing three-bit encoder.

Do not derive these fields from `direct_metrics`, substitute official terminal
values, map negative scores, change thresholds, or alter direct policy, Q8,
winner selection, aggregate rescue, study, persistence, or runtime integration.
The implementation gate is event-by-event parity against both repeated 33-row
projections, terminal score parity `33/33`, then full immutable score parity
`798/798` while retaining decision/index parity `798/798` and impostor accepts
`0/398`.

## Native Implementation Result

`goodix53x5-match-fallback.h` now owns the retained 31-pair workspaces, four
rejection accumulators, strict gate ordering, affine penalties, independent
adjusted maximum, lexicographic winner, and three-bit rejection encoder.
`goodix53x5-milan.c` preserves the caller's two fallback-enable paths, evaluates
enabled workspaces after direct selection and aggregate rescue fail, recomputes
the retained winner, and delegates positive final scoring to the existing native
score helper.

`re/milan/scripts/run-native-match-fallback-tests.sh` verifies strict count,
classifier, overlap, detail, penalty, reason-bit, tie, and winner boundaries. It
also replays the authoritative 33-row projection through the real native matcher
and compares all 36 evaluated events, accumulator transitions, and terminals.
The result is:

```text
native_fallback_finalizer=pass rows=33 events=36 tree_sha256=44ddc062e6ee6f15a9c8553902ae5dacef84308f47b7b239c24a5fc95180074d
```

Full immutable replay reaches score, decision, and candidate-index parity
`798/798`, accepted-score parity `318/318`, and zero impostor accepts.

## Match-Time Lifecycle Consequence

The caller invokes this fallback at `FUN_180055a40:0x1800570df`. A positive
score sets acceptance evidence at `0x1800570ea` and jumps directly out at
`0x1800570f5`; rejection also exits without returning to an increment loop.
There is no `be` write in this function and no caller write after the call. A
positive fallback therefore adds no lifecycle increment, preserves any prior
direct increment state, and leaves internal
selected feature `evidence+0x648` at `-1`; the retained fallback winner is not a
selected-feature lifecycle owner. No positive natural fallback occurs in either
798-row authority, so reachability frequency remains unclaimed, but the zero-
increment exit contract is exact static control flow rather than a Class-B
selected-feature assumption.

## Unit-18 Publication Integration

The production caller now routes positive type-12 fallback through the focused
contribution/publication state. It ORs acceptance evidence and publishes the
capped score, but leaves selected feature `-1`, transform/relation ownership
unchanged, and lifecycle increments empty. Positive, zero, negative, and
blocking-state return controls are byte-exact to the DLL. Natural positive
fallback remains absent from the immutable 798-row authority; no row or
threshold was added to manufacture reachability.

## Caller-Owned Reachability And Zero Preservation

The caller does not invoke this function after every failed type-12 direct or
no-direct result. `FUN_180055a40:0x180056d7a..0x180056d9e` first requires score
zero, accumulated decoded `c7` high class below four, live-probe auxiliary class
below two, and configuration word 13 nonzero. Auxiliary class two or greater
branches to the common return and leaves score zero; this function is not
entered. The compared value is copied from the first byte expanded by
`FUN_180073830`, not from `FUN_180058700`'s separate status counter.

This distinction fixes the ownership of the rejection accumulators. The
fallback enable array and 31-pair workspaces are caller stack state, but
`geometry_seen`, `count_sum`, best adjusted detail, and winner coverage are
initialized locally only after entry at `0x18005d65d..0x18005d676`. Staged
workspaces do not imply a rejection score. If the caller bypasses the call, no
candidate loop runs, no `count_sum` exists to publish, and the three-bit formula
must not be evaluated.

On an actual call, this function still owns both positive fallback score and the
exact negative reason, writes either result through its score pointer, and
returns status zero. `FUN_180055a40` owns call reachability, positive acceptance
publication, and blocking override. `FUN_18005edb0` does not remap the score.
Therefore the minimal native-equivalent correction belongs before this call,
not in `goodix_milan_match_fallback_rejection_score()` and not in this function's
thresholds or bit encoder.

## Physical Versus Lifecycle Ordering

`FUN_180055a40` traverses template order `+0x87e8`, but the loaded order entry is
a physical feature index. At `0x180056fc9..0x180056ff2`, it uses that index for
both the parallel enable word and the destination
`workspace_base + physical_feature * 62 dwords`. This function later advances
the feature-pointer, enable-word, and workspace arrays together at
`0x18005d8a7..0x18005d8be`, so native fallback finalization always scans
physical slots `0..feature_count-1`.

Current source appends `GoodixMilanMatchFallbackWorkspace` entries while
traversing `tail_state` and later scans the appended entries. Valid natural
profile-9 templates can make these orders different. One canonical 40-feature
input, SHA-256
`6c7a7fe5015ff4b51133acd8d657b5da091a3b282b77758b8e555a0d54d76992`,
has stored order:

```text
[37,19,18,31,27,34,23,22,20,36,39,25,32,28,38,8,33,26,35,2,
 17,24,29,21,16,12,30,15,14,13,0,7,5,4,3,10,9,6,1,11]
```

Therefore the source and DLL do have different tie-retention owners: source
keeps the first lifecycle-order candidate, while the DLL keeps the lowest
physical slot. The distinction has no effect unless at least two candidates
pass the filtered-count, transform, overlap, and detail gates and tie exactly on
the signed rank tuple:

```text
(adjusted_detail, filtered_count, scaled_coverage)
```

For profile 9/type 12, configuration word 16 is zero, so `adjusted_detail` is
the raw detail. On a complete rank tie, `count_sum`, independent maximum detail,
winner coverage, and the negative reason are order-independent. If the retained
winner reaches `FUN_1800608b0`, its alternate-policy positive gate is:

```text
filtered_count >= 8
adjusted_detail + low_bitmap_detail
  - 4 * (affine_penalty_12 + affine_penalty_13 + affine_penalty_14) >= 417
scaled_coverage >= 96
```

The positive score is determined only by the tied filtered count:

```text
q = (21 + filtered_count * 256) / 42
score = (q * 100) >> 8
```

Thus two tied winners that both pass produce the same score, and two that both
fail produce the same negative reason. Ordering can change acceptance only if
their low-bitmap detail or affine penalties place them on opposite sides of the
positive gate. Even then, fallback publishes only the score and acceptance
evidence: it does not publish the retained physical feature or affine, leaves
selected feature `-1`, adds no lifecycle bit, and leaves the after-match template
unchanged. A positive outer transaction can attempt study, but the absent
selected feature yields action zero and no candidate.

The strict natural current-versus-DLL boundary covers rejected scores `-1`,
`-2`, `-3`, `-4`, `-6`, and `-7`, plus accepted aggregate/rescue controls. Its
fresh six-operation report is
`<parity-state>/reports/fallback-order-independent-validation-20260820.json`
(SHA-256
`dad9e252a7e238a19916cf0cdcbc038b38fca9eee7e84d98797663c90dd6f29c`).
The natural fallback calls contain these five overlap/detail gate passes:

| Operation | Feature | Adjusted detail | Filtered count | Coverage | Final-gate failure |
|---|---:|---:|---:|---:|---|
| identify 326 | 23 | 206 | 7 | 83 | count and coverage |
| identify 326 | 22 | 198 | 5 | 100 | count |
| identify 424 | 27 | 208 | 8 | 73 | coverage |
| identify 11 | 12 | 203 | 5 | 133 | count |
| identify 67 | 21 | 198 | 6 | 189 | count |

There is no complete rank tie and no positive fallback. Direct workspace ties
establish the local order sensitivity but not a natural transaction divergence.
Without a valid positive natural witness that straddles the final gate, this
ordering difference is no-change and does not authorize a source correction.
