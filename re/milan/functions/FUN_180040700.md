# FUN_180040700

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180040700..0x1800407d2`.
- Role: select subtype defaults and an optional feature-allocation cap during
  template decoding.

## Call Graph

- Callers: complete decoder `FUN_18003e3a0` at `0x18003e755` and post-decode
  reconciliation `FUN_18003fef0` at `0x18003ff82`.
- Callees: none.

## Profile-9/Type-12 Contract

The second argument is the live template. For live type 12 the helper returns
default maximum features `40` and both record-capacity outputs as `150`. A
nonnull first argument points to an unsigned feature-allocation cap; when it is
less than 40, that lower value replaces the default feature maximum.

The natural parity caller passes null to exported `templateUnPack`, so no caller
cap is applied. `FUN_18003e3a0` then takes the larger of the relevant serialized
requirements and these type defaults before calling `FUN_18003f8b0`. This
allocates sufficient feature and record storage without changing the serialized
current feature count.

The helper returns `0x80000001` for a null live template and zero after writing
the three outputs.

## Confidence

- Confidence: high for the null parity boundary, type-12 constants, and optional
  cap behavior.
- Other subtype defaults are outside scope.
