# FUN_180040700

## Identity

- Binary: `GoodixEngineAdapter.dll` 2.0.310.900, SHA-256
  `6673db3874fea66a58e2da29e371d797b890c767ba0491134d4a372c5b27e3b4`.
- Entry/body: `0x180040700..0x1800407d2`.
- Role: select subtype defaults and an optional feature-allocation cap during
  template decoding.

## Call Graph

- Callers: complete decoder `FUN_18003e3a0` at `0x18003e755` and post-decode
  reconciliation `FUN_18003fef0` at `0x18003ff82`.
- Callees: none.

## Profile-9/Type-12 Contract

The second argument is the live template. Types 0 and 12 select the same arm:
both record-capacity outputs are `150`, and the nonnull-context feature maximum
is `min(40, *context)` using unsigned comparison. With a null first argument,
the helper instead preserves input live maximum `+0x20`; it does not force 40.
The null branch at `0x1800407ba` copies the entry value saved in `R11D` into
the output register. See `FUN_18003fef0.md` for reconciliation after decoding.

With null context, no caller feature cap is applied. `FUN_18003e3a0` takes the
larger of the relevant serialized requirements and these selected limits before
calling `FUN_18003f8b0`. This allocates sufficient feature and record storage
without changing the serialized current feature count.

The helper returns `0x80000001` for a null live template and zero after writing
the three outputs.

The common type-0/type-12 contract is also summarized in
`../PROFILE0-MATCH-TEMPLATE-CONTRACT.md`.
